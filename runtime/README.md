# Runtime data directory — git-ignored
# This directory is created by the application at runtime.
# Database, uploads, logs, and review results are stored here.
# For production deployment, mount this as a separate volume.