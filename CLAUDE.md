# 项目指引

## 会话结束任务同步（必须）

每次对话结束前，若本次涉及任务完成——包括但不限于 bug 修复、功能开发、代码审查、测试数据准备、文档更新、配置运维——必须：

1. 将新增条目、状态变更记录到 `task-list.md` 中，确保任务列表与实际进度同步；
2. 在对话最后一条回复中通知用户：已记录哪些条目。

## 开发与质量

1. 每次完成 bug 修复或功能开发时，都要为对应的 bug 或功能点设计至少一个自动化测试脚本，并至少跑一遍相关的自动化测试；如果存在全量自动化测试入口，收尾前优先补一次全量回归验证。

2. 遇到不理解的问题，主动查找官方文档和权威来源，不要凭猜测给出答案。

## 项目约束

1. **数据与代码分离原则**：runtime/ 目录存放所有运行时数据（数据库、上传文件、日志、结果），该目录已 git-ignored。不要将任何运行时数据或用户数据提交到 git。

2. **开源合规原则**：不要在代码中硬编码品牌名称、内部域名、特定企业标识。所有品牌元素应通过配置文件或环境变量注入。

3. **docx-to-markdown skill 上游同步原则**：`skills/docx-to-markdown/` 与开源仓库 https://github.com/wangminle/skills-doc-to-md 保持同源。**每次项目版本更新（发布新 VERSION）时，必须做一次该 skill 与 GitHub 上游的双向差异检查**：
   - 本地因实际项目多做的增强（如安全加固）且具备通用价值 → 给上游仓库提 issue 推动其更新；
   - GitHub 上游迭代领先（本地缺少的功能）→ 将上游版本合并更新到本地 skill；
   - 检查结果记录到 `task-list.md`（检查事项区，前缀 CHK）。

4. **上下文压缩规则**：当会话上下文接近约 170K tokens 时，应主动启动 `compact` 指令压缩上下文，避免上下文溢出影响任务连续性。