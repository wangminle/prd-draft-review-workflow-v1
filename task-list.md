# 任务跟踪列表

记录本项目所有任务：代码 bug、bug 转需求、新增需求、需求调整、功能开发、代码审查、测试数据、文档维护、配置运维等。

> 说明：本文件是当前项目的任务清单。所有新增事项、状态变更和完成记录都应同步写入本文件。
> Profile：**extended**（含规划/优化/调研扩展分区 + 统计摘要）。
> 字段说明：动作字段只允许以下 8 个固定枚举：修复、开发、优化、调整、规划、检查、文档、运维。
> 时间说明：发现时间和完成时间分开记录，格式为 `YYYY-MM-DD HH:MM`（24 小时制，本地时区）；未完成事项的完成时间填 `-`。
> 归并规则：审计、复核、核查、审查、验证、评估统一记为“检查”；重构、清理统一记为“优化”；方案、梳理统一记为“规划”；记录类文档事项统一记为“文档”。
> 最近标准化：2026-06-24（task-list-initialization 诊断 + 统计摘要写入；详见 `docs/task-list-standardize-report.md`）。

## 代码 Bug

| ID | 动作 | 问题描述 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | 修复 | 对照外部任务清单发现当前代码缺少 BUG-111~114 的部分修复：新对话未清理附件/URL、会话 ID 类型不一致导致高亮丢失、预分类缺少 prompt 级 schema、前端内联 onclick 依赖对象未挂到 window | 2026-06-01 00:00 | 2026-06-01 00:00 | 已修复 | `chat.js` 新增 `_clearAttachments()` 调用并改为字符串 ID 比较；`SkillSchemaLoader` 支持 prompt 级 schema 与递归校验/修复；`auth.js`/`chat.js`/`admin.js`/`review.js` 暴露全局对象；新增 `tests/test_skill_schema.py`、`tests/test_frontend_inline_handlers.py` 并扩展聊天前端契约测试；全量 `python3 -m pytest` 355 通过 |
| BUG-002 | 修复 | CHK-006 审查发现的三个 P2 回退：① 删除不存在上下文项误报成功（应返回 404）② FTS 搜索排序从相关性 rank 回退为时间倒序 ③ 逐篇分析缓存校验从固定 6 项 rule_key 放宽为 checks 长度 ≥ 6 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已修复 | `context_item_repository.py` delete_item 返回 bool + chat.py 404 判断；`conversation_repository.py` ORDER BY rank 恢复；`review_task_repository.py` 引入 `_REQUIRED_EXPERT_REVIEW_RULE_KEYS` frozenset 做集合校验；`.gitignore` 改用 `**/__pycache__/` + `*.py[cod]`；新增 `tests/test_code_review_fixes.py`(7)；全量回归 381 passed |
| BUG-003 | 修复 | CHK-008 审查发现的四个问题：① expert_review summary 非空校验缺失 ② review 文件删除仍在 router（os.remove 直接调用）③ 持久化扫描漏扫 os.remove/os.unlink ④ ChatApplicationService 未落地 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已修复 | `review_task_repository.py` 补充 summary 非空校验；`review_file_storage.py` 新增 delete_document_files 承接文件删除；`test_router_persistence_scan.py` 新增 os_remove/os_unlink 扫描模式；`chat_application_service.py` 创建 + chat.py 重构委派 prepare_chat_session；全量回归 388 passed |
| BUG-004 | 修复 | CHK-009 审查发现 ReviewFileStorage 固定使用 runtime_path("data", "review_uploads")，忽略 review.upload.upload_dir 配置 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | `review_file_storage.py` 新增 __init__(upload_dir)、_resolve_config_dir、_resolve_upload_root，优先注入路径→配置值→默认回退；save_uploaded_docx 和 delete_project_files 改用 _resolve_upload_root()；新增 `tests/test_code_review_fixes.py`(4)；全量回归 392 passed |
| BUG-005 | 修复 | 旧 runtime 实机复核发现 `prd-per-analysis/prompts/system-context.md` 缺失，且逐篇分析 prompt 未显式要求模型输出 `doc_id`，导致日志出现 prompt 缺失和 doc_id schema 自动修复告警 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | 新增 `skills/prd-per-analysis/prompts/system-context.md`；`per-doc-analysis.md` 增加文档 ID 输入、输出示例和规则；`SkillRunner._run_per_analysis()` 将 `doc_id` 传入 prompt 变量；新增/扩展 `tests/test_admin_skills.py`、`tests/test_skill_runner_integration.py`；全量回归 393 passed |
| BUG-006 | 修复 | P1 模型思考级别审查发现：前端选择”关”时 chat/review 请求不传 `thinking_level`，后端会回退到模型默认思考级别，导致无法关闭默认开启的思考参数 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | `chat.js`/`review.js` `_getThinkingLevel()`/`_getReviewThinkingLevel()` 改为始终返回当前选中值（含 `off`）；新增 `test_chat_sends_off_to_override_default` 测试；430 全量回归通过 |
| BUG-007 | 修复 | P1 模型思考级别审查发现：后台模型配置清空或关闭 custom JSON 时 `thinking_payload: null` 不会落库清空，旧自定义参数会继续残留在模型配置中 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | `model_config_repository.py` 引入 `_UNSET` sentinel 区分”未提供”和”显式清空”；`admin.py` update endpoint 使用 `req.model_fields_set` 判断是否传了 thinking_payload；新增 `test_thinking_payload_can_be_cleared` + `test_update_without_payload_preserves_existing` 测试；430 全量回归通过 |
| BUG-008 | 修复 | P2 Markdown/Mermaid 审查发现：`chat.js` 将 `_queueMermaidRender()` 改为空操作，但历史消息 `_appendMessage()` 仍调用它，导致刷新或切换历史会话后 Mermaid 图不再渲染 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | `_appendMessage()` 改为调用 `_renderMermaidOnStreamEnd(div)`，历史消息可立即渲染 Mermaid；新增契约测试覆盖；463 全量回归通过 |
| BUG-009 | 修复 | P2 Markdown fallback 审查发现：chat/review fallback 仅把列表项替换成裸 `<li>`，没有生成 `<ul>`/`<ol>` 容器，库不可用时列表 HTML 结构无效且与验收不一致 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | 新增 `_wrapListItems(html, pattern, tag)` 方法为连续无序/有序列表生成 `<ul>`/`<ol>` 容器；chat.js/review.js fallback 列表改用 `_wrapListItems`；新增契约测试覆盖；463 全量回归通过 |
| BUG-010 | 修复 | P2 Markdown fallback 复查发现：`_wrapListItems()` 会移除所有列表行并把 `<ul>/<ol>` 追加到内容末尾，导致”段落-列表-段落”等混排内容顺序被改变 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | `_wrapListItems` 重写为按行扫描、原地包裹连续列表块，保留列表在文档中的位置；chat.js/review.js 同步修复；新增混排顺序契约测试；465 全量回归通过 |
| BUG-011 | 修复 | P2 Markdown fallback 复查发现：`_wrapListItems()` 复用带 `g` 标志的 RegExp 连续 `exec` 不同字符串，连续列表项会被拆成多个 `<ul>/<ol>` 容器 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | `chat.js`/`review.js` `_wrapListItems()` 创建 RegExp 时用 `pattern.flags.replace('g', '')` 去掉 global 标志，防止 lastIndex 在不同字符串间泄漏；新增 2 项契约测试验证 g 标志已移除；612 全量回归通过 |
| BUG-012 | 修复 | 旧 runtime 实机体验发现 Mermaid 仍显示为蓝色空条：浏览器可能沿用缓存脚本，模型输出的未加引号长中文节点标签会触发 Mermaid parse 失败，且 DOMPurify 会清掉多行 `data-mermaid` 属性导致渲染函数找不到源码 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | `index.html` 为 vendor 与前端脚本增加 `?v=20260602-6` cache busting；`chat.js`/`review.js` 改为用隐藏 `<pre class="mermaid-source">` 保存 Mermaid 源码，避免 DOMPurify 清理多行属性；新增 `_getMermaidSource()` 与 `_normalizeMermaidCode()`，parse/render 前自动 quote 未加引号的 `[]`/`{}` 节点标签；真实 Chrome 验证最新消息 ID 58 可 render SVG；相关契约 149 passed，全量回归 472 passed |
| BUG-013 | 修复 | Mermaid 二次复核发现浏览器仍命中旧版 `chat.js`/`review.js` 缓存，页面实际运行代码仍依赖被 DOMPurify 清理掉的 `data-mermaid`，导致修复后仍显示空白占位 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已修复 | `index.html` 将 vendor 与前端脚本版本统一提升到 `?v=20260602-6`，强制刷新后加载新版 Mermaid 源码节点实现；定向回归 `pytest tests/test_markdown_contract.py -q` 42 passed；新开页面实测 `window.Chat` 已使用 `_getMermaidSource()`，并成功渲染出 SVG |
| BUG-014 | 修复 | reasoning_content 审查发现：当模型只返回思考内容无正文时，`chat.js` 流结束回退逻辑误清已渲染的 `.msg-reasoning`，显示"未收到回复"掩盖真实思考过程 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `chat.js` 将 `!fullText && !hadError` 改为 `!fullText && !reasoningText && !hadError`；新增 `!fullText && reasoningText` 分支保留 `.msg-reasoning`；新增契约测试覆盖；494 全量回归通过 |
| BUG-015 | 修复 | reasoning_content 审查发现：`llm.py` `stream_chat()` 的 `delta_obj.get("reasoning_content", "")` 当 API 返回 `null` 时得到 `None` 而非 `""`，破坏 `StreamChunk.reasoning_content: str` 类型约束 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `llm.py:195` 改为 `delta_obj.get("reasoning_content") or ""` 兜底 null；与 `non_stream_chat` line 341 的 `or ""` 保持一致；494 全量回归通过 |
| BUG-016 | 修复 | 品牌迁移工具资产路径安全未收口 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `migrate_branding.py` 新增 `_validate_asset_path_safe()` 复用 P3 同等约束（拒绝外部 URL、.. 穿越、绝对路径前缀），`build_branding_yaml()` 资产字段校验后拒绝非法值置空+警告日志，`copy_assets()` dest 写出前 `resolve().relative_to(branding_root)` 边界检查；新增 22 项路径安全+输入边界回归测试；598 全量回归通过 |
| BUG-017 | 修复 | 品牌迁移工具输入边界与需求不一致 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `migrate_branding.py` 新增 `_discover_static_dir()` 自动发现 static 目录（传入目录→src/static→static），`migrate()` 使用自动发现而非直接把传入路径当 static；CLI help 改为"旧项目代码目录路径（根目录或 static 目录均可）"；新增 5 项自动发现回归测试（含从项目根目录完整迁移）；598 全量回归通过 |
| BUG-018 | 修复 | 品牌迁移工具绝对路径校验仍有缺口：`/etc/passwd` 会被 `_validate_asset_path_safe()` 先 `lstrip(“/”)` 成 `etc/passwd` 并写入 YAML，未真正满足 P3 “拒绝绝对路径”约束 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `_validate_asset_path_safe()` 新增多段绝对路径拒绝：原路径以 `/` 开头且 lstrip 后仍含 `/` 分隔符的（如 `/etc/passwd`→`etc/passwd`）一律拒绝；仅允许单段前缀路径（如 `/favicon.svg`→`favicon.svg`）；新增 2 项 `_validate_asset_path_safe` 回归测试 + 2 项 `build_branding_yaml` 路径安全回归测试；604 全量回归通过 |
| BUG-019 | 修复 | 品牌迁移工具复制合法相对子目录资产时会崩溃：`icons/favicon.svg` 通过校验并写入配置，但 `copy_assets()` 未创建目标父目录，`shutil.copy2()` 抛 `FileNotFoundError` | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `copy_assets()` 三处 `shutil.copy2()` 调用前均新增 `dest.parent.mkdir(parents=True, exist_ok=True)`；新增 2 项嵌套资产路径回归测试（favicon 嵌套 + logo 嵌套）；604 全量回归通过 |
| BUG-020 | 修复 | 审查页顶栏标题与智能对话/管理后台不一致：审查页 topbar-title 使用 `data-branding="review-title"` 显示"需求审查工作台"，应与其他页面保持一致显示产品名"AI产品需求初审" | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | 将审查页 `<span class="topbar-title" data-branding="review-title">需求审查工作台</span>` 改为 `<span class="topbar-title" data-branding="topbar-title">AI产品需求初审</span>`；auth.js textMap 移除 `review-title` 映射；三个页面标题一致使用 `topbar-title` branding；badge（智能对话/审查模式/管理后台）仍区分页面功能 |
| BUG-021 | 修复 | 顶栏标题下方缺少版本号标注 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | branding_config.py DEFAULT_BRANDING 新增 `app_version: "0.2.7"` 字段；HTML 三页面 topbar-brand 内新增 `<div class="topbar-brand-text">` 包裹 title + version；CSS 新增 `.topbar-brand-text`（flex column）和 `.topbar-version`（10px、color-text-secondary）；auth.js textMap 新增 `'app-version': c.app_version`；API 返回 `app_version` 字段；迁移工具 `build_branding_yaml` 和 `scan_runtime` 同步新增 `app_version`；610 全量回归通过 |
| BUG-022 | 修复 | .gitignore 规则 `!runtime/config/` 会让本地 ui-branding.yaml 和扫描报告作为未跟踪文件出现在工作区，违反运行时数据不提交约定 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | .gitignore 新增两条 re-ignore 规则：`runtime/config/ui-branding.yaml` 和 `runtime/config/ui-branding.scan-report.md`，仅保留 example.yaml 可被追踪 |
| BUG-023 | 修复 | `_merge_branding()` 非法资产路径会覆盖已有合法值为空：runtime YAML 写 `https://...` 时会把 config.yaml 的合法 Logo 清空 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `_merge_branding()` 资产路径校验逻辑改为：非法值不替换已有合法值，只记录警告日志并保留原配置；新增回归测试 `test_invalid_asset_path_preserves_valid_base` |
| BUG-024 | 修复 | HTML 缺少 Logo 挂载点：登录页 `.brand-mark` 和三页面 `.brand-dot` 没有 `data-branding` 属性，Branding.apply() 的 Logo 替换代码无法生效 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | HTML 登录页 `.brand-mark` 新增 `data-branding="login-logo"`；三页面 `.brand-dot` 新增 `data-branding="topbar-logo"`；新增前端契约测试 `test_html_has_data_branding_login_logo` 和 `test_html_has_data_branding_topbar_logo` |
| BUG-025 | 修复 | `review_workspace_label` 配置字段前端不生效：go-review 按钮和 review 页的"需求审查工作台"仍是硬编码，auth.js textMap 未映射该字段 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | HTML go-review 按钮新增 `data-branding="review-workspace-label"`；auth.js textMap 新增 `'review-workspace-label': c.review_workspace_label`；新增契约测试覆盖 |
| BUG-026 | 修复 | 品牌迁移工具 `discover_assets()` 找到 `logo.png` 但不会写入 YAML 配置：通用 logo_file 只在 discovered_assets 中，不进 merged，导致迁移后配置为空 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `migrate()` 在合并后补充：将 discovered_assets 的 logo_file 以低置信写入 merged（不覆盖已有高置信值）；新增回归测试 `test_migrate_writes_discovered_logo_to_yaml` |
| BUG-027 | 修复 | 版本号显示丢失 "Ver." 前缀：auth.js textMap 直接用 `c.app_version` 替换 textContent，把 HTML 中的"Ver. 0.2.7"变成裸"0.2.7" | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | auth.js textMap 的 `'app-version'` 改为 `'Ver. ' + c.app_version`，保留前缀；新增契约测试验证 `'Ver. '` 在 AUTH_JS 中 |
| BUG-028 | 修复 | P3.6 迁移工具分类语义未真正生效：`confirm`/低置信来源仍会被 `apply()` 自动写入 YAML，与"需人工确认后才进入 apply"不一致 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `migrate_branding.py` 新增 `_can_auto_apply()` 门禁，`build_branding_yaml()` 仅写入 `auto` 且非低置信字段；`copy_assets()` 仅复制最终 YAML 会引用的自动 apply 资产；`confirm` 和低置信 auto 项进入报告人工确认区。新增回归测试覆盖 `theme_primary_svg` 不写入、项目根目录低置信 Logo 不写入/不复制；迁移工具测试 79 passed，品牌配置测试 68 passed，全量 645 passed |
| BUG-029 | 修复 | 品牌配置未区分登录页与工作页 Logo：通用 logo 会被自动同时映射到 `login_logo` 和 `topbar_logo`，导致顶栏错误复用登录页大字标 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `branding_config.py` 自动发现改为通用 logo 默认只填 `login_logo`，仅显式 `topbar`/`icon` 命名资产才填 `topbar_logo`；`migrate_branding.py` 的 scan/plan/apply 不再把通用 `logo_file` 自动写入 `topbar_logo_file`；`auth.js`/`main.css` 为 login/topbar logo 拆分独立类；本地 `runtime/config/ui-branding.yaml` 清空 `topbar_logo` 以恢复工作页默认小图标；定向 `tests/test_branding.py tests/test_migrate_branding.py` 148 passed，全量 `python3 -m pytest -q` 651 passed |
| BUG-030 | 修复 | 单 logo 文件时登录页与顶栏应互作 fallback 并按各自尺寸渲染：BUG-029 阻止了通用 logo 回退到顶栏，导致用户只放一张图时顶栏仍显示默认 SVG | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `branding_config.py` `_discover_branding_assets()` 单文件兜底：识别出的 login_logo/topbar_logo 若只有一个，回退填充到另一处；`auth.js` `Branding.apply()` `logoUrlMap` 互作 fallback：顶栏缺图时回退到 login_logo，登录页缺图时回退到 topbar_logo；CSS 已保留 `.branding-logo-login`(72px) 与 `.branding-logo-topbar`(28px) 独立尺寸；更新 `test_discover_logo_from_branding_dir`、`test_auto_discovery_used_when_yaml_missing` 断言，新增 `test_auto_discovery_topbar_fallback_to_login`、`test_auto_discovery_login_fallback_to_topbar`、`test_auth_js_has_logo_fallback`；72 品牌测试 passed，全量 `python3 -m pytest -q` 654 passed |
| BUG-031 | 修复 | 旧版 runtime 使用历史字段 `login_notice_text` 时，新版运行时和迁移扫描只识别 `login_notice`，导致登录页提示内容没有被保留 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已修复 | `branding_config.py` 新增旧字段别名归一化：`login_notice_text` fallback 映射为 `login_notice`，并覆盖 runtime YAML、config.yaml、`_merge_branding()`；`migrate_branding.py scan_runtime()` 支持旧字段兜底且不覆盖新版字段；`update.sh` 同步收口为只 scan/plan 不自动 apply；新增 3 项旧字段兼容回归测试；定向 154 passed，全量 657 passed |
| BUG-032 | 修复 | `scan_runtime()` 对通用 `logo.png` 只写入 `topbar_logo_file`（medium）而不写 `login_logo_file`，手动 `apply` 时顶栏误用大 Logo、登录页无图，与 BUG-029 语义相反 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已修复 | `scan_runtime()` logo 映射逻辑改为：显式 topbar/header/nav/icon/mark/symbol → `topbar_logo_file`(medium)；显式 login/brand → `login_logo_file`(high)；通用 logo → `login_logo_file`(medium) + `topbar_logo_file`(low 回退)；与 `_discover_branding_assets()` 语义一致；新增 4 项回归测试；全量 663 passed |
| BUG-033 | 修复 | 存在空的 `ui-branding.yaml`（含空 `login_logo`/`topbar_logo`）时，运行时资产自动发现被跳过，仅有 branding 资产目录的旧部署无法兜底 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已修复 | `get_branding_config()` 改为始终调用 `_discover_branding_assets()`，发现结果只填充 yaml 中为空的资产字段，不覆盖非空 yaml 值；yaml 优先级不变，自动发现只补空；新增 2 项回归测试（空资产字段填充、非空字段不被覆盖）；全量 663 passed |
| BUG-034 | 修复 | CHK-041 P0 对照审查发现团队资料库 MVP 仍有验收缺口：新建审查项目未写入 workspace_id，项目引用资料未校验同 workspace/成员权限/资料状态，资料列表缺少 tag/status API 过滤，资料详情缺少正文预览、原文件下载和引用项目信息，前端资料库管理按钮受成员加载顺序影响初始隐藏 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已修复 | `_project_to_info()` 补齐 `workspace_id` 响应映射；前端资料下载从裸链接改为 `API.downloadWorkspaceSource()` 携带 Bearer 鉴权后下载 blob；补充前端下载鉴权契约测试；P0 定向 91 passed，全量 `python3 -m pytest -q` 754 passed（3 条 aiosqlite 线程警告，无失败） |
| BUG-035 | 修复 | update.sh 旧 yaml 中 app_version 与代码实际版本不一致：runtime/config/ui-branding.yaml 中的旧版本号会覆盖代码 DEFAULT_BRANDING 中的新版本号，导致前端显示版本滞后 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已修复 | update.sh 新增 `sync_yaml_version()` 函数，在代码替换后、启动前自动将 yaml 中的 app_version 同步为 $NEW_VERSION；Python 行级替换保留注释、格式和其他字段；新增 5 项契约测试覆盖函数存在/调用顺序/已有版本更新/缺失版本插入/注释跳过/无引号版本处理；759 全量回归通过 |
| BUG-036 | 修复 | 四个页面侧边栏宽度应跟随各页面 topbar 徽章右侧分割线动态对齐，收起按钮应始终右对齐且位于底部（展开和折叠状态下均靠右侧底部） | 2026-06-05 00:00 | 2026-06-05 00:00 | 已修复 | 恢复 `_alignSidebarToDivider()` 函数及所有调用点（init/showChat/showAdmin/showReview/showWorkspace）+ resize handler；`.sidebar-toggle` 改为 `justify-content: flex-end; padding: var(--sp-3) var(--sp-5); margin-top: auto;`（右对齐+推至底部）；collapsed 状态统一 `padding: var(--sp-3)` 适配 56px 窄宽；`test_web_interface_guidelines.py` JS-003 改为检查 `_alignSidebarToDivider` 和 `_resizeTimer` 存在；759 全量回归通过 |
| BUG-037 | 修复 | P1.B.2 审查域 active member 校验未覆盖旧项目 owner 访问路径：成员被停用后仍可通过 `created_by` 访问 `GET /api/review/projects`、项目详情、文档、审查任务、上下文等接口 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已修复 | `_verify_project_owner()` 对 workspace_id=None 的 legacy 项目回退到默认 workspace 校验，不再跳过；`list_projects` 对 legacy 项目也做可见性过滤；新增 3 项 API 回归测试 + 2 项前端契约测试；791 全量回归通过 |
| BUG-038 | 修复 | P1.A.2/P1.A.3 停用成员从成员列表消失，前端恢复按钮不可达 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已修复 | 后端 `GET /api/workspace/default/members` 已使用 `list_members_all()` 返回含 inactive 成员；前端 `_renderMemberTable` 已有 `reactivate-member` 渲染分支 + owner 角色选项；新增 1 项 API 回归测试 + 2 项前端契约测试；791 全量回归通过 |
| BUG-039 | 修复 | P1.A.1 `PUT /api/workspace/default` 忽略 `status` 字段，与 WBS 中”更新团队名称、描述、状态”契约不一致 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已修复 | `update_default_workspace()` 已处理 `status` 字段，校验 active/archived 值并落库；新增 2 项 API 回归测试 + 1 项前端契约测试；791 全量回归通过 |
| BUG-040 | 修复 | P1.A.3 成员角色变更 UI 未满足 WBS 角色确认契约：下拉缺少 owner 选项，角色变更没有确认弹窗，owner 降级没有二次确认 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已修复 | `workspace.js` 角色下拉已包含 owner/admin/member/viewer 四选项；`_changeMemberRole` 已有 `_confirmMemberAction` 确认弹窗和 `_confirmRoleChange` owner 降级二次确认；新增 2 项前端契约测试；791 全量回归通过 |
| BUG-041 | 修复 | P2 EmbeddingService `_call_api()` 中 `time.sleep()` 阻塞 asyncio event loop，导致 rate limit 和重试期间所有并发请求被饿死 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | `embedding_service.py` 两处 `time.sleep(delay)` 改为 `await asyncio.sleep(delay)`；新增 `import asyncio`；833 全量回归通过 |
| BUG-042 | 修复 | P2 KnowledgeIngestionService 重入库时 content-less FTS5 表残留旧条目（幽灵结果）：`delete_by_source_id` 删除 chunks 表但 FTS5 独立存储不自动同步 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | `knowledge_ingestion.py` 新增 `_cleanup_fts_entries(source_id)` 方法，在 `ingest_source()` 删 chunks 之后、插新条目之前显式 `DELETE FROM knowledge_chunks_fts WHERE source_id = :source_id`；833 全量回归通过 |
| BUG-043 | 修复 | P2 KnowledgeIngestionService `search_fts()` FTS5 MATCH 查询未转义特殊字符：`"` `*` `(` `)` 等导致 FTS5 语法错误崩溃 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | `knowledge_ingestion.py` 新增 `_sanitize_fts5_query()` 函数，移除控制字符和空字符；833 全量回归通过 |
| BUG-044 | 修复 | P2 review.py `_load_project_knowledge_context()` 无 chunk 数量限制，全部加载可致 OOM 或 LLM token 溢出 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | 新增 `MAX_CHUNKS_PER_PROJECT = 50` 常量，查询加 `.limit(50)`；833 全量回归通过 |
| BUG-045 | 修复 | P2 RetrievalService 拒答策略逻辑 bug：文档说"top-1 超阈值则全部拒答"，但实现逐条判断，导致 top-1 被拒而 top-2（距离更远）反而被接受 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | `_apply_rejection_policy()` 改为先判断 `search_results[0]._distance > threshold`，若成立则一次性返回全部 rejected 结果；833 全量回归通过 |
| BUG-046 | 修复 | P2 KnowledgeIngestionService `_ensure_fts_index()` N+1 查询：每个 chunk 执行 2 次查询获取 source_id 和 workspace_id，200 chunks = 400 次额外查询 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | 将 source_id/workspace_id 查询提到循环外一次性执行；833 全量回归通过 |
| BUG-047 | 修复 | P2 KnowledgeDocument.source_id 缺少唯一约束，与文档描述的 1:1 关系不一致；KnowledgeChunk 缺少 (document_id, chunk_no) 唯一约束 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | `knowledge.py` KnowledgeDocument 加 `unique=True` + `UniqueConstraint("source_id")`；KnowledgeChunk 加 `UniqueConstraint("document_id", "chunk_no")`；KnowledgeChunk.document_id 和 embedding_status 加 `index=True`；833 全量回归通过 |
| BUG-048 | 修复 | P2 KnowledgeIngestionService `search_fts()` 未校验 trigram tokenizer 最小 3 字符要求，短于 3 字符的查询导致 FTS5 运行时错误 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | `search_fts()` 开头加 `len(sanitized) < 3` guard，不足时返回空列表；新增 `_FTS_TRIGRAM_MIN_LENGTH = 3` 常量；833 全量回归通过 |
| BUG-049 | 修复 | P2 workspace.py `update_source_tags()` 标签校验不完整：只校验外层 list 类型，不校验元素类型，且无数量上限 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | 新增逐元素 `isinstance(tag, str)` 校验 + `len(tags) > 20` 上限检查；833 全量回归通过 |
| BUG-050 | 修复 | P2 ChatApplicationService `build_knowledge_context()` 结果编号跳号：rejected 条目被 `continue` 跳过但编号仍递增，输出形如 `1. ... 3. ... 5. ...` | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | 改用独立 `seq` 计数器，仅在非 rejected 条目时递增；833 全量回归通过 |
| BUG-051 | 修复 | P2 workspace.py `download_source_file()` Content-Disposition 头未 sanitize 文件名：含 `\r\n` 的文件名可致 HTTP header 注入 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | 文件名清理：`replace('"', '_').replace('\r', '').replace('\n', '')`，值用双引号包裹；833 全量回归通过 |
| BUG-052 | 修复 | 提交 `5d1dd6ab62bb76779670f1733dd9159d1d021a15` 复查发现 P2 检索可用性缺口：上传后未自动入库、LanceDB 空索引不降级 FTS、删除/重入库后索引残留、FTS 特殊字符未真正清理、审查上下文仍可能注入 archived 资料 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | `upload_source()` 上传后同步 `KnowledgeIngestionService.ingest_source()` 建立 chunk+FTS；`RetrievalService.retrieve()` LanceDB 不可用时跳过 embedding 并在空结果时降级 FTS；`delete_source()` 和 `ingest_source()` 清理 LanceDB/FTS 旧索引；`_sanitize_fts5_query()` 移除 FTS 语法字符和 AND/OR/NOT；`_load_project_knowledge_context()` 仅注入 active source；新增 5 项回归测试，887 全量回归通过 |
| BUG-053 | 修复 | 提交 `5d1dd6ab62bb76779670f1733dd9159d1d021a15` 触碰 `chat.js sendMessage()` 后复查发现 SSE 解析仍按网络 chunk 直接 split 行，跨 chunk JSON 半行会被丢弃导致回复内容缺失 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已修复 | `chat.js` 新增 `sseBuffer` 累积未完整行，按换行解析完整 SSE 行并保留尾部半行；新增前端契约测试 `test_chat_sse_parser_buffers_partial_lines`；887 全量回归通过 |
| BUG-054 | 修复 | Pi Agent 配置更新过滤 `None`，导致已配置的可空字段（Search/Vision API Base、Extension path、Skill registry、System Prompt 等）无法被清空 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `pi_agent_config_repository.py` 为 7 个可空字段引入 `_UNSET` sentinel（与 `model_config_repository.py` 同一模式），区分未传与显式清空；`pi_agent.py` update endpoint 改用 `req.model_fields_set` 只传显式提供的字段；新增 4 项回归测试（clear single、clear all、omitted unchanged、clear system_prompt）；897 全量回归通过 |
| BUG-055 | 修复 | Pi Agent 连接测试忽略 `llm_provider`，UI 允许 Anthropic/OpenAI/DeepSeek 等 provider，但后端固定按 OpenAI-compatible `check_connection()` 测试 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `pi_agent.py` test-connection 和 speed-test 端点加载 `config.llm_provider` 并与 `_OPENAI_COMPATIBLE_PROVIDERS` 集合（deepseek/openai/openai_compatible）比对；不匹配时返回明确的 unsupported status 而非误导性连接错误；新增 4 项回归测试（anthropic rejected x2、deepseek/openai_compatible proceed）；897 全量回归通过 |
| BUG-056 | 修复 | `PiAgentConfigRepository` 单行配置没有唯一约束，并发首次访问可能创建多行，后续 `scalar_one_or_none()` 抛 `MultipleResultsFound` | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `PiAgentConfig` 模型新增 `singleton_key` 列（`String(20)`, `unique=True`, `default="default"`）；`database.py` 新增 `_migrate_pi_agent_singleton()` 迁移函数（添加列、去重、建唯一索引）；`get_or_create()` 改用 ORM add + `IntegrityError` 捕获防并发重复；`_ensure_pi_agent_config()` 同步改为 ORM + IntegrityError 模式；新增 2 项回归测试（并发 10 次 get_or_create 只产生 1 行、同 key 重复 INSERT 抛 IntegrityError）；897 全量回归通过 |
| BUG-057 | 修复 | Pi Agent 前端数值字段使用 `\|\|` 默认值，合法的 `temperature=0` 会在重新打开页面时显示为 `0.7` 并可能被覆盖 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `admin.js` 第 1217/1221 行将 `cfg.llm_max_tokens \|\| 4096` 和 `cfg.llm_temperature \|\| 0.7` 改为 `!= null` 显式判空；同步补齐 `cfg.search_max_results` 与 `cfg.extension_max_tool_calls` 两处同类数值默认值渲染，保留合法 0 值；JS 语法检查通过，目标前端测试 150 passed，全量 897 passed |
| BUG-058 | 修复 | POC-B v3 脚本 `read_all_stderr()` 在 Pi 子进程仍运行时调用 `stderr.read()`，可能等待 EOF 导致脚本卡死 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `run_pocb_pi_rpc_v3.py` `read_all_stderr()` 改为：进程已退出时安全 `read()` 直到 EOF；进程仍存活时用 `select` + `os.read` 非阻塞读取已有 stderr 数据 |
| BUG-059 | 修复 | POC-B v2 脚本 `read_stderr()` 在 `select` 可读后仍调用 `.read()`，子进程未退出时可能阻塞 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `run_pocb_pi_rpc_v2.py` `read_stderr()` 改为：进程已退出时安全 `read()` 直到 EOF；进程仍存活时用 `select` + `os.read` 非阻塞读取已有 stderr 数据；与 v3 `read_all_stderr()` 同一模式；Python 语法检查通过 |
| BUG-060 | 修复 | Pi Agent Extension 中 `MAX_STEPS` 只打印不执行拦截/停止，步数限制能力与注释和 POC 目标不一致 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `agent-limiter.ts` 的 `turn_end` 已补充步数递增与 `MAX_STEPS` 超限告警日志；因当前仓库未引入可验证的 Pi Extension 终止 API，安全实现口径收窄为可观测拦截（stderr `[EXCEEDED]`）而非强制进程终止 |
| BUG-061 | 修复 | POC-B v3 脚本仍读取旧 `model_configs.model_id='pi-agent'` API Key，与新增 `pi_agent_config` 独立配置源不一致 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `run_pocb_pi_rpc_v3.py` 已优先读取 `pi_agent_config.llm_encrypted_api_key`，缺失时回退兼容旧 `model_configs.model_id='pi-agent'`，避免新配置页上线后历史脚本取不到 Key |
| BUG-062 | 修复 | agent.py router 使用本地 `get_db()` 而非 `app.database.get_db`，绕过 FastAPI `dependency_overrides`，导致 ASGI 测试无法注入测试数据库 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `agent.py` 改为从 `app.database` 导入 `get_db`；删除本地 `async_session` import 和本地 `get_db` 定义；`create_authorization` 和 `create_run` endpoint 的 "profile not found" 改为自动创建（与 GET /profile 行为一致）；912 全量回归通过 |
| BUG-063 | 修复 | test_agent_p3.py 使用硬编码 URL 和缺失的 `http_client` fixture，无法运行 ASGI 测试 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | 重写为使用 `make_test_app` + `ASGITransport` 测试模式；conftest.py 添加 agent router 注册；所有 URL 改为相对路径 `/api/agent/...`；15/15 P3 测试通过 |
| BUG-064 | 修复 | 旧运行库中 `pi_agent_config.extension_path` 可能仍保存 `poc-b/scripts/extensions/agent-limiter.ts`，PiAgentBridge 会优先使用旧 POC 路径，导致正式 Extension 目录迁移后运行态仍指向旧位置 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已修复 | `PiAgentBridge._resolve_extension_path()` 新增旧 POC 路径兼容和不存在路径回退：配置为空、包含旧 `poc-b/scripts/extensions/agent-limiter.ts` 或文件不存在时统一回退到 `src/agent/extensions/agent-limiter.ts`；运行面发现 17957 仍有旧配置，代码层已兼容 |
| BUG-065 | 修复 | P4 二次复核7项高置信 Bug：① decide_round() 任意用户可审批 ② 协作审查接口缺少权限校验 ③ presentation 模式可读任意项目上下文 ④ 旧库迁移先 UPDATE 再 ALTER TABLE 崩溃 ⑤ 通知 SSE 用 Bearer 但 EventSource 不带头 ⑥ 驳回重新提交丢失 approver_id ⑦ chat.js 发消息不传 mode/project_id | 2026-06-11 00:00 | 2026-06-11 00:00 | 已修复 | Bug1: decide_round 加 approver 权限校验（指定审批人/Approver角色）；Bug2: 7个接口加项目归属/参与者校验+_can_access_request 辅助函数；Bug3: build_presentation_context 加 user_id 权限校验；Bug4: 迁移改为先 ALTER 再 UPDATE；Bug5: notification SSE 改用 ticket 认证；Bug6: resubmit 继承上一轮 approver_id + 通知审批人；Bug7: chat.js chatStream 补传 mode/project_id；新增 13 项 bugfix 回归测试；1000 全量回归通过 |
| BUG-066 | 修复 | 删除项目时 500：ReviewRequest.project_id 有 NOT NULL 约束，SQLAlchemy lazy flush 先设 NULL 再 CASCADE，导致 IntegrityError | 2026-06-11 00:00 | 2026-06-11 00:00 | 已修复 | `review.py` delete_project 在 `db.delete(p)` 前手动删除关联的 ReviewRound、ReviewParticipant、ReviewRequest；实机验证删除成功，1000 回归通过 |
| BUG-067 | 修复 | auth.js 插入 _bindPasswordToggles 时产生编辑残留（savePassword 尾部 5 行重复），导致 JS 语法错误，页面卡在加载中 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已修复 | 删除第 136-141 行重复残留代码；新增 JS-004 自动化测试：所有前端 JS 文件必须通过 `node --check` 语法检查，防止类似问题再漏出 |
| BUG-068 | 修复 | 密码可见性切换图标语义反转 + 图标消失：① 隐藏态应显示 eye-off（看不见）而非 eye（能看见）② icon-eye-off 内联 `style="display:none"` 优先级高于 CSS 无法覆盖导致切换后图标消失 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已修复 | ① CSS 翻转：默认态 icon-eye 隐藏 + icon-eye-off 显示（浅灰 `--gray-5`），`.showing`态 icon-eye 显示（纯黑 `--color-text`）+ icon-eye-off 隐藏；② HTML/auth.js 移除所有内联 `style="display:none"`，CSS 加 `!important` 兜底；更新 US-01 契约测试覆盖翻转语义+颜色区分；1011 全量回归通过 |
| BUG-069 | 修复 | 智能对话页侧边栏宽度与其他三个页面不一致：`.chat-shell` grid 第一列用 `var(--sidebar-width)` 固定 280px，JS `_alignSidebarToDivider()` 内联宽度无法突破 grid column 限制 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已修复 | `.chat-shell` grid-template-columns 第一列从 `var(--sidebar-width)` 改为 `auto`，使 sidebar 实际宽度（由 JS 对齐 topbar divider 计算）生效；响应式断点同步改为 `auto`；保留 `_alignSidebarToDivider()` 及全部调用点；更新 2 项契约测试；1011 全量回归通过 |
| BUG-070 | 修复 | 登录/注册切换时卡片高度跳动：部署提示 notice 在 `#login-form-block` 内绝对定位浮在上方，注册态没有 notice 导致内容上移 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已修复 | notice HTML 从 login-form-block 移到 `.auth-card-inner` 共享容器（与两态 form-block 同级），CSS 从绝对定位改为正常文档流+`margin-bottom`，低高度响应式改为 sticky；两态切换时标题 Y 位置一致无跳动；1011 全量回归通过 |
| BUG-071 | 修复 | P5/P6 代码审查发现 8 个 bug：① governance.py `User.role` 类属性误用 ② governance.py SkillConfig 无 metadata_json 导致 AttributeError ③ workspace.js personal scope 删除按钮不可见 ④ workspace.js _showSourceDetail personal scope 调错 API ⑤ workspace.js _downloadSource personal scope 无下载端点 ⑥ KnowledgeSourceRepository 缺 selectinload(owner) 可能 MissingGreenlet ⑦ quality_stats_service.py ReviewTask 无 metadata_json 导致 avg_score 始终 0 ⑧ app.js Agent 设置模态框 allowed_tools_json 应为 allowed_tools | 2026-06-11 00:00 | 2026-06-11 00:00 | 已修复 | ① 简化为 `user.role != "admin"` ② 改为输出 SkillConfig 实有字段 ③ personal scope 始终显示删除按钮 ④ 按 scope 调用不同 API ⑤ 后端新增 GET /api/personal/sources/{id}/download + 前端 API.downloadPersonalSource + scope 分支 ⑥ list_by_workspace/list_personal_sources 添加 selectinload(KnowledgeSource.owner) ⑦ 改为从 DocAnalysis.quality_score 计算平均分 ⑧ 改为 `profile?.allowed_tools \|\| []`；1051 全量回归通过 |
| BUG-072 | 修复 | governance.py BudgetUpdate 缺少数值校验：monthly_token_limit/monthly_cost_limit 可传负值，warning_threshold_pct 无上下限约束 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已修复 | BudgetUpdate 字段改为 pydantic Field(ge=0/ge=0/ge=0,le=100)；新增 5 项 BudgetUpdate 校验回归测试 |
| BUG-073 | 修复 | governance.py update_budget 响应缺少 id 字段，与 get_budget 不一致 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已修复 | update_budget return dict 新增 `"id": budget.id` |
| BUG-074 | 修复 | governance.py get_budget/update_budget 不验证 workspace 存在性，可创建孤儿 budget 记录 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已修复 | 两个端点新增 Workspace 存在性查询，不存在时返回 404 |
| BUG-075 | 修复 | governance.py require_action 导入未使用 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已修复 | 移除未使用的 require_action 导入 |
| BUG-076 | 修复 | workspace.py delete_source 个人资料不校验 workspace_id 归属，审计日志上下文可能错误 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已修复 | 个人资料分支新增 `source.workspace_id != workspace_id` 校验（workspace_id 非 None 时） |
| BUG-077 | 修复 | cost_stats_service.py 聚合时 mode 始终硬编码为 "chat"，与 P6.A.1 "按模式统计" 验收标准不符 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已修复 | mode 从模型 ID 推断（含 review→review, agent→agent, 否则 chat）；workspace_id/user_id 保持 None 作为已知限制（JSONL 中无此字段） |
| BUG-078 | 修复 | 团队资料列表默认不过滤 owner_type/visibility，可暴露同 workspace 其他成员的私有资料 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已修复 | list_by_workspace/count_by_workspace 默认只返回 owner_type=workspace + visibility=team 的团队可见资料；个人私有资料只能通过 /personal/sources 访问；新增 3 项隔离回归测试 |
| BUG-079 | 修复 | 对话页 Agent 模式只 createAgentRun + getAgentRun，未调用 POST /runs/{id}/stream，Pi 子进程从未启动，用户看到空结果 | 2026-06-24 00:00 | 2026-06-24 00:00 | 已修复 | api.js 新增 agentStream(runId)；chat.js Agent 分支先 SSE 监听 Pi 事件（message_update 实时渲染），流结束后再 getAgentRun 补全 traces/steps；抽取 _buildAgentRunHtml；新增契约测试 test_agent_mode_streams_run_before_fetching_result |
| BUG-080 | 修复 | 仓库缺失 `src/app/storage/` 模块（review/chat/knowledge file storage），review.py 等无法 import，应用与全量 pytest 启动失败 | 2026-06-25 08:30 | 2026-06-25 08:45 | 已修复 | 从 commit 13a15af 恢复 `__init__.py`、`chat_file_storage.py`、`review_file_storage.py`、`knowledge_file_storage.py` |
| BUG-081 | 修复 | BUG-079 二次审查：Agent 前端未处理 run.status=failed、Mermaid 不渲染、tool_blocked/step_limit_exceeded 无提示、流式无滚动 | 2026-06-25 08:30 | 2026-06-25 08:45 | 已修复 | chat.js 抽取 processAgentSseLine；补 failed 态/error_message、_renderMermaidOnStreamEnd、_scrollBottom、SSE 尾行处理；扩展契约测试 |
| BUG-082 | 修复 | BUG-077 二次审查：`"pi" in model` 会把 `pipeline` 等模型误判为 agent 模式 | 2026-06-25 08:30 | 2026-06-25 08:45 | 已修复 | 提取 `infer_mode_from_model()`，改用 pi-agent/pi- 前缀等精确规则；新增 3 项回归测试 |
| BUG-083 | 修复 | get_budget 无 budget 记录时响应缺少 `id` 字段，与有记录/update_budget 响应不一致 | 2026-06-25 08:30 | 2026-06-25 08:45 | 已修复 | 默认响应补 `"id": None`；新增 API 回归测试 test_get_budget_without_record_includes_null_id |
| BUG-084 | 修复 | P4 协作审查 E2E 无法完成审批：前端发起不传 `approver_ids`，详情弹窗无通过/驳回按钮，`decideReviewRound` 未被调用 | 2026-06-26 10:00 | 2026-06-26 15:30 | 已修复 | 后端要求至少 1 名 approver；`review.js` 增加审批人多选、`_decideCollabRound` 决策 UI；`list_review_requests` 审批人可按项目过滤；契约+API 回归 |
| BUG-085 | 修复 | Review 项目列表与详情权限不一致：owner/admin 在列表可见成员项目，但 `_verify_project_owner()` 要求 `created_by==user_id` 导致详情/上传/审查 404 | 2026-06-26 10:00 | 2026-06-26 15:30 | 已修复 | `_verify_project_owner()` 与 `list_projects` 对齐；`ReviewProjectRepository.get_project_if_accessible()` + `user_can_access_project()` |
| BUG-086 | 修复 | 个人私有资料在 workspace 检索/详情/下载路径泄露：列表已过滤（BUG-078），但向量/FTS 仅按 workspace_id 过滤，详情/下载未校验 owner_type/visibility | 2026-06-26 10:00 | 2026-06-26 15:30 | 已修复 | 向量 prefilter 排除他人 private；FTS `_allowed_source_ids_for_user` 后过滤；workspace get/download `_assert_team_source_readable()`；检索链路传 user_id |
| BUG-087 | 修复 | Agent 审批决策缺少审批人校验：任意登录用户知道 request_id 可代批/代拒 | 2026-06-26 10:00 | 2026-06-26 15:30 | 已修复 | `decide_approval()` 校验 `approval.approver_id == user.id` |
| BUG-088 | 修复 | Agent 授权撤销缺少归属校验：知道 auth_id 可撤销他人 Agent 授权 | 2026-06-26 10:00 | 2026-06-26 15:30 | 已修复 | `revoke_authorization()` 校验 auth 属于当前用户 profile；`AgentAuthorizationRepository.get_by_id()` |
| BUG-089 | 修复 | CHK-077 遗留：`archive_agent` TOCTOU 竞态，活跃运行检查与 status 更新非原子 | 2026-06-26 10:00 | 2026-06-26 15:30 | 已修复 | `archive_agent()` 使用 `select(...).with_for_update()` 行锁后再检查活跃 run |
| BUG-090 | 修复 | P6 治理配额 `hard_limit_action=block` 仅可配置不可拦截，chat/agent/review 路径不读取 WorkspaceBudget | 2026-06-26 10:00 | 2026-06-26 15:30 | 已修复 | 新增 `budget_guard.py`；chat/agent run/stream 调用 `ensure_workspace_llm_allowed()`；基于 CostDailySummary 当月 token 汇总 |
| BUG-091 | 修复 | 通知分页前后端契约不一致 + Agent 审批 deep link 无效：`api.js` 传 page/page_size，后端用 limit/offset；`Admin._showAgentApprovals` 不存在 | 2026-06-26 10:00 | 2026-06-26 15:30 | 已修复 | 后端兼容 page/page_size；前端改 limit/offset；`admin.js` 新增 `_showAgentApprovals()` |
| BUG-092 | 修复 | BUG-084 回归：`create_review_request` 中 approver_ids 的 422 校验放在权限校验（403）之前，导致非项目创建者因缺少 approver_ids 收到 422 而非 403，既泄露接口存在性也与越权拦截语义不一致；同时 `test_p4_collaborative.py`/`test_bugfix_7issues.py` 中 8 处创建请求测试未同步补 approver_ids 导致 KeyError/422 | 2026-06-28 09:00 | 2026-06-28 09:20 | 已修复 | `review_request.py` 调整校验顺序：先 404（项目存在）→ 403（权限）→ 422（approver_ids）；补全 p4/7issues 测试的 approver_ids 参数；全量 1067 回归通过 |
| BUG-093 | 修复 | admin.js `_govTodayStr`/`_govDaysAgoStr` 仍用 `.slice(0,10)` 格式化日期，违反 JS-001 统一使用 Intl.DateTimeFormat 的前端规范 | 2026-06-28 09:00 | 2026-06-28 09:20 | 已修复 | 抽取 `_govDateStr()` 用 `Intl.DateTimeFormat().formatToParts()` 生成 YYYY-MM-DD（后端成本统计所需格式），`node --check` 通过 |
| BUG-094 | 修复 | `branding_config._validate_asset_path` 仅用 `Path.is_absolute()` 判断绝对路径，Windows 上 `Path("/etc/passwd").is_absolute()` 返回 False 导致 Unix 风格绝对路径被错误放行，与 `migrate_branding._validate_asset_path_safe`（BUG-018 已修）约束不一致 | 2026-06-28 09:00 | 2026-06-28 09:20 | 已修复 | 额外检查 `path_str.startswith(("/", "\\"))` 拒绝 Unix 风格绝对路径，跨平台行为一致 |
| BUG-095 | 修复 | `.gitignore`（ADJ-018）放行 `runtime/config/ui-branding.example.yaml` 但该模板文件从未创建，前端契约测试 `test_example_yaml_exists` 失败 | 2026-06-28 09:00 | 2026-06-28 09:20 | 已修复 | 新增 `runtime/config/ui-branding.example.yaml` 模板，含全部字段注释与默认值 |
| BUG-096 | 修复 | `migrate_branding._check_write_path_allowed` 用 `str(rel).startswith(allowed)` 匹配白名单，Windows 上相对路径用反斜杠（`assets\branding\...`）与白名单正斜杠（`assets/branding`）不匹配，导致合法 branding 资产写入被误拒 | 2026-06-28 09:00 | 2026-06-28 09:20 | 已修复 | 比较前统一 `rel_str.replace("\\", "/")` 归一化路径分隔符 |
| BUG-097 | 修复 | Windows 平台测试 teardown 文件锁定：16 个测试文件 fixture/内联 teardown 在 `os.unlink(tmp_db)` 前未 `await engine.dispose()`，aiosqlite 连接未释放导致 WinError 32，产生 158 个 error/failed 噪声（不影响 Linux 生产） | 2026-06-28 09:00 | 2026-06-28 09:20 | 已修复 | 全部 fixture teardown 增加 `await engine.dispose()` + `try/except PermissionError`；内联 teardown 同步包裹；跨平台测试路径改用 `tempfile.gettempdir()`；`test_update_script` 在非 POSIX 跳过 +x/bash 检查 |
| BUG-098 | 文档 | CHK 审查记录：`governance.archive_agent` 声称用 `with_for_update()` 消除 TOCTOU（BUG-089），但 SQLite/aiosqlite 忽略 `FOR UPDATE` 不产生行锁，并发归档仍有竞态窗口；属低风险（SQLite 写串行 + 单机场景） | 2026-06-28 09:00 | 2026-06-28 09:20 | 已关闭 | 当前 SQLite 单机部署下实际风险低；若迁移到 PostgreSQL 等支持行锁的库则 `with_for_update()` 自然生效；暂不改动，记录为已知限制 |
| BUG-099 | 修复 | 评审进度 SSE 端点权限校验仍使用 `created_by==user`，与项目详情/上传/审查等 `_verify_project_owner()` 的 workspace 可见性规则不一致，owner/admin 无法跟踪成员项目进度 | 2026-07-07 15:18 | 2026-07-07 15:18 | 已修复 | `review_progress_sse()` 改用 `_verify_project_owner()`；新增 `tests/test_bugfix_099_104.py` 覆盖 workspace owner 访问成员项目 SSE |
| BUG-100 | 修复 | chat 流式对话 finish chunk 已有真实 usage，但未写入 `log_llm_session`，导致成本统计和 BudgetGuard 对 chat 流量不可见，前端 token_count 也使用 chunk 数 | 2026-07-07 15:18 | 2026-07-07 15:18 | 已修复 | `chat.py` 在 finish 分支记录 `llm_sessions.jsonl`，写入 response/usage/elapsed_ms/reasoning_content；SSE done token_count 与 assistant message token_count 改用真实 usage |
| BUG-101 | 修复 | MCP 工具策略 `allowed_roles_json` 解析失败时被吞掉并按空列表处理，导致损坏策略 fail-open 放行所有角色 | 2026-07-07 15:18 | 2026-07-07 15:18 | 已修复 | `mcp_adapter.check_policy()` 对 JSON 解析失败或非 list 结构 fail-closed；新增损坏 JSON 策略拒绝测试 |
| BUG-102 | 修复 | 通知 SSE 按 user_id 共享单个 list，任一连接断开会 `pop` 全局 channel，破坏同用户多标签页/多端实时通知 | 2026-07-07 15:18 | 2026-07-07 15:18 | 已修复 | 通知 channel 改为每连接独立 list，`clear_channel(user_id, channel)` 只移除当前连接，推送时广播到该用户所有在线连接 |
| BUG-103 | 修复 | `resubmit_review_request` 的通知失败复用主 `AsyncSession` 且吞异常不回滚，可能提交半应用通知或污染主事务 | 2026-07-07 15:18 | 2026-07-07 15:18 | 已修复 | 复提交通知包裹在 `db.begin_nested()` savepoint 中，通知异常只回滚通知写入并记录 warning，主流程继续提交 |
| BUG-104 | 修复 | 协作审查发起人同时出现在 `approver_ids` 时会插入重复 `ReviewParticipant`，同一用户出现 Reviewer/Approver 两条记录 | 2026-07-07 15:18 | 2026-07-07 15:18 | 已修复 | `ReviewParticipantRepository.add_participant()` 按 `(request_id,user_id)` 幂等写入并按角色优先级升级；参与者查询改为稳定取首条；旧参与者测试改为真实双用户场景 |
| BUG-105 | 修复 | `cost_stats_service.aggregate_daily` upsert 查询缺 workspace/user 维度，可能覆盖同 date/model/mode 的 workspace 专属统计行或多行时报错 | 2026-07-07 15:18 | 2026-07-07 15:18 | 已修复 | upsert 查询补 `workspace_id` 与 `user_id` 过滤（含 NULL 维度）；新增全局聚合不覆盖 workspace 专属行测试；验证：新增回归 7 passed、相邻回归 71 passed、全量 `pytest -q` 1074 passed |
| BUG-106 | 修复 | `resubmit_review_request` 虽把通知写入包进 savepoint，但 `NotificationService.notify_review_request_created()` 在事务提交前就执行 `_push_event()`；一旦后续 approver 通知创建异常触发 savepoint 回滚，数据库通知会消失，但已在线用户仍会收到幽灵 SSE 消息 | 2026-07-07 17:20 | 2026-07-07 18:10 | 已修复 | 复现：内存库中让第 2 次 `NotificationRepository.create()` 抛异常，结果 `Notification` 行数回滚为 0，但第 1 个 approver 的 channel 里仍残留 1 条事件；修复：`NotificationService` 新增 `defer_push` 缓冲机制（`_push_event` defer 时仅缓冲、`flush_pending` commit 成功后推送 / `discard_pending` savepoint 回滚时丢弃），`review_request` 创建/decide/resubmit + `artifact` 确认 + `notification` 评论端点全部改用 `defer_push=True` + commit 后 flush；新增 `tests/test_bugfix_106_107.py`（6 项，含 defer/discard/flush 单元 + 集成幽灵场景 + 重复参与者去重/收敛）；全量 pytest 1087 passed |
| BUG-107 | 修复 | BUG-104 仅阻止后续重复写入，未处理存量脏数据；`ReviewParticipantRepository.list_by_request()` 仍直接返回所有行，升级后历史重复参与者在参与者列表里依然重复展示 | 2026-07-07 17:20 | 2026-07-07 18:10 | 已修复 | 复现：预置同 `(request_id,user_id)` 两条记录后再调用 `add_participant()`，`list_by_request()` 仍返回 2 条且角色都为 `Approver`；修复：`list_by_request()` 按 user_id 去重（保留角色优先级最高的行）；`add_participant()` 查询全部历史行后收敛重复（删除多余、升级角色）；新增 `tests/test_bugfix_106_107.py` 覆盖去重/收敛；全量 pytest 1087 passed |
| BUG-108 | 修复 | `/api/workspace/{id}/sources` 透传 `owner_type=user&visibility=private`，仓储 `list_by_workspace` 不绑定 `owner_id`，成员可枚举同空间他人私有资料元数据 | 2026-07-07 18:00 | 2026-07-07 18:15 | 已修复 | 路由 `list_sources` 在查 workspace 存在性之前先拒绝 `owner_type=user`（个人资料有专门端点 `/workspace/personal/sources`）；新增 `tests/test_bugfix_108_110.py`（含合法默认过滤路径回归：404 而非 400）；全量 pytest 1087 passed |
| BUG-109 | 修复 | `budget_guard.get_monthly_token_usage` 把 `workspace_id IS NULL` 的全局汇总行计入每个 workspace，导致 `current_month_tokens` 偏大、`ensure_workspace_llm_allowed` 在 block 模式下误封未超限 workspace | 2026-07-07 18:00 | 2026-07-07 18:15 | 已修复 | `get_monthly_token_usage` 去掉 `OR workspace_id IS NULL` 分支，只统计该 workspace 专属行；新增 `tests/test_bugfix_108_110.py`（含无专属行返回 0、不含 NULL 全局行）；全量 pytest 1087 passed |
| BUG-110 | 修复 | `KnowledgeFileStorage.read_file/delete_file` 在上传目录不存在时直接 `iterdir()` 抛 `FileNotFoundError`，下载路径退化成 500 而非 404 | 2026-07-07 18:00 | 2026-07-07 18:15 | 已修复 | `read_file/delete_file` 加 `root.exists()` 保护，缺失时返回 `None/False`（与调用方 None→404 契约一致）；新增 `tests/test_bugfix_108_110.py`（含目录存在正常路径回归）；全量 pytest 1087 passed |
| BUG-111 | 修复 | LWA 别名/子路径下 `index.html` 用根绝对路径引用 CSS/JS/vendor/favicon，浏览器打到网关根导致静态资源 404（IMP-023 应用侧根治） | 2026-07-09 16:30 | 2026-07-09 16:35 | 已修复 | `src/static/index.html` 全部改为 `./css/...`、`./js/...`、`./vendor/...`、`./favicon.svg`；新增 `tests/test_bugfix_111_relative_static_assets.py`；同步 markdown/chat/web-guidelines 契约断言；定向 165 passed，全量 `pytest -q` 1089 passed。部署侧需将更新后源码导入 LWA 后执行 `lwa rebuild prd-workflow` |
| BUG-112 | 修复 | P0 对象级权限缺失：Artifact、KnowledgeSnapshot、Comment 接口只校验登录态，未校验当前用户对 conversation/review request/project/workspace 的访问权；任意登录用户可按 ID 读取或改写他人产物、读写快照、读取/创建/解决他人评论 | 2026-07-17 13:05 | 2026-07-17 13:30 | 已修复 | 新增 `object_access.py`；`artifact.py`/`notification.py` 全端点接入对象归属校验；新增 `tests/test_bugfix_112_118.py` BOLA 回归；全量 1103 passed |
| BUG-113 | 修复 | P0 Agent 安全约束未进入执行链：`allowed_tools_json` 被读取但未传给 Pi 或 Extension，`AgentAuthorization`/`default_scope_type` 仅用于 CRUD/UI，低风险工具默认放行；高风险工具“批准”仅更新数据库状态，不能恢复被拦截调用 | 2026-07-17 13:05 | 2026-07-17 13:30 | 已修复 | Bridge 注入 `AGENT_ALLOWED_TOOLS`/`AGENT_SCOPE_JSON`/`AGENT_ONE_SHOT_APPROVED`；Extension 强制白名单；活动 bridge 注册表 + `decide_approval` followUp/一次性放行恢复；全量 1103 passed |
| BUG-114 | 修复 | P1 Agent RAG 工具仍返回硬编码模拟结果，可能把不存在的“需求评审流程规范/PRD写作规范”作为真实检索结果提供给用户 | 2026-07-17 13:05 | 2026-07-17 13:30 | 已修复 | Extension `rag_search` 改调 `POST /api/agent/runs/{id}/rag`（run token + RetrievalService）；失败返回显式 error，移除 mock；全量 1103 passed |
| BUG-115 | 修复 | P1 知识库异步 embedding 管线未实现：上传/重入库只创建 pending chunks 和 FTS，仓库没有任务消费者调用 `EmbeddingService.embed_batch()` + `KnowledgeVectorService.upsert()` 并更新 done/failed；个人检索又显式禁用 FTS 降级，导致个人资料检索在当前链路恒为空 | 2026-07-17 13:05 | 2026-07-17 13:30 | 已修复 | 新增 `embedding_worker.py` 并在 lifespan 启动；`search_fts_personal` + personal FTS 降级；全量 1103 passed |
| BUG-116 | 修复 | P1 默认部署密钥校验失配：`.env.example` 提供公开固定 JWT secret，但启动校验只拒绝另一个字符串；照 README 执行 `cp .env.example .env` 后服务可直接启动，攻击者可用已知 secret 伪造任意用户/管理员 Token | 2026-07-17 13:05 | 2026-07-17 13:30 | 已修复 | 新增 `jwt_secret.assert_jwt_secret_safe`（拒绝示例/过短密钥）；`.env.example` 留空 JWT_SECRET；支持 `ADMIN_INITIAL_PASSWORD`；全量 1103 passed |
| BUG-117 | 修复 | P1 MCP 全局配置接口缺少管理员和 workspace 权限：任意普通用户可列出全部 Server/Policy，并创建任意 stdio endpoint 或修改任意 server 的工具策略 | 2026-07-17 13:05 | 2026-07-17 13:30 | 已修复 | 全局 MCP 需 admin；workspace 级需 manage；list 按可见范围过滤；全量 1103 passed |
| BUG-118 | 修复 | P2 应用生命周期未释放全局 SQLAlchemy engine；完整测试虽 1090 passed，但解释器退出时出现 4 个 `_connection_worker_thread` 异常，生产优雅停机也没有 `engine.dispose()` | 2026-07-17 13:05 | 2026-07-17 13:30 | 已修复 | lifespan finally 停止 embedding worker 并 `await engine.dispose()`；新增 lifespan 回归测试；全量 1103 passed |

| BUG-119 | 修复 | P0 Agent 授权范围仍未真正执行：Bridge 虽注入 `AGENT_SCOPE_JSON`，Extension 完全不读取；空工具白名单等价于允许全部工具，`read` 无需审批，普通用户还可自行配置 `allowed_tools` 并自行批准 bash/write/edit，Pi 子进程又继承服务进程文件权限 | 2026-07-29 00:00 | 2026-07-29 18:30 | 已修复 | Extension 读 scope+deny-by-default；`read` 需审批；非管理员禁高风险自配/自批；Pi cwd 沙盒；RAG 端点服务端校验授权 workspace；`tests/test_bugfix_119_124.py` |
| BUG-120 | 修复 | P0 聊天附件 `file_id` 可目录穿越读取服务端任意受支持文本文件：`ChatFileStorage.read_text()` 直接 `os.path.join(upload_dir, file_id)`，聊天请求与上下文创建均接受用户自填 `file_id` | 2026-07-29 00:00 | 2026-07-29 18:20 | 已修复 | `_resolve_safe_path` 拒绝路径分隔/非 basename，并校验解析后仍在上传根目录；生产仍生成 UUID 文件名；全量 1116 passed |
| BUG-121 | 修复 | P0 Workspace 配额在真实调用链中基本不生效：`log_llm_session()` 不记录 workspace/user/mode，`aggregate_daily()` 固定写入 `workspace_id=None`，而 BUG-109 后的 guard 只统计指定 workspace 行；聊天还可用客户端传入的不存在 `knowledge_workspace_id` 选择无配额记录的空间 | 2026-07-29 00:00 | 2026-07-29 18:20 | 已修复 | 日志写入 workspace/user/mode；聚合按归属字段分组；聊天校验 knowledge_workspace 存在且成员可读；全量 1116 passed |
| BUG-122 | 修复 | P1 SQLite 外键约束未启用，模型声明的 `ondelete=CASCADE/SET NULL` 和外键引用不会由数据库执行，可写入不存在的 approver/workspace/project ID，并在删除项目后留下审查请求、快照等孤立数据 | 2026-07-29 00:00 | 2026-07-29 18:20 | 已修复 | Engine connect event `PRAGMA foreign_keys=ON`；同步修复 6 个孤儿插入测试；全量 1116 passed |
| BUG-123 | 修复 | P1 协作审查产物写权限退化为读权限：`update/confirm/unconfirm` 仅调用 `assert_artifact_access()`，而任意 active Participant（含 Observer）或 workspace owner/admin 都会通过只读访问校验，可篡改、确认或解冻他人产物 | 2026-07-29 00:00 | 2026-07-29 18:20 | 已修复 | 新增 `assert_artifact_write_access`；Observer 只读，发起人/Reviewer/Approver/项目管理员可写确认；全量 1116 passed |
| BUG-124 | 修复 | P2 审查后台任务使用裸 `asyncio.create_task()` 且不保存/等待任务句柄，应用 shutdown 只停止 embedding worker 后即 dispose engine；任务可能在数据库引擎释放后继续运行，测试退出仍产生多条 aiosqlite `Event loop is closed` 线程异常 | 2026-07-29 00:00 | 2026-07-29 18:20 | 已修复 | `track_pipeline_task`/`stop_all_pipeline_tasks`；lifespan 在 dispose 前取消等待审查任务；全量 1116 passed |
| BUG-125 | 修复 | P0（CHK-086）高风险工具审批后永远无法放行：Bridge 剥离 bash/write/edit/read 且 Extension 先白名单后 one-shot，`AGENT_ONE_SHOT_APPROVED` 仍被白名单拦死；活进程也无法看到 Python 侧 grant | 2026-07-29 18:55 | 2026-07-29 19:20 | 已修复 | Extension `consumeOneShot` 优先于白名单；sidecar `.agent_one_shot_approved` 通知活进程；`_build_extension_env` 把 one-shot 并入 `AGENT_ALLOWED_TOOLS`；`tests/test_bugfix_119_124.py` +10 |
| BUG-126 | 修复 | P1（CHK-086）BUG-121 未闭环：审查主路径 `retry.py`/`skill_runner`/`llm.non_stream_chat` 仍不写 workspace/user/mode，配额对审查 token 漏计 | 2026-07-29 18:55 | 2026-07-29 19:20 | 已修复 | `retryable_chat`/`structured_chat`/`non_stream_chat` 透传归属；`SkillRunner`+`review._run_pipeline` 注入 workspace/user、mode=`review` |
| BUG-127 | 修复 | P1（CHK-086）BLOCKED 工具名用子串匹配（`if blocked_tool in line`），`read` 会误命中 `already_read` 等并误建审批 | 2026-07-29 18:55 | 2026-07-29 19:20 | 已修复 | `extract_blocked_tool` 正则精确解析 `工具/高风险工具/当前` + 高风险白名单校验 |
| BUG-128 | 修复 | 子路径前缀部署（如 `/prd-review/`）下前端所有 API 调用写死根绝对路径 `/api/...`（`API._base` 恒为空 + 12 处绕过 request() 的直接 fetch + 2 处 EventSource），请求打到站点根被 Caddy 吞掉返回空 200，浏览器登录"进不去"（后端与默认账号实测正常） | 2026-07-31 17:50 | 2026-07-31 18:20 | 已修复 | `api.js` 新增 `_base` 自动推导（优先 `window.__BASE_PATH__` 注入，否则从脚本加载路径 `.../js/api.js` 上溯）与 `_url()` 统一拼接；12 处直接 fetch（chatStream/upload/下载/审查文档上传/报告导出/日志上报/Agent 流式）与 sse-ticket 全部收敛；审查进度 EventSource 经 `_base` 拼接；`notification.js` 通知 SSE（ticket+EventSource）与 `auth.js` branding fetch 改用 `API._url()`；`index.html` 脚本版本号统一提升至 `?v=20260731-1` 防旧缓存；前端 JS `node --check` 全通过 |
| BUG-129 | 修复 | 后端三处子路径前缀部署缺陷：① `/api/app/branding` 返回 `/assets/branding/...` 根绝对 URL，前缀下 logo/favicon 破图 ② NoCacheMiddleware 用 `request.url.path.startswith("/api/")` 判断，root_path 部署下失效导致 API `no-store` 缓存头静默丢失 ③ FastAPI 无 root_path 入口，无法声明子路径挂载 | 2026-07-31 17:50 | 2026-07-31 18:20 | 已修复 | branding 资产 URL 改相对路径 `assets/branding/...`（浏览器相对页面解析）；中间件先剥离 `scope["root_path"]` 再判断；FastAPI 新增 `root_path=os.environ.get("ROOT_PATH")`，版本号抽为 `APP_VERSION` 常量供构造器与 /api/health 单源复用；ROOT_PATH=/prd-review ASGI 冒烟：带前缀与直连 /api/health 均 200 + no-store 正常、/prd-review/ 页面 200；新增 `tests/test_bugfix_128_129.py` 8 项契约测试（8 passed）；同步修正 3 项过期测试断言（test_sse_progress 契约、update.sh 黑名单契约、BUG-125 遗留的 test_bug113 one_shot 预期）；全量回归 **1127 passed / 0 failed** |
| BUG-130 | 修复 | 目录重组（poc-a/b/c → eval/poc/、scripts/ → eval/p2d1/scripts/）导致的路径断裂：tests/test_p2d1_eval.py 全部引用旧路径失效；评估脚本 PROJECT_ROOT 层级错误（文件下移两级后 parent 链不足）、POC_SAMPLES_DIR 指向已不存在的 poc-a/samples | 2026-07-31 20:44 | 2026-07-31 20:44 | 已修复 | `tests/test_p2d1_eval.py` 统一改指 `eval/p2d1/scripts/` 与 `eval/poc/poc-a/samples/`（SCRIPT_PATH、4 处样例目录、10 处 sys.path、2 处 skip 提示）；`eval/p2d1/scripts/run_p2d1_eval.py` PROJECT_ROOT 改为四级 parent、样例路径同步更新；顺带移除脚本中读取 `poc-c/openai-key.txt` 明文密钥的回退逻辑（对齐 OPT-003 合规要求，只保留 env/--api-key 两途径）；定向回归 34 passed，全量回归 **1135 passed / 0 failed**（6 warnings，160s） |
| BUG-131 | 修复 | P1 代码审查发现：`update.sh` 版本比较逻辑缺陷--旧流程先用硬编码 `NEW_VERSION` 常量与当前版本比较，相同则直接 exit 0，导致更新包从未被解包读取实际版本；若 `NEW_VERSION` 常量未随发布同步，真实更新会被静默跳过 | 2026-08-09 23:30 | 2026-08-09 23:50 | 已修复 | `update.sh` 重排流程：find_package -> validate -> safe_extract -> `read_version_from_main()` 读取包内实际 `APP_VERSION` -> 再与 `CURRENT_VERSION` 比较；移除对硬编码 `NEW_VERSION` 常量的依赖（部分收口 OPT-001）；`bash -n` 通过；全量回归 **1143 passed / 0 failed** |
| BUG-132 | 修复 | CHK-091 残留问题修复：① 4.3 Skill 禁用开关在分类/逐篇分析步骤缺失门控（preflight 只覆盖 steps 3+，步骤 1/2 为死代码）② 3.2 七维 partial 失败未映射 completed_with_warnings、review_dimensions_meta 未持久化 ③ 4.4 线上分类白名单仅在 standalone 脚本、路由路径缺失 ④ 4.6 分析缓存未绑定源文件哈希、源文件变更后仍复用旧分析 ⑤ 4.2 非关键字段 enum 回退静默修正、diagnostics 无记录 | 2026-08-12 00:10 | 2026-08-12 00:40 | 已修复 | **4.3**: `_run_pipeline` 新增 preflight gate（`_FULL_STEP_TO_SKILL_ID` 覆盖分类+逐篇分析+体系Review+报告+PRD草稿），inactive 必需 Skill 在 `task.status=running` 前直接 fail+return；per-step gate 注释更新。**3.2**: 体系Review 完成后 `_merge_task_step_details(task, review_dimensions_meta=...)` 持久化；final status 判断增加 `has_dim_warnings`/`has_degraded` 条件；degraded_steps 也持久化到 step_details。**4.4**: 路由 classify 后从 `default-categories.json` 加载白名单，非白名单类别标记为"待确认"。**4.6**: 分析保存时注入 `_source_content_hash`；缓存查找时比对 `doc.content_hash` 不匹配则跳过缓存重分析。**4.2**: `skill_schema.py` `_repair_node` 增加 `notes` 参数收集所有修复动作；`repair()` 返回 `_schema_repair_notes`；`skill_runner.py` 将 notes 追加到 diagnostics。新增 `tests/test_chk091_residual_fixes.py` **21 项**（4.3 preflight 4 + 3.2 状态判定 4 + 4.4 白名单 2 + 4.6 哈希 3 + 4.2 修复笔记 7 + 集成 1）。`test_router_persistence_scan.py` WHITELIST review.py db_commit 34->35。定向回归 **72 passed**，schema/skill/audit 关键词 **220 passed / 0 failed**。全量回归 **1287 passed / 0 failed** |
| BUG-133 | 修复 | CHK-091 残留二期修复（6 项）：① 4.4 版本链 schema `dependencies` 未加入 required 数组 ② 4.2 维度/洞察子路径（`_run_dimension_with_retry`/`_run_insight_substep_with_retry`）未门控 `_schema_critical` ③ 5.6 脚本 docstring 仍写 `python`（7 个文件） ④ 4.4 standalone insights 无 `--target-baseline` CLI ⑤ 4.6 DOCX 缺图片像素上限和单张大小限制 ⑥ 5.7 ruff 332->0（修改文件）+ pyproject.toml ruff 配置 | 2026-08-12 01:00 | 2026-08-12 01:30 | 已修复 | **4.4 schema**: `output-schema.version-chain.json` 的 `required` 数组添加 `dependencies`，确保 LLM 必须输出依赖关系。**4.2 子路径门控**: `_run_dimension_with_retry` 和 `_run_insight_substep_with_retry` 在 schema repair 后检查 `_schema_critical`，critical 失败时 raise ValueError 触发重试；同时 surface `_repair_notes` 到结果。**5.6 docstring**: 7 个脚本文件（classify.py/pm_assess.py/review.py/analyze.py/insights.py/generate.py/md_to_pdf.py）的 `python` 统一为 `python3`。**4.4 target-baseline**: `insights.py` 新增 `--target-baseline` CLI 参数，加载目标能力基线 JSON 并注入 feature-extraction LLM prompt 的 `## 目标能力基线` 部分；提供基线时清空 `baseline_warning`。`run_gap_analysis` 函数签名增加 `target_baseline` 参数。**4.6 图片限制**: `convert_docx.py` 新增 `MAX_IMAGE_PIXELS`（5000万）和 `MAX_SINGLE_IMAGE_SIZE`（20MB）常量；新增 `_get_image_pixel_count()` 辅助函数从 PNG/JPEG/GIF/BMP 头部解析像素数；图片提取时检查文件大小和像素数超限跳过。**5.7 ruff**: `pyproject.toml` 新增 `[tool.ruff]` 配置（select E701/E731/E741/F401/F541/F811/F821/F823/F841，ignore E402/E712）；review.py 9 处 `if ...: return` 拆为多行（E701）；test_review_backend_contract.py 2 处 lambda->def（E731）；test_review_frontend_contract.py 变量 `l`->`line`（E741）；pi_agent_bridge.py `select`->`sa_select`（F823）+移除未使用 import。新增 `tests/test_chk091_residual_phase2.py` **29 项**（schema 3 + 子路径门控 4 + docstring 7 + target-baseline 3 + 图片限制 8 + ruff 1 + 代码质量 3）。全量回归 **1316 passed / 0 failed**（146s） |
| BUG-134 | 修复 | CHK-091 残留三期修复（4 项，子代理复查发现）：① [P1] DOCX 安全拒绝被 fallback 路径绕过--skill 转换的 ZIP bomb/超大图片检查在 except 兜底中被 Mammoth 重新处理，无等价安全限制，且无转换超时 ② [P1] 分类失败未中止管线--`classify_result.status == "error"` 或 schema critical 失败时不 abort，空分类传播到版本链及下游步骤，`version_chain_result` 也未检查 ③ [P1] 缓存 SystemReview 丢失 partial 失败状态--cache 恢复 `review_dimensions` 但不恢复 `review_dimensions_meta`，复用时 final status 回退为 `completed` 而非 `completed_with_warnings` ④ [P2] 旧格式缓存无 content hash 时 fail-open--仅校验含 `_source_content_hash` 的缓存，缺 hash 的旧缓存被无凭证复用 | 2026-08-12 02:00 | 2026-08-12 02:45 | 已修复 | **Fix 1 安全拒绝不可降级**: `convert_docx.py` 新增 `DocxSecurityError(ValueError)` 异常类，`validate_zip_safety()` 中 3 处 `raise ValueError` 改为 `raise DocxSecurityError`（ZIP entry 过大/压缩比过高/总解压过大）；`review_file_storage.py` 新增 `_NeverMatches(BaseException)` sentinel，pre-import `DocxSecurityError as _DocxSecurityError`（import 失败回退 sentinel），转换 try 块中 `except _DocxSecurityError: raise` 不降级到 Mammoth；新增 `asyncio.wait_for` 转换超时（`_docx_conversion_timeout_seconds()`）；Mammoth fallback 路径也调用 `validate_zip_safety()`（带 `try/except ImportError` 容错）。**Fix 2 分类失败中止管线**: `review.py` 在 `classify_result` 后检查 `is_error`，error 时 `raise RuntimeError` 中止；`version_chain_result` 后同步检查 `is_error` 并 raise。**Fix 3 dimensions_meta 持久化**: `models/review.py` 新增 `dimensions_meta: Mapped[str \| None]` 列；`database.py` `_ensure_review_schema` 新增 `ALTER TABLE system_reviews ADD COLUMN dimensions_meta TEXT` 迁移；`review_task_repository.py` `SystemReviewPayload` 新增 `dimensions_meta` 字段，`save_system_review` 传递到 model；`review.py` fresh path 保存 `review_dimensions_meta` JSON 到 DB，cache path 从 DB 恢复 `dimensions_meta`（解析 JSON 失败时扫描 dimension 数据中的 `{"error": ...}` 重建 partial meta）。**Fix 4 缓存哈希 fail-closed**: `review.py` 缓存查找时缺 `_source_content_hash` 的旧格式缓存标记为 stale 并重新分析，不再 fail-open。新增 `tests/test_chk091_residual_phase3.py` **23 项**（DocxSecurityError 6 + 分类中止 5 + 缓存 meta 8 + 缓存哈希 fail-closed 4）。`test_router_persistence_scan.py` WHITELIST review.py db_add 0->1（`covered_doc_ids.add()` 匹配）。`test_data_compat.py` 补充 `dimensions_meta` 迁移步骤。全量回归 **1341 passed / 0 failed**（162s） |
| BUG-135 | 修复 | BUG-134 DOCX 资源安全复核残留：① 超过 20MB/像素上限的图片被 ZIP 提取阶段跳过后，Mammoth `convert_image()` 回调会再次写入 assets ② `wait_for(to_thread(...))` 超时仅停止等待，后台线程仍持续运行和写文件 | 2026-08-12 03:00 | 2026-08-12 03:45 | 已修复 | **图片限制闭环**：`convert_docx.py` 新增 `_image_resource_rejection_reason()`，ZIP 提取与 Mammoth 回调统一执行文件大小/像素限制，真实 21MB 图片完整转换后不再生成超限 asset。**进程隔离**：新增 `docx_conversion_worker.py`，Skill 与 Mammoth 降级均在独立 Python 子进程执行；`ReviewFileStorage` 使用 `create_subprocess_exec`，超时或任务取消后按进程组 `SIGTERM`→等待→`SIGKILL` 并回收；转换仅写同级临时目录，成功后原子发布，失败/超时清理临时产物；无 Skill 降级路径内置等价 ZIP 安全检查。新增 `tests/test_docx_resource_isolation.py` 3 项真实行为测试（超限图片、超时终止、取消终止）并完成 RED→GREEN；兼容测试改为验证隔离目录产物。验证：相关回归 **137 passed**，Skill 审计 **192 passed**，相关 Ruff `All checks passed`，全量回归 **1344 passed / 0 failed**（6 个既有警告） |
| BUG-136 | 修复 | [P0] `_get_model_config()` 在 SQLAlchemy 查询中使用 Python `not` 关键字取反 Column 对象，`not ModelConfig.deleted_by_user` 在 Python 层求值为 `False`（Column `__bool__` 恒 `True`），传给 `.where()` 等价于 `WHERE 0`，导致未指定 model_id 时查不到任何启用模型，报"无可用的LLM模型配置"，Pi Agent 和审查管线全部不可用 | 2026-08-12 04:00 | 2026-08-12 04:10 | 已修复 | `review.py` import 加 `not_`；第 308 行 `not ModelConfig.deleted_by_user` -> `not_(ModelConfig.deleted_by_user)`（按 model_id 查）；第 314 行同上（默认查）。全项目扫描确认无其他 `not ORMClass.attr` 模式残留。新增 `tests/test_bug135_sqlalchemy_not.py` **5 项**（not_ 导入验证 1 + `not ModelConfig.` 模式扫描 1 + 全 src 目录 `not ORMClass.attr` 扫描 1 + not_() 使用验证 2）。Ruff `All checks passed`。全量回归 **1350 passed / 0 failed**（156s） |
| BUG-137 | 修复 | [P0] 智能对话报 400 错误：管理员将模型配置 `max_tokens` 设为 200000（模型上下文窗口大小），该值被直接传给 OpenAI API 的 `max_tokens`（输出 token 上限），导致 prompt 无空间。错误信息："This model's maximum context length is 200000 tokens. However, you requested 200000 output tokens and your prompt contains 106 characters" | 2026-08-12 04:15 | 2026-08-12 04:30 | 已修复 | **运行时安全上限**：`llm.py` 新增 `_cap_max_tokens()` 函数 + `_MAX_OUTPUT_TOKENS_HARD_LIMIT=32768` 常量，`stream_chat()` 和 `non_stream_chat()` 的 payload 改用 `_cap_max_tokens(max_tokens)`；`retry.py` 同步新增等价函数和调用。**Admin 入口校验**：`admin.py` `ModelConfigCreate` 和 `ModelConfigUpdate` 新增 `validate_max_tokens` field_validator，拒绝 1-32768 范围外的值。新增 `tests/test_bug137_max_tokens_cap.py` **10 项**（_cap_max_tokens 函数验证 4 + llm.py 使用验证 2 + retry.py 使用验证 1 + admin 校验 3）。Ruff `All checks passed`。全量回归 **1360 passed / 0 failed**（158s）。LWA 部署已同步修复并重启 |
| BUG-138 | 修复 | 单篇快速审查无法执行：原始上传的 docx 文件不在本地 runtime 目录中（从 LWA 部署同步 DB 但未同步上传文件），导致 step 0 预处理阶段 `_convert_docx()` 抛出 FileNotFoundError，所有文档标记为 failed，管线中止 | 2026-08-12 17:05 | 2026-08-12 17:15 | 已修复 | `review.py` `_run_pipeline` step 0 预处理循环新增 fallback：当源 docx 文件不存在时，检查 `doc.md_path` 是否已有转换好的 Markdown，若存在则复用并标记 `status=converted`，跳过转换；历史文档（draft 模式）同步添加等价 fallback。源文件和 md 均不存在时仍报 FileNotFoundError。新增 `tests/test_docx_fallback.py` **8 项**（源文件缺失+md存在 1 + 源文件缺失+md不存在 1 + 源文件存在不触发 1 + read_markdown 成功/失败 2 + md_path 保留 1 + 历史文档 fallback 2）。Ruff `All checks passed`。相关回归 **46 passed / 0 failed** |
| BUG-139 | 修复 | [P1] 测试体系盲区：BUG-136 回归未被任何测试捕获。根因：审查启动测试 monkeypatch 了 `_get_model_config`（SQL 查询从未真正执行），模型配置 API 测试走 Repository 层（一直使用正确的 `== False`），前端契约测试只匹配 JS 字符串。三层测试全部绕过了出 bug 的代码路径 | 2026-08-12 18:00 | 2026-08-12 18:20 | 已修复 | 新增 `tests/test_bug136_e2e_model_config.py` **5 项**端到端测试，关键设计：**不 mock `_get_model_config`**，让它执行真实 SQLAlchemy 查询，只 mock `_run_pipeline` 避免真实 LLM 调用。测试场景：① 有启用模型时审查启动成功 ② 无启用模型时返回 400 ③ 指定 model_id 查询成功 ④ 有 enabled+!deleted 模型时成功（deleted_by_user=True 的模型被忽略）⑤ 只有 deleted_by_user=True 模型时返回 400。验证：临时回退 `not_()` 为 `not` 后 3 个正面测试立即失败并精确指向 BUG-136，恢复后 5/5 通过 |
| BUG-140 | 修复 | [P0] 单篇 requirement 文档 quick 审查永远失败：V0.3.8（d8bda85）给 `output-schema.version-chain.json` 新增 `versions.minItems: 2` 约束，要求每条版本链至少 2 个版本。但 quick/review/pm/insight/full 模式不处理历史文档，只有 requirement 文档参与版本链分析。当项目只有 1 篇 requirement 文档时，版本链必然只有 1 个版本，不满足 minItems: 2，导致 classify_version_chain 3 次重试全部失败，管线中止 | 2026-08-12 18:40 | 2026-08-12 18:50 | 已修复 | `output-schema.version-chain.json` `versions.minItems` 从 `2` 改为 `1`。单篇文档本身就是一个合法的版本链。新增 `tests/test_bug140_version_chain_minitems.py` **5 项**（schema 文件存在 1 + minItems 值校验 1 + 单版本链通过校验 1 + 多版本链回归 1 + 空版本链仍拒绝 1）。新增 `tests/test_e2e_live_deployment.py` **8 项**针对 127.0.0.1:17957 真实部署的集成测试（健康检查 1 + 模型配置 1 + 项目存在 1 + 单篇文档 1 + quick 审查全管线 1 + docx fallback 1 + 结果查询 1 + 全自动创建项目+上传+审查 1），7 passed 1 skipped |
| BUG-141 | 修复 | [P1] 上传文档无去重：`_save_project_documents` 不检查同项目同类型同名文件是否已存在，重复上传同名 docx 直接入库产生重复记录，导致前端历史文档列表出现重复项 | 2026-08-12 19:00 | 2026-08-12 19:15 | 已修复 | `review.py` `_save_project_documents` 新增去重检查：上传前查询同一 `project_id` + `document_type` + `filename` 是否已存在，若存在则跳过并在返回值新增 `skipped` 字段告知前端。新增 `tests/test_bug141_upload_dedup.py` **5 项**（requirement 去重 1 + historical 去重 1 + 不同名不去重 1 + 跨类型不去重 1 + 批量部分去重 1）。同时清理了项目 9 的重复历史文档数据 |
| BUG-142 | 修复 | [P2] 审查完成后底部"审查已完成/准备讲解"提示条（review-presentation-entry）无关闭按钮，用户无法收起该提示 | 2026-08-12 19:20 | 2026-08-12 19:25 | 已修复 | `index.html` 提示条新增 `presentation-entry-close` 关闭按钮（aria-label 关闭提示）；`review.js` `_bindP4Actions` 绑定点击隐藏 `review-presentation-entry`；`main.css` 新增 `.presentation-entry-close` 样式。契约测试 `test_presentation_entry_has_close_button` |
| BUG-143 | 修复 | [P1] 模式状态不向下传播：full/insight/draft 等高级模式完成后，其覆盖的低级别模式（quick/review/pm/insight）卡片不显示"已完成"。根因：`_loadReviewHistory` 与 `_upsertReviewTask` 无条件跳过 `mode==='full'` 且不展开模式包含关系，导致 `_reviewDocMap`/`_reviewTaskMap` 缺失子模式记录，badge 判定失效 | 2026-08-12 19:30 | 2026-08-12 19:45 | 已修复 | `review.js` 新增 `MODE_COVERS` 模式包含层级表（quick⊂review/pm⊂insight/full⊂draft，full 覆盖 insight）；`_loadReviewHistory` 与 `_upsertReviewTask` 用 MODE_COVERS 向下展开 `_reviewTaskMap`/`_reviewDocMap`。**防遮蔽规则**：running/pending/completed/completed_with_warnings 才向下传播，failed/cancelled 只记录自身模式，避免失败的 full 任务遮蔽已完成的 quick 任务。更新契约测试：`test_mode_covers_hierarchy_defined`、`test_load_review_history_expands_mode_coverage`、`test_upsert_review_task_expands_mode_coverage`、`test_failed_tasks_do_not_propagate_to_submodes`，并修正既有 `test_batch_system_review_tasks_do_not_populate_single_doc_history` 的过时段言（不再断言无条件跳过 full） |
| BUG-144 | 修复 | [P0] 审查管线完全绕过 Workspace 月度配额拦截：`start_review` 中未调用 `ensure_workspace_llm_allowed`，而 chat.py 和 agent.py 均在 LLM 调用前调用此函数。审查管线触发数十次 LLM 调用（分类、逐篇分析、七维评审、洞察、报告），全部绕过 `hard_limit_action=block` 的配额管控，多租户场景下严重成本泄漏 | 2026-08-12 22:00 | 2026-08-12 22:15 | 已修复 | `review.py` `start_review` 在 `_verify_project_owner` 之后、文档查询之前新增 `ensure_workspace_llm_allowed(db, project.workspace_id)` 调用。新增 `tests/test_bug144_148_code_audit.py` TestBUG144QuotaCheck **2 项**（mock 验证函数被调用 1 + 配额超限返回 429 1） |
| BUG-145 | 修复 | [P0] `delete_project` 级联删除逻辑错误 + 孤儿数据残留：① ReviewParticipant 删除放在 round 循环内，N 个 round 执行 N 次同样的 DELETE；② Artifact / Comment / KnowledgeSnapshot 完全未清理，在 PRAGMA foreign_keys=ON 后触发外键约束失败或留下孤儿数据 | 2026-08-12 22:00 | 2026-08-12 22:20 | 已修复 | `review.py` `delete_project` 重写级联删除：收集 round_ids → 先删 review_round 类型 comments（object_id IN round_ids）→ 删 review_request 类型 comments → 删 review_request 类型 artifacts → 删 rounds → 删 ReviewParticipant（移到 round 循环外，每个 req 只删一次）→ 删 ReviewRequest → 删 KnowledgeSnapshot。新增 TestBUG145DeleteProjectCascade **2 项**（全关联记录清理验证 1 + 多 round 时 Participant 不重复删除 1） |
| BUG-146 | 修复 | [P1] `resolve_comment` 通知未使用 `defer_push`，可能产生幽灵通知（BUG-106 修复未覆盖此路径）：commit 在前、通知在后且用默认 `defer_push=False`，事务部分失败时已推送的 SSE 指向的 notification_id 会因主事务回滚而消失 | 2026-08-12 22:00 | 2026-08-12 22:15 | 已修复 | `notification.py` `resolve_comment` 改用 `NotificationService(db, defer_push=True)`，将 `db.commit()` 移到通知调用之后，commit 成功后调用 `notif_service.flush_pending()` 推送 SSE。与 `create_comment` 完全对齐。新增 TestBUG146ResolveCommentDeferPush **2 项**（源码断言 defer_push=True + flush_pending 1 + mock 验证 _deliver 在 commit 后才被调用 1） |
| BUG-147 | 修复 | [P2] FTS5 sanitize 未处理 NEAR 关键字和 Unicode 智能引号：`_sanitize_fts5_query` 已处理 `"`/`*`/`(`/`)`/AND/OR/NOT（BUG-043/052），但漏了 `NEAR` 关键字（FTS5 语法 `term1 NEAR term2`）和 Unicode 智能引号 `“”‘’`（中文输入法常见）。用户粘贴含这些字符的查询时 FTS5 可能报语法错误导致 500 | 2026-08-12 22:00 | 2026-08-12 22:10 | 已修复 | `knowledge_ingestion.py` `_sanitize_fts5_query` 新增 `unicodedata.normalize("NFKC", query)` 归一化全角字符；正则补充 `“”‘’` 四个 Unicode 智能引号；关键字过滤正则补充 `NEAR`。新增 TestBUG147FTS5Sanitize **5 项**（NEAR 过滤 1 + near 大小写不敏感 1 + 智能双引号 1 + 智能单引号 1 + 全角＠归一化 1） |
| BUG-148 | 修复 | [P2] embedding worker 崩溃后 processing chunks 永久卡住：`process_pending_embeddings` 将 chunks 标记为 `"processing"` 后 `await db.commit()`，此后若 embedder 崩溃（OOM、API 异常），chunks 永远卡在 `"processing"`，`list_pending_embedding` 只查 `"pending"` 不会重新消费 | 2026-08-12 22:00 | 2026-08-12 22:10 | 已修复 | `embedding_worker.py` `process_pending_embeddings` 每次循环开始时执行 `UPDATE knowledge_chunks SET embedding_status='pending' WHERE embedding_status='processing'`，重置上次崩溃遗留的 stale chunks。正常流程中 processing→done/failed 在一次循环内完成，任何残留的 processing 都是崩溃遗留。新增 TestBUG148EmbeddingWorkerStaleProcessing **1 项**（stale processing chunk 被重置并最终变为 done）。修复 `test_bug115_embedding_worker_processes_pending` mock 适配新增的 stale reset 查询 |
| BUG-149 | 修复 | [P2] 无协作审查请求时前端仍调用评论接口：`_showP4PanelsAfterResult` 使用 `review_request/object_id=0` 加载评论，后端返回 404，导致选择已有结果的项目或文档时浏览器控制台出现资源加载错误 | 2026-08-12 22:43 | 2026-08-12 22:45 | 已修复 | `review.js` 在无协作审查请求分支清空评论对象上下文、渲染空态并隐藏 P4 面板，不再请求无效对象。新增前端契约测试 `test_p4_empty_request_state_does_not_fetch_comments_for_object_zero`；Playwright 复测项目、文档、上下文切换无 4xx 与控制台错误 |
| BUG-150 | 修复 | [P1] 从登录页新登录或注册后通知模块未初始化：`Notification.init()` 只在刷新页面且已有 Token 的启动路径调用，导致铃铛无点击事件、未读数不加载、SSE 不连接；退出时也未关闭通知 SSE | 2026-08-12 22:45 | 2026-08-12 22:48 | 已修复 | `app.js` 在登录和注册成功后调用 `Notification.init()`，四个退出入口调用 `Notification.destroy()`。新增前端契约测试 `test_notification_lifecycle_follows_fresh_auth_session`；Playwright 复测下拉框可见、SSE/未读计数/通知列表请求均为 200，能展示 3 条未读通知 |
| BUG-151 | 修复 | [P2] 上传去重非原子（BUG-141 未完全闭环）：`_save_project_documents` 的 SELECT-then-INSERT 之间存在 TOCTOU 竞态，两个并发上传同项目同文件名都 SELECT 得到"不存在"、都 INSERT，仍会产生重复行 | 2026-08-12 23:20 | 2026-08-12 23:35 | 已修复 | `review.py` 模型 `ReviewDocument` 新增 `UniqueConstraint("project_id", "document_type", "filename", name="uq_review_doc_proj_type_filename")` 作为 DB 级兜底；`_save_project_documents` 在 `repo.add_document` 后 `await db.flush()` 尽早触发约束，捕获 `IntegrityError` 后 rollback 并将冲突文件加入 `skipped`。`test_router_persistence_scan.py` flush 计数 1→2。新增 `TestBUG151UploadDedupDBConstraint` **3 项**（约束存在性 1 + 直接重复插入触发 IntegrityError 1 + 跨类型同名允许 1 + API 去重仍正常 1） |
| BUG-152 | 修复 | [P1] 缓存查找 TOCTOU 竞态导致重复 LLM 分析：`start_review` 的 `_find_active_review_task` SELECT 与 `create_task` INSERT 之间无锁，两个并发请求同时查询都得到"无 active task"→都创建 task→两条管线并行运行重复 LLM 分析，浪费 token、成本翻倍 | 2026-08-12 23:20 | 2026-08-12 23:40 | 已修复 | `review.py` 新增模块级 `_review_start_locks: dict[int, asyncio.Lock]` 和 `_get_review_start_lock(project_id)`；`start_review` 用 `async with _get_review_start_lock(project_id)` 包裹"查询 active task + 创建新 task + commit + 注册后台管线"临界区。锁只覆盖查询+创建+commit，`_run_pipeline` 通过 `track_pipeline_task` 异步执行不在锁内，不阻塞管线实际运行。`conftest.py` fixture 清理 `_review_start_locks` 避免跨 event loop 复用。新增 `TestBUG152StartReviewLock` **4 项**（锁字典定义 1 + 同 project 返回同一 Lock 1 + 源码断言 async with 1 + 并发请求返回同一 task_id 1） |
| BUG-153 | 修复 | [P1] 旧数据库不会获得 ReviewDocument 新增唯一约束：UniqueConstraint 只在新建表时生效，init_db 对已有库调用 create_all 不改表现有结构，_ensure_review_schema 也未建唯一索引；从 V0.3.10 升级的数据库仍无法阻止并发重复上传 | 2026-08-13 13:21 | 2026-08-13 13:43 | 已修复 | [[BUG-151]] 闭环。`_ensure_review_schema` 调用 `_ensure_review_document_unique_index`：先按 (project_id, document_type, filename) 保留最小 id 去重（并清理多余 doc_analyses / parent_document_id），再 `CREATE UNIQUE INDEX IF NOT EXISTS uq_review_doc_proj_type_filename`。`test_data_compat.py` 改为直接调用 `_ensure_review_schema`。新增 `tests/test_bug153_155_audit_followup.py` TestBUG153（源码契约 + 旧库去重再建索引）。全量 pytest 1448 passed / 8 skipped |
| BUG-154 | 修复 | [P1] 上传并发 IntegrityError 会遗留孤儿文件并回滚整批事务：文件在 flush 前已写入磁盘，冲突后 rollback+continue 不删文件；同批已 flush 成功的文档也会被整段事务撤销，但 saved 仍计为成功，造成接口/数据库/磁盘不一致 | 2026-08-13 13:21 | 2026-08-13 13:43 | 已修复 | `_save_project_documents` 用 `db.begin_nested()` savepoint 包裹 add_document+flush；捕获 IntegrityError 后调用 `delete_document_files` 删除本次 stored 文件，不再 `session.rollback()`。`delete_document_files` 仅在 document_id>0 时清理 converted 目录。新增 TestBUG154（源码契约 + 同批后一文件失败时保留前一文档、删除孤儿 docx） |
| BUG-155 | 修复 | [P2] 测试 fixture 直接 clear `_pipeline_tasks` 且未 cancel/await 未完成审查管线；全量测试退出后 aiosqlite 后台线程访问已关闭 event loop。另：`test_bugfix_112_118.py` 模块级 asyncio 标记套到 6 个同步测试产生 warning | 2026-08-13 13:21 | 2026-08-13 13:43 | 已修复 | `tests/conftest.py` 新增 `drain_review_pipeline_tasks`，autouse fixture 改为 async 并 await `stop_all_pipeline_tasks`；`stop_all_pipeline_tasks` 等待取消后再 pump 一轮 loop。`test_bug136_e2e_model_config.py` 同样 await drain。移除 `test_bugfix_112_118.py` 模块级 `pytestmark`（asyncio_mode=auto 仍覆盖 async 测试）。新增 TestBUG155。全量 1448 passed / 8 skipped / 0 warning；进程退出仍可能有存量 aiosqlite 线程异常（不使 pytest 失败） |

| BUG-156 | 修复 | [P0] Skill 禁用门控未接入生产路径（CHK-091 复核 4.3 残留）：`_preflight_skill_gate` 挂在 `SkillRunner.run_pipeline()` 但生产路径 `review.py` `_run_pipeline` 手工编排步骤从不调用（死代码）；`start_review` 创建任务不检查 Skill 状态（先创建后失败而非拒绝启动）；`_STEP_TO_SKILL_ID` 未覆盖分类/逐篇分析两个必需步骤；`degraded_steps` 仅内存态未持久化到 step_details | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：start_review 创建前门控（必需 Skill 禁用 409 拒绝+审计日志，可选禁用记录 planned_degraded_steps）；_STEP_TO_SKILL_ID 补全分类/逐篇分析映射，执行期预检+逐步门控覆盖全部 6 步；degraded_steps 结构化落库 step_details；顺带修复任务拒绝原因写入不存在列 error_message 从不落库的问题（改 _merge_task_step_details）；新增 tests/test_bug156_157_158.py 9 项全绿 |
| BUG-157 | 修复 | [P1] 七维评审部分失败无状态传播（CHK-091 复核 3.2 残留）：`sr_meta.status=="partial"` 时失败维度 error 对象仍合并存库，最终任务状态只看 completed_docs，部分失败不会进入 `completed_with_warnings`；`review_dimensions_meta` 仅存 runner 内存态，事后无法从 DB 审计失败维度 | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：partial 维度失败 → 任务终态 completed_with_warnings；review_dimensions_meta（executed/failed/counts）持久化到 step_details；新增测试锁定行为（test_bug156_157_158.py 2 项） |
| BUG-158 | 修复 | [P1] 线上分类回填无类别白名单（CHK-091 复核 4.4 残留）：`review.py:1448-1457` 直接接受模型返回的任意 category 写入 doc.category，任意类别还会生成为 categories 列表；白名单校验（unknown→待确认）只在独立脚本 classify.py:203-282 生效，线上主入口未闭环 | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：路由回填前白名单校验（default-categories.json + ReviewContext.category_overrides），非白名单转「待确认」并把原始值记入 step_details.category_whitelist_corrections；配置缺失降级放行+warning 不硬失败；2 项测试全绿 |
| BUG-159 | 修复 | [P1] Schema 修复对非 critical 字段 enum 静默回退（CHK-091 复核 4.2 残留）：`skill_schema.py:295-300` enum 无法纠正时取 `enum[0]` 兜底，仅当路径命中 `_CRITICAL_FIELD_HINTS` 才标 critical——`key_points.type="nonsense"` 会被静默改为 "technical" 后以 success 通过，语义错误被掩盖 | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：enum 无法纠正回退/数值钳制/类型不可转换兜底一律无条件标 critical（skill_schema.py:300-323,376-388），删除死代码 _path_touches_critical；顺带修复 dict 嵌套下 child critical 被吞的传播漏洞；tests/test_skill_schema_constraints.py 扩至 20 项全绿；同步翻转 tests/test_chk091_residual_fixes.py 旧行为断言 |
| BUG-160 | 修复 | [P1] DOCX 转换缓存与防线缺口（CHK-091 复核 4.6 残留）：`.converted` sentinel 仅记录 folder_name 未绑定源文件哈希，源 DOCX 变更后重跑仍被误判"已完整转换"而跳过；另缺总压缩比检查（仅单 entry>100x）、图片像素/维度检查（只限数量与字节，不防 decompression bomb）、单文档处理超时 | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：sentinel 改 JSON 记录 source_sha256（原子写），batch_convert 跳过需哈希一致、不一致/旧格式自动清理重转；validate_zip_safety 补总压缩比>100x（总量>1MB 才判定）；batch_convert 加 --timeout（默认 300s，SIGALRM，Windows 跳过）；像素检查上轮已实现（MAX_IMAGE_PIXELS=5000万，免 PIL）本次补测试；usage-guide.md 已同步；test_skill_docx_audit.py 扩至 22 项全绿 |
| BUG-161 | 修复 | [P1] requirement-insights 独立脚本链路不完整（CHK-091 复核残留）：`target_baseline` 硬编码为空 `[]`（insights.py:412-413）且无 `--target-baseline` CLI，基线在脚本侧永不提供；feature_id/evidence/confidence 在汇总时被丢弃（insights.py:301-312）最终输出不可见；gap 评估按数组位置回填（insights.py:483-492）而非 prompt 规则要求的 feature_id；templates/output-schema.json 停留 6/1 旧版未同步新字段 | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：--target-baseline CLI 真正生效（JSON 文件，基线能力合并进覆盖矩阵、未覆盖标 gap、baseline_source=user_provided）；evolution matches 保留 issue_id/evidence/confidence；gap/overlap 评估改按 feature_id 对齐（_align_assessments，降级位置匹配并在 assessment_alignment 标注）；templates/output-schema.json 全量同步并经 SkillSchemaLoader 实测 0 错误；SKILL.md/usage-guide.md 同步；新增 tests/test_skill_insights_baseline.py 6 项全绿 |
| BUG-162 | 修复 | [P2] 报告链路渲染与嵌入缺口（CHK-091 复核残留）：review.py pydantic 模型（:139-160）与 generate_full_report_md（:576-586）仍按字符串列表消费竞品 key_players/differentiation，LLM 按新格式输出 dict 时报告渲染成 dict repr；report-generator 的 build_dependency_graph/build_version_chain_timeline 结果只写入 _generate_result.json 的 mermaid_charts，未嵌入任何 .md 报告正文 | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：review.py pydantic 模型兼容 str/dict 双形态，_format_competition_item 渲染来源/置信度标注，open_questions 渲染待调研小节；generate.py 依赖图嵌入文档概览章节、版本链时间线嵌入演进脉络章节（空数据不输出占位）；两测试文件扩至 48 项全绿 |
| BUG-163 | 修复 | [P2] 审计建议测试清单 8 项缺失（审计文档第九节 vs 现有 tests/）：docx 同名覆盖、docx 源变化缓存失效（依赖 BUG-160）、超大 ZIP entry 拒绝、中文 PDF 可提取文本、per-analysis 专家规则缺失/重复/非法失败、system-review 错误结果不写缓存、--enable-vision 已移除的行为断言、report-generator 缺可选输入标注；另 5 项仅部分覆盖（doc_id 端到端一致性、越界评分专属用例、缓存键 Prompt 版本、重叠演进/冗余区分、--format 一致性） | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：新增 tests/test_skill_audit_remaining.py 18 项全绿（同名覆盖×3、超大 ZIP entry×2、中文 PDF pdfminer 提取验证、专家规则校验×4、错误结果不写缓存 DB 集成、--enable-vision 移除断言×2、缺可选输入标注×5；源变化缓存失效已由 TestSentinelSourceHash 覆盖不重复）；过程中新发现并修复 2 处真缺口：_validate_expert_review_block 增重复 rule_key/非法 status 拒绝、报告缺数据章节补显式标注 |
| BUG-164 | 修复 | [P2] src/+tests/ 残留 327 个 ruff 问题（ruff 0.15.22 默认规则集：F401×139、E402×79、F841×42、E712×28、F811×21、E701×9、F541×3、E731×2、F821×2、E741/F823×1；161 个可自动修复）；skills/ 目录已清零 | 2026-08-21 00:18 | 2026-08-21 08:32 | 已修复 | 已修复：ruff check src/ tests/ 从 168 个清零（--fix 安全修复 126 个 + 手工确认删除死代码 41 处，无一处 noqa 豁免）；F821 已在前期修复中消失、F811 全为测试局部重定义安全修复；修复中误改 test_migrate_branding.py 3 处已恢复 |
| BUG-165 | 修复 | [P1] BUG-158 白名单边界缺陷（CHK-101 文档审查发现）：default-categories.json 存在但 categories 为空（仓库模板默认态）时 `_cat_cfg_loaded=True` 而白名单仅剩 {未分类, 待确认} 兜底值，无 category_overrides 的项目走生产管线会把 LLM 返回的所有真实类别批量改写为「待确认」；「配置缺失放行」降级只覆盖文件不存在/解析失败，不覆盖空列表 | 2026-08-21 12:50 | 2026-08-21 13:05 | 已修复 | 已修复：review.py 仅在实际加载到配置类别时才置 `_cat_cfg_loaded`（空 categories 等同配置缺失，降级放行+warning）；test_bug158_non_whitelist_category_marked_pending 改用临时白名单配置不再依赖仓库空模板；新增 test_bug158_empty_categories_config_passes_through 回归锁定；test_bug156_157_158.py 10 项全绿。顺带修复：system-review 报告「镋期」错别字→「长期」；ReviewTask.status 列宽 String(20)→String(32)（completed_with_warnings 22 字符，SQLite 不强制但 PG/MySQL 会截断） |
| BUG-166 | 修复 | GitHub Issue #1：空壳 uv.lock（3 行、无 packages）误导 LWA 走 `uv sync --frozen`，装不到 requirements.txt 中的依赖，容器启动找不到 uvicorn | 2026-08-25 12:57 | 2026-08-25 13:10 | 已修复 | 方案 A：`git rm uv.lock` + `.gitignore` 加入 `uv.lock`（依赖源仍为 requirements.txt，迁 uv 后续再考虑）。契约测试 `test_gitignore_blocks_uv_lock` / `test_uv_lock_is_not_tracked`；gitignore 定向 4 passed |
| BUG-167 | 修复 | [P0] GitHub Issue #3：system-review 7 个维度 prompt 均无 per-prompt schema，SkillSchemaLoader 回退 skill 级 output-schema.json（完整报告结构，顶层 required project_name/output_type/dimensions/metadata），维度扁平输出必校验失败→output_type 枚举回填触发 critical→3 次无效重试→七维评审全错 | 2026-08-27 10:30 | 2026-08-27 11:20 | 已修复 | 已修复：新增 7 份 output-schema.<dimension>.json 对齐各 prompt 扁平输出结构（补齐顶层 required/枚举/minItems-maxItems；business-value、pm-assessment 自带 $defs；pm 评分维度键不锁死以兼容 scoring_overrides 量规替换）；顺带修复 normalize_dimension_result 解包误伤——tech-evolution 合法顶层 tech_evolution 字段与包裹键同名，改为仅当包裹键是唯一有效顶层键时才解包；tests/test_bug167_dimension_schemas.py 13 项全绿（合法结果首验通过零重试、缺 score 判 critical、缺 evidence 仍报错、success_count=7）。2026-08-27 复核：代码在工作区未提交（schema 文件仍 untracked），origin/main 与 GitHub Issue 仍未合入 |
| BUG-168 | 修复 | [P1] GitHub Issue #2：Pi Agent 表单新填 Key 后「连接测试/测速」仍报「未配置 LLM API Key」——前端按钮无参数请求后端，后端只读数据库已保存配置，保存前无法测试新值 | 2026-08-27 11:25 | 2026-08-27 12:10 | 已修复 | 已修复：POST /pi-agent/config/test-connection 与 speed-test 增加可选请求体 api_key/llm_provider/llm_api_base/llm_model，传值优先、未传字段回退数据库；临时 Key 仅本次请求内存使用——不落库、不更新 last_test_status、不写日志（测试前不自动持久化无效 Key）；响应携带 config_saved=False，前端 admin.js 仅在表单与已保存配置存在差异时携带请求体，成功反馈显示「连接成功 ✓（当前配置尚未保存）」；api.js 两个方法透传 body；tests/test_bug168_pi_agent_temp_config.py 7 项全绿（新 Key 未保存即可测、空串 Key 回退旧 Key、部分字段回退、不支持 provider 明确报错、测速同逻辑、临时值零持久化）。2026-08-27 复核：代码在工作区未提交，GitHub Issue 仍 open |
| BUG-170 | 修复 | [P2] GitHub Issue #4：report 步骤每次触发 Schema validation errors for report（project_name/files/summary missing）+无效 repair 注入假字段——report-polish prompt 产出 Markdown 文本，却回退用 skill 级 output-schema.json（scripts/generate.py CLI 文件清单结构）校验，#3 同源遗留，不阻塞但污染日志 | 2026-08-27 19:30 | 2026-08-27 19:50 | 已修复 | 已修复：新增 skills/report-generator/templates/output-schema.report-polish.json（#3 引入的 per-prompt 优先加载机制），声明润色产出为自由 Markdown 文本（{"raw_text":...}）不做结构校验；CLI 文件清单 schema 保留不动仍约束 generate.py 产物；tests/test_bug170_report_polish_schema.py（编号统一）初版 5 项；review 修正（2026-08-27）：初版空 schema 过度放宽——①任意 JSON 对象/空正文都能通过；②report-polish 仍走 structured_chat，Markdown 正文含 JSON 示例时 _parse_json_response 会提取该 JSON 并丢弃整篇正文（实测报告被解析成 {"timeout": 30} 后仍零错误通过，路由取不到 raw_text 致报告不落库）。改为：report 步骤走新增纯文本调用链 retry.plain_chat（不做 JSON 解析，始终包装 {"raw_text": text}）；schema 收紧为 required=[raw_text]+minLength=1 并把 raw_text 加入 critical hints，空响应/JSON 残留一律判失败重试；测试扩充为 10 项（含 JSON/Mermaid 代码块正文逐字保留、4 类坏产出全部失败、classify 仍走 structured_chat） |
| BUG-171 | 修复 | [P0] review 发现：requirement-insights 三个子步骤 evolution-match/feature-extraction/gap-assessment 均无 per-prompt schema，回退 skill 级聚合 schema（required=project_name/output_type/metadata），三个 prompt 合法扁平输出实测全部 _schema_critical=True 并各重试 3 次返回 error——insight/full/draft 模式洞察链路不可用，#3 完全同源遗漏 | 2026-08-27 20:30 | 2026-08-27 21:00 | 已修复 | 已修复：补齐 skills/requirement-insights/templates/output-schema.{evolution-match,feature-extraction,gap-assessment}.json 对齐各 prompt 扁平结构（matches 条数由运行时守恒校验保证不锁条数；feature_dimensions 上限 15 下限不锁 5；gap/overlap 关键字段全 required）；_INSIGHT_SUB_STEPS 提为模块常量供契约派生；prd-per-analysis 补 output-schema.per-doc-analysis.json 统一显式声明；test_bug171_insights_schemas.py 10 项全绿（首验通过零重试、关键字段缺失判 critical、完整链路 sub_step_status 全 success） |
| BUG-169 | 修复 | 管理后台模型配置弹窗触发浏览器登录密码管理弹框：浏览器把「LLM 模型名」文本框 + API Key 密码框启发式识别为登录表单，弹出保存用户名/密码提示 | 2026-08-27 11:20 | 2026-08-27 11:45 | 已修复 | 原误标为重复的 BUG-167，CHK-104 复核改为 BUG-169。admin.js 7 处密码框改用 `autocomplete="new-password"`；模型名等文本框加专用 `name` + `autocomplete="off"`；登录页保持 `username`/`current-password`。新增 3 项契约测试；admin 契约含在 48 项定向回归中。代码尚未提交 |
| BUG-172 | 修复 | GitHub Issue #6：智能对话/评审报告的 LaTeX 数学公式与化学方程式无法渲染——marked v15 无数学扩展且会把 `\[` `\(` 定界符转义吞掉，KaTeX/MathJax 未引入，公式以源码文本显示 | 2026-08-27 20:45 | 2026-08-27 21:40 | 已修复 | 已修复：引入 KaTeX 0.16.22 完整发布包到 `src/static/vendor/katex/`（katex.min.js/css + contrib/auto-render + contrib/mhchem + fonts + LICENSE，本地化无 CDN）；新增 `src/static/js/rich-content.js` 共用富内容模块：`protectMath()` 在 marked 解析前把 `$$…$$`/`\[…\]`/`\(…\)` 提取为 data-lat 占位符（跳过代码块/行内代码），DOM 插入后 `enhanceMath()` 用 katex.render 直接渲染（throwOnError:false、错误公式降级显示源码不影响整条消息）+ auto-render 兜底二次扫描；不启用单 `$` 避免金额误判；mhchem 支持 `\ce{}` 化学方程式；chat.js/review.js `_renderMarkdownWithLibraries` 统一接入。真实验证见 DEV-077 |
| BUG-173 | 修复 | [P1] Issue #5/#6/#7 验收残留：report-polish `raw_text` 仅 `minLength: 1`，纯空格/换行仍通过校验并作为成功报告持久化 | 2026-08-27 22:13 | 2026-08-27 22:20 | 已修复 | schema 增加 `"pattern": "\\S"`；`raw_text` 已在 critical hints，pattern 失败会 `_schema_critical` 并失败重试。`test_bug170_report_polish_schema.py` 补 `"   "`、`"\\n\\t"` 的 schema 拒绝 + `run_skill_with_retry` 耗尽重试。定向 12 项 + 全量 1614 passed / 8 skipped |
| BUG-174 | 修复 | [P2] Issue #5/#6/#7 验收残留：`tests/test_rich_content_rendering.py` 中 `sanitize_pos` 赋值未使用，ruff F841 | 2026-08-27 22:13 | 2026-08-27 22:20 | 已修复 | 将该变量纳入渲染顺序断言：`protectMath` → `DOMPurify.sanitize(marked.parse(...))`（parse 嵌在 sanitize 调用内）。`ruff check` 该文件 All checks passed |
| BUG-175 | 修复 | [P1] SVG 预览黑底：模型常用 `<stop style="stop-color:...">` 写渐变，`sanitizeSvg()` 的 `FORBID_ATTR:['style']` 会整体剥除 style 属性，`<stop>` 回退规范默认 `stop-color:black`，4 个渐变（背景+三卡片）全部退化为黑到黑渐变，整图黑底、浅色文字不可读（像素取证：卡片区纯黑 0,0,0，纯色 fill 的元素颜色正常）；且图形视图无白色衬底，SVG 自身背景缺失时直接露出容器灰底 | 2026-08-28 09:30 | 2026-08-28 10:40 | 已修复 | `rich-content.js` 新增 `_normalizeSvgStyles()`+`_isUnsafeStyleValue()`：预检通过后、DOMPurify 清洗前，把白名单 style 声明（fill/stroke/stop-color/stop-opacity/font-*/text-anchor 等 19 项）归一化为同名表示属性再移除 style，取值安全检查拦截 `javascript:`/`expression(`/`-moz-binding`，`url()` 仅放行 `#` 片段引用；XML 解析失败原样返回保持旧路径，安全姿态不变（style 仍在 FORBID_ATTR）。`main.css` `.svg-body` 增加白色衬底（background:#fff + padding + 圆角），图形视图下 SVG 下方恒有白底。`index.html` 缓存版本统一提升 `?v=20260828-1`（含 main.css 首次加版本参数）。`tools/verify_rich_content_browser.py` 新增 `svg_style_gradient_normalized`/`svg_preview_white_backing` 两项浏览器检查（harness 补载 main.css）；`test_rich_content_rendering.py` 新增 3 项契约测试并把 2 项新浏览器检查纳入 required；`test_bugfix_128_129.py` 版本断言同步 20260828-1。真实 Chromium 验证 24/24 checks passed，另用化学对比图全链路（清洗→Blob→白衬底）截图验证渐变/衬底/化学勘误（⁹⁹Tc vs ⁹⁹ᵐTc 区分、删除惰性电子对误用）全部生效。**2026-08-28 复审追加修复**：① 快路径 `indexOf('style=')` 漏掉 `style = "..."`（XML 合法空白等号写法）致渐变再次变黑 → 改为 `/\bstyle\s*=/i`；② `_isUnsafeStyleValue` 字符串包含检查可被 CSS 转义绕过（`fill:u\72l(https://...)` 经 CSSOM 解析为外链）→ 重写为 `_validStyleValue` 按属性整串白名单（颜色 hex/rgb/hsl/关键字/#片段 url()、数值、枚举、字体名），全局拒绝反斜杠/CSS 注释/尖括号，非白名单属性默认拒绝；③ 修正版 SVG 从 /tmp 迁入 `tests/fixtures/chemcmp-oxidation-v2.svg` 作为常青回归（pytest 静态断言 + 浏览器全链路检查）；④ `<img>` 按 SVG width/height/viewBox 预留宽高属性（新增 `_svgIntrinsicSize`），消除 Blob 加载后的布局跳动（CLS）。浏览器工具新增 `svg_style_spaced_eq_normalized`/`svg_style_evasion_urls_rejected`/`svg_fixture_chemcmp_pipeline_ok`/`svg_img_dimensions_reserved` 4 项检查，28/28 passed；pytest 契约同步重写（快路径正则、按属性白名单标记、尺寸预留、fixture 断言）**2026-08-28 二轮复审追加修复**（3 项：2×P1+1×P2）：① [P1] 原生表示属性绕过 URL 白名单——DOMPurify 只按属性名白名单放行、不校验取值，直接写在 fill/filter/mask/marker-*/clip-path 属性上的外链与 data: url() 会原样保留。新增 `_sanitizeSvgUrlAttrs()`：清洗后的 SVG DOM 上统一遍历 URL 型属性（`_SVG_URL_ATTRS` 清单 + marker-* 前缀），取值经 `_validSvgUrlAttrValue()` 整串校验（仅放行本地片段引用 url(#id)/url('#id') 及可选纯色 fallback，其余删除属性）；② [P1] 极端 viewBox 布局型 DoS——`viewBox="0 0 1 100000000"` 会写入同等 width/height，Chromium 实测页面高度达 33554432px，聊天页几乎无法滚动。`_svgIntrinsicSize()` 增加 `_SVG_SIZE_MAX`(20000px)/`_SVG_AREA_MAX`(4×10⁶)/`_SVG_RATIO_MAX`(20) 三重上限，超限不写尺寸属性；CSS `.svg-preview-img` 增加 `max-height:60vh` 兜底；③ [P2] 安全渐变 fallback 误删——`url(#g) #fff` 与 `url('#g')` 是常见安全写法，旧正则只接受单独 url(#id) 导致整条声明被删、元素回退黑底（与防黑底目标冲突）。颜色类取值校验改复用 `_validSvgUrlAttrValue()`，放行片段引用+严格白名单纯色 fallback 与带引号片段引用。缓存版本 `?v=20260828-1`→`-2`。浏览器工具新增 `svg_native_attr_url_rejected`/`svg_extreme_viewbox_bounded`/`svg_gradient_fallback_kept` 3 项检查（31/31 passed），pytest 新增 4 项契约测试并同步旧断言；定向 32 passed，全量回归 1629 passed / 8 skipped |
| BUG-176 | 修复 | CHK-110 复核发现的两处轻微残留（用户指令全部修复）：① tools/migrate_branding.py migrate() 存量 2×F841（scan_result/plan_result 赋值后未使用，与上游合并无关）② test_chk091_residual_phase3.py 测试名/docstring/断言消息仍引用已随上游 V0.1.7 替换移除的 validate_zip_safety（现行 validate_docx_zip_security），并残留无实际断言的临时 zip 脚手架与悬空 import os | 2026-08-28 18:45 | 2026-08-28 19:05 | 已修复 | ① migrate() 保留 scan/plan/apply 三调用（各自控制台输出/报告写入副作用与返回契约不变，main() 仅消费 apply 返回字段），仅去除两处未使用赋值，全仓 ruff 归零。② 测试更名 test_security_checks_raise_docx_security_error，docstring/断言消息对齐现行函数名，删除脚手架及 import os；核心断言不变（DocxSecurityError 为 ValueError 子类 + raise DocxSecurityError( 计数 ≥3，上游现行 5 处）。task-list 历史记录中旧函数名按史实保留；worker 自有 _validate_zip_safety 为 skill 缺失兜底，有意保留。验证：定向 3 文件 125 passed；全量回归 1720 passed / 16 skipped / 0 failed；ruff check . 0 error |
| BUG-177 | 修复 | [P1] CHK-111 发现：docx_conversion_worker 调用 convert_docx_to_markdown 新增关键字 on_limit="skip" 后，tests/test_review_backend_contract.py 的 2 参 skill 桩 TypeError；该测试固定 document_id=123，命中 runtime/data/converted/doc_123 缓存时假绿 | 2026-08-28 19:53 | 2026-08-28 20:20 | 已修复 | 清空缓存后复现：`TypeError: convert_docx_to_markdown() got an unexpected keyword argument 'on_limit'`。生产 skill 已有该参数，Web 上传主路径不受影响。建议：桩函数补 `**kwargs`/`on_limit`；测试改用临时 document_id 或隔离 converted 目录，避免跨运行缓存污染。修复落地：① 桩函数签名补 `**kwargs`（worker 传 `on_limit="skip"` 不再 TypeError）② `monkeypatch.setenv("RUNTIME_ROOT", tmp_path)` 隔离 runtime 根，缓存读写均在临时目录，不再触真实 `runtime/data/converted/` ③ 清理 2026-08-12 起被该测试污染的真实缓存 `runtime/data/converted/doc_123/`（内容为桩输出 `# converted`，非真实转换产物）。验证：连续 2 次运行通过（无缓存依赖）、test_review_backend_contract.py 33 passed、全量回归 1723 passed / 16 skipped / 0 failed |
| BUG-178 | 修复 | [P2] 补丁评审发现：docx skill 输出子目录符号链接越界删除——create_subfolder 模式下 output_dir/<folder_name> 若是指向外部目录的符号链接，assets 目录本身是普通目录可过现有最后一层检查，prune_stale_assets 会沿链接删除 output_dir 之外的所有未引用文件 | 2026-08-28 20:30 | 2026-08-28 21:00 | 已修复 | convert_docx.py 在 os.makedirs 之前加两道防线：① `os.path.islink(final_output_dir)` 直接拒绝（转换的 md 写入同样不再穿透）② realpath 解析后校验 final_output_dir 仍位于 output_dir 真实路径内（纵深防御，覆盖未来嵌套子目录结构）。红/绿验证：剔除防线的前置代码实测会把 victim/assets/keep.bin 删除清空（漏洞真实存在）；加防线后 ValueError 且链接目标零改动。新增 test_docx_security_and_batch.py::TestOutputSubfolderSymlinkGuard（40 passed）。注意：本地 convert_docx.py 自此偏离上游 93afbb1，后续上游同步时需保留此安全补丁（可循 CHK-106 先例向上游提 issue） |
| BUG-179 | 修复 | [P2] 补丁评审发现：管理侧栏分组后的矮视口裁剪——4 个分组标题 + 间距使导航总高约在视口 <630px 时超过 .app-layout 可用高度，父布局 overflow:hidden 且无纵向滚动，底部 Skills/Pi Agent/我的 Agent 项被裁掉无法访问 | 2026-08-28 20:30 | 2026-08-28 21:00 | 已修复 | main.css `.admin-nav` 加 `flex: 1 1 auto; min-height: 0; overflow-y: auto`：导航占据剩余高度并内部滚动，底部折叠按钮（导航外）保持固定。验证：test_admin_frontend_contract.py 新增纵向滚动契约断言（28 passed 含真实浏览器）；Playwright 1440×600 实测 scrollHeight 588 > clientHeight 500、滚到底最后一项「我的 Agent」可见、折叠按钮固定底部未被裁剪 |
| BUG-180 | 修复 | [P2] CHK-111 发现：管理侧栏 4 个分组头用 `aria-hidden="true"` 隐藏，分组本为区分「Pi Agent 配置（全局）vs Agent 设置（个人）」，读屏用户反而听不到分组语义 | 2026-08-28 22:10 | 2026-08-28 22:40 | 已修复 | index.html 4 个 `.admin-nav-group` 移除 `aria-hidden="true"`（纯视觉样式不变；收起态/窄屏退化为 1px 分隔线时视觉不变）。契约测试 test_admin_nav_groups_tab_items_by_scope 断言改为「分组头无 aria-hidden 且可见」；浏览器工具 verify_admin_nav_groups_browser.py 新增「分组头对读屏可见（无 aria-hidden）」检查。验证：浏览器工具 18/18 passed；全量回归 1725 passed / 16 skipped / 0 failed |
| BUG-181 | 修复 | [P2] CHK-111 发现：main.css 窄屏规则 `@media (max-width:1100px) .admin-nav-item span { display:none }` 依赖标签包在 `<span>` 里，但 index.html 9 个导航标签是裸文本节点，≤1100px 图标栏下 tab 文字不被隐藏、可能溢出 56px 侧栏（收起态用 `font-size:0` 不受影响，两态不对称） | 2026-08-28 22:10 | 2026-08-28 22:40 | 已修复 | index.html 9 个 admin-nav-item 标签文字统一包 `<span>`（svg+span 走 flex gap，展开态视觉不变）。契约测试新增 9 标签逐一 `<span>` 包裹断言；浏览器工具新增「窄屏 tab 文字隐藏（span 生效）」检查（实测 hidden=True）。验证：浏览器工具 18/18 passed；全量回归 1725 passed / 16 skipped / 0 failed |
| BUG-182 | 修复 | [P2] CHK-111 发现：浏览器契约用例只检测 playwright 包可导入、不检测 Chromium 二进制，本机未下载浏览器时用例直接失败而非 skip；test_admin_frontend_contract.py 与 test_rich_content_rendering.py 同型 | 2026-08-28 22:10 | 2026-08-28 22:40 | 已修复 | 两文件各新增 `_chromium_binary_available()`（sync_playwright 取 `p.chromium.executable_path` 检查存在性，异常一律视为不可用），浏览器用例体内运行时 `pytest.skip("Playwright Chromium binary not installed")`，避免二进制缺失时误报失败。验证：装包+装浏览器环境全量回归 1725 passed / 16 skipped / 0 failed |
| BUG-183 | 修复 | 未提交变更残留两处（CHK-111 后续全量回归暴露）：① index.html 缓存版本已提升 `?v=20260828-3` 但契约测试仍断言 20260828-2（test_rich_content_rendering.py::test_cache_version_bumped_for_url_sanitizer_fix、test_bugfix_128_129.py::test_index_html_scripts_cache_busted_for_prefix_fix）② test_review_frontend_contract.py:533 多余 f 前缀（ruff F541），test_chk091_residual_phase2.py::TestRuffClean 失败 | 2026-08-28 22:10 | 2026-08-28 22:40 | 已修复 | ① 版本断言同步 20260828-3 / not 20260828-2（128_129 保留 not 20260828-1 历史链）② 去除多余 f 前缀。验证：定向 127 passed；全量回归 1725 passed / 16 skipped / 0 failed；ruff check . 0 error |
| BUG-184 | 修复 | GitHub Issue #8：需求审查工作台任务完成后六个模式卡片状态角标不实时刷新，需手动 F5 才更新（进行中/已完成/部分完成/未完成/已取消） | 2026-08-29 11:31 | 2026-08-30 10:40 | 已修复 | 根因已由 [[CHK-114]] 核对，HEAD（V0.3.16）仍在。不只是 completion 回调未 await `loadProjectDetail`：① `_upsertReviewTask` 只写 `_reviewTaskMap` 不写 `_reviewDocMap`，而 `_updateActionCardStatus` 的「已完成」只读 docMap，SSE/轮询终态从不直接刷角标 ② `_loadReviewHistory` 先清空两张 map 再异步拉历史，未 await 时与 `_showResult` 竞态 ③ `loadProjectDetail`/`_renderDocList` 不对 running 任务 re-attach SSE，刷新后完成事件被丢弃 ④ EventSource 流结束后 onerror 用已消费 ticket 重连 401。按评审方案 P0+P1 完整修复（review.js + review.py）：**P0** ① `_upsertReviewTask` 终态同步写 `_reviewDocMap`（沿用 MODE_COVERS 传播：成功展开子模式、失败/取消仅自身模式），并在函数尾部统一 `_updateActionCardStatus()`（启动/进度/终态三路径同拍刷角标）② `_updateActionCardStatus` 无 running 时优先读 `_reviewTaskMap` 终态（新增 `_terminalStatusLabel` 映射），docMap 作补充 ③ SSE onmessage 与轮询终态均先 `await loadProjectDetail` 再 `_showResult`（handler 改 async）④ `_loadReviewHistory` 重构为局部 taskMap/docMap/history 构建成功后一次性原子替换，失败保留现数据不再清空-再-await。**P1** ⑤ 新增 `_reattachProgressListener`：loadProjectDetail 收尾对选中文档/全局 full/draft 的 running/pending 任务重挂 `_listenProgress`（SSE 失败内建回退轮询；已有监听时不打扰）⑥ SSE 收口：handler 闭包持有 source（防误关后续新连接）、onerror 经 `_getTaskInfo` 终态判断后不再轮询/重连（消除一次性 ticket 401 循环，重连路径经 getReviewProgress 重新签发）⑦ `review.py` event_generator 结束条件补 `completed_with_warnings`（原先流不收口）。**P2** 契约测试 5 项入 test_review_frontend_contract.py（终态回调 await 顺序、upsert 写 docMap+刷角标+map 原子替换、角标优先 taskMap 终态、reattach 监听、SSE 收口含后端结束条件）；`_isDocModeReviewed` 保持原内联映射不动旧契约，`test_failed_review_is_included_in_tasks_with_results` 两处字面量随原子替换重构同步。验证：node --check 通过；契约文件 106 passed；全量回归 1766 passed / 16 skipped / 0 failed；ruff 全过 |
| BUG-185 | 修复 | [严重] 团队资料标题/文件名与成员用户名经 `DOMPurify.sanitize()` 后直接拼入双引号 HTML 属性，可逃逸为 `autofocus`/`onfocus` 等存储型 XSS | 2026-08-30 10:29 | 2026-08-30 10:50 | 已修复 | `workspace.js` 新增 `_esc`/`_escAttr`（对齐 admin.js）；属性位改 `_escAttr`，可见文本改 `_esc`，禁止 `DOMPurify.sanitize` 拼进属性。缓存 `?v=20260830-1`。回归 `tests/test_security_audit_fixes.py` |
| BUG-186 | 修复 | [重要] Agent 禁用后已创建 run 仍可 `/execute`；同步 execute 缺预算检查；并发请求可重复启动子进程 | 2026-08-30 10:29 | 2026-08-30 10:50 | 已修复 | 新增 `AgentApplicationService.prepare_and_claim_run`（属主/启用/状态/预算）+ `AgentRunRepository.try_claim_for_execution` 原子抢占；`/execute` 与 `/stream` 共用。禁用后 execute 返回 400，超配额 429，并发第二请求 409 |
| BUG-187 | 修复 | [重要] DOCX `document.xml` 无解压体积/压缩比限制；URL 抓取 `resp.text` 整包入内存后再截断，已认证用户可耗尽服务内存 | 2026-08-30 10:29 | 2026-08-30 10:50 | 已修复 | `file_text._extract_docx_text` 读入前校验单 entry 10MB/总量 50MB/压缩比 100x（未压缩>64KB 才套用比值）；`upload.submit_url` 改为 `client.stream` + `aiter_bytes`，上限 `_MAX_URL_BODY_BYTES=512KB` |
| BUG-188 | 修复 | [部署阻断] 未设环境变量时创建 `admin/admin@2026`，仅告警仍继续启动 | 2026-08-30 10:29 | 2026-08-30 10:50 | 已修复 | 新建 admin 禁止哈希内置口令：无 `ADMIN_INITIAL_PASSWORD` 则 `secrets.token_urlsafe(24)` 并写入启动日志；存量默认/弱口令无 `ALLOW_DEFAULT_ADMIN_PASSWORD=1` 时 `RuntimeError` 阻断启动。登录页去掉明文默认口令。测试夹具仍用 admin@2026，不走 `_ensure_default_admin`。**已部署实例若仍是默认口令，升级后将无法启动，须先改密或临时设 ALLOW 标志** |
| BUG-189 | 修复 | 个人 Agent 授权非默认 Workspace 后，RAG 未显式传 `workspace_id` 仍回退全局默认空间，导致已授权资料检索 403 | 2026-08-30 10:29 | 2026-08-30 10:50 | 已修复 | `agent_rag_search` 禁止 `get_default()` 回退；授权恰好 1 个 workspace 则使用它，0 个 403，多个 400 要求显式传入 |
| BUG-190 | 修复 | Agent 授权表缺少唯一约束，重复授权后删除一条可能仍保留访问权限 | 2026-08-30 10:29 | 2026-08-30 10:50 | 已修复 | 应用层 `get_by_scope` 查重，重复 POST 返回 409；迁移 `uq_agent_auth_scope`（`IFNULL(scope_id,0)`）并去重保留 MIN(id) |
| BUG-191 | 修复 | [P1] URL 压缩响应可绕过 512KB 内存限制：`aiter_bytes()` 返回解压后数据，截断发生在解压之后（实测 20KB gzip 瞬时解压成 20MB+，解压炸弹） | 2026-08-30 11:10 | 2026-08-30 11:50 | 已修复 | `_read_capped_response_text` 对压缩响应（gzip/x-gzip/deflate）改读原始字节 `aiter_raw()` + zlib.decompressobj 增量解压，按**解压输出**截断（超限探测仅多解 1 字节即中止流）；压缩输入累计量同受上限约束；br/zstd 等无法安全限流的编码直接 400 拒绝；deflate 支持裸流降级重试。identity 路径行为不变。测试 tests/test_bugfix_191_url_decompression.py 6 项（炸弹截断且提前中止流/正常 gzip/identity 截断/编码拒绝/deflate/裸 deflate）；全量 1783 passed |
| BUG-192 | 修复 | [P2] BUG-184 硬刷新恢复监听仍有缺口：刷新后 selectedDocumentId=null，单文档模式（quick/review/pm/insight）running 任务不会重挂监听（历史回退仅限 full/draft），selectDocument() 也不补挂，角标可能停留「进行中」（GitHub Issue #8 关联） | 2026-08-30 11:10 | 2026-08-30 11:50 | 已修复 | ① `_reattachProgressListener` 历史回退查找移除 `['full','draft']` 模式限定，任何 running/pending 任务均可命中；② `selectDocument()` 末尾补调 `_reattachProgressListener()`（已有活跃监听时内部早退，幂等无副作用）。测试 test_review_frontend_contract.py 新增 2 项契约（回退不限模式/选文档补挂） |
| BUG-193 | 修复 | [P2] BUG-190 存量授权迁移去重保留 MIN(id) 策略错误：旧记录已过期而新记录有效时误留过期授权；旧记录权限较宽而新记录已收窄时误留过宽权限 | 2026-08-30 11:10 | 2026-08-30 11:50 | 已修复 | `_migrate_agent_authorization_unique` 去重改为按组保留「最新有效」：组内存在未过期记录（expires_at NULL 视为永久）时保留其中 id 最大者，全部过期时保留 id 最大者（已过期不授予访问，仅作历史）；无 expires_at 列的极旧库退化 MAX(id)；唯一索引 uq_agent_auth_scope 创建不变。新增防重（409）仍由 BUG-190 应用层保证。测试 tests/test_bugfix_193_auth_migration_dedup.py 6 项（过期旧/有效新、宽旧/窄新、全过期、NULL 永久、分组独立、旧库退化） |
| BUG-194 | 修复 | [P2] BUG-188 随机管理员初始密码永久写入普通日志：`[INIT]` 告警把 token_urlsafe 密码直接打进 runtime/logs/app.log（权限 0644），日志读取者获得长期有效管理员凭据 | 2026-08-30 11:10 | 2026-08-30 11:50 | 已修复 | 初始密码只写入一次性保密文件 runtime/secrets/admin_initial_password.txt（目录 0700/文件 0600），日志仅记文件路径与处置指引；写入失败阻断启动（密码既不入日志也不无声丢失），指引改用 ADMIN_INITIAL_PASSWORD 环境变量；`auth.change_password` 对 admin 改密成功后立即删除该文件（凭据生命周期收口，消费幂等）。测试：test_auth_bootstrap.py 强化+新增 3 项（0600/0700 权限、密码不入 caplog、env 路径不产文件、消费幂等）+ change_password 契约；存量既有测试补 RUNTIME_ROOT 隔离 |
| BUG-195 | 修复 | [P2] BUG-192 收口残留：`selectDocument` 先 `_reattachProgressListener` 再 `_navigateOnDocSwitch`，后者关掉刚挂上的 SSE 且不清理 `_pollTimer`；硬刷新后首次选文档 Chromium 复现 `calls: []`，角标停在「进行中」（GitHub Issue #8） | 2026-08-30 13:37 | 2026-08-30 13:50 | 已修复 | ① `selectDocument` 改为先 `_navigateOnDocSwitch`（关旧流）再 `_reattachProgressListener`；② 抽出 `_stopProgressListener`（SSE+轮询），`destroy`/`_navigateOnDocSwitch` 共用。契约测试 2 项（顺序断言 + navigate 必调 stop 且 stop 清 `_pollTimer`）。缓存 `?v=20260830-3`。验证：定向 159 passed；全量 1786 passed / 16 skipped；ruff、`git diff --check` 通过 |

## 调整事项

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| ADJ-001 | 调整 | `.gitignore` 补充 Python 构建产物和前端依赖排除规则 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 新增 `*.egg-info/`、`*.egg`、`*.whl`、`node_modules/`；验证 git 追踪中无缓存、临时或安装包文件 |
| ADJ-002 | 调整 | task-list.md 编号一致性修复：(1) DOC-013 重复编号改为 DOC-042 (2) 补入 CHK-050（P1 口径调整确认）填补 CHK-049→CHK-051 间隙 (3) DEV-015 重复行合并为一条 (4) WBS 快速验证标记 P2 子任务数从旧 18 更新为 20 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 4 处编号/重复/缺失问题已修复；849 全量回归通过 |
| ADJ-003 | 调整 | `.env.example` 补充 Pi Agent / RAG 依赖的环境变量 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 新增 `OPENAI_API_KEY`、`OPENAI_API_BASE`（注释）、`EMBEDDING_MODEL`、`EMBEDDING_DIMENSIONS`；POC-C / P2 EmbeddingService 运行所需 |
| ADJ-004 | 调整 | `requirements.txt` 补充 openai SDK 依赖 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 新增 `openai>=1.0.0`；POC-C 脚本和 P2 EmbeddingService 调用 OpenAI embedding API 所需 |
| ADJ-005 | 调整 | 删除过期 `agent-limiter.js`（旧 module.exports 格式） | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 该文件使用 Pi 旧版 Extension API（`module.exports`），与当前 Pi CLI 0.78.1 不兼容，且不含 BUG-060 修复；仅保留 `.ts` 源文件 |
| ADJ-006 | 调整 | `start.sh` 补充 Pi CLI / Node.js 可选依赖检测 + 修复 status 命令 `$PORT` 未定义 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | `start` 分支新增 `command -v pi` / `command -v node` 可选检测（WARN 不阻塞）；`status` 分支补充 `PORT="${SERVER_PORT:-17957}"`；`bash -n` 语法检查通过 |
| ADJ-007 | 调整 | Extension 从 poc-b 迁移到正式目录 src/agent/extensions/ + PiAgentBridge EXT_DIR 跟随迁移 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | `agent-limiter.ts` 复制到 `src/agent/extensions/`；`pi_agent_bridge.py` EXT_DIR 从 poc-b/scripts/extensions 改为 src/agent/extensions |
| ADJ-008 | 调整 | `update.sh` 扩大 Python 依赖检查 + 补充 Pi/Node 可选检测 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 依赖检查从 4 个扩展为 10 个（+cryptography/dotenv/httpx/openai/lancedb/pyarrow/numpy）；新增 `command -v pi` / `command -v node` 可选检测；`bash -n` 语法检查通过 |
| ADJ-009 | 调整 | `database.py` 补充 pi_agent_config 列级迁移（ALTER TABLE 兼容） | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 新增 `_migrate_pi_agent_columns()` 函数：检查表是否存在 → 逐列检查是否缺失 → ALTER TABLE ADD COLUMN 补充；init_db 在 create_all 后调用；覆盖 5 个可能缺失的列 |
| ADJ-010 | 文档 | WBS 文档标注 Phase 3 已开始 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | `Cowork版-WBS任务分解.md` 第 44 行改为"已开始：Phase 3"；第 339 行标题改为"Phase 3：Agent 对话与工具注册（已开始）"；补充实现摘要 |
| ADJ-011 | 调整 | Pi Agent npm 依赖决策：使用 `@earendil-works/pi-coding-agent` 为主依赖（提供 `pi` CLI 二进制 + 内置工具 + RPC 模式 + Extension 加载），`pi-ai` 和 `pi-agent-core` 作为其子依赖自动安装 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | package.json 精简为仅 pi-coding-agent（0.79.0）；npm install 成功；`node_modules/.bin/pi --version` 0.79.0；PiAgentBridge PI_BIN 改为 `node_modules/.bin/pi`（项目本地路径） |
| ADJ-012 | 调整 | PiAgentBridge PI_BIN 从全局 `pi` 改为项目本地 `node_modules/.bin/pi` | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | `pi_agent_bridge.py` 新增 `_PROJECT_ROOT` 常量（项目根目录）；PI_BIN 从 `"pi"` 改为 `str(_PROJECT_ROOT / "node_modules" / ".bin" / "pi")`；EXT_DIR 也改为从 `_PROJECT_ROOT` 导出 `src/agent/extensions` |
| ADJ-013 | 调整 | `.gitignore` 补充 `node_modules/` 和 `package-lock.json` 排除规则 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | Pi Agent npm 部署后 node_modules 和 lockfile 属于运行时产物不应提交；`package.json` 本身保留追踪（依赖声明）；验证 `git status` 不显示 node_modules/ 和 package-lock.json |
| ADJ-014 | 调整 | start.sh/update.sh 的 Pi Agent 依赖检查改为优先项目本地 `node_modules/.bin/pi`，并补 npm/版本提示 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | `start.sh` 和 `update.sh` 均优先检测 `$PROJECT_DIR/node_modules/.bin/pi` 并输出 `pi --version`，缺失时才回退全局 `pi` 且给出警告；同时补充 `npm` 检查；`bash -n start.sh update.sh` 通过，`node_modules/.bin/pi --version` 为 0.79.0 |
| ADJ-018 | 调整 | `.gitignore` 一轮收口：runtime/config 白名单收紧、IDE/Agent 本地配置、覆盖率与 linter 缓存、env 变体、DB/pid 兜底、冲突临时文件 | 2026-06-26 00:00 | 2026-06-26 00:00 | 已完成 | ① `runtime/config/*` 默认忽略，仅放行 `ui-branding.example.yaml` ② 新增 `.claude/*.local.json`、`.cursor/`、`htmlcov/`、`.mypy_cache/`、`.ruff_cache/`、`.env.*`、`*.db`、`*.md_[0-9]*` ③ `git rm --cached .claude/settings.local.json` ④ 新增 `tests/test_gitignore_contract.py` 2 项契约测试通过 |
| ADJ-015 | 调整 | 删除过期 `AGENTS.md`（已由 CLAUDE.md 替代） | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 项目指引已统一由 `CLAUDE.md` 管理，`AGENTS.md` 不再需要 |
| ADJ-016 | 调整 | P4-P6 WBS 重构：基于团队讨论确认的三阶段业务流程（个人自审→讲解准备→协作审查）重写 Phase 4 WBS | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 删除 ReviewStageExecution 表（阶段追踪用 ReviewTask+ReviewRound）、删除逐条确认（改为物料确认冻结）、删除阶段重跑（改为文档版本链+重走快速审查）；新增 ReviewRound 表（多轮次追踪）、新增 Artifact draft→confirmed 状态、新增 Conversation mode=presentation（迭代对话）、新增 ReviewDocument 版本链、新增 Notification SSE 端点；讲解产物管线降级为 P4.v2 增强版；P5/P6 同步标注与 P3/P4 已有结构的衔接关系 |
| ADJ-017 | 调整 | P4.Pre 协作底座收口 6 项前置任务回填 + P5.A.1/P6.B.2/P6.B.4 验收标准修正 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 代码差距验证发现 6 处"假设过满"：① 审查域 list_projects 仍 owner-only → P4.Pre.1 改造为 workspace 可见性 ② Conversation 无 mode/project_id → P4.Pre.2 扩展 presentation 模式 ③ snapshot_version 只写不读 → P4.Pre.3 补快照回读 ④ Agent 审批无指定审批人 → P4.Pre.4 补 approver_id 落库和过滤 ⑤ 消息无锚点 → P4.Pre.5 补 Message anchor_type/anchor_id ⑥ SkillConfig 无 status → P4.Pre.6 补 status/version 字段；P5.A.1 从 0.5d 调为 2d 联动改造；P6.B.2 标注依赖 P4.Pre.6 前置；P6.B.4 改为基于现有 workspace_access+审计日志而非未落地的 ResourceACL |
| ADJ-019 | 调整 | 预设管理员口令由随机生成改为固定 `Admin@5797` | 2026-07-09 16:36 | 2026-07-09 16:39 | 已完成 | `_ensure_default_admin()` 使用 `DEFAULT_ADMIN_PASSWORD=Admin@5797`；登录页部署提示同步；测试夹具/契约与 E2E 文档凭据同步；保留对历史 `admin123` 弱口令告警；`tests/test_auth_bootstrap.py` 重写；定向相关测试通过，全量 `pytest -q` 1090 passed |
| ADJ-020 | 调整 | 预设管理员口令由 `Admin@5797` 改为 `admin@2026` | 2026-07-09 16:43 | 2026-07-09 16:44 | 已完成 | 同步 `DEFAULT_ADMIN_PASSWORD`、登录页提示、测试夹具/契约与 E2E 文档；定向 136 passed，全量 `pytest -q` 1090 passed |
| ADJ-021 | 调整 | 模型配置 max_tokens（输出 token 上限）由 32768 提升至 100000 | 2026-08-27 11:20 | 2026-08-27 11:45 | 已完成 | 同步 5 处代码：llm.py/retry.py `_MAX_OUTPUT_TOKENS_HARD_LIMIT` 32768→100000、admin.py Create/Update 校验 1-32768→1-100000、admin.js 两处提示文案；docs/admin-guide.md 中英与 docs/configuration.md 中英 4 处文档同步；thinking_adapter.py 的 `thinking_budget: 32768`（HIGH 思考预算，不同语义）不动。test_bug137_max_tokens_cap.py 更新并新增 100000/100001 边界用例；定向 48 passed，全量 1531 passed / 8 skipped。DB 无需变更：本地 DB 既有 65536/65534 配置此前被运行时钳到 32768，现按原值生效；部署实例 DB 200000/360000 配置将按 min(值, 100000)=100000 生效。注意：lwa 部署实例（V0.3.12）需发新版并重新部署后此调整才在其上生效 |
| ADJ-022 | 调整 | LLM 调用重试机制增强：退避序列扩为 2/4/8/16/32/64 秒（1 首发共 7 次尝试），重试等待前向用户页面推送 toast | 2026-08-27 14:10 | 2026-08-27 14:55 | 已完成 | retry.py：RetryConfig 默认 5→7 次、max_delay_ms 30000→64000；新增 `_notify_retry`（4 条失败路径：限流/5xx/超时/连接失败均推送），并修复存量缺陷——最后 1 次尝试失败不再多余等待/推送（原实现会多等 64s 且推送"第 7/6 次"错误文案）。notification_service.py 新增 `push_transient_event`（纯内存 SSE 透传，不落库、用户离线静默丢弃）。notification.js：`llm_retry` 事件仅弹 toast（5s），不计未读数、不入通知列表。review.py `_build_review_retry_config` 代码默认值与 src/config.yaml review.retry 显式配置同步 7/64000。llm.py 新增 `_MODEL_TEST_RETRY_CONFIG`（5 次/30s 上限）钉住管理后台「测试模型」轻量重试，不随管线默认放大。新增 tests/test_llm_retry_toast.py 12 用例（退避序列/耗尽行为/toast 推送与离线丢弃/429 文案/模型测试配置钉住/前后端契约）；定向 29 passed，全量 1543 passed / 8 skipped。注意：lwa 部署实例（V0.3.12）需发新版并重新部署后才生效 |
| ADJ-023 | 调整 | 审查页 action-card 状态徽章配色区分：「未完成」（failed）从绿色改为橘黄色（badge-failed：--orange-6/#FFF7E8），与「已完成」绿色形成视觉区分；「已取消」保持既有橙色系 | 2026-08-27 15:05 | 2026-08-27 15:15 | 已完成 | main.css 新增 .action-card-badge.badge-failed；review.js _updateActionCardStatus 按 status==failed 切换类；契约测试补 2 条断言（JS 类切换 + CSS 橙色变量）防回退，test_review_frontend_contract.py 101 项全绿 |
| ADJ-024 | 调整 | 内部部署管理口令策略：`ALLOW_DEFAULT_ADMIN_PASSWORD=1` 豁免范围从「存量内置口令放行启动」扩大到 `ADMIN_INITIAL_PASSWORD` 内置口令黑名单（支持全新部署用 `admin@2026` 首次建号） | 2026-08-31 10:40 | 2026-08-31 11:00 | 已完成 | database.py 创建路径黑名单检查加开关豁免,默认口令建号打 [SECURITY] 警告;本机 .env 配 `ADMIN_INITIAL_PASSWORD=admin@2026`+开关组合,存量库 admin 哈希重置为 admin@2026（原哈希备份 runtime/secrets/admin_password_hash.backup-20260831.txt,0600）;`.env` 不入 git,部署同学需私下分发;.env.example 补组合说明并修正「随机密码写入启动日志」过时注释;docs/getting-started、admin-guide、configuration 中英共 14 处同步（随机密码落点改为一次性保密文件 0600/改密后删除 + 开关语义扩大）;test_auth_bootstrap.py 新增 2 用例（无开关拒绝建号/有开关放行建号）,定向 23 passed。对外部署前须移除开关并改密（沿用 OPS-022 教训） |

## 检查事项

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| CHK-001 | 检查 | 项目运行方式检查 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 确认项目为 FastAPI + SQLite + 原生 JS SPA；端口 17957；启动方式为 `./start.sh`，包含 start/stop/restart/status；依赖文件为 `requirements.txt`，尚未创建 `.venv` |
| CHK-002 | 检查 | 敏感信息排查 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 全量搜索具体企业和个人信息；docs/、skills/、src/、tests/ 及 git 历史均未发现目标敏感词 |
| CHK-003 | 检查 | 运行时兼容性验证 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 确认匿名化修改不破坏现有数据结构和运行逻辑；数据库 category/title 等字段为自由文本；SkillRunner 分类关键词动态注入；默认分类为空列表；此前全量回归 pytest 344 通过，两次验证均无破坏 |
| CHK-004 | 检查 | 外部任务清单 2026-05-31/2026-06-01 bug 记录差异检查 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 已确认 2026-05-31 巡检提到的运行中任务后端去重、DOCX source hash 写入失败非致命化、登录页低高度响应式回退在当前代码已有实现或测试覆盖；随后修复 BUG-001 |
| CHK-005 | 检查 | 三份持久化与分层文档合理性检查 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 阅读 `后端持久化接口草案.md`、`架构分层改造清单.md`、`运行时数据与持久化接口基线.md`；对照当前 models、runtime 路径、FTS、SSE ticket、JSONL 日志及 review/chat/upload/admin/auth router 耦合点；结论为主方向合理，但发现接口草案重复、相对链接不准、第一阶段优先级口径需收敛 |
| CHK-006 | 检查 | 未提交更改代码审查 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 审查 staged、unstaged、untracked 当前改动；发现上下文项删除 404 语义回退、FTS 搜索排序回退、逐篇分析缓存校验放宽、未跟踪 `__pycache__` 文件等问题；本次为审查任务，未修改业务代码，未运行自动化测试 |
| CHK-007 | 检查 | CHK-006 修复结果复查 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 复查上下文项删除、FTS rank 排序、逐篇分析缓存 rule_key 校验和 `.gitignore`；相关测试 `python3 -m pytest tests/test_code_review_fixes.py tests/test_review_backend_contract.py -q` 39 通过，全量 `python3 -m pytest -q` 384 通过；仍发现 expert_review summary 非空校验未完全保留、项目删除文件清理异常处理语义变化 |
| CHK-008 | 检查 | 架构分层 WBS 完成度与 bug 复查 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 对照 `docs/3-design/架构分层改造清单.md` 复查 WBS 0~F；专项 `python3 -m pytest tests/test_router_persistence_scan.py tests/test_code_review_fixes.py tests/test_review_backend_contract.py -q` 47 通过，全量 `python3 -m pytest -q` 384 通过；发现 `ChatApplicationService` 未落地、Review 文件删除仍在 router、扫描测试漏扫 `os.remove()`、expert_review summary 非空校验缺失等问题 |
| CHK-009 | 检查 | 未提交更改代码审查 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 审查 staged、unstaged、untracked 当前改动；重点覆盖 repository/storage/service 分层、chat/review/admin/auth 路由重构、prompt 级 schema、前端全局对象暴露和运行时数据隔离；发现 ReviewFileStorage 上传目录忽略 `review.upload.upload_dir` 配置的回归风险；相关测试 `python3 -m pytest tests/test_data_compat.py tests/test_review_backend_contract.py tests/test_chat_integration.py tests/test_context_panel.py tests/test_skill_schema.py tests/test_router_persistence_scan.py` 82 通过 |
| CHK-010 | 检查 | V0.2.5-Build0166-20260602 提交前变更汇总与 commit message 准备 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 汇总所有未提交代码、测试、文档、配置和任务清单变更；输出不超过 200 字的 commit message；本次仅做提交前整理说明，未修改业务实现 |
| CHK-011 | 检查 | 新版代码替换旧版代码并复用旧 runtime 的验证方案梳理 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 梳理基于旧 runtime 副本的启动验证、数据库完整性检查、业务冒烟、日志观察、正式替换与回滚要点；强调保留旧 `JWT_SECRET` 和先备份 runtime |
| CHK-012 | 检查 | 新版代码使用旧版 runtime 实机启动验证 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 使用历史参考文件中的旧 runtime 与旧 `src/.env` 启动新版代码，端口 17957；验证 health、首页静态资源、SQLite integrity、旧用户/会话/审查项目/报告读取、admin 模型 API Key 解密均正常；服务已通过 `screen` 后台会话 `prd-runtime-17957` 保持运行 |
| CHK-013 | 检查 | 旧版 runtime 登录操作日志与数据落库复核 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 复核登录后的审计日志、应用日志、SQLite 落库和报告 API；确认 admin 登录、页面跳转、审查启动和报告读取均有成功日志；任务 118 已 completed，1 条逐篇分析结果有效落库并可读取；仅发现提示词文件缺失与 doc_id 自动修复两条非阻断告警 |
| CHK-014 | 检查 | V0.2.6 功能迭代任务分解合理性检查 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 对照 `docs/2-discussion/2026-06-02-小版本功能迭代讨论稿.md` 与 `docs/3-design/V0.2.6-功能迭代任务分解.md`，复核模型思考级别、Markdown/Mermaid、公共文件库、飞书导入四阶段拆解；结论为总体合理但需补充迁移命名、思考参数映射准确性、公共库引用/快照策略、飞书凭证存储和验收口径 |
| CHK-015 | 检查 | P1 模型思考级别全栈开发代码审查 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 审查前端 Chat/Review 下拉、Admin 模型配置、后端 Schema/Repository/Adapter/LLM extra_body 透传链路；发现 BUG-006、BUG-007；验证 `python3 -m pytest tests/test_thinking_adapter.py -q` 34 passed |
| CHK-016 | 检查 | P1 模型思考级别 BUG-006/BUG-007 修复复查 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 复查 `chat.js`/`review.js` 已显式发送 `off`，`model_config_repository.py` + `admin.py` 已支持 `thinking_payload` 显式置空；针对性回归 208 passed，全量 `python3 -m pytest -q` 430 passed；结论为可进入下一个需求 |
| CHK-017 | 检查 | P2 Markdown 与 Mermaid 渲染修复方案选型调研 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 对照当前 [src/static/index.html](src/static/index.html)、[src/static/js/chat.js](src/static/js/chat.js)、[src/static/js/review.js](src/static/js/review.js) 与 Marked、markdown-it、Mermaid、DOMPurify 官方文档；结论为继续沿用 `marked + DOMPurify + mermaid` 主组合最稳妥，优先补本地 vendor、完整 fallback、Mermaid 在流结束后统一 `parse/render` 与错误源码回显；`markdown-it` 作为后续增强备选，本次调研未修改业务代码、未运行自动化测试 |
| CHK-018 | 检查 | P2 Markdown/Mermaid 二次评审意见评估 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 评估 CDN fallback、fallback 复杂度、流式结束兜底、P2.3/P2.4 合并交付、浏览器真实验证、DOMPurify 与 Mermaid SVG 顺序/配置风险；结论为意见基本可采纳，建议后续同步修订两份 V0.2.6 文档 |
| CHK-019 | 检查 | P2 Markdown 与 Mermaid 全栈开发代码审查 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 审查 `src/static/vendor/`、`index.html`、`chat.js`、`review.js`、`main.css` 与 P2 契约测试；发现 BUG-008、BUG-009；验证 `python3 -m pytest tests/test_markdown_contract.py tests/test_chat_frontend_contract.py tests/test_review_frontend_contract.py -q` 137 passed、`python3 -m pytest tests/test_chat_integration.py tests/test_review_backend_contract.py -q` 44 passed、全量 `python3 -m pytest -q` 460 passed |
| CHK-020 | 检查 | 17957 本机服务替换可行性判断 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | `curl -sS http://127.0.0.1:17957/api/health` 返回 `{"status":"ok","version":"0.1.0"}`；结合 CHK-019 发现的 BUG-008/BUG-009，结论为当前代码可用于受控冒烟测试，但不建议立即作为可替换旧服务的稳定版本运行 |
| CHK-021 | 检查 | P2 BUG-008/BUG-009 修复复查 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 复查历史消息 Mermaid 已改为 `_renderMermaidOnStreamEnd(div)`，原裸 `<li>` 问题已补 `<ul>/<ol>` 容器；但发现 BUG-010：fallback 列表容器会被移动到内容末尾；验证 P2 前端契约 140 passed、chat/review 后端相关 44 passed、全量 `python3 -m pytest -q` 463 passed |
| CHK-022 | 检查 | 17957 本机服务替换为当前新版代码 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 复查 BUG-008/BUG-009/BUG-010 修复后，P2 前端契约 142 passed、chat/review 后端相关 44 passed、全量 `python3 -m pytest -q` 465 passed；停止旧 17957 进程后用 `screen` 会话 `prd-runtime-17957` 启动当前代码，`/api/health`、首页 `/`、`/vendor/mermaid.min.js` 均返回 200；另记录非阻断 fallback 边界 BUG-011 |
| CHK-023 | 检查 | 17957 本机服务切换为新版代码复用历史 runtime | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 停止原先使用当前仓库 `runtime/` 的 17957 进程；复用历史站点 `src/.env`，以 `RUNTIME_ROOT=/Users/fenix-macmini/Documents/VSCode/1-AI-Coding/10-真实业务工作流系统-2026/1-需求评审和团队协作工作流V1-2026/历史参考文件/20260515-需求评审内网小网站/runtime` 重新启动新版代码；确认进程已打开历史 runtime 下的 `logs/app.log` 与 `data/app.db`，`/api/health`、首页 `/`、`/vendor/mermaid.min.js` 均返回 200 |
| CHK-024 | 检查 | 品牌与界面个性化配置需求可行性讨论 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 盘点当前硬编码项：登录页 Logo、工作页顶栏 Logo、favicon、页面 title、产品名、登录页说明、顶栏入口文案与主题色；结论为适合新增 runtime/local 配置优先机制，用于新版通用代码替换旧项目时保留本地品牌资产与展示文案 |
| CHK-025 | 检查 | reasoning_content 全链路代码审查 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 审查 StreamChunk/log_llm_session/chat.js/review.js reasoning_content 链路；发现 BUG-014（思考内容误清）和 BUG-015（null 类型不安全）；确认零测试覆盖和 4 处文档滞后；修复后新增 `tests/test_reasoning_content.py`(22)；494 全量回归通过 |
| CHK-026 | 检查 | DEV-012 品牌迁移工具代码审查 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 审查 `tools/migrate_branding.py` 与 `tests/test_migrate_branding.py`；定向测试 `python3 -m pytest tests/test_migrate_branding.py -q` 30 passed，全量 `python3 -m pytest -q` 576 passed；发现 BUG-016（资产路径安全未收口）和 BUG-017（旧项目根目录输入/JS 扫描能力不一致） |
| CHK-027 | 检查 | BUG-016/BUG-017 修复后品牌迁移工具复审 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 定向测试 `python3 -m pytest tests/test_migrate_branding.py -q` 52 passed，全量 `python3 -m pytest -q` 598 passed；复审发现 BUG-018（绝对路径被错误改写为相对路径写入 YAML）和 BUG-019（嵌套相对资产复制缺少父目录导致崩溃），建议修复后再实际应用 |
| CHK-028 | 检查 | BUG-018/BUG-019 修复后品牌迁移工具上线前复核 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 复核 `_validate_asset_path_safe()` 已拒绝 `/etc/passwd` 并保留 `/favicon.svg`、`icons/favicon.svg` 合法路径；复核 `copy_assets()` 可复制 `icons/favicon.svg` 并创建父目录；定向测试 `python3 -m pytest tests/test_migrate_branding.py -q` 58 passed，全量 `python3 -m pytest -q` 604 passed；未发现阻断实际应用的问题 |
| CHK-029 | 检查 | DEV-014 P3.6 迁移工具架构升级完成度审查 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 审查 `tools/migrate_branding.py`、`src/app/services/branding_config.py`、`tests/test_migrate_branding.py`、`tests/test_branding.py`；定向测试 `python3 -m pytest tests/test_migrate_branding.py tests/test_branding.py -q` 146 passed，全量 `python3 -m pytest -q` 644 passed；发现 BUG-028（confirm/低置信项仍自动进入 apply），DEV-014 不建议按已完成验收 |
| CHK-030 | 检查 | BUG-028 修复后 P3.6 迁移工具复核 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 红绿验证：新增 `test_build_branding_yaml_only_includes_auto_fields` 期望 `theme_primary_svg` 不写入、`test_apply_does_not_write_low_confidence_project_root_logo` 期望低置信扩展 Logo 不写入不复制，修复前 2 failed、修复后 2 passed；定向 `python3 -m pytest tests/test_migrate_branding.py -q` 79 passed，`python3 -m pytest tests/test_branding.py -q` 68 passed，全量 `python3 -m pytest -q` 645 passed |
| CHK-031 | 检查 | `update.sh` 更新脚本方案与当前草稿审查 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 认可版本更新脚本作为标准交付组件的方向；检查当前草稿并执行 `bash -n update.sh` 语法通过；发现正式落地前需收口：不能把 `task-list.md` 作为旧代码包覆盖对象、不能在 update 脚本内自动 apply 品牌迁移、回滚恢复 `.env` 逻辑有路径缺陷、`rm -rf src/tools/tests/docs` 覆盖前需更严格包校验、脚本尚未 chmod 可执行 |
| CHK-032 | 检查 | DEV-015 标准更新脚本修复完善后复核 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 红绿验证：`tests/test_runtime_config.py` 新增 5 项 update.sh 契约测试，修复前 5 failed，修复后 11 passed；补充验证 `bash -n update.sh`、`python3 -m pytest tests/test_migrate_branding.py tests/test_branding.py -q` 147 passed，全量 `python3 -m pytest -q` 650 passed |
| CHK-033 | 检查 | BUG-031 旧版 `login_notice_text` 兼容修复验证 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 红绿验证：新增 3 项测试覆盖 `_merge_branding()`、`_load_runtime_branding()`、`scan_runtime()` 对 `login_notice_text` 的兼容，修复前 3 failed、修复后 3 passed；定向 `python3 -m pytest tests/test_branding.py tests/test_migrate_branding.py -q` 154 passed；`tests/test_runtime_config.py` 更新脚本契约 2 passed；全量 `python3 -m pytest -q` 657 passed |
| CHK-034 | 检查 | 未提交代码（P3.6 品牌/迁移/update.sh）缺陷审查 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 全量 `python3 -m pytest -q` 657 passed；`bash -n update.sh` 通过；发现 BUG-032（`scan_runtime` 通用 logo 只映射顶栏）、BUG-033（空 yaml 阻断资产自动发现）；`update.sh` 仅 scan/plan 不自动 apply，上线路径相对安全；`task-list.md` 存在重复 DEV-015 行（文档问题） |
| CHK-035 | 检查 | 外部开源项目调研：Cloud co-worker、团队知识库、AI Agent 对话、RAG/内部知识库检索、MCP/工具调用、协作消息、需求/文档审查与 AI 原型生成相关项目 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 已按 star >= 600 过滤，整理 12 个候选项目，并精选 8 个项目做架构/设计拆解；结论已沉淀到 `docs/2-discussion/2026-06-04-Cloud-coworker-远期规划与开源项目调研.md`，并进一步产出 `docs/3-design/Cloud-co-worker远期规划与架构演进方案.md` 作为现有系统延续式演进方案 |
| CHK-036 | 检查 | 对照 cowork 版工具设计规划复核 V0.2.x P4/P5 后续归位 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 结论：旧 P4“公共文件库”不应按原范围直接开发，应改名并收束为新规划 P0“团队资料库 MVP”；旧 P5“飞书导入”可整体后置为后续连接器/知识源导入能力，待团队资料库、空间权限和检索底座稳定后再做 |
| CHK-037 | 检查 | Milvus 作为 cowork 规划向量库候选的选型判断 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 结论：Milvus 可纳入向量库候选，尤其适合中后期数据规模扩大、需要 standalone/distributed 服务化部署时评估；近期 P0/P2 仍建议以 SQLite FTS5 + LanceDB/Milvus Lite/Chroma 小样本 POC 为主，不直接把 Milvus 集群作为默认底座 |
| CHK-038 | 检查 | workflow-cookbook 对 cowork 版工具设计的借鉴价值评估 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 阅读 `AGI-is-going-to-arrive/workflow-cookbook` README 与关键章节；结论：不直接复用其 Claude Code Workflow 运行时，但可吸收确定性编排、phase 进度、schema 结构化输出、parallel/pipeline、预算/续传、对抗验证和 Workflow 库版本化等设计模式，用于补强 SkillRunner、AgentRun、ReviewStageExecution 和多角色审查流程 |
| CHK-039 | 检查 | OpenCoworkAI/open-cowork 对 cowork 版工具设计的借鉴价值评估 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 阅读 `OpenCoworkAI/open-cowork` README、README_zh、ROADMAP 与 package 信息；结论：不直接复用其 Electron 桌面形态，但可借鉴本地工作区授权、WSL2/Lima VM 沙盒、Skills/MCP 管理、Trace Panel、Feishu/Slack 远程控制与审批面板、多模型配置、记忆检索和工作区模板等产品化设计，用于补强个人 Agent、团队空间、工具调用观测与人在环控制 |
| CHK-040 | 检查 | onyx-dot-app/onyx 对 cowork 版工具设计的借鉴价值评估 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 阅读 `onyx-dot-app/onyx` README、中文 README、官方连接器与核心概念文档；结论：Onyx 对团队资料库、连接器同步、权限感知检索、混合索引、Agent/Action/MCP、查询审计和使用分析有强参考价值，但其 Standard 栈包含 PostgreSQL、Redis、MinIO、Vespa/OpenSearch、后台 worker 等，近期不宜直接照搬；P0/P1 先吸收 Document/Section/Connector/Credential/AccessType/SyncStatus 模型与轻量索引状态管理 |
| CHK-041 | 检查 | Cowork 版 WBS P0 开发完成度对照审查 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 对照 `docs/3-design/Cowork版-WBS任务分解.md` 与 `task-list.md` 审查 P0 改动；全量 `python3 -m pytest -q` 747 passed；结论：P0 主体已形成，但发现 BUG-034 所列验收缺口，暂不建议直接进入 P1/P2 下一阶段开发 |
| CHK-042 | 检查 | BUG-034 修复结果复查 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 复查 P0 五项验收缺口：引用资料权限/状态校验、资料列表 tag/status 过滤、资料详情正文/引用项目/后端下载、成员加载顺序已有改动；但新建项目 `workspace_id` API 响应仍为 `None`，前端下载仍使用裸链接无法携带 Bearer 鉴权；定向测试 1 failed, 89 passed，暂不建议进入下一阶段 |
| CHK-043 | 检查 | BUG-034 最终修复验证 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 红灯复现 `test_create_review_project_writes_workspace_id` 失败、前端下载契约 2 failed；修复后针对性 3 passed，P0 定向 `python3 -m pytest tests/test_workspace_api.py tests/test_workspace.py tests/test_frontend_workspace_contract.py -q` 91 passed，全量 `python3 -m pytest -q` 754 passed；结论：BUG-034 已收口 |
| CHK-044 | 检查 | 扣子 3.0 最新更新对 Cowork 版协作工作流启示调研 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 调研扣子 3.0 项目空间、多端协同、本地/云端 Agent 接入、成员与 Agent 混合协作、Agent 待办和状态可视化等更新；结论：应将本项目后续规划从单一审查工具升级为团队空间内的人机协同编排面，优先补强 Agent 注册/运行/待办/审计、项目级协作上下文和权限边界 |
| CHK-045 | 检查 | Cowork 版 WBS 完成状态快速核验 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 对照 `docs/3-design/Cowork版-WBS任务分解.md`、`task-list.md`、`src/app/routers/workspace.py`、`src/static/js/{workspace,review,api}.js` 与相关测试，定向执行 `pytest -q tests/test_workspace.py tests/test_workspace_api.py tests/test_frontend_workspace_contract.py`，91 passed；结论：P0.A.1~P0.E.4 已有实现与测试证据，P1~P6 本轮未标记完成 |
| CHK-046 | 检查 | `pewdiepie-archdaemon/odysseus` 对 cowork 版工具设计的借鉴价值评估 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 阅读 `odysseus` 官方 README 与 `ACKNOWLEDGMENTS.md`；结论：它对当前项目最有价值的是 Web 化自托管 AI 工作台、一体化 workspace、Deep Research 产物流、管理员高权限工具治理、127.0.0.1 默认绑定和运行时数据 `data/` 隔离；但不应直接照搬其邮件/日历/笔记/任务全家桶，也不应向普通成员默认开放 shell/file/webhook 等高权限能力 |
| CHK-047 | 检查 | Cowork 版 P0 阶段收口确认 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | P0 全部子任务（P0.A.1~P0.E.4）已有实现与测试覆盖；BUG-034 验收缺口已修复；BUG-036 侧边栏 UI 一致性已修复；update.sh 版本号同步已实现；全量 759 passed；正式确认 P0 阶段收口，可进入 P1 阶段规划 |
| CHK-048 | 检查 | P1 启动条件与后续 WBS 调整复核 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 复核 `workspace.py`、`workspace_repository.py`、`workspace.js`、`review.py`、`auth.py` 与 P1 WBS；定向执行 `pytest -q tests/test_workspace.py tests/test_workspace_api.py tests/test_frontend_workspace_contract.py`，91 passed；结论：P1 可以开始，但需先把默认团队空间从名称匹配改为稳定标识，P1.A.3 应标记为部分完成，P1.B 应先统一权限 helper 并补 review 域访问对齐，而不是立即落 `RolePolicy/ResourceACL` 表 |
| CHK-049 | 检查 | Cowork 版 P1 开发完成后代码审查 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 对照 `docs/3-design/Cowork版-WBS任务分解.md` 审查 P1 相关改动，执行 `pytest -q tests/test_workspace_api.py::TestMemberManagementEndpoints tests/test_workspace_api.py::TestReviewWorkspaceActiveMemberCheck`，8 passed；另用临时 API 脚本复现停用成员仍可访问旧 review 项目、停用成员不再出现在成员列表；新增 BUG-037~BUG-040 待修复 |
| CHK-050 | 检查 | P1 口径调整与实现一致性确认 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 确认 P1 口径调整结论与代码实现一致：团队 status 更新后端保留但前端不暴露归档入口、owner 降级确认弹窗保留作为最小安全保护、P1.B.3 统一鉴权入口已通过 require_action + _verify_project_owner 落地、文档能力仅做上传/删除/提取/引用不做编辑；为 CHK-051 收口确认提供前置判断 |
| CHK-051 | 检查 | Cowork 版 P1 阶段收口确认 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | P1 全部子任务已有实现与测试覆盖；BUG-037~BUG-040 已修复；791 全量回归通过；CHK-050 口径调整结论与代码实现一致；正式确认 P1 阶段收口，可进入 P2 规划 |
| CHK-052 | 检查 | P2 阶段需求与功能点产品视角梳理 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 从产品经理视角梳理 P2 四大功能模块（文档解析与切块、检索服务、对话与审查 RAG 集成、检索评估与测试）+ POC-A 前置闸门 + 与 P1 衔接关系 + 工时节奏 + 产品风险；P2 总计 15 子任务估算 4-6 周，关键路径 G0→G1→G3 约 13.5 天 + POC-A 约 9.5 天 |
| CHK-053 | 检查 | POC-A 检索方案对比实验完成度验证 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 30份样例文档（274 chunks）+ 30个检索问题 + 10个权限过滤用例；FTS5 baseline 56.7%/0.3ms；LanceDB 66.7%/254ms；Milvus Lite 66.7%/1582ms（延迟过高淘汰）；Chroma 66.7%/242ms；决策结论：首选 FTS5 + LanceDB 混合检索，备选 Chroma；对比报告 poc-a/reports/POC-A-对比报告.md；WBS POC-A 状态已更新为 ✅ 已完成 |
| CHK-054 | 检查 | POC-A 检索方案对比报告二次复核 | 2026-06-06 00:00 | 2026-06-06 00:00 | 已完成 | 复核 `poc-a/reports/POC-A-对比报告.md` 与 POC runner/evaluate 脚本；发现报告结论证据不足：norm 期望 ID 使用 `NORM-xxx` 但实际 source_id 为 `norm-xxx-中文标题`，精确匹配导致部分真实命中被错判；三套向量库检索函数均在查询时重建 chunk/TF-IDF/vocab，延迟混入查询向量构造成本；权限正确率未使用 `expect_prefixes` 细判；无答案类缺少阈值/拒答逻辑。结论：Milvus Lite 不应因当前 1582ms 数据直接淘汰，可保留为二次 POC 候选，但需补真实嵌入、缓存 query encoder、权限/锁/并发验证后再定首选 |
| CHK-055 | 检查 | Runtime 真实需求 Markdown 检索复验与 POC-A 脚本追踪规则调整 | 2026-06-06 00:00 | 2026-06-06 00:00 | 已完成 | 新增 `poc-a/scripts/runtime_eval.py` 与 `poc-a/tests/test_runtime_eval.py`，从旧 runtime 转换目录读取 `review_documents` 指向的真实需求 Markdown，过滤 failed/缺失/重复文档，生成精确 source_id 测试集并写入 `runtime/poc-a-real/`；复验 45 份文档、943 chunks、84 个问题：FTS5 top5 42.9%/0.4ms，LanceDB top5 95.2%/16.9ms，Milvus Lite top5 95.2%/530.0ms，Chroma top5 95.2%/4.8ms，权限正确率均 100%；`.gitignore` 已调整为追踪 `poc-a/scripts/*.py` 与 `poc-a/tests/*.py`，继续忽略 samples/results/reports/runtime 派生数据；定向测试 `python3 -m pytest poc-a/tests/test_runtime_eval.py -q` 2 passed |
| CHK-056 | 检查 | POC-A 对比报告修正：纳入 Runtime 真实数据复验结论 + sqlite-vec POC | 2026-06-06 00:00 | 2026-06-06 00:00 | 已完成 | 修正 `poc-a/reports/POC-A-对比报告.md`：(1) 加入两组独立评估数据对比（合成样本 vs Runtime 复验），标注合成样本的 expect_ids 格式偏差和查询向量构建偏差；(2) 修正 Milvus Lite 结论从"淘汰"改为"强候选"（Runtime 复验显示三方案检索质量完全一致 95.2%/89.3%/100%）；(3) 加入 sqlite-vec POC 数据（合成 73.3%/6.1ms，标注未在 Runtime 复验）；(4) 调整向量库候选优先级为 LanceDB 首选 > Milvus Lite 强候选 > Chroma 备选 > sqlite-vec 轻量备选；(5) 建议新增 POC-B 验证真实嵌入+并发+锁恢复后再定首选 |
| CHK-057 | 检查 | POC-B 结果进一步分析与 POC-A+B 综合选型最终结论 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 深入分析 POC-B 原始数据，发现三个关键问题：(1) Hybrid 退化根因——FTS5 bigram 误召回 + TF-IDF 未归一化导致 L2 距离负分 + Hybrid score normalization 失真叠加（2/84=2.4% 退化），生产使用真实嵌入模型后负分问题不复现；(2) LanceDB 权限 90.9%（20/22）是 POC lambda 未实现 role 参数拦截所致，workspace 过滤本身 100%；(3) 无答案阈值拒答所有方案误答率 25%-100%，不可用。基于 POC-A+B 全部数据确定 P2 最终选型：LanceDB 单引擎首选（95.2%/89.3%/10ms/120QPS/100%/7ms备份），FTS5 降级回退，分数差+LLM 证据判断拒答，OpenAI text-embedding-3-small 嵌入。产出文档：`docs/3-design/检索引擎选型最终结论.md`；修订 `poc-b/reports/POC-B-混合检索深度验证报告.md` 和 `poc-a/reports/POC-A-对比报告.md`、`poc-a/reports/POC-A-Runtime真实需求复验报告.md` 决策建议部分 |
| CHK-058 | 检查 | P2 实施规划 Review：基于 POC-A+B 全量数据对 WBS P2 任务进行完整审查 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 发现并修正6处问题：(1) P2.A.3 职责混乱，把 embedding API 集成混入 IngestionService；(2) 缺少独立 EmbeddingService 任务（新增 P2.A.5）；(3) P2.E.1 与 P2.A.3 职责边界不清，拆分为"IngestionService 编排"和"KnowledgeVectorService 实现"；(4) P2.E.3 拒答策略 metric 不精确，补充 _distance 分数差定义和阈值校准说明；(5) G5 对 G1 的前置依赖未标注；(6) P2.D.1 评估门槛只写 ≥80% 未体现 POC 95.2% 基准；修改后 P2 任务数 18→20，并行分组依赖关系明确 |
| CHK-059 | 检查 | POC-C 真实嵌入检索验证完成度与结果分析 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 使用 OpenAI text-embedding-3-small（1536维）替代 TF-IDF fallback，对 LanceDB + Chroma + FTS5 在 45份真实文档/943 chunks/104问题（84自动+20手写）上跑 5轮评估；关键发现：(1) LanceDB 标题/章节定位 100%（首版场景达标），整体 80.8%（被语义/跨文档手写难题拉低）；(2) 端到端延迟瓶颈是 embedding API（~150-200ms），向量库差异占比<5%；(3) dist[0] 绝对阈值拒答可行——LanceDB 设 1.0、Chroma 设 0.5（可答/无答案分布有清晰分离）；(4) gap 阈值 LanceDB 0.065、Chroma 0.032；(5) LanceDB P99=1167ms（冷启动）→ 需启动预热；(6) LanceDB 备份 5ms/6.1MB 验证通过；结论：LanceDB 选型确认不变，P2.E.3 拒答阈值从 0.15 更新为 dist[0]=1.0+gap=0.065，新增 EmbeddingService 预热和查询缓存需求 |
| CHK-060 | 检查 | P2 全量代码审查：4 个审查代理并行检查 8 个新文件 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 发现并修复 11 个问题：CRITICAL 4 个（embedding_service time.sleep 阻塞 event loop→改 asyncio.sleep；FTS5 重入库幽灵结果→清除旧条目；FTS5 MATCH 特殊字符→sanitize；chunk 加载无限制→LIMIT 50）、HIGH 4 个（拒答策略 top-1 逻辑 bug→先判绝对阈值再遍历；N+1 查询→循环外查一次；缺唯一约束→source_id unique + (doc_id,chunk_no) unique；trigram 最小 3 字符→guard）MEDIUM 3 个（标签校验→元素类型+数量上限；编号间隔→连续编号；http header 注入→文件名清理）；833 全量回归通过 |
| CHK-061 | 检查 | P0～P2 阶段完成状态全面回顾 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | P0 24/24 ✅（CHK-047 收口）、P1 10/10 ✅（CHK-051 收口）、P2 19/20（P2.D.1 检索评估待完成）、POC-A/B/C 13/13 ✅；合计 66/67 = 98.5%；唯一阻塞项 P2.D.1 |
| CHK-062 | 检查 | P2.D.1 检索评估验收确认 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 评估数据：30份文档/464 chunks/80问题（含8 no_answer）；验收结果全部通过：top-5 命中率 95.8% ≥ 92%、no_answer 拒答率 87.5% ≥ 50%、无越权召回（0权限违规）；延迟 P50=430ms/P95=786ms/P99=1441ms；P2 阶段正式收口，可进入 P3 规划 |
| CHK-063 | 检查 | 提交 5d1dd6ab62bb76779670f1733dd9159d1d021a15 简化向代码审查 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 按用户要求聚焦 changed code 中不必要复杂度、冗余状态、复制粘贴与死代码；输出 6 个候选 JSON；本次为审查任务，未修改业务代码，未运行自动化测试 |
| CHK-064 | 检查 | 提交 5d1dd6ab62bb76779670f1733dd9159d1d021a15 高度/分层角度代码审查 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 按用户要求聚焦改动是否落在正确抽象层级、是否为脆弱补丁；输出候选 JSON；本次为审查任务，未修改业务代码，未运行自动化测试 |
| CHK-065 | 检查 | 提交 5d1dd6ab62bb76779670f1733dd9159d1d021a15 删除行为审查 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 按用户要求聚焦 `git diff 5d1dd6a^ 5d1dd6a` 中删除/替换行，逐项核对被移除的不变量/保护是否在新代码重建；本次为审查任务，未修改业务代码，未运行自动化测试 |
| CHK-066 | 检查 | P2 阶段完成度与提交 5d1dd6ab62bb76779670f1733dd9159d1d021a15 bug 修复后复核 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 复查 BUG-052/BUG-053（上传自动入库、LanceDB 降级 FTS、SSE 半行缓冲等）；定向回归 7 passed，全量 887 passed；P2 检索可用性缺口已收口，可进入 P3 |
| CHK-067 | 检查 | POC-B Pi Agent RPC 集成首轮验证结果分析 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 首轮 7/8 通过；关键发现：① Pi RPC JSONL 协议完整可用（stdin 命令/stdout 事件） ② Extension 需用 export default function(pi) 格式而非 module.exports ③ OpenAI API 超时需切换 DeepSeek ④ 进程崩溃后可自动恢复 ⑤ 3 用户并发可行（每进程 ~130-190MB RSS） ⑥ --no-tools/--tools 参数限制有效 ⑦ 自定义 system prompt 可注入受限行为 |
| CHK-068 | 检查 | Pi Agent 模型配置项连接测试验证 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | model_id=pi-agent 通过连接测试（DeepSeek API），模型在 chat/models 列表中可见；服务版本 0.2.11 运行正常 确认 P2 20/20 已完成；针对提交复查发现并修复 BUG-052/BUG-053；定向回归 7 passed，全量 `python3 -m pytest -q` 887 passed；`git diff --check` 通过；未发现 runtime/env/key 数据进入 diff |
| CHK-069 | 检查 | Pi Agent 独立配置页面 API 与前端功能验证 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 新增 PiAgentConfig ORM 模型（LLM/Search/Vision/Extension/Skill/SystemPrompt 字段）、PiAgentConfigRepository、pi_agent router（7 个 API：GET/PUT config、PUT llm/search/vision api-key、POST test-connection/speed-test）、前端 admin.js loadPiAgentConfig/savePiAgentConfig 方法（7 个配置分区：全局开关/大模型/Search/Vision/Extension/Skill安装/SystemPrompt）、api.js 7 个 Pi Agent API 方法、CSS .pi-agent-section 样式、HTML 新增 pi-agent 导航项和面板；API 全链路端到端验证通过：GET config ✓、PUT config ✓、PUT llm-api-key ✓（脱敏显示 sk-****525d）、PUT search-api-key ✓、PUT vision-api-key ✓、test-connection ✓（status=ok）、speed-test ✓（1032ms）；DB pi_agent_config 表数据正确；前端 JS 语法检查通过；全量 API 回归测试通过（模型列表 5/Skills 6/用户 12/对话模型 5）；现有功能未受影响 |
| CHK-070 | 检查 | P3 完成度、未提交代码、Pi Agent npm 部署、依赖与脚本核查 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 原检查结论指出 P3 核心 WBS 未完成、BUG-054~BUG-061 待修、缺 package.json/node_modules/lockfile、依赖全局 `pi` CLI、AgentProfile/AgentRun/ToolRegistry/审批等生产模型缺失、start.sh/update.sh 和 Extension 正式目录/迁移缺口。后续已通过 BUG-054~BUG-064、ADJ-011~ADJ-014、DEV-051~DEV-056、DOC-043、CHK-071/CHK-072 完成修复与复核；当前源码层 P3 已完成并验证，17957 既有运行实例已于 2026-06-11 重启生效（CHK-073 闭环） |
| CHK-071 | 检查 | Pi Agent npm 包选型验证：pi-coding-agent vs pi-agent-core | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 结论：pi-coding-agent 是主依赖（唯一提供 `pi` CLI 二进制、--mode rpc、内置工具、Extension 加载）；pi-agent-core 是运行时 SDK（无 CLI、无内置工具、需要自己写宿主程序）；pi-ai 是 LLM API 层。当前方案 A（子进程 RPC）必须依赖 pi-coding-agent。pi-agent-core 和 pi-ai 作为 pi-coding-agent 的子依赖自动安装，不需要显式声明。package.json 精简为仅 pi-coding-agent@0.79.0 |
| CHK-072 | 检查 | P3 上午检查项修复闭环核验 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 真实运行面核验：17957 当前服务可登录、Pi 配置 API 可用、前端 Agent UI 已加载，但 OpenAPI 未包含 `/api/agent/*`，说明运行实例尚未重启加载最新后端路由；当前源码在隔离端口 18089 启动后 OpenAPI 暴露 12 个 `/api/agent/*` 路由，AgentProfile/Authorization/Run/ToolRegistry/MCPPolicy/Approval/前端资源均通过真实 HTTP 验证；`node_modules/.bin/pi --version` 为 0.79.0，PiAgentBridge 使用项目本地 `node_modules/.bin/pi --mode rpc` 和 `src/agent/extensions`。结论：代码层已达成上午 P3 预期，17957 运行实例需要重启后才能体现全部 P3 后端能力 |
| CHK-073 | 检查 | 17957 运行实例重启验证：P3 全部 `/api/agent/*` 路由生效 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 停止旧 17957 进程后通过 `./start.sh restart` 重新启动；`/api/health` 返回 `{"status":"ok","version":"0.2.12"}`；OpenAPI 暴露 12 个 `/api/agent/*` 路由（profile/authorizations/runs/approvals/mcp/tools）；Pi Agent CLI 检测到项目本地 `node_modules/.bin/pi`；CHK-070/CHK-072 提出的"17957 既有运行实例需重启后生效"问题已闭环 |
| CHK-074 | 检查 | commit 5d1dd6ab62bb76779670f1733dd9159d1d021a15 效率角度代码审查 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 审查新增知识库/检索/embedding/vector/评估相关代码，按 wasted work、blocking I/O、redundant computation、sequential independent operations 输出候选发现 |
| CHK-075 | 检查 | P4-P6 WBS 6 项代码差距验证：对照代码实际实现与 WBS 假设 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 发现 6 项真实差距：(1) list_projects 仍硬过滤 created_by，workspace 成员无法看到他人项目 (2) Conversation 无 mode/project_id 字段，不支持 presentation 模式 (3) snapshot_version 仅写入不查询，_load_project_knowledge_context 加载最新而非冻结版本 (4) AgentApprovalRequest.approver_id 创建时不填入、list_pending 不按审批人过滤 (5) KnowledgeSource.workspace_id 是 NOT nullable，无个人私有知识支持；owner_id 存在但不用于权限过滤 (6) ResourceACL 完全不存在，SkillConfig 无 status 字段；6 项均需新增开发任务 |
| CHK-076 | 检查 | P5/P6 代码 bug 审查 + 文档一致性检查 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 发现 8 个 bug（BUG-071）并全部修复；文档更新：WBS 快速验证标记+章节标题已更新，用户故事清单 P5-P6 新增 14 条（US-31~US-44），README 和版本号待后续统一提升 |
| CHK-077 | 检查 | governance.py 12 端点 bug 审查 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已完成 | 发现 6 个高置信度 bug：① update_budget/get_budget 不验证 workspace_id 存在可创建孤立记录 ② update_budget 响应缺少 id 字段与 get_budget 不一致 ③ archive_agent 检查无活跃运行与状态更新之间存在 TOCTOU 竞态 ④ BudgetUpdate.warning_threshold_pct 无边界校验可传负值或>100 ⑤ BudgetUpdate.monthly_token_limit/monthly_cost_limit 无下限校验可传负值 ⑥ require_action 已导入未使用，workspace 级端点缺少工作空间归属校验 |
| CHK-078 | 检查 | 智能体运行状态与回归测试全量核查 | 2026-06-24 00:00 | 2026-06-24 19:27 | 已完成 | 全量 pytest 1060 passed；发现 BUG-079（前端 Agent 模式未调 stream）并修复；Pi CLI 0.79.0 子进程 smoke test 通过；runtime pi_agent_config 仍存旧 extension_path 但代码层已回退；update.sh 缺可执行位已修复 |
| CHK-079 | 检查 | 全链路流程 bug 审查（Login→Chat→Review→Workspace→Admin/P4/P5/P6） | 2026-06-26 10:00 | 2026-06-26 15:30 | 已完成 | 发现 BUG-084~091；已全部修复；新增 `tests/test_bugfix_084_091.py` + 前端契约 3 项；定向 19 passed |
| CHK-080 | 检查 | 全量回归测试 + Windows 平台适配审查（BUG-084~091 未提交改动复核） | 2026-06-28 09:00 | 2026-06-28 09:25 | 已完成 | 全量 pytest 从 34 failed/158 errors 修复至 **1067 passed, 0 failed, 0 errors**；发现并修复 BUG-092（BUG-084 回归：approver_ids 校验顺序）、BUG-093（admin.js .slice 残留）、BUG-094（branding_config 跨平台绝对路径）、BUG-095（缺失 example.yaml）、BUG-096（migrate_branding 路径分隔符）、BUG-097（Windows teardown 文件锁定）；BUG-098（governance with_for_update SQLite 无效）记录为待观察低风险项 |
| CHK-081 | 检查 | 提交 289af60（BUG-099~105）残留缺陷复查 | 2026-07-07 17:20 | 2026-07-07 17:20 | 已完成 | 定向执行 `pytest -q tests/test_bugfix_099_104.py tests/test_review_request.py`，14 passed；静态复查 + 临时内存库脚本复现 2 个残留问题：BUG-106（savepoint 回滚后仍推送幽灵 SSE 通知）与 BUG-107（历史重复 `ReviewParticipant` 记录升级后仍重复展示）；同时排除 `allowed_roles_json=NULL` 的误报，当前行为仍为 allow-all |
| CHK-082 | 检查 | BUG-106/107 修复实现复审 + 新增 BUG-108/109/110 复审与修复 | 2026-07-07 17:30 | 2026-07-07 18:20 | 已完成 | 独立读码确认 CHK-081 报告的 BUG-106（幽灵 SSE：通知 push 早于 savepoint 提交）/ BUG-107（历史重复参与者存量去重）成立并已修复（`defer_push` outbox 缓冲 + 读写双向去重）；复审又新发现并修复 3 项：BUG-108（`/workspace/{id}/sources` 透传 `owner_type=user` 越权枚举他人私有资料 → 路由拒绝）、BUG-109（`get_monthly_token_usage` 含 `workspace_id IS NULL` 全局行导致 per-workspace token 虚高与误封 → 去掉 OR NULL 分支）、BUG-110（`KnowledgeFileStorage` 目录缺失 `iterdir` 抛 500 → 加 `exists()` 保护）；新增 `tests/test_bugfix_106_107.py`（6 项）+ `tests/test_bugfix_108_110.py`（7 项）；再次排除 `allowed_roles_json=NULL` 误报；全量 pytest 1087 passed |
| CHK-083 | 检查 | V0.3.4 当前仓库全量 Bug 与功能缺失审查 | 2026-07-17 13:05 | 2026-07-17 13:05 | 已完成 | 盘点需求/WBS、后端路由、权限、Agent、知识检索、前端契约与部署配置；全量 pytest 1090 passed，Python compileall、全部前端 JS `node --check`、三份 shell `bash -n` 通过；动态复现跨用户 Artifact 读取/篡改与 personal retrieval 恒空；确认 BUG-112~118 共 7 项未修复问题，其中 P0 2 项、P1 4 项、P2 1 项 |

| CHK-084 | 检查 | V0.3.5 当前仓库全量代码与 Bug 复审 | 2026-07-29 00:00 | 2026-07-29 00:00 | 已完成 | 审查受控源码、路由权限、存储路径、Agent 执行、配额统计、数据库约束与后台任务生命周期；Python compileall、前端 JS `node --check` 通过，全量 pytest 1103 passed/6 warnings，但退出后出现 aiosqlite 线程异常；Ruff 报 237 项（以未使用导入/格式问题为主）；动态复现目录穿越并确认 BUG-119~124 共 6 项待修复问题 |
| CHK-085 | 修复 | BUG-119~124 安全与生命周期修复收口 | 2026-07-29 18:00 | 2026-07-29 19:10 | 已完成 | 按 CHK-084 报告逐项修复：BUG-119 Extension 读 `AGENT_SCOPE_JSON`+deny-by-default+`read` 需审批+非管理员禁自配/自批+Pi cwd 沙盒；BUG-120 `_resolve_safe_path` 拒绝目录穿越；BUG-121 日志补 workspace/user/mode 归属+聊天校验 knowledge_workspace；BUG-122 connect event 启用 FK；BUG-123 `assert_artifact_write_access` Observer 只读；BUG-124 `track_pipeline_task`/`stop_all_pipeline_tasks` 纳入 lifespan。新增 `tests/test_bugfix_119_124.py`（13 项）；外键开启后同步修正 `test_knowledge_retrieval.py` 2 个孤儿插入测试。验证：定向 13 passed、相邻回归 20 passed、FK 敏感套件 103 passed；全量 pytest 连续 3 次 **1116~1117 passed / 0 failed**，且退出阶段 `Event loop is closed` 噪声降为 **0**（BUG-118/124 闭环） |
| CHK-086 | 检查 | 未提交代码复审（BUG-119~124 修复后） | 2026-07-29 18:55 | 2026-07-29 19:20 | 已完成 | 核对未提交改动与 `tests/test_bugfix_119_124.py`（初 14 passed）；确认路径穿越/Observer/FK/pipeline/RAG/非管理员禁高风险仍 OK。发现并修复 BUG-125（审批死循环+sidecar）、BUG-126（审查归属漏计）、BUG-127（工具名子串误匹配）；同步修正统计摘要。定向 **24 passed**，相邻 agent/skill **51 passed** |
| CHK-087 | 检查 | 前后端+部署链路硬编码路径与部署丢失全面审计（10.181.237.97:8080 `/prd-review/` 登录故障根因排查） | 2026-07-31 17:50 | 2026-07-31 18:20 | 已完成 | 实测确认默认账号 admin/admin@2026 后端登录正常，根因为前端 API 绝对路径打到站点根被 Caddy 吞掉返回空 200；三路并行审计产出：前端 api.js `_base` 恒空 + 12 处绕过 request() 的直接 fetch + 2 处 EventSource + auth.js branding 绝对路径；后端 main.py branding 返回 `/assets/branding/` 根绝对 URL、NoCacheMiddleware root_path 下判断失效、无 root_path 入口、pi_agent_bridge 自回调写死 127.0.0.1:17957；部署侧 package.sh 漏 package-lock.json、update.sh 黑名单缺 runtime/vector 且同步清单缺 package*.json/.env.example、sync_yaml_version 用错变量、start.sh --host 0.0.0.0 与安全方案矛盾、JWT_SECRET 静默生成与文档矛盾、版本号 4~5 处硬编码。高优先级项已全部修复（BUG-128/129、OPS-009、DOC-059），行为矛盾类遗留记入 OPT-001/002 |
| CHK-088 | 检查 | V0.3.6 未提交代码复审：用户代码审查发现 6 项问题（3 P1 + 2 P2 + 1 P3）并全部修复 | 2026-08-09 23:30 | 2026-08-09 23:50 | 已完成 | **P1-1** `tests/test_p2d1_eval.py` 依赖被 gitignore 的 `eval/` 目录，干净克隆/CI 会全部失败 -> 新增模块级 `pytestmark = pytest.mark.skipif(not EVAL_DIR.exists())` 自动跳过（34 passed 本地 / 干净环境 skip）**P1-2** `docs/packaging-and-deployment.md` 反代示例 `handle` 应为 `handle_path`、Nginx `proxy_pass` 缺尾部 `/` 不剥前缀 -> CN+EN 均改为 `handle_path` + `proxy_pass http://...:17957/;`，并补充剥离前缀说明（与 .env.example 已有文档一致）**P1-3** `update.sh` 版本比较用硬编码 `NEW_VERSION` 常量，相同则 exit 0 不解包读实际版本 -> 见 BUG-131 **P2-1** `tests/test_bugfix_128_129.py` 断言旧的 `${this._base}/api/` 实现已过时 -> 改为断言 `this._url(\`/api/review/projects/\`` 新实现（8 passed）**P2-2** `.gitignore` 排除 `package-lock.json` 但 `package.sh` MEMBERS 列表包含它 -> 从 .gitignore 移除 `package-lock.json`（npm lock 文件应入库）**P3-1** `package.sh:304` 引用旧路径 `docs/4-deployment/2026-07-07-打包与部署指南.md` -> 改为 `docs/packaging-and-deployment.md`；`docs/packaging-and-deployment.md` 中"不会同步 package.json"过时声明 -> 改为正确描述（含 package.json 和 package-lock.json）。验证：定向 43 passed（test_bugfix_128_129 8 + test_p2d1_eval 34 + test_runtime_config 1），三脚本 `bash -n` 通过，全量回归 **1143 passed / 0 failed**（169s） |
| CHK-089 | 检查 | 复核审查报告 6 项（3 P1 + 2 P2 + 1 P3）是否已修复 | 2026-08-10 10:28 | 2026-08-10 10:35 | 已完成 | **P1×3 / P2×2 均已修复**：eval 测试 skipif；反代文档 CN+EN 改为 handle_path 剥离前缀；update.sh 先解包读 PACKAGE_VERSION 再比较；契约测试改断言 `_url`；`.gitignore` 已移除 package-lock 忽略且已 staged。**P3 未完全收口**：`package.sh` 旧路径与中文部署 FAQ 已改对，但 `docs/packaging-and-deployment.md` 英文 §5.2/5.3/§7（仍写 NEW_VERSION 决定更新、doesn't sync package.json）与 `docs/troubleshooting.md` 中英（仍写不会同步 package.json）残留过期描述。定向 `test_bugfix_128_129`+`test_p2d1_eval` **42 passed** |
| CHK-090 | 检查 | 项目 Skill 审查与业务漏洞分析（6 个 Skill + SkillRunner + SkillSchemaLoader + 路由）：依据 `design/3-plans/项目Skill审查与业务漏洞分析-20260811.md` 审计文档，系统性修复 P0/P1/P2 共 13 项问题 | 2026-08-11 09:00 | 2026-08-11 18:00 | 已完成 | **P0-3.1** requirement-insights coverage_matrix 确定性构建（feature_id/status/target_baseline/baseline_warning；`_build_insight_inputs`/`_build_coverage_matrix`/`_build_subsequent_docs_map` 重写；evolution-match issue_id/confidence + 数据守恒）**P0-3.2** system-review 全失败传播（`dimensions_executed`/`dimensions_failed`/`review_dimensions_meta`，all_failed 抛 RuntimeError）**P1-4.1** evolution-match 问题守恒（`_backfill_missing_issues`/`_compute_issue_conservation`，后续文档内容结构化注入）**P1-4.2** SkillSchemaLoader 全面重写（enum/min/max/exclusiveMin/Max/minItems/maxItems/minLength/maxLength/pattern/additionalProperties:false/$ref；`_RefResolver`；`_CRITICAL_FIELD_HINTS`；`_schema_critical` 标记；run_skill 在 critical 失败时标记 error）**P1-4.3** SkillConfig.status 门控（`SkillInactiveError`；`_REQUIRED_STEPS`/`_OPTIONAL_STEPS`；`_preflight_skill_gate`；路由按步骤 required=fail/optional=degraded+skip）**P1-4.4** classify 版本链跨分类误合并修复（`(subcategory_name, category)` 元组分组；类别白名单 unknown→待确认；dependencies 扩展 references+same_topic；CLI `--version-pattern` 优先级；version-chain.md prompt + output-schema.version-chain.json minItems:2/dependencies/category）**P1-4.5** prd-per-analysis 占位符填充（`_fill_prompt_placeholders` 填 `{{doc_id}}`/`{{category}}`/`{{version}}`/`{{image_descriptions}}`；batch_analyze 缓存键升至 v3 + `--review-context-version` CLI）**P1-4.6** docx-to-markdown 原子写入（tempfile+rename 保证 .md/.converted 原子性；MAX_IMAGE_COUNT/MAX_XLSX_SIZE 在 extract_content_from_docx 实际生效；batch_convert 跳过逻辑校验 md+sentinel；失败清理半成品）**P2-5.1** system-review `--enable-vision` 已从源码移除（仅 .pyc 残留已清）**P2-5.2** competition.md source 标签（input_evidence/industry_template/model_inference 三级；has_competition_data；open_questions；output-schema.json 强化 required 与 enum）**P2-5.3** report-generator 参数一致性（`_resolve_sections` 解析 --sections；FORMAT_PDF 实际生成 PDF；total_reports 统计报告类型数；requirements.txt 含 anthropic；UnicodeCIDFont 中文字体；`_escape_md_cell` 表格转义；`_escape_mermaid_label` Mermaid 转义；build_evolution_flowchart/build_dependency_graph/build_version_chain_timeline 三种图表实际接入主流程）**P2-5.4** classify doc_id 统一（`_build_classify_inputs` 注入 DB id `[doc_id] filename`；路由仅按 `str(c.get("doc_id")) == str(doc.id)` 精确匹配，移除文件名回退）**P2-5.5** run_skill 安全默认分区（`_CRITICAL_FIELD_HINTS` 扩充 score/action_plan/action_items/recommendations；repair 标记 _schema_critical；run_skill 在 critical 时 step_status=error）**P2-5.6** 文档命令统一 python3（skills/requirement-insights SKILL.md+usage-guide.md、skills/docx-to-markdown usage-guide.md、design/3-plans/安全加固方案.md）**P2-5.7** Ruff 修复（skills/ 全部通过：F541 f-string 无占位符 ×2 已修；review.py 恢复 runtime_path noqa:F401 供测试 monkeypatch；test_router_persistence_scan WHITELIST review.py db_commit 32→34 对齐 P1-4.3 新增 2 处）。新增 10 个测试文件共 **88 项测试**（test_skill_audit_p0_p1 13、test_skill_schema_constraints 14、test_skill_inactive_gate 6、test_skill_classify_audit 12、test_skill_per_analysis_audit 9、test_skill_docx_audit 11、test_skill_competition_audit 10、test_skill_report_generator_audit 22、test_skill_classify_docid_audit 4、test_skill_safe_default_audit 14）。全量回归 **1232 passed / 0 failed**（6 warnings，aiosqlite 线程清理噪声为良性） |
| CHK-091 | 检查 | 复核 `项目Skill审查与业务漏洞分析-20260811.md` 所列问题是否均已修复 | 2026-08-11 22:03 | 2026-08-11 22:20 | 已完成 | 对照源码+审计测试：严重 3.1/3.2、高风险 4.1/4.2/4.3/4.5/4.6、中低 5.1–5.4/5.7 **已修复**；4.4/5.5/5.6 **部分残留**（版本链 LLM 校验未接入、schema dependencies 非 required；维度/洞察子路径未门控 `_schema_critical`；脚本 docstring 仍写 `python`）。另：七维 `partial` 未映射任务 `completed_with_warnings`；standalone insights 无 `--target-baseline` CLI；DOCX 缺进程级超时/像素上限。定向审计测试 **115 passed** |
| CHK-092 | 检查 | CHK-091 残留 5 项修复验证 | 2026-08-12 00:10 | 2026-08-12 00:40 | 已完成 | 逐项核对源码修改：4.3 preflight gate 在 `task.status=running` 前检查全部必需 Skill（分类/逐篇分析/体系Review/报告），inactive 直接 fail；3.2 `review_dimensions_meta` 通过 `_merge_task_step_details` 持久化到 step_details，final status 判断增加 partial/degraded 条件；4.4 路由 classify 后加载 `default-categories.json` 白名单并标记非白名单类别为"待确认"；4.6 分析保存注入 `_source_content_hash`，缓存查找比对 `doc.content_hash` 不匹配跳过；4.2 `_repair_node` 收集修复笔记并通过 `_schema_repair_notes` 返回，`skill_runner.py` 追加到 diagnostics。新增 `tests/test_chk091_residual_fixes.py` 21 项全部通过；`test_router_persistence_scan.py` WHITELIST 对齐 db_commit 35。定向回归 72 passed，关键词 220 passed / 0 failed。全量回归 **1287 passed / 0 failed**（155s） |
| CHK-093 | 检查 | CHK-091 残留二期 6 项修复验证 | 2026-08-12 01:00 | 2026-08-12 01:30 | 已完成 | 逐项核对：① `output-schema.version-chain.json` 的 `required` 包含 `dependencies` ② `_run_dimension_with_retry`/`_run_insight_substep_with_retry` 含 `_schema_critical` 检查和 `raise ValueError` ③ 7 个脚本文件无 bare `python ` ④ `insights.py` 含 `--target-baseline` CLI 和 `baseline_section` 注入逻辑 ⑤ `convert_docx.py` 含 `MAX_IMAGE_PIXELS`/`MAX_SINGLE_IMAGE_SIZE`/`_get_image_pixel_count` ⑥ ruff 对修改文件 All checks passed。E712 unsafe fix 破坏 SQLAlchemy `== False` 查询导致 38 测试失败，已回退所有 ruff auto-fix 到 src/+tests/，仅保留手动修复。`pyproject.toml` ruff 配置排除 E712。新增 `tests/test_chk091_residual_phase2.py` 29 项全部通过；全量回归 **1316 passed / 0 failed** |
| CHK-094 | 检查 | tests 覆盖率与场景审计：跑 `pytest --cov=src/app --cov=src/agent`，评估 74 个测试文件对 100 个源码文件的覆盖广度与关键场景缺口 | 2026-08-12 19:00 | 2026-08-12 19:30 | 已完成 | 全量 1407 测试：**1406 passed / 1 failed / 1 skipped**（216s），整体行覆盖率 **56%**（10790 stmts，4759 missed）。**发现 1 个失败测试**：`test_skill_classify_audit.py::test_version_chain_schema_requires_min_items_2` 与新 `test_bug140_version_chain_minitems.py` 冲突——BUG-140 把 schema `versions.minItems` 从 2 放宽到 1（修复单篇 quick 审查），但旧测试未删除/更新、schema 改动与 `tests/test_bug140_*.py` 均未提交（按：BUG-140 实际已记于本表 BUG 编号区、状态已修复，原记 未入 task-list 系记录误差）。**关键覆盖缺口**：`routers/review.py` 27%（1144 stmts，836 missed）、`routers/workspace.py` 23%、`routers/review_request.py` 32%、`routers/governance.py` 27%、`routers/agent.py` 40%、`database.py` 34%、`services/pi_agent_bridge.py` 29%、`services/mcp_adapter.py` 28%、`services/object_access.py` 24%、`services/chat_application_service.py` 32%、`services/review_pipeline_persistence.py` 37%、`services/tool_registry.py` 38%、`services/budget_guard.py` 60%。**零覆盖模块**：`storage/docx_conversion_worker.py`、`services/quality_stats_service.py`、`services/agent_application_service.py`、`schemas/admin.py`、`schemas/workspace.py`。建议补：review/workspace/review_request/governance 端点 happy+权限拒绝路径、object_access 跨用户越权、budget_guard 配额拦截、docx_conversion_worker 超时/取消、quality/agent_application_service 单元测试，并清理 BUG-140 冲突测试 |
| CHK-095 | 检查 | 全量回归验证（按 CLAUDE.md 开发质量要求收尾回归） | 2026-08-12 19:35 | 2026-08-12 19:38 | 已完成 | 执行 `python3 -m pytest -q`：**1422 passed / 1 skipped / 0 failed**（6 warnings，201.67s）。CHK-094 报告的 BUG-140 冲突测试 `test_version_chain_schema_requires_min_items_2` 已被清理（not found），原 1 failed 已消除；测试数较 CHK-094 增加 16 项（1406→1422）。控制台 `RuntimeError: Event loop is closed` 为 aiosqlite 后台线程 teardown 已知良性噪声，不影响结果。结论：当前全量自动化测试满足要求 |
| CHK-096 | 检查 | 复核当前任务清单完整性与项目 Skill 执行收口（对照审计文档 + CLI check/standardize） | 2026-08-13 12:57 | 2026-08-13 13:10 | 已完成 | 对照 CLI check/standardize + 源码/文档抽查。结构：check 通过（无非法 ID/重复/列数/枚举/悬空引用/摘要漂移）。standardize 质量警告 2 条：ADJ-018→ADJ-014、DOC-068→DOC-061 行序回退（历史回填，不改语义）。未完成仅 OPT-001/OPT-002。Skill 审计定向 203 passed。CHK-091 备注仍写「部分残留」属当时快照，后续 CHK-092/093 + BUG-132~135 已收口。审计原文在 gitignored 的 design/ 下。发现 CHK-089 英文部署文档过期描述未转后续任务（见 DOC-076）。 |
| CHK-097 | 检查 | CHK-096 遗留项收口复核：DOC-076 部署文档口径修正、task-list 行序回退、更新脚本日志口径 | 2026-08-13 13:30 | 2026-08-13 13:45 | 已完成 | ① DOC-076 完成：中文 §5.2/英文 §5.2/§5.3 改为解包后以包内版本比对（NEW_VERSION 仅读取失败回退）；英文 §7 FAQ 修正「doesn't sync package.json」自相矛盾；troubleshooting 中英原因条目同步。最终口径与并行会话 DOC-077（V0.3.11 版本单源化，根目录 VERSION 为唯一事实来源）合并收敛 ② 行序回退修复：ADJ-014 前置于 ADJ-018、DOC-061 前置于 DOC-068 ③ `update.sh:449` 日志「目标版本: $NEW_VERSION」改为「回退目标版本(包内读取失败时使用)」消除误导（实际比对用 PACKAGE_VERSION）④ 新增 `tests/test_doc_version_contract.py` 契约测试（VERSION 文件口径）防文档再漂移；`bash -n update.sh` 通过。OPT-001 的 package.json 强制单源项已随 VERSION 单源方案落地（package.json 仍手工同步，见 DOC-077）；server 块读取、agent-limiter 端口仍待决策（OPT-001），OPT-002 三项行为变更仍待决策。；2026-08-13 补记：OPT-001/002 已于同日完成（端口 env、JWT 写入根 .env）。troubleshooting package.json/JWT 空值/token_hex 长度残留由 [[DOC-078]] 收口。 |
| CHK-098 | 检查 | 复核 2026-08-11～13 台账（BUG-132~155 / CHK-090~097 / DOC-074~077 / OPT-001~002 / DEV-076）与代码、文档是否口径打架 | 2026-08-13 15:29 | 2026-08-13 15:29 | 已完成 | 对照 39 条记录与当前源码/文档。真正还在打架：① troubleshooting.md 中英 §9 仍写 update.sh 不会同步 package.json，与 update.sh 同步清单、packaging-and-deployment.md、DOC-076/CHK-097 声称已改矛盾；契约测试只锁部署文档未锁 troubleshooting。② JWT：configuration.md 规则表仍写空值硬阻断，troubleshooting 写不走 start.sh 会报「JWT secret 未配置」；实际 config.py _resolve_env 会静默生成临时密钥，assert 几乎碰不到空值。③ configuration.md 把 token_hex(32) 写成 64 字节/128 字符，实为 32 字节/64 字符。台账备注过期：CHK-097 仍写 OPT-001/002 待决策（现已完成）。已对齐不记为打架：VERSION 0.3.11 单源、根目录 .env、端口 env、minItems=1、审查配额、max_tokens 32768、公开注册维持 true。；同日已按确认口径落地，见 [[DOC-078]]。 |
| CHK-099 | 检查 | task-list.md 全量格式审查：空行、ID 行序、时间格式、管道符转义、枚举值、统计摘要 | 2026-08-13 16:00 | 2026-08-13 16:10 | 已完成 | 逐行扫描全量 445 条记录。发现 4 类格式问题：① 表格内空行 2 处（行 134 BUG-118/119 间、行 285 CHK-083/084 间，破坏表格渲染连续性）② ID 行序回退 2 个区域（ADJ-018 在 ADJ-015~017 前；DOC-061/062 在 DOC-063~067 后、DOC-068 在 DOC-061 与 DOC-062 之间，CHK-096/097 已记录为历史回填不改语义）③ 时间格式缺 HH:MM 2 行（DOC-061 行 377、OPS-001 行 483 仅写日期）④ 代码 span 内 `\|` 转义 5 行（BUG-057/071/134、DOC-049、OPS-003，GFM 下可正确渲染但非通用）。确认无问题项：无重复 ID、无编号跳号、动作/状态枚举全合法、列数对齐正确、统计摘要计数准确。 |
| CHK-100 | 检查 | 复核 task-list-initialization 在本项目落地：结构、格式是否被 skill 改偏、维护规则是否对齐 V0.3.2 | 2026-08-13 20:37 | 2026-08-13 20:50 | 已完成 | 对照 skill V0.3.2 CLI check/standardize + 模板/维护规则。结构：check 通过（dual schema、extended、446 条、无非法 ID/重复/列数/枚举/悬空引用/摘要漂移）。standardize 质量警告 2 条：ADJ-018→ADJ-015、DOC-067→DOC-061 行序回退（历史回填，CHK-096/097/099 已记）。Skill 未改坏 7 列 schema。仍存格式问题：① 表内空行 2 处（BUG-118/119、CHK-083/084，GFM 会断表，check 不报）② DOC-061/OPS-001 仅日期（CLI 故意兼容 YYYY-MM-DD）③ 调研事项夹在功能开发与配置运维之间（改名落地未重排）④ 文件头缺当前模板「状态说明」，且指向已不存在的 docs/task-list-standardize-report.md ⑤ CLAUDE.md 规则标题已装、Stop hook 未装、正文比 canonical 短（无「无任务则不提示」/hook 免责）。管道符 `\\|` 为 DOC-049 skill 转义，GFM 可渲染。未自动修复，待确认。 |
| CHK-101 | 检查 | V0.3.12 发版前全量文档一致性审查（BUG-156~165 改动后）：skills 6 个 skill 的 SKILL.md/usage-guide/prompt/schema 与脚本行为三方对照；docs/ 全部文档 + README + CLAUDE.md 与 src 代码对照（API 端点/配置/流程/新 409 门控） | 2026-08-21 12:40 | 2026-08-21 13:00 | 已完成 | 双路并行审查结论：本轮 BUG-156~164 改动未使任何现有文档说法变错（无「文档与代码冲突」），问题为新增行为未写入 + 存量漂移。发现并转后续：① 行为级真 bug 1 处→BUG-165（空 categories 白名单全改待确认）② 主文档缺口/漂移→DOC-079（api-reference 缺 409、admin-guide 错误端点路径 /start、troubleshooting §5 与 packaging §4.3 反代口径相反、README routers 漏 pi_agent）③ skills 文档漂移（system-review domain_rules 声称支持但代码未实现、--target-doc 描述失实；report-generator SKILL.md 幽灵第六节+缺新小节；prd-per-analysis 缺 expert_review 文档；prd-overview-classify 白名单机制未入文档+prompt 与代码冲突+空默认配置误导；docx 4 处低级别遗漏；insights 示例缺 2 字段）→DOC-079 一并修复。确认一致：configuration/getting-started/security/collaboration-review/deployment-hardening/packaging（除 §4.3 争议点外）/CLAUDE.md/docx 核心常量/insights 三方一致。顺带发现 system-review「镋期」错别字与 ReviewTask.status 列宽不足（并入 BUG-165） |
| CHK-102 | 检查 | GitHub 全量 Issue 审查并列出解决方案 | 2026-08-25 12:57 | 2026-08-25 13:05 | 已完成 | 仓库仅 1 条开放 Issue：[#1](https://github.com/wangminle/prd-draft-review-workflow-v1/issues/1) 空壳 uv.lock 导致 LWA `uv sync --frozen` 装不到依赖、容器启动找不到 uvicorn。已对照本地 uv.lock（3 行/f2a8d7c 误入）、pyproject 无 [project]、requirements.txt 21 包、start.sh/update.sh/package.sh 均走 pip。推荐方案 A：删除 uv.lock 并加入 .gitignore；不建议迁 uv（方案 B）或只加注释（方案 C，治标不治本）。zip 分发包不受影响（package.sh 不含 uv.lock）。已按方案 A 落地，见 [[BUG-166]] |
| CHK-103 | 检查 | docx-to-markdown skill 与 GitHub 上游（wangminle/skills-doc-to-md）双向同步检查与合并 | 2026-08-27 10:00 | 2026-08-27 10:40 | 已完成 | 双向差异：**本地领先**（安全加固：ZIP bomb 防线 500MB/100MB/100x、DocxSecurityError 独立异常类、defusedxml 可选依赖、SHA-256 绑定 .converted sentinel、batch --timeout 超时保护、半成品目录清理、文件名防碰撞 hash）→ 已提上游 issue [skills-doc-to-md#1](https://github.com/wangminle/skills-doc-to-md/issues/1)；**上游领先**（V0.1.5：TIFF/WMF/EMF 图片格式检测、无法识别时保留原扩展名、mammoth content_type 扩展名推断、表格占位符残留告警、md_to_pdf xelatex+CJK 字体回退链、reportlab XML 转义、auto 模式 pandoc 失败回退 python）→ 已全部合并到本地 convert_docx.py/md_to_pdf.py/SKILL.md/usage-guide.md/requirements.txt。验证：py_compile 通过、test_skill_docx_audit 22 passed、test_skill_regression+test_data_compat 40 passed、TIFF/WMF/EMF/None 检测与 escape 冒烟断言通过。同步规则已写入 CLAUDE.md 项目约束第 3 条：每次版本更新必须做一次该 skill 与上游的双向差异检查 |
| CHK-104 | 检查 | 复核 GitHub 3 条 Issue 与 task-list Bug 是否全部真正修完 | 2026-08-27 18:56 | 2026-08-27 19:10 | 已完成 | GitHub 3 条仍全部 open。本地代码：#1 已在 HEAD 02824af（未 push，origin 仍有空壳 uv.lock）；#2/#3 工作区已改且定向测试 48 passed，但未提交（7 份维度 schema 与 test_bug167/168 仍 untracked）。task-list 条目均标已修复，但存在重复 BUG-167、重复 CHK-103、统计摘要未跟上；已将密码弹窗改为 BUG-169、本条为 CHK-104。历史 BUG-001~165 未逐条重跑，仅确认清单无「待修复」状态 |
| CHK-105 | 检查 | 全局 schema 覆盖契约：所有实际执行的 prompt 必须有显式 per-prompt schema，禁止无意回退 skill 级 schema（review 方案第 6 点） | 2026-08-27 21:00 | 2026-08-27 21:10 | 已完成 | 新增 tests/test_schema_coverage_contract.py 16 项：从 SkillRunner 执行映射（_SKILL_NAMES/_prompt_name_for/_REVIEW_DIMENSIONS/_INSIGHT_SUB_STEPS）派生 14 个执行 prompt 全集逐项断言 per-prompt 文件存在；清单本身与硬编码期望比对防漏配；断言无任何管线 prompt 正被 CLI 文件清单结构校验（#4 形态）。注意 prd-per-analysis 为单 prompt skill，per-prompt 文件与 skill 级同内容属显式声明 |
| CHK-106 | 检查 | 核对上游 skills-doc-to-md V0.1.6（3def6e3）对本仓 docx skill 全部功能的覆盖情况，未覆盖项提上游 issue | 2026-08-27 22:20 | 2026-08-27 22:35 | 已完成 | 逐文件 diff 结论：batch_convert/md_to_pdf/requirements/SKILL.md/usage-guide 已与上游一致（md_to_pdf 仅保留本仓 python3 用法契约）；convert_docx.py 剩余差异均为本地领先，归纳为两项功能缺口：①资源超限降级跳过策略（本仓「warning+跳过单资源、mammoth 回调禁止重落盘」vs 上游 V0.1.6 硬拒绝整篇）；②convert_docx_to_markdown 缺 output_name 参数。已向上游提交 issue [#2](https://github.com/wangminle/skills-doc-to-md/issues/2)（可选 skip 策略，含实测数据与实现要点、恶意特征防线不受开关影响的边界说明）与 [#3](https://github.com/wangminle/skills-doc-to-md/issues/3)（output_name 参数），均附生产验证背景；本仓兼容层维持上述行为直至上游跟进 |
| CHK-107 | 检查 | 全量文档是否与当前代码/VERSION 0.3.14 一致 | 2026-08-27 22:26 | 2026-08-27 22:40 | 已完成 | 结论：用户文档仍停在 V0.3.12，且 0.3.13/0.3.14 行为未写入。已转 [[DOC-081]]。仍一致未改：CLAUDE.md、security.md、collaboration-review.md、deployment-hardening.md、docx skill（DOC-080 已新）、task-list 历史记录。已知缺口不属文档：aiosqlite Event loop is closed 噪声、GitHub Issue 仍 open/未 push |
| CHK-108 | 检查 | 0.3.15 发版前置：docx skill 与 GitHub 上游 skills-doc-to-md 双向差异检查（上游已从 V0.1.6/3def6e3 迭代到 V0.1.7/93afbb1，2026-08-28） | 2026-08-28 15:40 | 2026-08-28 16:10 | 已完成 | codeload tarball 全量 diff 结论：上游 V0.1.7 已采纳 CHK-106 提交的 issue #2/#3——新增 `--output-name`、`--on-limit reject\|skip`、`validate_on_limit()`、`ResourceLimitExceeded`、sentinel 记录 on_limit 策略（源或策略变更自动重转）、`prune_stale_assets()` 清理 skip 模式残留资源，且仓库结构改为 `skills/docx-to-markdown/` 子目录。但与本地实现分叉：本地为「恒定跳过超限资源 + `_get_image_pixel_count` 像素炸弹防线 + `validate_zip_safety`」，上游为「默认 reject 可选 skip 的开关式」；直接采上游会使 Web 上传含超限附件的文档从「跳过继续」退化为「整篇拒绝」（本地调用方未传 on_limit）。convert_docx.py 双向 diff 776 行，属安全敏感代码，冒进合并风险高。处置：立 [[PLN-002]] 跟踪合并；本地兼容层维持现状直至合并完成。本地独有 python3 用法契约（md_to_pdf）合并时需保留 |
| CHK-109 | 检查 | 全量文档与 VERSION 0.3.15 / BUG-175 行为一致性检查（0.3.15 发版收口） | 2026-08-28 16:10 | 2026-08-28 16:20 | 已完成 | 版本字面量：仓库（除 task-list 历史记录按惯例不改）已无 0.3.14/V0.3.14 残留，VERSION/package.json/package-lock.json/update.sh/README 中英/index.html 顶栏×4/api-reference×3/packaging×4+适用日期/troubleshooting×4/getting-started×2 全部 0.3.15，`test_version_single_source`/`test_doc_version_contract` 钉同步更新。行为口径：SVG 预览 6 处描述（README 中英、user-guide 中英×2）未反映 BUG-175（渐变归一化、白色衬底），已转 [[DOC-082]] 补齐。无需改动：CLAUDE.md、security.md、collaboration-review.md、deployment-hardening.md、admin-guide/configuration（与版本/BUG-175 无关且口径仍准）、design/ 历史文档（按 CHK-107 惯例不动） |
| CHK-110 | 检查 | docx skill 上游 V0.1.7（93afbb1）全量替换合并落地（[[PLN-002]] 收口，用户指令「就用远端源头 skills 的内容替换本地」） | 2026-08-28 17:30 | 2026-08-28 18:10 | 已完成 | 执行方式：放弃 PLN-002 原定「上游基线 + 回植本地兼容层」方案，按用户收敛指令直接以 codeload tarball（93afbb1，2026-08-27）5 文件全量替换本地 `skills/docx-to-markdown/`（SKILL.md 徽标 0.1.6→0.1.7、references/usage-guide.md、scripts/convert_docx.py、scripts/batch_convert.py、scripts/md_to_pdf.py；requirements.txt 比对无差异不动）。调用侧适配：`docx_conversion_worker.py` 转换调用加 `on_limit="skip"`（保留 Web 上传含超限附件文档「跳过继续」语义；ZIP bomb 恶意特征任何模式仍整篇拒绝），`output_name=原始文件名` 透传不变。API 迁移：资源上限集中为 `DOCX_SECURITY_LIMITS` dict，`validate_zip_safety(path)`→`validate_docx_zip_security(zip_ref)`，`compute_file_sha256`→`sha256_file`，`read_sentinel_source_sha256`→`read_conversion_sentinel`（含 on_limit 策略字段），`_get_image_pixel_count`→`image_pixel_count`（多格式头解析 PNG/JPEG/GIF/BMP/WEBP/TIFF）；本地旧兼容符号全部随替换移除。测试适配：采纳上游 5 个测试文件入 tests/（test_docx_excel_tables/markdown_quality/on_limit/output_name/security_and_batch.py，91 passed + 8 skipped 真实夹具未随上游发布）；适配 4 个旧测试文件到新 API（test_skill_docx_audit.py 重写资源上限断言与 sentinel 读写、test_skill_audit_remaining.py TestOversizedZipEntry 改 validate_docx_zip_security + patch.dict、test_docx_resource_isolation.py 改 image_file_size + on_limit="skip"、test_chk091_residual_phase2.py TestDocxPixelLimits 改 image_pixel_count 并将 md_to_pdf.py 移出 python3 docstring 检查清单——上游文档用 "python" 属正常差异）。验证：docx 相关 10 个测试文件 188 passed/8 skipped；ruff 全过；真实 docx 双链路冒烟（skill 层直调 + worker 子进程，output_name/on_limit/sentinel/assets 均符合预期）；worker 自有兜底 ZIP 校验（skill 缺失时）不受影响。遗留：PLN-002 标记完成；上游 issue #2/#3 已被 V0.1.7 采纳，本地无待反向提交项 |
| CHK-111 | 检查 | 未提交变更缺陷审查（docx V0.1.7 合并、管理侧栏分组、Pi Agent 0.84.3、相关测试/文档） | 2026-08-28 19:53 | 2026-08-28 20:05 | 已完成 | 范围：工作区全部已改/未跟踪文件。结论：生产主路径（worker `on_limit="skip"` + 真实 skill）未见功能性回归；docx 相关定向 196 passed。发现 [[BUG-177]]（P1，契约桩 TypeError + doc_123 缓存假绿）。P2：① 分组头 `aria-hidden="true"` 对读屏隐藏分组语义 ② 窄屏 `@media (max-width:1100px)` 用 `.admin-nav-item span{display:none}` 但 index.html 标签文字无 span，与收起态 `font-size:0` 不对称，tab 文字可能溢出图标栏 ③ 浏览器用例只检测 playwright 包不检测 Chromium 二进制，无浏览器时失败而非 skip。P3：skill 文档「reject 与历史行为完全一致」指上游历史，本仓 Web 路径历来是 skip；CLI reject 模式 mammoth 回调仍无界读取。未修代码，待用户指令。**2026-08-28 22:40 跟进**：BUG-177 此前已修复；P2 三项与回归暴露的残留已按用户指令全部修复（[[BUG-180]]/[[BUG-181]]/[[BUG-182]]/[[BUG-183]]），全量回归 1725 passed / 16 skipped / 0 failed |
| CHK-112 | 检查 | 个人 Agent 信息架构方案是否完整落地（统一账号菜单 / 系统级 vs 我的 Agent / 已授权团队资料） | 2026-08-28 20:47 | 2026-08-28 21:20 | 已完成 | 对照用户方案逐条核对后按「最佳方向」收口，见 [[DEV-079]]/[[DOC-083]]：四页右上角同一账号菜单（含 workspace-user-display）；管理后台侧栏移除「Agent · 个人」；个人 Agent 全员面板含授权 CRUD/运行/审批；范围文案「我的资料 / 已授权的团队资料」并无授权禁用；agent_approval 通知改开个人面板；用户名改为 button+ARIA。系统级 Pi Agent 仍仅管理员侧栏 |
| CHK-113 | 检查 | OPS-019（V0.3.16 部署）日志审计：容器/构建日志零异常（无 ERROR/Traceback/5xx，数据卷完整），lwa 侧 3 类告警定位处置——①「RUNNING（验证有警告）」根因为 lwa update 流程时序缺陷（先生成计划后原子换入，evidence.root 指向已改名的 staging，源码健康端点发现静默失效），提 [[lwa#19]]；临时缓解 `lwa probe set prd-review-v035 /api/health` + `lwa scan`（契约已恢复 discovered，显式探针随 BUG-584 机制跨更新保留）②[no_user] Dockerfile root 运行 34 次告警，提 [[lwa#20]] ③构建 22.6 分钟与 issue lwa#18 同源，已补数据点评论。另发现部署实例应用日志告警「默认预设口令 admin」，需用户尽快修改管理员密码 | 2026-08-28 23:35 | 2026-08-29 00:50 | 已完成 | 审计范围：lwa.log 升级段（23:02-23:26）、容器运行日志全量、build 日志、docker events（确认单次 down/up，restarts=0）。「4 轮 down/up」为三次部署 run 日志累积非本次异常；patent-disclosure 构建失败与 Caddy connection refused 系 08-04/08-10 历史条目被过滤误带出。Gate-C app.sqlite→app.db 回退与 A.R01 DATABASE_URL 跳过注入均为设计内行为无害。lwa 探针机制核实：mandatory 门槛仅来自 `lwa probe set`/管理页（verificationOverrides，BUG-584 跨重导入保留），包内清单不支持声明 |
| CHK-114 | 检查 | GitHub Issue #8 根因核对与修复方案（对照 HEAD V0.3.16 / review.js 与 review.py SSE） | 2026-08-29 11:31 | 2026-08-29 11:31 | 已完成 | 唯一开放 Issue [#8](https://github.com/wangminle/prd-draft-review-workflow-v1/issues/8)。证实：L1123/L1166 完成回调仍未 await `loadProjectDetail`；`_updateActionCardStatus` 仅 2 个调用点（loadProjectDetail 结束、selectDocument），SSE/轮询/`startReview` upsert 后均不刷角标；`_reviewDocMap` 只由 `_loadReviewHistory` 在拉完 analyses 后填充。页面加载不对 running 任务 `_listenProgress`。SSE `event_generator` 未把 `completed_with_warnings` 当终态；ticket 一次性消费 + EventSource 自动重连会 401。转 [[BUG-184]] 待修，不采用「只补 await」单点补丁 |
| CHK-115 | 检查 | V0.3.17 发版前置：docx skill 与 GitHub 上游 skills-doc-to-md 双向差异检查 | 2026-08-30 12:10 | 2026-08-30 12:30 | 已完成 | 上游最新仍为 V0.1.7-Build0122-20260828（93afbb1，2026-08-27），与 [[CHK-110]] 合并基线一致、无新提交 → 上游→本地无需合并。本地→上游：BUG-178 输出子目录符号链接越界防线（2026-08-28 落地后本地 convert_docx.py 偏离上游）属通用安全加固，已按 CLAUDE.md 向上游提 issue [skills-doc-to-md#4](https://github.com/wangminle/skills-doc-to-md/issues/4)（含复现与红/绿验证过的修复补丁建议）。后续上游若采纳，合并时需以新基线重放该防线核对 |

## 测试数据

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| TST-001 | 调整 | 测试数据开源合规匿名化 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | `tests/test_review_backend_contract.py`、`tests/test_review_report_contract.py` 中将“有埋点”改为“有数据采集”，共 3 处 |

## 文档维护

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| DOC-001 | 文档 | docs/ 文档开源合规匿名化 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | `3-design/需求审查工作流平台-需求说明书.md`、`2-archive/20260515-业务逻辑层整合技术方案.md`、`2-archive/三栏式UI界面设计指南.md`、`4-deployment/2026-05-22-ubuntu-nginx-systemd-security-plan.md`、`1-discussion/Skill-as-a-Service人机协同工具开发范式.md` 完成示例产品名、内部目录名、服务名和 LLM 选型口径泛化 |
| DOC-002 | 文档 | skills/ Prompt 与模板开源合规匿名化 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 覆盖 `prd-overview-classify/`、`prd-per-analysis/`、`system-review/`、`requirement-insights/`、`report-generator/`；将具体业务词汇泛化为核心策略、智能联动、交互体验、功能预约、数据追踪、响应时延等中性表达 |
| DOC-003 | 文档 | 持久化与分层文档合并重构 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 将三份文档收敛为"两份主文档 + 一个兼容入口"：`运行时数据与持久化接口基线.md` 升级为数据结构与持久化接口规约；`架构分层改造清单.md` 重写为 WBS 计划书；`后端持久化接口草案.md` 改为已合并说明；验证不再存在旧 `docs/...` 错误相对链接，接口草案重复标题已消除，三份文档行数收敛为 474/344/21 |
| DOC-004 | 文档 | 任务清单格式规范化 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 将原来的日期分组 bullet list 改为与外部项目一致的分类表格结构；新增固定动作枚举、归并规则、ID 编号、状态字段和备注字段；按代码 Bug、调整事项、检查事项、测试数据、文档维护重新归类现有记录 |
| DOC-005 | 文档 | V0.2.6 小版本功能迭代讨论稿 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 新增并更新 `docs/2-discussion/2026-06-02-小版本功能迭代讨论稿.md`；覆盖模型思考级别、后台公共文件库、飞书链接导入/CLI 集成、Markdown/Mermaid 渲染修复四项需求的调研结论、推荐方案、前后端改造建议、风险和实施顺序；已将模型思考级别修正为模型选择器右侧 `关/low/high` 三档运行时下拉，作用于智能对话和需求审查后续请求 |
| DOC-006 | 文档 | V0.2.6 功能迭代任务分解 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 新增并持续更新 `docs/3-design/V0.2.6-功能迭代任务分解.md`；4 阶段 79 子任务：P1 模型思考级别(16)、P2 Markdown/Mermaid 修复(36)、P3 公共文件库(19)、P4 飞书导入(8)；含 7 项待决策问题；已根据 CHK-014、CHK-017、CHK-018 审查/调研意见补强 |
| DOC-007 | 文档 | P2 Markdown 与 Mermaid 渲染修复方案专项修订 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 更新 `docs/2-discussion/2026-06-02-小版本功能迭代讨论稿.md` 与 `docs/3-design/V0.2.6-功能迭代任务分解.md`；明确 P2 不换栈，继续采用 `marked + DOMPurify + mermaid`，本地 vendor 为主、CDN 仅可选兜底、fallback 只做最小保底且控制复杂度、Mermaid 完整消息后 `parse/render`，补 SSE 异常/idle debounce 兜底，并显式处理 DOMPurify 与 Mermaid SVG 顺序风险 |
| DOC-008 | 文档 | README 双语重写并聚焦架构与需求评审工作流 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 重写 `README.md` 为中文在前、英文在后的双语结构；在主标题下增加中英文跳转标签；内容聚焦当前代码已落地的分层架构、SkillRunner 驱动的需求评审工作流、功能要点与后台管理能力，并保留最小启动说明与 runtime 数据隔离说明 |
| DOC-009 | 文档 | Claude Cowork 对需求初审工作流的迭代启示调研文档 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 新增 `docs/2-discussion/2026-06-02-Claude-Cowork-需求初审工作流迭代启示.md`；调研 Claude Cowork 官方博客、产品页和帮助中心资料，拆解任务委托、项目工作空间、插件包、多角色并行、计划任务、人在环审批、企业观测等设计思路，并映射为当前需求初审平台的迭代方向 |
| DOC-010 | 文档 | V0.2.6 P3 品牌与本地个性化配置文档修订 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 更新 `docs/2-discussion/2026-06-02-小版本功能迭代讨论稿.md` 与 `docs/3-design/V0.2.6-功能迭代任务分解.md`；将品牌与本地个性化配置设为 P3，明确 `runtime/config/ui-branding.yaml`、`runtime/assets/branding/`、配置优先级、后端 branding API、前端 Logo/title/favicon/主题色覆盖、路径安全和旧 runtime 覆盖验收；公共文件库顺延 P4、飞书导入顺延 P5 |
| DOC-011 | 文档 | reasoning_content 功能描述文档更新 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 更新 4 处文档：① `V0.2.6-功能迭代任务分解.md` 补充 P1.7 思考内容展示子任务 ② `小版本功能迭代讨论稿.md` 将"展示推理过程是另一个功能"改为已交付说明 ③ `运行时数据与持久化接口基线.md` llm_sessions.jsonl 典型字段补 reasoning_content ④ `README.md` 功能要点和管理功能补思考过程流式展示与适配方式 |
| DOC-012 | 文档 | V0.2.7 版本号统一与文档完成状态标注 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | ① 全代码版本号统一为 0.2.7：main.py(2处)、branding_config.py、HTML(3处)、ui-branding.example.yaml、conftest.py、test_app_frontend_contract.py ② `V0.2.6-功能迭代任务分解.md` 实施顺序表新增状态列，P1/P2/P3 标注已完成，P4/P5 标注未开始；各阶段标题加完成标记 ③ README.md 中英文新增版本号、品牌配置功能要点、品牌配置管理功能、品牌目录和迁移工具目录说明 |
| DOC-013 | 文档 | Cloud co-worker 远期规划与架构演进方案 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 新增 `docs/3-design/Cloud-co-worker远期规划与架构演进方案.md`；基于 star >= 600 的开源项目调研与当前仓库基线，系统化梳理团队空间、账号 Agent、知识层、消息协作、审查平台升级和分阶段路线图，并明确 Dify/AnythingLLD/RAGFlow/Onyx/Rowboat/Mem0/n8n/Figma MCP 的可借鉴边界 |
| DOC-014 | 文档 | Cloud co-worker 子 Agent 调研与 Plan 规划汇总 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 新增 `docs/2-discussion/2026-06-04-Cloud-co-worker-子Agent调研与Plan规划汇总.md`；独立汇总 6 路子 Agent 调研 + Plan 模式四大支柱/技术选型/三阶段路线图，不引用另两份规划稿；含 50+ 项目池、9 条设计模式、讲解管线 MVP 与模块增量建议 |
| DOC-015 | 文档 | 需求业务工作流 cowork 版工具设计规划终稿 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 新增 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；整合 5 份讨论稿并补充团队评审、资源成本估算、技术选型对比、POC 计划、技术架构、数据层级、开源工具支持和功能业务流程流转 |
| DOC-016 | 文档 | V0.2.x P4/P5 与 cowork 规划归位说明更新 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 更新 `docs/3-design/V0.2.x-功能迭代任务分解.md` 和 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；明确原 P4 公共文件库转入 cowork P0“团队资料库 MVP”，原 P5 飞书导入后置为连接器/知识源导入规划，并将原 P4/P5 清单标记为归档参考 |
| DOC-017 | 文档 | cowork 版工具设计规划补充 Milvus 向量库候选 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；将 Milvus Lite 纳入 Phase 2 本地向量库 POC，将 Milvus Standalone/Distributed 与 Qdrant 归入中后期服务化向量库评估，并同步补充 POC 决策门槛和官方资料来源 |
| DOC-018 | 文档 | cowork 版工具设计规划补充角色、权限与个人 Agent 分工 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；新增空间角色、流程角色、个人 Agent/团队 Agent/审查 Agent 分工，明确个人空间默认私有、团队空间粗粒度角色、审查关键动作确认人，并补充 RolePolicy、AgentAuthorization、ReviewParticipant 等领域对象 |
| DOC-019 | 文档 | 三个 cowork 相关开源项目设计借鉴评估文档 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 新增 `docs/2-discussion/2026-06-04-三个cowork相关开源项目设计借鉴评估.md`；系统整理 `workflow-cookbook`、`open-cowork`、`onyx` 三个项目对确定性工作流、本地 Agent 产品化、团队知识库/权限检索的借鉴点、不建议照搬边界、领域对象映射和分阶段吸收路线 |
| DOC-020 | 文档 | cowork 版工具设计规划吸收调研报告并正式修订 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；参考 `docs/2-discussion/调研报告/` 下 4 篇 Markdown 和 1 份 PDF，补充评审快照空间、M 型监督者/T 型专家/AI 增强型一线人员、Agent 生命周期治理、MCP Hub/Gateway、ToolCallTrace、MCPToolPolicy、AgentApprovalRequest、WorkflowEvent、AI 讲解产物管线、黑板/发布订阅协作机制和风险控制 |
| DOC-021 | 文档 | cowork 版工具设计规划吸收三个开源项目过程讨论稿 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；参考 `docs/2-discussion/过程讨论稿/2026-06-04-三个cowork相关开源项目设计借鉴评估.md`，补充 `workflow-cookbook`、`open-cowork`、`onyx` 的分层借鉴表、阶段吸收路线，并新增/强化 WorkflowTemplate、WorkflowRun、WorkflowBudget、DocumentSet、ConnectorCredential、SyncJob、IndexAttempt、AccessPolicy、AnswerFeedback、SkillPackage、SandboxPolicy 等领域对象和“不照搬”边界 |
| DOC-022 | 文档 | V0.2.8 版本号统一与 BUG-032/033 修复记录 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 全代码版本号 0.2.7→0.2.8：main.py(2处)、branding_config.py、HTML(3处)、update.sh、conftest.py、test_app_frontend_contract.py、README.md(2处)、ui-branding.example.yaml；BUG-032 修复 scan_runtime 通用 logo 映射；BUG-033 修复 get_branding_config 空资产字段自动发现；663 全量回归通过 |
| DOC-023 | 文档 | Cowork 版 WBS 任务分解文档 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 新增 Cowork版-WBS任务分解.md；将规划文档 P0~P6 六个阶段、3 类 POC、领域对象和业务流程拆为 124 条可量化任务，含验收标准、工时估算和前置条件 |
| DOC-024 | 文档 | Cowork 版 WBS 单团队部署与团队空间页面口径修订 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；补充单团队部署范围修正，明确系统只初始化 1 个默认团队空间、注册用户默认加入本团队、新增“团队空间”一级并列页面；P0 前置团队空间页面壳/导航/资料库主入口，P1 收束为默认团队设置与成员管理，移除多空间创建/切换器，P5 个人资料改为私有知识作用域而非一级个人空间，并修正实施总览子任务数 |
| DOC-025 | 文档 | Cowork 版 WBS 并行开发分组与命名开发规范补充 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；为 P0~P6 和 POC 增加按依赖关系拆分的并行开发组，补充任务 ID、数据模型、API、前端、runtime、状态枚举、分层开发、权限测试等命名标准和开发规范 |
| DOC-026 | 文档 | Cowork 版 WBS 完成状态快速标记 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；新增“快速验证标记（2026-06-05）”说明，明确仅 P0.A.1~P0.E.4 标记为“已验证完成”，并在 Phase 0 及 P0.A~P0.E 标题补充完成标记与测试依据 |
| DOC-027 | 文档 | Cowork 版 WBS P1~P6 完成状态补充标记 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；P1.A.4、P1.B.4 标记 ✅ 已验证完成，P1.A.1、P1.A.2、P1.C.2 标记 ⚠️ 部分完成，P1.A.3/P1.B.1~B.3/P1.C.1 标记未开始；Phase 2~6 和 POC 标题补充（未开始） |
| DOC-028 | 文档 | Cowork 版 WBS P0.E 前端设计要素记录 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md` P0.E 小节；补充前端设计要素记录：页面结构(topbar+sidebar+content)、资料库表格列定义、详情页结构(元数据网格+正文预览+引用项目+鉴权下载)、空状态规范、权限显隐规则(owner/admin/member/viewer)、品牌注入点(data-branding 属性)、CSS 类名体系(ws-/workspace- 前缀) |
| DOC-029 | 文档 | Cowork 版设计规划补充团队空间 MVP 前端 UI 原则 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md` §4.2.1；补充 7 条前端 UI 原则：页面并列原则、sidebar+content 布局、权限显隐而非阻断、资料库交互规范、空状态组件、品牌注入机制、CSS 前缀规范 |
| DOC-030 | 文档 | 四个 cowork 相关开源项目设计借鉴评估补充 `odysseus` | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 更新 `docs/2-discussion/过程讨论稿/2026-06-04-三个cowork相关开源项目设计借鉴评估.md`；标题改为“四个 cowork 相关开源项目设计借鉴评估”，补充 `pewdiepie-archdaemon/odysseus` 专节，并同步扩展总体结论、抽象映射、统一领域模型、MVP 边界、阶段吸收路线和来源列表 |
| DOC-031 | 文档 | Cowork 版 WBS P1 启动条件与权限任务边界调整 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；补充 P1 可启动的复核结论与 91 条定向测试依据，新增默认团队空间稳定标识前置约束；将 P1.A.3 从未开始改为部分完成；将 P1.B 从提前落 `RolePolicy/ResourceACL` 调整为优先统一权限 helper、补审查域访问对齐 |
| DOC-032 | 文档 | POC-A Runtime 真实需求检索复验报告 | 2026-06-06 00:00 | 2026-06-06 00:00 | 已完成 | 新增 `poc-a/reports/POC-A-Runtime真实需求复验报告.md`，单独整理 runtime 真实需求 Markdown 复验的方法、数据规模、四方案指标、无答案问题解释、Milvus Lite 选型判断和 POC-B 后续建议；报告属于 POC 输出数据，仍按 `.gitignore` 不纳入 git |
| DOC-033 | 文档 | POC-A 对比报告决策建议更新（最终版） | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 更新 `poc-a/reports/POC-A-对比报告.md` §6 决策建议，将推荐方案从"Hybrid 混合检索"改为"LanceDB 单引擎 + FTS5 降级 + 分数差拒答"，新增决策变化对照表、POC-A/B 完成状态汇总和未完成事项清单 |
| DOC-034 | 文档 | POC-A Runtime 复验报告决策更新（最终版） | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 更新 `poc-a/reports/POC-A-Runtime真实需求复验报告.md` §7-8 推荐决策和后续动作，将推荐从"混合检索"改为"LanceDB 单引擎 + 分数差拒答 + OpenAI 嵌入"，将后续动作从"建议新增 POC-B"改为"POC-B 已完成、下一步为 P2 开发" |
| DOC-035 | 文档 | POC-B 混合检索深度验证报告修订版 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 修订 `poc-b/reports/POC-B-混合检索深度验证报告.md`，新增 §6 混合检索退化根因深度分析（FTS5 bigram 误召回 + TF-IDF 未归一化负分问题）、§7 POC 数据修正说明（LanceDB 权限 90.9% 是 lambda role 疏漏而非能力问题）、更新 §8 综合结论为 LanceDB 单引擎 + 分数差拒答 + OpenAI 嵌入 |
| DOC-036 | 文档 | 检索引擎选型最终结论文档 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 新增 `docs/3-design/检索引擎选型最终结论.md`；基于 POC-A+B 全部数据，确定 P2 检索服务选型：LanceDB 单引擎（95.2% top-5 / 89.3% top-1 / P50=10ms / 120 QPS / 100% 权限过滤 / 7ms 备份），FTS5 降级回退，分数差 + LLM 证据判断拒答，OpenAI text-embedding-3-small 嵌入；含实验总览、质量结论、关键发现（Hybrid 退化根因、权限修正、阈值拒答失败）、综合评估矩阵、架构建议、风险缓解和后续动作 |
| DOC-037 | 文档 | Cowork 版 WBS P2 任务更新：对齐最终选型结论 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；(1) POC-A 决策结论从"FTS5+LanceDB 混合检索"改为"LanceDB 单引擎首选+FTS5 降级回退"（6 处）；(2) P2 并行分组新增 P2-G5 LanceDB 集成与降级；(3) 新增 P2.E.1~E.3 任务（LanceDB 索引构建查询、FTS5 降级回退、拒答策略分数差）；(4) P2.B.1 延迟标准从"混合检索可放宽到 3s"改为"LanceDB 单引擎 P50=10ms"；(5) P2 任务数从 15 更新为 18；(6) POC-A.4 Milvus Lite 状态从"淘汰"改为"远期候选" |
| DOC-038 | 文档 | cowork 版设计规划技术选型更新：对齐最终选型结论 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；(1) §2 P2/P3 描述从"混合检索 POC"改为"LanceDB 单引擎检索+拒答策略"；(2) §8.1 技术选型表全文检索/向量库/Embedding 行更新为 POC-A+B 结论；(3) §8.2 向量库候选表重写为已确定选型+POC-A+B 数据+关键发现；(4) §8.3 嵌入模型选型确定为 OpenAI text-embedding-3-small+归一化向量说明 |
| DOC-039 | 文档 | 检索引擎 POC 标准测试方案文档 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 新增 `docs/3-design/检索引擎POC标准测试方案.md`；定义标准 POC 应怎么做（真实嵌入、4类问题集、端到端延迟、分数差拒答），对 POC-A/B 做差距分析，给出 POC-C 补测建议 |
| DOC-040 | 文档 | POC-C 真实嵌入检索验证报告 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 新增 `poc-c/reports/POC-C-真实嵌入检索验证报告.md`；基于 OpenAI text-embedding-3-small 真实嵌入的 LanceDB/Chroma/FTS5 检索验证；标题/章节定位 100%、整体 80.8%、端到端延迟瓶颈在 embedding API、dist[0] 阈值拒答可行（LanceDB 1.0/Chroma 0.5）、LanceDB 备份 5ms/6.1MB |
| DOC-041 | 文档 | 检索引擎选型最终结论纳入 POC-C 数据更新 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 更新 `docs/3-design/检索引擎选型最终结论.md`；(1) §2 新增 POC-C 真实嵌入数据（80.8%/100%标题/章节定位）；(2) §3.5 拒答策略从"阈值不可用"改为"dist[0] 绝对阈值可行+POC-C 数据支撑"；(3) §4.2 选型结论补充 POC-C 验证确认；(4) §4.3 决策门槛核验更新为 POC-C 数据；(5) §6.1 架构图增加 EmbeddingService 预热+缓存；(6) §7 风险表增加 embedding 冷启动和语义匹配命中率低；(7) §8 后续动作更新（POC-C 已完成） |
| DOC-042 | 文档 | Cloud co-worker 远期规划与开源项目调研文档 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 新增 `docs/2-discussion/2026-06-04-Cloud-coworker-远期规划与开源项目调研.md`；覆盖 GitHub star>=600 项目调研、精选项目架构拆解、团队空间/知识库/Agent 对话/消息/审查平台升级业务逻辑、现有代码承接点、阶段路线图和风险约束 |
| DOC-043 | 文档 | Cowork 版 WBS Phase 3 完成状态修正 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 将 `docs/3-design/Cowork版-WBS任务分解.md` Phase 3 标题从”已开始”更新为”已完成并验证”；实施总览同步从”已开始 Phase 3”改为”已完成并验证 Phase 3”；补充 2026-06-09 状态说明：P3.A~P3.E 已完成，当前源码含 12 个 `/api/agent/*` 路由和 Agent 生产模型/API/前端/测试，17957 运行实例已于 2026-06-11 重启后加载全部后端路由（CHK-073 闭环） |
| DOC-044 | 文档 | V0.2.12 版本号统一与 README Agent 功能补充 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | ① 全代码版本号 0.2.11→0.2.12：main.py(2处)、branding_config.py、mcp_adapter.py、HTML(4处)、update.sh、conftest.py、test_app_frontend_contract.py、package.json ② README.md 中英文架构层新增”Agent 编排层”描述 ③ 功能要点新增”Agent 对话与工具注册”条目 ④ 后台管理新增”Agent 管理”条目 ⑤ 目录说明新增 agent/extensions、package.json |
| DOC-045 | 文档 | 用户故事清单 V0.2.13 代码一致性修正 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 对照实际代码检查全量 30 条用户故事，发现 7 处偏差并修正：US-01 登录后默认页面与导航可见性、US-04 详情面板布局描述、US-07 Tab 标签名、US-16 审批导航路径、US-18 MCP 前端 UI 缺失加备注、US-25 产物类型项、US-26 讲解准备模式展示方式；全量回归验证通过 |
| DOC-046 | 文档 | P5/P6 实施后文档一致性审查 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已完成 | 5 类待更新项全部收口：(1) 用户故事清单已通过 DOC-047 补充 US-31~US-44 共 14 条；(2) README.md 中英文已补充 P5/P6 功能要点（个人知识/Agent/治理运营/配额/成本统计/Skill回归/Agent退役/权限审计）、治理层架构描述、governance router 和 CostStatsService/QualityStatsService 目录说明；(3) WBS P4/P5/P6 共 48 个已完成任务行补充内联 ✅ 标记，P4.C 增强版保留未标记；(4) 版本号从 0.2.13 提升为 0.3.0 里程碑版本并统一全代码 11 处；(5) task-list.md 补充 DEV-063(P5.D.1) 和 DEV-064(P6.D.1) 独立条目 |
| DOC-047 | 文档 | P5/P6 用户故事清单：新增 US-31~US-44 共 14 条用户故事 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 新建 `docs/3-design/用户故事清单-P5到P6.md`，覆盖个人知识、Agent 设置、Agent 对话通知、消息中心、@提及、评论 resolve、成本统计、质量趋势、Skill 管理、Agent 退役、权限审计、配额管理 |
| DOC-048 | 文档 | V0.3.0 里程碑版本号统一与 README 治理层/P5/P6 内容补充 | 2026-06-12 00:00 | 2026-06-12 00:00 | 已完成 | ① 全代码版本号 0.2.13→0.3.0：main.py(2处)、branding_config.py、mcp_adapter.py、HTML(4处)、update.sh、conftest.py、package.json、README.md(2处)、用户故事清单(2处) ② README.md 中文：架构从四层改为五层（新增治理与运营层）、功能要点新增个人知识/Agent/治理运营条目、后台管理新增治理与运营条目、目录说明新增 governance router 和 CostStatsService/QualityStatsService ③ README.md 英文同步更新 ④ WBS P4/P5/P6 共 48 个已完成任务行补充 ✅ 内联标记 ⑤ task-list.md 补充 DEV-063/064 独立条目 |
| DOC-049 | 文档 | task-list.md 标准化整理：修复重复 ID、记录错位、重复章节、Schema 迁移 | 2026-06-22 00:00 | 2026-06-22 00:00 | 已完成 | 使用 task-list-initialization 技能整理全量 297 条记录：(1) Schema 迁移 6 列单日期 → 7 列「发现时间/完成时间」；(2) 修复 10 对重复 ID——DEV-047~052/058~061 第二批 P4/P5/P6 记录重编号为 DEV-065~074，CHK-059/062~064 第二批重编号为 CHK-074~077；(3) 移动 ~20 条错位记录到正确章节（DEV→功能开发，CHK→检查事项）；(4) 合并重复的「文档维护」章节，CHK-058 从文档维护回归检查事项填补编号空缺；(5) 删除「测试数据」重复表头；(6) TD-001→TST-001 前缀规范化；(7) 「开源项目调研」→「调研事项」；(8) 补齐缺失的「配置运维/规划事项/优化事项」空分区；(9) 转义 BUG-057/071 单元格内未转义的 `\|\|` 管道符；整理后 CLI check 通过，统计摘要 297 条全部已完成/已修复 |
| DOC-050 | 文档 | task-list.md 二次标准化：诊断报告、统计摘要、空备注补全 | 2026-06-24 19:27 | 2026-06-24 19:27 | 已完成 | CLI `check` 通过（301 条）；生成 `docs/task-list-standardize-report.md`；补全 DEV-047/CHK-066 空备注；15 条仅日期记录统一补 `00:00`；文件头补充 extended profile 与时间格式说明；写入「统计摘要」分区（301/301 完成）；CHK-078 更新为 1060 passed；CLAUDE.md 对齐 canonical 维护规则标题 |
| DOC-051 | 文档 | V0.3.1 用户故事与 E2E 测试用例图（52 条 + 5 张用例图 + 18 项 TC 矩阵） | 2026-06-26 00:00 | 2026-06-26 00:00 | 已完成 | 新建 `docs/3-design/用户故事与E2E测试用例图-V0.3.1.md`；合并 P0~P6 旧清单为 US-01~52；区分 5 类 Actor；P6 升级为治理 UI；拆分 Agent 设置（US-21/44）；附 E2E-Smoke/Core/Full 三阶段建议与 8 条待讨论问题 |
| DOC-052 | 文档 | V0.3.1 版本号全量同步 + README 治理能力补全 + BUG-098 状态标准化 | 2026-06-29 00:00 | 2026-06-29 00:00 | 已完成 | (1) 全代码版本号 0.3.0→0.3.1：main.py(2处)、branding_config.py、mcp_adapter.py、index.html(4处)、conftest.py、update.sh(NEW_VERSION)、package.json、package-lock.json(顶层2处)、ui-branding.example.yaml(0.2.13→0.3.1)、README.md(2处)、用户故事清单-P0到P4/P5到P6(各1处)；test_runtime_config.py 的 0.2.8 和 test_router_persistence_scan.py 的 WBS 0.2.1 为测试夹具/注释引用保留不动；(2) README.md 中英文补全 V0.3.1 能力：治理与运营新增 BudgetGuard 硬限制拦截、services 目录补 BudgetGuard、storage 目录改为三模块归口（Chat/Knowledge/ReviewFileStorage）、内网部署新增 Linux/Windows 跨平台支持；(3) BUG-098 状态「待观察」→「已关闭」（已评估为已知 SQLite 限制，单机低风险），完成时间补齐；(4) 验证：前端契约+API+runtime_config 106 passed、bugfix_7issues+gitignore_contract 15 passed |
| DOC-053 | 文档 | 新增「打包与部署指南」文档，README 增补打包部署小节与根脚本目录说明（配合 OPS-002 的 package.sh） | 2026-07-07 16:12 | 2026-07-07 16:12 | 已完成 | 新建 `docs/4-deployment/2026-07-07-打包与部署指南.md`（8 章：三套脚本分工 / 打包 / 首次部署 / 版本更新 / 运行时数据迁移 / 常见问题 / 验收清单，内容与 package.sh + update.sh 实际行为逐项对齐）；README.md 中英文各增「打包与部署 / Packaging and Deployment」小节并补充 `start.sh / update.sh / package.sh` 目录说明，链接指向 4-deployment 两篇文档（已验证目标文件存在）；中英标题结构与 License 前后顺序校验通过 |
| DOC-054 | 文档 | README 与「打包与部署指南」同步 package.sh 默认 zip 的变更（配合 OPS-004） | 2026-07-07 16:40 | 2026-07-07 16:47 | 已完成 | README.md 中英文「打包与部署 / Packaging and Deployment」代码块更新为默认 zip + `--format tar.gz` 备选；部署指南：流程图改为 zip（默认）/tar.gz 双分支、3.1 用法补充 --format 选项与默认 zip 说明、3.4 自检清单新增 macOS AppleDouble（._*）拦截项、4.2 首次部署解压命令由 tar -xzf 改为 unzip（保留 tar.gz 备选） |
| DOC-055 | 文档 | V0.3.3 版本号全量同步（顺带修正 V0.3.2 提交漏 bump 版本号的历史遗漏） | 2026-07-07 18:30 | 2026-07-07 18:30 | 已完成 | 发现提交 289af60 标记为 V0.3.2-Build0598 但版本号字串从未实际 bump（`git log -S 'version="0.3.2"'` 证实从未出现在 main.py），故本次直接 0.3.1→0.3.3；全代码/配置/文档 10 个文件 18 处同步：main.py(2: FastAPI version + /api/health)、branding_config.py(DEFAULT_BRANDING.app_version)、mcp_adapter.py(MCP clientInfo.version)、index.html(4: topbar Ver. 标注)、conftest.py(测试健康检查 mock)、update.sh(NEW_VERSION 硬编码 fallback——若不同步会在 line 448 误判"已是该版本"跳过部署)、package.json、ui-branding.example.yaml、README.md(中英 2 处)、打包与部署指南(5 处：适用版本/解压命令/健康检查返回/update.sh 示例)；package-lock.json 虽含 0.3.1 但已 git-ignored 且不入包、由 package.json 在 npm install 时自动重生成，故不手改；docs/3-design 下 3 篇历史设计基线文档（用户故事与E2E测试用例图-V0.3.1.md 等）的版本号+日期头是历史快照，刻意保留不动；验证：无测试硬断言版本字串值、全量 pytest 1087 passed |
| DOC-056 | 文档 | V0.3.4 版本号全量同步 | 2026-07-09 17:07 | 2026-07-09 17:08 | 已完成 | 0.3.3→0.3.4：main.py(2)、branding_config.py、mcp_adapter.py、index.html(4)、conftest.py、update.sh(NEW_VERSION)、package.json、ui-branding.example.yaml、README.md(中英 2)、打包与部署指南(5)；docs/3-design 历史快照与 package-lock.json（git-ignored）不改；定向 166 passed，全量 `pytest -q` 1090 passed |
| DOC-057 | 文档 | V0.3.5 版本号全量同步 + README/部署指南对齐 BUG-112~118 | 2026-07-17 15:52 | 2026-07-17 15:52 | 已完成 | 0.3.4→0.3.5：main.py(2)、branding_config.py、mcp_adapter.py、index.html(4)、conftest.py、update.sh(NEW_VERSION)、package.json、ui-branding.example.yaml、README.md（版本+能力+快速启动 JWT 必填）、打包与部署指南（版本/解压/健康检查/update 示例 + JWT 校验说明）；skills/* 无应用版本号无需改；docs/3-design 历史快照与 package-lock.json 不改；定向 branding+runtime_config+bugfix_112_118 共 105 passed，全量 `pytest -q` 1103 passed |
| DOC-058 | 文档 | V0.3.6 版本号全量同步 + README/部署指南/运行时基线对齐 BUG-119~124 | 2026-07-29 18:45 | 2026-07-29 18:45 | 已完成 | 0.3.5→0.3.6：main.py(2)、branding_config.py、mcp_adapter.py、index.html(4)、conftest.py、update.sh、package.json、ui-branding.example.yaml、README.md（中英版本+Agent/安全/协作/配额能力）、打包与部署指南（5）、运行时基线（agent_sandboxes、llm 归属字段、ChatFileStorage 路径安全）；历史用户故事快照与 package-lock 不改 |
| DOC-059 | 文档 | 打包与部署指南新增「4.3 子路径前缀部署（可选）」章节 | 2026-07-31 17:50 | 2026-07-31 18:20 | 已完成 | 说明 ROOT_PATH/SERVER_HOST 配置（start.sh 透传 uvicorn --root-path，前端自动推导 API 前缀）；给出 Caddy 与 Nginx 整段转发示例（不 strip 前缀，FastAPI root_path 处理路由）；强调 `/prd-review` → `/prd-review/` 尾斜杠 308 跳转必要性（前端静态资源为 `./` 相对路径）；提供本机直连（无前缀）与经反代（带前缀）双端点验证方法 |
| DOC-060 | 文档 | 新增 eval 目录数据说明文档 | 2026-07-31 20:44 | 2026-07-31 20:44 | 已完成 | 新建 `eval/eval目录数据说明-20260731.md`：说明 eval/ 集中存放检索能力验证证据（poc/ 三个选型 POC + p2d1/ 正式验收）的来龙去脉、各子目录作用、四条基本结论（LanceDB+FTS5+OpenAI embedding 选型、放弃混合检索、dist/gap 拒答策略、P2.D.1 验收指标）与使用注意（280M 可再生二进制数据、密钥已删除、重跑命令） |
| DOC-063 | 文档 | 竞赛材料《业务背景与痛点》合规检查与修订 | 2026-07-31 20:52 | 2026-07-31 21:00 | 已完成 | 检查 `design/4-竞赛汇报-20260731/` 三件套：md 正文 906 字超 900 上限（修订后 893 字）、未引用 SVG 图（文末补 `![流程对比图]` 嵌入）；SVG 渲染检查通过——标题观点鲜明、顶部 1-3天vs分钟级对比条、左右六步流程对照、底部六痛点→价值映射，素材丰富无排版问题，未改动；内容三章（流程现状/六维痛点/AI 重构必要性）覆盖要求未改结构 |
| DOC-064 | 文档 | 竞赛材料 prd-review-before-after.svg 评估修复与 PNG 重渲染 | 2026-07-31 20:52 | 2026-07-31 20:58 | 已完成 | 评估发现四处问题并修复：① 补核心创新「评审上下文注入」（新模式第 3 步副文 + 新增亮点标签，旧模式对应补「规范未注入」痛点标签）② 第 5 步「草稿生成」强化为「PRD 草稿生成」+「评审与输出一体」，输出侧对比更鲜明 ③ 逐篇分析补具体维度数「6 维」；修复后 Chrome headless 重渲染 PNG（首次直接截图 SVG 被裁切，改临时 HTML 包装 img 缩放至 1000px 白底，二次渲染完整无截断）；渲染图逐项目检通过 |
| DOC-065 | 文档 | 竞赛材料《AI流程重构方案》及两张 SVG 合规检查与修复 | 2026-07-31 21:05 | 2026-07-31 21:25 | 已完成 | ① md 正文 1254 字超 950 上限，分三轮精简至 949 字：删除与正文重复的流程代码块、压缩五章表述，保留任务角色拆解表/流程设计/六大系统策略/颠覆性总结全部要素 ② 工作流全景 SVG 修复「人类角色/AI 专家团/知识沉淀」三个分区标签不可见问题——根因是 CSS 类 `.sec-tag{fill:#ffffff}` 优先级高于 fill 呈现属性，白字落浅色底，改 inline style 覆盖；画布高度 1060→810 裁掉 280px 底部留白 ③ 系统策略矩阵 SVG 渲染检查通过（六卡片痛点/策略/价值结构完整，未改动）④ 两张 SVG 按 DOC-062 同参数导出配套 PNG（1000px 白底），目检通过 |
| DOC-066 | 文档 | 竞赛材料《落地效果》及三张 SVG 合规检查与修复 | 2026-07-31 21:30 | 2026-07-31 21:36 | 已完成 | ① md 正文 1189 字超 950 目标，精简至 949 字，保留效率/成本/业务增长/可复用扩展/合规底座五章与全部量化点（3–5天→半天内降90%、24→2工时降92%、20→120份6倍、覆盖率30%→100%、95%+、年化12000工时）② 三处 `--` 误用改为破折号 `——` ③ 图文口径对齐：正文"8–15分钟初稿+人工1–2小时"与图表"0.5天/▼90%"矛盾，改为"含人工决策全周期压至半天以内"④ 三张 SVG（效率成本对比/复用与扩展/价值全景）渲染目检通过未改动，按 1000px 白底导出配套 PNG |
| DOC-067 | 文档 | 竞赛材料《作品封面》SVG 重画与 PNG 导出 | 2026-07-31 21:36 | 2026-07-31 21:46 | 已完成 | 原封面信息过载（右侧 6 卡片×4 行小字+4 徽章，封面像 dashboard）、左侧 1/3 大面积空白、12px 小字投影不可读、故事线被特性卡片淹没，决定整图重画（2400×1350 16:9）：标题带+三个 KPI 胶囊（▼90% 周期 / ×6 吞吐 / ▼92% 人力成本，与落地效果口径一致）→ 旧模式弱化对照胶囊（删除线 1–3 天）→ 主故事线「PM 上传 PRD → 六技能专家团流水线（实名技能+角色+产出）→ 一键生成三件套（报告/草稿/讲解素材）」→ 人机协同条（PM 提出→AI 评审→审批人决策）→ 绿色闭环回流带（落库→知识基线→自动复用）→ 四块合规底座 → 金句条+署名；全部矢量绘制不依赖 emoji 字体；Chrome headless 导出 1920×1080 PNG，局部全分辨率复检无裁切 |
| DOC-061 | 文档 | 参赛材料：输出 PRD 评审模式重构演进对比 SVG（旧模式 vs Agent 驱动工作流） | 2026-07-31 | 2026-07-31 | 已完成 | 新建 docs/5-competition/ 目录；产出 prd-review-before-after.svg（1180x1040/16.9KB）：左栏旧模式 6 节点（单次问答/人工搬运/不可追踪）+ 5 痛点标签，右栏新模式 6 节点（docx 转换->概览分类->逐篇分析->系统评审->洞察报告->协作审查）+ 6 亮点标签（Agent 审批门控/白名单沙盒/知识库 RAG/治理/流式可追踪/内网部署），中间「模式重构」演进箭头，底部 5 维度对比表（交互形态/执行主体/评审深度/安全治理/协作沉淀）；视觉对齐现有 docs/3-design/SVGs（系统字体栈+CSS class+浅色卡片+role=img 无障碍）；Python XML well-formed 校验通过、76 文本节点均无溢出 |
| DOC-068 | 文档 | 作品封面第二版：去 AI 风格化 + 疏松排版 + 箭头强化 | 2026-07-31 21:46 | 2026-07-31 21:54 | 已完成 | 按反馈三点改版：① 标题去掉紫蓝渐变横幅+金黄高亮的典型 AI 风，改干净排版风（白底、深灰蓝标题、单一靛蓝点缀、细分割线），KPI 胶囊改浅色卡片 ② 元素疏松化：节点 218→220 宽且节点间隙 18→40px，主流程各区间距全面加大（输入/输出卡 260 宽、主箭头通道 80px、回流虚线绕大圈），人机协同条与合规底座上下留白增至 70px+ ③ 箭头强化：主流程箭头 5→6px 靛蓝加长三角头，节点间箭头 3→3.5px 深灰独立箭头（原为小三角挤在 12px 缝隙），回流虚线 3px 大箭头；结语条由金黄改浅靛蓝与整体一致；重新导出 1920×1080 PNG 目检通过 |
| DOC-062 | 文档 | 参赛材料：将两份演进对比 SVG 导出为 PNG（宽 1000px、高度按宽高比自适应、白底不透明） | 2026-07-31 20:47 | 2026-07-31 20:47 | 已完成 | 输出 design/4-竞赛汇报-20260731/业务背景与痛点-流程对比.png（1000×834，源 SVG 1200×1000）与 prd-review-before-after.png（1000×882，源 SVG 1180×1040）；cairosvg 因缺 libcairo 库不可用、qlmanage 产出固定 1000×1000 正方形缩略图且带透明背景均不符要求，改用 Chrome 150 无头渲染（HTML wrapper 包 `<img width=1000>` + body 白底，--force-device-scale-factor=1 精确控寸，sips 校验像素尺寸与 hasAlpha=no）；附记：修正本段 DOC-060 重复 ID（eval 目录说明与参赛 SVG 此前同号），参赛 SVG 条目重编为 DOC-061 |
| DOC-069 | 文档 | 竞赛材料《落地效果》撰写：量化重构前后价值 + 3 张配套 SVG | 2026-07-31 21:05 | 2026-07-31 21:10 | 已完成 | 新建 design/4-竞赛汇报-20260731/落地效果.md（中文 909 字/总 1341 字，五节：效率跃迁/成本重塑/业务增长/可复用扩展/合规底座，预期按已达成口径表述）；配套 3 张 SVG（落地效果-价值全景.svg 1200×780 雷达+四指标卡+价值落点、落地效果-效率成本对比.svg 1200×640 左右双柱状 5天vs0.5天/24hvs2h、落地效果-复用与扩展.svg 1200×680 六技能+模式递进复用+横向扩展）；Python xml.etree 校验三张 SVG 均 well-formed；承接 业务背景与痛点.md(why) 与 AI流程重构方案.md(how)，本文定位已达成效果(quantified value) |
| DOC-070 | 文档 | 竞赛材料整合：六部分合一为《Pi-Agent需求生产和评审工作流重构方案》汇报材料 | 2026-07-31 21:55 | 2026-07-31 22:00 | 已完成 | 新建 design/4-竞赛汇报-20260731/Pi-Agent需求生产和评审工作流重构方案-20260731.md：按「AI重构流程简介 / 流程重构图 / 业务背景和痛点 / AI流程重构方案 / 落地效果 / 演示体验入口」六段顺序整合现有三份 md（原文保留、标题降级、未改实质内容）；嵌入全部 8 张相关 SVG（作品封面、prd-review-before-after、业务背景与痛点-流程对比、AI流程重构-工作流全景、AI流程重构-系统策略矩阵、落地效果-效率成本对比/复用与扩展/价值全景），其中 AI方案两张图由原文文本提及升级为 ![]() 真正嵌入；演示入口 http://10.181.224.39:8080/prd-review-workflow/ ；正文中文 2649 字；并修复本段 DOC-065 重号（落地效果撰写重编为 DOC-069）、文档维护统计少算 1 条 |
| DOC-071 | 文档 | 作品封面第三版：箭头实心块状化（三角+箭杆一体清晰） | 2026-07-31 21:56 | 2026-07-31 22:01 | 已完成 | 用户反馈箭头三角和线段都要清晰可见（误以为画布不足 2400，实际本就是 2400×1350）；方案：节点卡片 220→208px 宽、节点间箭头通道 40→56px，全部箭头由 line+marker 改为实心块状 path（箭杆+三角一体填充）——主箭头 .ar-main #4338ca 位于输入→流水线/流水线→输出，5 个节点间箭头 .ar-node #64748b，回流虚线加粗 3.5px(dasharray 11 7)+实心大三角指向输入卡底部；输出卡右移 x=2052 宽 248 与主箭头尖端留 6px 间隙；Chrome headless 渲染 1280 宽目检通过后导出 1920×1080 PNG（205KB） |
| DOC-072 | 文档 | 整合文档《Pi-Agent需求生产和评审工作流重构方案-20260731.md》质检修正 + PDF 重导出 | 2026-07-31 22:03 | 2026-07-31 22:09 | 已完成 | 检查发现 3 处问题并修复：① 数学口径错误——"按月评审 50 份、年化节省约 12000 工时"实为 22 工时/份×50×12=13200，改为"约 1.3 万工时"（整合文档与源文件 落地效果.md 同步修正）② 正文 8 处英文双连字符 "--" 统一为中文破折号 "——"（正则 (?<!-)--(?!-)，表格分隔行不受影响；另两个源文件本无此问题）③ 首段「上传->分类->…->报告」实为 6 节点却称"五段闭环"，改为「分类->…->报告」与第四节口径一致；8 张 SVG 引用全部存在无缺链；PDF 因导出于封面 v3 之前（21:58）含旧封面，用 python-markdown(tables)+打印 CSS+Chrome headless --print-to-pdf 重新导出（4.3MB），sips 转首页 PNG 目检通过（新封面、标题样式、破折号均正确） |
| DOC-073 | 文档 | 全量文档审计与修复（README.md + packaging-and-deployment.md + .gitignore） | 2026-08-09 12:00 | 2026-08-09 12:00 | 已完成 | ① README.md 修复 4 个失效链接：`docs/2026-07-07-打包与部署指南.md`→`docs/packaging-and-deployment.md`、`docs/2026-05-22-ubuntu-nginx-systemd-security-plan.md`→`docs/deployment-hardening.md`（中英文各 2 处）② docs/packaging-and-deployment.md 清理 2 处过时 poc-a/b/c 引用，改为仅 `eval/`（中英文各 1 处，§3.3 排除列表）③ .gitignore 删除 poc-a/b/c 旧规则块（26 行），保留 eval/ 和 design/4-竞赛汇报 规则 ④ README.md 中英文目录结构补充 `docs/`、`design/`、`eval/` 三个顶级目录 ⑤ 发现 design/ 有 57 个文件但 0 个被 git 跟踪；用户决定暂不纳入 git，.gitignore 中 design/4-竞赛汇报-20260731/ 规则升级为整个 design/ 全目录排除，README.md 目录结构标注已 git-ignored，待后续敏感信息审查后再放部分技术文档入 git |
| DOC-074 | 文档 | 版本号升级 0.3.8→0.3.9 + context_window 文档补全 | 2026-08-12 06:00 | 2026-08-12 06:30 | 已完成 | **版本号升级**：VERSION、package.json、update.sh、tests/test_version_single_source.py、tests/conftest.py、README.md（中英文）、docs/packaging-and-deployment.md、docs/api-reference.md、docs/getting-started.md、docs/troubleshooting.md 全部 0.3.8→0.3.9。grep 验证无残留 0.3.8 引用（排除 task-list.md 历史记录）。版本单源测试 **8 passed**。**文档补全**：docs/configuration.md 中英文模型字段列表新增 `context_window` 字段说明（与 `max_tokens` 区分：context_window=上下文窗口大小触发自动压缩，max_tokens=输出 token 上限传给 API）；docs/admin-guide.md 中英文创建模型端点说明新增 `context_window` 字段。**全量回归 1382 passed / 0 failed**（143s，6 既有警告） |
| DOC-075 | 文档 | 版本号升级 0.3.9→0.3.10（BUG-138/140/141/142/143 + object_access 安全测试收尾版本） | 2026-08-12 20:00 | 2026-08-12 20:05 | 已完成 | VERSION、package.json、update.sh、README.md（中英文）、docs/api-reference.md、docs/troubleshooting.md、docs/getting-started.md、docs/packaging-and-deployment.md、tests/conftest.py、tests/test_version_single_source.py、tests/test_e2e_live_deployment.py 全部 0.3.9→0.3.10。**未动** package-lock.json（`0.3.9` 系 `@mariozechner/clipboard` 依赖版本，根 lockfile 本与包版本脱钩）与 task-list.md 历史记录。grep 验证无残留 0.3.9（排除历史记录与依赖 lock）。版本单源测试 **8 passed**，真实部署 health check 返回 `0.3.10`，E2E 集成测试 **7 passed / 1 skipped** |
| DOC-076 | 文档 | 收口 CHK-089 英文/中文部署文档过期描述（NEW_VERSION 决定更新、package.json 同步口径） | 2026-08-10 10:35 | 2026-08-13 13:40 | 已完成 | 中文 §5.2 步骤 1、英文 §5.2/§5.3 全部改为解包后以包内版本为准比较、`NEW_VERSION` 仅读取失败回退；英文 §7 FAQ 修正「doesn't sync package.json」矛盾（同步清单含 package.json/package-lock.json，仅不自动 npm install）；`docs/troubleshooting.md` 中英原因条目改为「发布前未 bump 版本号」。最终口径与 DOC-077 一致：优先根目录 `VERSION` 文件、旧包回退 `src/app/version.py` / `src/main.py` 的 `APP_VERSION`。新增 `tests/test_doc_version_contract.py` 契约测试防口径漂移；`bash -n update.sh` 通过。见 CHK-097 |
| DOC-077 | 文档 | 版本号升级 0.3.10→0.3.11 + 文档口径对齐 VERSION 单源、审查配额与 max_tokens 硬上限 | 2026-08-13 13:08 | 2026-08-13 13:20 | 已完成 | 0.3.10→0.3.11。VERSION 为唯一事实来源；同步 package.json、update.sh NEW_VERSION 回退、README 中英、index.html 顶栏兜底、ui-branding.example.yaml。文档：packaging-and-deployment / troubleshooting / api-reference / getting-started 健康检查与示例包名；版本读取口径改为优先根目录 VERSION（旧包回退 version.py/main.py）；admin-guide/security/README 补审查启动也走 BudgetGuard；configuration/admin-guide 补 max_tokens 32768 硬上限。conftest 健康检查改读 APP_VERSION；e2e 健康检查改读 VERSION 文件。定向 106 passed / 8 skipped，全量 1442 passed / 8 skipped / 0 failed。task-list 历史记录未改。 |
| DOC-078 | 文档 | 收口 CHK-098 文档口径：troubleshooting package.json 同步、JWT 空值不拒绝启动、token_hex(32) 为 64 字符 | 2026-08-13 15:51 | 2026-08-13 15:51 | 已完成 | 对照已确认行为改文档。① troubleshooting.md 中英 §9：update.sh 会同步 package.json/package-lock.json，但不自动 npm install（与 packaging-and-deployment.md、update.sh 一致）。② JWT：configuration/troubleshooting/security 中英改为空值不拒绝启动——start.sh 写入根目录 .env；直接 uvicorn 时 config.py 生成进程内临时密钥。硬阻断仅占位值与不足 32 字符。③ configuration.md 中英 token_hex(32) 改为 32 字节/64 个十六进制字符，去掉错误的 128 字符。CHK-097 备注追加 OPT-001/002 已完成说明。契约测试 tests/test_doc_version_contract.py 增至 11 项。定向 11 passed；全量 1458 passed / 8 skipped。 |
| DOC-079 | 文档 | V0.3.12 发版：版本号 0.3.11→0.3.12 全量更新 + CHK-101 发现的文档漂移批量修复 | 2026-08-21 12:40 | 2026-08-21 13:10 | 已完成 | ① 版本号：VERSION（单源）、package.json、update.sh NEW_VERSION 回退值、README 中英、index.html 顶栏兜底×4、ui-branding.example.yaml、troubleshooting/packaging-and-deployment/getting-started/api-reference 示例与包名、契约测试断言值；packaging 适用日期改 2026-08-21；test_version_single_source + test_doc_version_contract 19 passed；bash -n 通过。② 主文档修复：api-reference 中英补 start_review 409 门控说明；admin-guide 修正 2 处错误端点（/api/review/projects/{id}/start → /projects/{project_id}/reviews）+ §6 补 Skill 禁用后果；troubleshooting §5 反代口径与 packaging §4.3 统一（剥离前缀转发）+ 新增 409 故障条目；README routers 清单补 pi_agent；user-guide 补 409/降级用户说明。③ skills 文档修复：system-review domain_rules 改为「未实现规划中」+ prompt 改引 writing_standard/scoring_rubric、--target-doc 仅 prd_draft 生效、补竞品双形态/新字段说明、表格排版；report-generator SKILL.md 报告结构对齐代码（删幽灵第六节、补依赖图/时间线/缺数据标注、图标与标题修正）；prd-per-analysis 补 6+1 维度与 expert_review 文档、Batch Options 补 --force/--review-context-version、prompt 补 rule_key 不可重复；prd-overview-classify 补白名单/待确认机制、SKILL.md 修正空默认配置误导、prompt 删「其他」类别与 confidence 依赖（改直接返回待确认）、usage-guide 修正默认纯关键词分类；docx usage-guide 补 1MB 前置条件/timeout 形参/三项统计/正文颜色、SKILL.md 补 --timeout 与哈希 sentinel；insights SKILL.md 示例补 matches/feature_id。 |
| DOC-080 | 文档维护 | 内置 docx-to-markdown skill 同步升级至上游 skills-doc-to-md 最新版 V0.1.6-Build0098-20260827（commit 3def6e3） | 2026-08-27 21:40 | 2026-08-27 22:10 | 已完成 | 6 个文件全部更新（SKILL.md/usage-guide/requirements/batch_convert/md_to_pdf/convert_docx）。上游新增：defusedxml 安全 XML 解析、多格式图片像素解析、非法文件名清洗、批处理 sentinel 源哈希语义与失败清理、更完善的超时恢复。保留本仓集成接口兼容层（convert_docx.py 尾部）：MAX_* 常量别名/validate_zip_safety/compute_file_sha256/write_converted_sentinel/read_sentinel_source_sha256/output_name 参数/_get_image_pixel_count(本地宽松头解析)/_image_resource_rejection_reason。行为对齐本仓评审契约：入口仅做 zip bomb/条目/总量防线，超大 xlsx 与图片保持「降级跳过」而非上游 V0.1.6 的「硬拒绝整篇」，mammoth 回调禁止超限图片重新落盘。验证：docx 相关 5 套件 87 项全绿，全量 1600 passed / 8 skipped，ruff 清零 |
| DOC-081 | 文档 | 收口 CHK-107：用户文档从 V0.3.12 对齐 VERSION 0.3.14，并补 0.3.13/0.3.14 行为口径 | 2026-08-27 22:26 | 2026-08-27 22:40 | 已完成 | 版本号：README 中英、index.html 顶栏兜底×4、update.sh NEW_VERSION、ui-branding.example.yaml、getting-started/troubleshooting/api-reference/packaging 健康检查与包名（packaging 适用日期 2026-08-27）。行为：configuration/troubleshooting 重试改为 7 次/64000ms + toast；admin-guide/api-reference 补 Pi Agent 先测后存与 config_saved；user-guide/README 补 KaTeX/`\\ce{}`/隔离 SVG/重试 toast；packaging 明确 pip+requirements.txt、不跟踪 uv.lock；system-review/insights/per-analysis/report-generator skill 文档补 per-prompt schema 与 report-polish plain_chat。契约测试 tests/test_doc_version_contract.py 增版本锁定/重试默认/Pi Agent 临时配置/uv.lock/KaTeX 5 项。定向 24 passed；全量 1619 passed / 8 skipped。task-list 历史记录未改 |
| DOC-082 | 文档 | 收口 CHK-109：README 中英 + user-guide 中英×2 共 6 处 SVG 预览描述补 BUG-175 行为口径（0.3.15） | 2026-08-28 16:20 | 2026-08-28 16:25 | 已完成 | 措辞：中文「渐变等内联样式自动归一化，预览自带白色衬底」，英文「inline gradient styles are auto-normalized and previews sit on a white backing」；README.md:77/234、docs/user-guide.md:166/238/399/471。既有口径（Blob URL 隔离、恶意 SVG 拒绝、源码/图形切换）不变 |
| DOC-083 | 文档 | 个人 Agent 信息架构：user-guide / admin-guide 区分系统级 Pi Agent 与全员「个人 Agent」，并写清已授权团队资料口径 | 2026-08-28 21:10 | 2026-08-28 21:20 | 已完成 | user-guide 中英：右上角统一账号菜单（个人 Agent / 修改密码 / 退出登录）；默认范围「我的资料 / 已授权的团队资料」；无授权禁用并列出当前已授权团队空间。admin-guide 中英 §8：系统级 Pi Agent 在管理后台；个人 Agent 仅影响当前账号、不进侧栏。契约 tests/test_personal_agent_ia.py 钉文档关键字 |

## 功能开发

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| DEV-001 | 开发 | WBS E.2 Admin 配置域分层：新增 ModelConfigRepository、PromptTemplateRepository、SkillConfigRepository、UserRepository；重构 admin.py router 全部持久化调用收口到 repository 层 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | admin.py db_add 从 2 降为 0；新增 `src/app/repositories/{model_config,prompt_template,skill_config,user}_repository.py`、`src/app/stores/sse_ticket_store.py` |
| DEV-002 | 开发 | WBS E.3 Auth 账户域分层：新增 SseTicketStore（包装 in-memory SSE ticket dict）、UserRepository 收口注册/改密/登录活跃时间更新；重构 auth.py router、services/auth.py、middleware/auth.py | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | auth.py db_add 从 1 降为 0；services/auth.py SSE ticket 逻辑委派到 SseTicketStore；middleware/auth.py 用户查询委派到 UserRepository |
| DEV-003 | 检查 | 全量回归验证（排除 4 个预存失败） | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 373 passed, 4 deselected；预存失败为 test_docx_conversion_accepts_skill_string_return、test_legacy_runtime_file_path_resolves_to_workspace_runtime、test_parent_relative_runtime_file_path_resolves_to_workspace_runtime、test_source_hash_write_failure_is_non_fatal |
| DEV-004 | 开发 | WBS 0+A+B+C.1 基线保护与分层模板实现：数据兼容性测试、持久化扫描、ChatFileStorage、ConversationRepository、ContextItemRepository、AuditLogWriter/Reader、FrontendLogWriter、LlmSessionLogWriter、ReviewFileStorage | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 新增 `tests/test_data_compat.py`(14)、`tests/test_router_persistence_scan.py`(8)；新增 `src/app/storage/{chat_file_storage,review_file_storage}.py`、`src/app/log_writers/{audit_log_writer,audit_log_reader,frontend_log_writer,llm_session_log_writer}.py`、`src/app/repositories/{conversation_repository,context_item_repository}.py`；重构 upload/chat/history/auth/admin/review router 委派到 storage/repository/log_writer 层；修复 4 个契约测试（skill 路径、runtime_path monkeypatch、_write_source_hash 导入）；377 全量回归通过 |
| DEV-005 | 开发 | WBS D.1+E.1+D.2+F 审查 pipeline 分层收口：ReviewTaskRepository、ReviewProjectRepository、ReviewContextRepository、ReviewPromptRepository、ReviewPipelinePersistenceService；review.py db_add 从 9 降为 0，builtin_open 从 2 降为 0 | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | 新增 `src/app/repositories/{review_task,review_project,review_context,review_prompt}_repository.py`、`src/app/services/review_pipeline_persistence.py`；重构 review.py 所有 Task/DocAnalysis/SystemReview/Project/Document/Context/Prompt 的 db.add 收口到 repository 层；pipeline MD 文件读取改用 ReviewFileStorage.read_markdown()；384 全量回归通过 |
| DEV-006 | 开发 | CHK-008 四项问题修复：summary 非空校验补回、文件删除委派到 ReviewFileStorage、扫描测试补扫 os.remove/os.unlink、ChatApplicationService 落地重构 chat.py | 2026-06-01 00:00 | 2026-06-01 00:00 | 已完成 | BUG-003 修复：`review_task_repository.py` _analysis_has_required_expert_review 增加 summary 非空判断；`review_file_storage.py` delete_document_files 承接 os.remove 调用；`test_router_persistence_scan.py` 新增 os_remove/os_unlink 模式+白名单+测试方法；`chat_application_service.py` 新建+`chat.py` 重构将准备逻辑委派到 ChatApplicationService.prepare_chat_session，移除 _get_jwt_secret/_get_model_config/_truncate_context_text 死代码+清理未用导入；388 全量回归通过 |
| DEV-007 | 开发 | CHK-009 ReviewFileStorage upload_dir 配置注入：ReviewFileStorage 新增 __init__(upload_dir)、_resolve_config_dir、_resolve_upload_root，优先注入路径→配置值→默认回退 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | BUG-004 修复：`review_file_storage.py` 新增 __init__(upload_dir=None) + _resolve_config_dir + _resolve_upload_root（优先注入→review.upload.upload_dir 配置→runtime/data/review_uploads 默认回退）；save_uploaded_docx 和 delete_project_files 改用 _resolve_upload_root()；新增 `tests/test_code_review_fixes.py`(4) 覆盖默认/注入/配置/相对路径四种场景；392 全量回归通过 |
| DEV-008 | 开发 | LLM 思考过程记录：llm.py StreamChunk 新增 reasoning_content 字段 + stream_chat 捕获 delta.reasoning_content；non_stream_chat/retryable_chat 提取 message.reasoning_content；log_llm_session/LlmSessionLogWriter 新增 reasoning_content 参数，content 非空时写入 JSONL；流式路径 chat.py 收集 reasoning_content 并通过 SSE 下发前端 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 影响到 `src/app/services/llm.py`、`src/app/services/retry.py`、`src/app/logging_config.py`、`src/app/log_writers/llm_session_log_writer.py`、`src/app/routers/chat.py`、`src/static/js/chat.js`、`src/static/css/main.css`；修复 3 个 bug：non_stream_chat content=None 处理、llm_session_log_writer 缺换行、chat.py 流式路径 reasoning_content 丢失；468 全量回归通过 |
| DEV-009 | 开发 | P1 模型思考级别全栈开发（P1.1~P1.6）：DB migration + ORM 4字段、Schema/Repository/Router 扩展、ThinkingAdapter 服务层、LLM extra_body 透传、前端 Chat/Review/Admin UI、34 项自动化测试 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 新增 `src/app/services/thinking_adapter.py`（ThinkingLevel/ThinkingAdapter 枚举 + build_thinking_payload + THINKING_ADAPTER_TEMPLATES）；ORM 新增 thinking_supported/thinking_level/thinking_adapter/thinking_payload 四字段 + DB migration；Schema 新增 field_validator 校验；Repository update 扩展；llm.py/retry.py stream_chat/non_stream_chat/retryable_chat/structured_chat 新增 extra_body 参数；chat_application_service.py 集成 thinking adapter；review.py 集成 thinking adapter；前端 chat.js/review.js 新增思考等级下拉联动；admin.js 新增模型编辑/新建思考配置表单 + 条件显隐逻辑 + 表格思考标签；新增 `tests/test_thinking_adapter.py`(34) 覆盖单元测试(25) + API 集成测试(9)；427 全量回归通过 |
| DEV-010 | 开发 | P2 Markdown 与 Mermaid 渲染修复全栈开发（P2.1~P2.5）：前端依赖本地化、Fallback 增强、Mermaid 渲染时机修复、加载态/错误态样式、30 项自动化测试 | 2026-06-02 00:00 | 2026-06-02 00:00 | 已完成 | 新增 `src/static/vendor/{purify,marked,mermaid}.min.js` + `NOTICE.md`；`index.html` CDN 引用改为本地路径 + onerror 加载失败检测；`chat.js`/`review.js` `_renderMarkdown` 拆分为 `_renderMarkdownWithLibraries` + `_renderMarkdownFallback`；DOMPurify `USE_PROFILES` 新增 `svg:true, svgFilters:true` 解决 SVG 兼容；fallback 支持 h1~h6、列表、粗斜体、行内代码、fenced code block、Mermaid 容器；`chat.js` 流式过程 `_queueMermaidRender` 改为空操作，新增 `_schedulePostStreamMermaid`(debounce 兜底) + `_renderMermaidOnStreamEnd`；Mermaid 渲染前先 `parse` 校验；错误态保留源码 + 折叠查看；`main.css` 新增加载动画(`mermaid-spin`) + 错误态 + 源码折叠样式；更新 `test_chat_frontend_contract.py`/`test_review_frontend_contract.py` 契约测试；新增 `tests/test_markdown_contract.py`(30) 覆盖 vendor 本地化(6) + 渲染拆分(10) + 渲染时机(7) + 错误处理(4) + 样式(3)；460 全量回归通过 |
| DEV-011 | 开发 | P3 品牌与本地个性化配置全栈开发（P3.1~P3.5）：BrandingConfigService 配置服务、GET /api/app/branding API、/assets/branding/ 安全静态路由、前端 Branding JS 初始化与 DOM 替换、ui-branding.example.yaml、51 项自动化测试 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 新增 `src/app/services/branding_config.py`（DEFAULT_BRANDING + get_branding_config + _validate_asset_path + _merge_branding + _load_runtime_branding + ensure_branding_dirs）；`main.py` 新增 GET /api/app/branding + /assets/branding/{path} 安全资产路由 + BrandingConfigService 引入 + lifespan 调用 ensure_branding_dirs；`index.html` 关键品牌元素加 data-branding/id 属性（login-title、login-subtitle、login-notice、topbar-title、review-title、admin-label、admin-badge、page-title、favicon-link、theme-color-meta）；`auth.js` 新增 Branding 对象（load/apply/renderLoginNotice），支持 `login_notice` 多行纯文本安全渲染；`app.js` App.init() 调用 Branding.load()；`.gitignore` 保留 runtime/config/ 和 ui-branding.example.yaml；新增 `runtime/config/ui-branding.example.yaml` 并补充通用多行提示示例；新增 `tests/test_branding.py`(51) 覆盖配置结构与默认值(3) + 路径安全(8) + 配置合并(5) + Runtime 加载(3) + 服务层(4) + API 集成(4) + 前端契约(23)；修复 3 个旧契约测试；546 全量回归通过 |
| DEV-012 | 开发 | 品牌迁移工具：本地静态扫描旧代码 HTML/CSS/JS + 旧 runtime 配置和资产，生成 ui-branding.yaml + 复制资产 + 扫描报告，不自动覆盖已有配置（需 --force） | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 新增 `tools/migrate_branding.py`（scan_html/scans_css/scans_runtime/discover_assets/merge_findings/build_branding_yaml/build_scan_report/copy_assets/migrate CLI）；新增 `tests/test_migrate_branding.py`(30)；576 全量回归通过 |
| DEV-013 | 规划 | 品牌迁移工具架构升级：① 拆分 scan/plan/apply 三模式 ② 扫描结果增加三级分类标记 ③ branding_config.py 运行时兼容层 ④ 保障业务数据层完全不动 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 规划已转化为 DEV-014 开发并完成 |
| DEV-014 | 开发 | P3.6 迁移工具架构升级与运行时兼容层 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | BUG-028 已修复；CHK-030 复核通过 |
| DEV-015 | 开发 | 标准版本更新脚本 `update.sh`：停服务→备份→迁移工具 scan/plan（代码替换之前执行，确保旧 HTML 文本配置不丢失）→替换代码→启动→健康检查→失败回滚 | 2026-06-03 00:00 | 2026-06-03 00:00 | 已完成 | 迁移必须在代码替换前执行，否则旧版 HTML 中的 login_notice 等文本配置会丢失；`update.sh` 修复为可执行脚本；移除自动品牌 apply，仅生成 scan/plan 报告；不复制过程清单；更新包校验拒绝绝对/穿越路径和 `runtime/data`、`runtime/uploads`、`runtime/logs`、`runtime/results`、`runtime/storage`；`.env` 备份恢复拆分为 `project.env`/`src.env`；成功健康检查后才自更新脚本；新增 5 项契约测试；CHK-032 复核通过，全量 650 passed |
| DEV-016 | 开发 | P0.A.1 新增 Workspace 表：id/name/description/created_by/status/created_at/updated_at，种子数据 1 条默认 workspace，migration 幂等 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | 新增 src/app/models/workspace.py Workspace 模型、src/app/repositories/workspace_repository.py CRUD、database.py _ensure_default_workspace 种子与注册；25 项自动化测试覆盖表创建/CRUD/种子/幂等；688 全量回归通过 |
| DEV-017 | 开发 | P0.A.2 新增 WorkspaceMember 表：workspace_id/user_id/role/status/created_at，外键约束，角色枚举(owner/admin/member/viewer) | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | WorkspaceMember 模型含 FK CASCADE、role/status 枚举常量；Repository 方法 add_member/get_member/list_members/update_member_role/remove_member/get_user_workspaces；__init__.py 导出 WorkspaceRepository；测试覆盖角色枚举/软删除/跨空间隔离；688 全量回归通过 |
| DEV-018 | 开发 | P0.A.3 新增 KnowledgeSource 表：workspace_id/source_type/title/filename/content_hash/version/owner_id/status/metadata_json | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | KnowledgeSource 模型含 FK CASCADE、source_type/status 枚举常量；KnowledgeSourceRepository 含 create/list_by_workspace/count/archive/set_status/update_version；50 项 workspace 测试全量通过；722 全量回归通过 |
| DEV-019 | 开发 | P0.A.4 ReviewProject 新增 workspace_id 外键（nullable，旧数据兼容）+ P0.A.5 project_source_refs 关联表 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | ReviewProject 新增 workspace_id FK(nullable)、source_refs relationship；ProjectSourceRef 模型含 project_id/source_id/ref_type/snapshot_version；ProjectSourceRefRepository 含 add_ref/list_by_project/remove_ref/freeze_snapshot；database.py _ensure_review_schema 添加 workspace_id 列迁移；50 项 workspace 测试全量通过；722 全量回归通过 |
| DEV-020 | 开发 | P0.A.6 数据迁移脚本：为所有现有 ReviewProject 自动归入默认 workspace | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | database.py _ensure_default_workspace 调用 _migrate_projects_to_default_workspace，将 workspace_id=None 的项目归入默认空间；50 项 workspace 测试覆盖迁移与兼容；722 全量回归通过 |
| DEV-021 | 开发 | P0.A.7 团队空间页面壳 + 导航：新增并列页面、侧栏（资料库+成员两个Tab）、workspace.js、workspace router（list_workspaces/list_members/list_sources）、CSS 样式 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | index.html 新增 #workspace-page 含 topbar+sidebar+content+两个Tab；workspace.js 含 init/load/tab切换/API调用；app.js 新增 _showWorkspacePage+导航绑定；workspace.py router 含 GET /api/workspace + GET /api/workspace/{id}/members + GET /api/workspace/{id}/sources；main.css 新增 workspace 样式；722 全量回归通过 |
| DEV-022 | 开发 | P0.E.1 Workspace 权限校验：delete_source 和 update_source_tags 端点新增 WorkspaceMember 权限检查，非成员返回 403；2 项权限回归测试 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | workspace.py delete_source/update_source_tags 新增 ws_repo.get_member 校验；移除冗余 db.flush()（commit 隐含 flush）；test_workspace_api.py 新增 test_delete_source_non_member_blocked + test_update_tags_non_member_blocked；持久化白名单 workspace.py db_flush 1→0；723 全量回归通过 |
| DEV-023 | 开发 | P0.B.3 删除资料 API（软删除）：DELETE /api/workspace/{id}/sources/{source_id}，status→archived，含空间存在/成员权限/跨空间隔离校验 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | workspace.py delete_source 端点含 ws_repo.get_by_id + get_member 权限校验 + ks_repo.archive 软删除 + audit log；test_workspace_api.py 4 项测试覆盖成功删除/404/跨空间/非成员阻断；723 全量回归通过 |
| DEV-024 | 开发 | P0.B.4 更新标签 API：PUT /api/workspace/{id}/sources/{source_id}/tags，tags 数组写入 metadata_json，version 自增 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | workspace.py update_source_tags 端点含权限校验 + tags 类型验证(422) + metadata_json 合并 + version+1 + audit log；test_workspace_api.py 3 项测试覆盖更新成功/无效类型/非成员阻断；723 全量回归通过 |
| DEV-025 | 开发 | P0.C.1 项目引用资料 API：POST /api/review/project/{id}/sources 添加引用(ref_type/snapshot_version)、GET 列表 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | review.py 新增 add_project_source_ref + list_project_source_refs 端点；ProjectSourceRefRepository.add_ref/list_by_project；ref_type 枚举校验(reference/context/background)；snapshot_version 可选；审计日志；test_workspace_api.py 3 项测试覆盖添加/非owner/无效ref_type；723 全量回归通过 |
| DEV-026 | 开发 | P0.A.7 注册自动加入默认 workspace：注册后自动查找默认空间并加入为 member 角色，幂等处理 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | auth.py register 新增 WorkspaceRepository.get_default + add_member 逻辑；conftest.py init_test_db 种子默认 workspace + admin 为 owner；test_workspace_api.py 2 项测试覆盖自动加入与幂等 |
| DEV-027 | 开发 | P0.B.1 资料上传 API：POST /api/workspace/{id}/sources，文件保存+hash+文本提取+KnowledgeSource 创建 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | KnowledgeFileStorage 新增 save_upload/read_file/delete_file；workspace.py upload_source 端点含 20MB 限制+权限校验+audit log；test_workspace_api.py 4 项测试覆盖上传成功/大小限制/非成员阻断/详情获取 |
| DEV-028 | 开发 | P0.D.1 角色权限校验（owner/admin/member/viewer 区分）：workspace 所有端点新增 _require_role 角色校验 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | workspace.py 新增 _MANAGE_ROLES/_WRITE_ROLES/_READ_ROLES + _require_role helper；delete/tags→owner/admin，upload→owner/admin/member，list/detail→owner/admin/member/viewer；test_workspace_api.py 4 项测试覆盖 member 阻断删除/标签、viewer 阻断上传、admin 可操作 |
| DEV-029 | 开发 | P0.C.3 审查启动时自动冻结引用资料快照：_run_pipeline 在 task.status=running 前调用 freeze_snapshot | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | review.py _run_pipeline 新增 ProjectSourceRefRepository.freeze_snapshot + db.flush()；ProjectSourceRefRepository.freeze_snapshot 将 source.version 写入 snapshot_version；test_workspace_api.py 1 项测试覆盖 freeze_snapshot |
| DEV-030 | 开发 | P0.B.5 前端资料库 Tab（上传+列表+删除）+ P0.B.6 前端资料详情页 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | workspace.js 含 upload/list/delete/detail UI；AgentB 开发，DEV-031 修复后通过验收 |
| DEV-031 | 开发 | P0.C.4 前端资料选择器（审查项目引用资料） | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | review.js _openSourcePicker/_confirmSourcePicker；AgentB 开发，DEV-032 修复后通过验收 |
| DEV-032 | 开发 | AgentB 前端代码 bug 修复：6 个关键问题 | 2026-06-04 00:00 | 2026-06-04 00:00 | 已完成 | ① api.js 移除不存在的 getDefaultWorkspace() ② workspace.js 移除 getDefaultWorkspace fallback ③ workspace.js 移除 listProjectSourceRefs('__all__') ④ workspace.js 移除不存在下载链接 ⑤ workspace.js 删除按钮按角色隐藏(owner/admin) ⑥ index.html 移除重复 workspace-page HTML 块 ⑦ main.css 修复未定义 --color-bg-hover → --gray-1；747 全量回归通过 |
| DEV-033 | 开发 | P1.A.1 默认团队稳定标识收口 + GET/PUT /api/workspace/default 端点 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | Workspace 模型新增 is_default: bool 字段；WorkspaceRepository.get_default() 从 name=="默认空间" 改为 is_default==True 查询；_ensure_default_workspace() 含旧数据 name 兼容自动标记 is_default；database.py _ensure_review_schema 新增 is_default 列 migration；新增 GET /api/workspace/default（读取默认团队信息）+ PUT /api/workspace/default（owner/admin 更新名称/描述，空名称 422 拒绝，改名后注册自动入队验证通过）；list_workspaces 响应新增 is_default 字段；test_workspace_api.py 新增 6 项测试；765 全量回归通过 |
| DEV-034 | 开发 | P1.A.2 成员管理 API + P1.B.1 权限集中化 + P1.B.2 审查域 active member 校验 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | ① workspace_access.py 集中权限服务（ROLE_ACTION_MAP + require_action + is_active_member + can），workspace.py 全部 _require_role 替换为 require_action；② 新增 GET/PUT /api/workspace/default/members（成员列表+角色变更+停用/恢复），禁止自身变更，无效角色/状态 422 拒绝，审计日志；③ WorkspaceRepository 新增 get_member_any_status + list_members selectinload(User) 避免 async lazy-load MissingGreenlet；④ review.py create_project 新增 is_active_member 校验（停用成员无法创建项目），add_project_source_ref 将手动角色判断替换为 is_active_member；⑤ 8 项自动化测试覆盖成员列表/角色变更/停用恢复/自身变更阻断/member权限阻断/无效值拒绝/停用创建项目阻断/停用引用资料阻断；773 全量回归通过 |
| DEV-035 | 开发 | P1.A.3 前端成员管理 UI（角色变更下拉+停用/恢复按钮+确认对话框） | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | ① workspace.js 新增 _renderMemberTable（表格化成员列表，owner/admin 显示角色下拉和停用/恢复按钮，自身行只读）+ _changeMemberRole + _toggleMemberStatus + _confirmMemberAction；② api.js 新增 getDefaultWorkspace + updateDefaultWorkspace + getDefaultWorkspaceMembers + updateDefaultWorkspaceMember；③ main.css 新增 .ws-members-table/.ws-role-select/.ws-status-active/.ws-status-inactive；④ _getDefaultWorkspace 改用 API.getDefaultWorkspace() 替代列表遍历；⑤ 新增 5 项前端契约测试；778 全量回归通过 |
| DEV-036 | 开发 | POC-A 检索实验样本数据：5份团队规范文档 + 5份审查报告文档 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | poc-a/samples/norms/ 下 5 份中文团队规范（需求评审流程规范、PRD写作规范、代码提交规范、接口设计规范、安全合规要求）；poc-a/samples/reports/ 下 5 份中文审查报告（智能对话系统、需求审查工作台、团队空间、知识库资料管理、飞书集成）；每份 1000-2500 汉字，含完整章节结构与专业术语 |
| DEV-037 | 开发 | POC-A 检索实验PRD样本数据：20份中文产品需求文档 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | poc-a/samples/prds/ 下 20 份中文PRD文档（智能对话系统、需求审查工作台、团队空间管理、知识库资料管理、用户认证与安全、数据导出与报表、消息通知系统、后台管理配置、品牌个性化、Mermaid图表渲染、飞书集成导入、思考过程展示、权限RBAC模型、审查快照与版本、Agent对话与工具、知识库检索RAG、审查协作化、AI讲解产物、个人Agent与消息、运营治理工具）；每份 1500-3000 汉字，含完整章节结构与平台术语（SkillRunner、workspace_id、require_action、content_hash、FTS5、ToolCallTrace等） |
| DEV-038 | 开发 | POC-A 全流程实验：FTS5 baseline + LanceDB + Milvus Lite + Chroma 对比 | 2026-06-05 00:00 | 2026-06-05 00:00 | 已完成 | 30份样例文档（274 chunks）、30个检索问题、10个权限用例；FTS5 top-5 56.7% / 0.3ms；LanceDB top-5 66.7% / 254ms；Milvus Lite top-5 66.7% / 1582ms（淘汰）；Chroma top-5 66.7% / 242ms；决策结论：FTS5 + LanceDB 混合检索为首选，Chroma 为备选；对比报告见 poc-a/reports/POC-A-对比报告.md |
| DEV-039 | 开发 | POC-A sqlite-vec 向量检索实验 | 2026-06-06 00:00 | 2026-06-06 00:00 | 已完成 | sqlite-vec 0.1.9 + pysqlite3 替代标准 sqlite3（Python 3.13 移除 load_extension）；vec0 虚拟表 + partition key 做 workspace 隔离；TF-IDF 降维到 8192 维（混合选取：tier1 freq≥2 常见词项 + tier2 高 IDF 罕见词项）；sqlite-vec top-5 73.3% / 6.1ms / 权限 80.0%；数据 33092 KB / 319 条记录；备份恢复（单文件复制）验证通过；结论：sqlite-vec 不作为 P2 MVP 首选（维度限制 + Python 依赖），但作为 LanceDB 的轻量替代备选 |
| DEV-040 | 开发 | POC-A 合成样本校正口径复验（5 方案 + 预计算 encoder + startswith 匹配） | 2026-06-06 00:00 | 2026-06-06 00:00 | 已完成 | 使用 runtime_eval.py 的预计算 TfidfEncoder (max_features=4096) + startswith ID 匹配，在 POC-A 合成样本上跑全部 5 方案；FTS5 56.7%/0.2ms，LanceDB 70.0%/15.0ms，Milvus Lite 70.0%/181.1ms，Chroma 73.3%/3.3ms，sqlite-vec 70.0%/6.3ms，权限 70-90%；核心发现：LanceDB/Milvus Lite/sqlite-vec 检索质量完全一致（70.0%/46.7%），验证"向量库之间质量差异来自评估口径偏差"结论；sqlite-vec 延续"极快"特征（6.3ms） |
| DEV-041 | 开发 | POC-A Runtime 真实需求 5 方案完整复验（含 sqlite-vec） | 2026-06-06 00:00 | 2026-06-06 00:00 | 已完成 | 使用旧 runtime（45 份真实需求 Markdown、943 chunks、94 个自动生成问题、TfidfEncoder max_features=4096）跑全部 5 方案；FTS5 43.6%/0.2ms/100%，LanceDB 95.7%/15.5ms/100%，Milvus Lite 95.7%/508.5ms/100%，Chroma 95.7%/4.7ms/100%，**sqlite-vec 95.7%/6.5ms/100%**；sqlite-vec 维度 4096 < 上限 8192 无需降维；四向量库检索质量完全一致，进一步验证"质量差异来自评估口径偏差"结论 |
| DEV-042 | 开发 | POC-B FTS5+LanceDB 混合检索深度验证 | 2026-06-06 00:00 | 2026-06-06 00:00 | 已完成 | 45 份真实需求、943 chunks、84 问题；5 轮延迟 P50/P95/P99；5/10/20 并发压测；无答案阈值评估；LanceDB 备份恢复；关键发现：① Hybrid 92.9% 低于纯 LanceDB 95.2%（FTS5 bigram 误召回拖累排序）② Hybrid P99 从 46ms 降到 17ms ③ LanceDB 稳定 120 QPS ④ 原始分数阈值拒答不可用（误答率 100%），需 top-1/top-2 分数差或 LLM 证据判断替代 ⑤ 备份恢复 7ms/15.3MB 验证通过；P2 建议：首版用 LanceDB 单引擎，FTS5 作降级方案 |
| DEV-043 | 开发 | POC-C 脚本开发与实验执行 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 新增 `poc-c/scripts/run_pocc.py`（390行），复用 POC-A runtime_eval 数据加载和问题生成，替换 TfidfEncoder 为 OpenAIEmbeddingService（真实 API 调用+批处理+重试+缓存），新增 20 条手写补充问题（8语义匹配+4跨文档+8无答案），新增端到端延迟评估（含 embedding API）、分数差拒答阈值分析（dist[0] 和 gap 分布）、LanceDB 备份恢复验证；成功执行并产出 `poc-c/results/pocc_results.json` 和 `poc-c/reports/POC-C-真实嵌入检索验证报告.md` |
| DEV-044 | 开发 | P2-G0 数据模型基座 + P2-G5 LanceDB 集成 + P2-G1 检索服务（P2.A.1/A.2/A.3/A.4/A.5 + P2.B.1/B.3/B.4 + P2.E.1/E.2/E.3 + P2.D.3） | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | ① 新增 KnowledgeDocument/KnowledgeChunk/RetrievalLog/AnswerFeedback 4 张表及 Repository；② KnowledgeIngestionService 完整入库编排（解析→切块→FTS5 trigram 索引→pending embedding）；③ 切块策略 chunking.py（512 chars max、64 overlap、章节保留）；④ EmbeddingService（OpenAI text-embedding-3-small、批处理 100/batch、重试、查询缓存、预热）；⑤ KnowledgeVectorService（LanceDB upsert/search/delete/backup/restore、workspace prefilter）；⑥ RetrievalService（embedding→向量检索→confidence→FTS5 降级）；⑦ 拒答策略（dist[0]=1.0 绝对阈值 + gap=0.065 低置信）；⑧ 检索 API + 入库 API；⑨ 42 项自动化测试；833 全量回归通过 |
| DEV-045 | 开发 | P2-G2/G3 对话与审查 RAG 集成（P2.C.1 + P2.C.2） | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | ① ChatRequest 新增 enable_knowledge + knowledge_workspace_id 参数；② ChatApplicationService 新增 build_knowledge_context() 方法（检索→权限校验→格式化注入上下文）；③ review.py 新增 _load_project_knowledge_context() 方法（项目引用资料→切块→注入 SkillRunner context）；833 全量回归通过 |
| DEV-046 | 开发 | P2.C.1(前端) + P2.C.3 + P2.C.4 知识库引用前端展示 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | workspace.js 含 upload/list/delete/detail UI；AgentB 开发，DEV-031 修复后通过验收 |
| DEV-047 | 开发 | P2.D.1 检索评估脚本开发与评估执行 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 新增 `scripts/run_p2d1_eval.py` + `tests/test_p2d1_eval.py`（34 项）；30 份样例/80 问题验收：top-5 95.8%、top-1 84.5%、no_answer 拒答 87.5%、无越权召回；CHK-062 确认 P2 阶段收口 |
| DEV-048 | 开发 | POC-B Pi Agent RPC 集成可行性验证（首轮 8 项） | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | 验证 8 项：① 基础 RPC 通信 ✅ ② SSE 流式转换 ✅ ③ 工具限制(--tools/--no-tools) ✅ ④ Extension 机制 ❌（factory 格式需修正） ⑤ 延迟测量 ✅ ⑥ 进程崩溃恢复 ✅ ⑦ 3 用户并发 ✅ ⑧ 自定义 system prompt ✅；首轮 7/8 通过，Extension 格式问题已识别并修复（module.exports → export default function）；OpenAI API 超时导致流式文本为空需切换到 DeepSeek |
| DEV-049 | 运维 | 管理后台新增 Pi Agent 模型配置项 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | model_id=pi-agent，name=Pi Agent (DeepSeek V4)，provider=openai_compatible，api_base=https://api.deepseek.com/v1，llm_model=deepseek-chat，使用 DeepSeek API Key 加密存储；连接测试通过（status=ok）；模型在 /api/chat/models 列表中可见 新增 `scripts/run_p2d1_eval.py`（500+行）评估脚本：使用独立评估数据库+LanceDB索引（不影响生产runtime）；导入30份POC-A样例文档到KnowledgeSource+切块+FTS5索引+OpenAI embedding+LanceDB索引；生成80个评估问题（30 title_find+30 section_find+8语义匹配+4跨文档+8 no_answer）；通过真实RetrievalService→EmbeddingService→KnowledgeVectorService服务链执行检索；验收通过：top-5 95.8%、top-1 84.5%、no_answer拒答87.5%、无越权召回；34项自动化测试覆盖；新增 `tests/test_p2d1_eval.py` |
| DEV-050 | 开发 | Pi Agent 独立配置页面（管理后台新增功能入口） | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 新增 PiAgentConfig ORM 模型（pi_agent_config 表）和 PiAgentConfigRepository；新增 pi_agent router（/api/pi-agent 前缀，7 个 API）；前端 admin.js 新增 loadPiAgentConfig/savePiAgentConfig（7 个配置分区）；api.js 新增 7 个 Pi Agent API 方法；CSS 新增 .pi-agent-section 样式；HTML 新增 pi-agent 导航项和面板；database.py 新增 _ensure_pi_agent_config 初始化；与通用模型配置页面完全分离，独立管理 LLM/Search/Vision/Extension/Skill/SystemPrompt |
| DEV-051 | 开发 | P3.A.1 AgentProfile + P3.A.2 AgentAuthorization 数据模型 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | `user.py` 新增 AgentProfile（owner_type/owner_id/name/system_policy/allowed_tools_json/status/version）和 AgentAuthorization（agent_id/granted_by/scope_type/scope_id/permissions_json/expires_at）ORM 模型；database.py 新增 `_ensure_agent_profiles()` 为已有用户自动创建默认 AgentProfile；导入验证通过 |
| DEV-052 | 开发 | P3.A.3 Agent Profile & Authorization API + P3.B AgentRun/Step/Trace API + P3.C MCP API + P3.D Approval API | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | `user.py` 新增 AgentRun/AgentStep/ToolCallTrace/MCPServerConfig/MCPToolPolicy/AgentApprovalRequest 共 6 个 ORM 模型；新增 `agent_repository.py`（AgentProfileRepository/AgentAuthorizationRepository/AgentRunRepository/AgentApprovalRepository）；新增 `agent.py` router（14 个 API：GET/PUT profile、GET/POST/DELETE authorizations、POST/GET runs、GET approvals、POST approvals/{id}/decide、GET/POST mcp/servers、GET/POST mcp/servers/{id}/policies）；main.py 注册 agent router 到 /api/agent；所有 Python 导入和语法检查通过 |
| DEV-053 | 开发 | P3.A.4 前端 Agent 设置页 + P3.E.1 Agent 模式 UI 骨架 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | admin.js 新增 loadAgentSettings 方法（Agent 配置/授权范围/最近运行/待审批请求 4 个区块）和 _saveAgentProfile/_revokeAgentAuth/_addAgentAuth/_decideApproval 辅助方法；api.js 新增 14 个 Agent API 方法；index.html 新增 agent 导航项和 tab-agent 面板；JS 语法检查通过 |
| DEV-054 | 开发 | P3.E.3 自动化测试（15 条覆盖 P3.A~P3.D） | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 新增 `tests/test_agent_p3.py`：TestAgentProfile（6 条：获取默认/更新名称/更新工具/更新策略/无效状态/禁用启用）、TestAgentAuthorization（2 条：列表/创建撤销）、TestAgentRun（4 条：创建/列表/详情/禁用阻断）、TestMCPConfig（2 条：Server 创建列表/Policy 创建）、TestApproval（1 条：列表）；共 15 条测试，满足 P3.E.3 ≥12 条要求 |
| DEV-055 | 开发 | 方案 A 改造：删除 Python 自建 ReAct 循环，改为 PiAgentBridge 子进程模式 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | 重写 `agent_application_service.py`：删除自建 ReAct 循环，改为通过 PiAgentBridge 委派给 Pi 子进程；新增 `pi_agent_bridge.py`（spawn pi --mode rpc、JSONL stdin/stdout 通信、监听事件流写入 AgentRun/Step/Trace、Extension BLOCKED 日志创建审批请求、非阻塞 stderr 读取）；重写 `tool_registry.py`：删除 Python handler 函数，改为纯 schema 注册（7 个工具含 bash/write/edit 高风险标记）；agent.py 新增 POST /runs/{id}/stream SSE 流式端点；导入验证通过，ToolRegistry 注册 7 工具，agent router 17 routes |
| DEV-056 | 开发 | Pi Agent npm 项目本地部署 + PI_BIN 路径更新 + agent router 依赖注入修复 + 测试重构 | 2026-06-09 00:00 | 2026-06-09 00:00 | 已完成 | (1) package.json 精简为仅 pi-coding-agent@0.79.0（pi-ai/pi-agent-core 作为子依赖自动安装）；(2) PiAgentBridge PI_BIN 从全局 `pi` 改为项目本地 `node_modules/.bin/pi`；(3) agent.py router 改用 `app.database.get_db` 而非本地 `async_session`，修复 dependency_overrides 注入；(4) conftest.py 添加 agent router 注册；(5) test_agent_p3.py 重写为 ASGI Transport 测试模式；(6) create_authorization/create_run endpoint 改为自动创建缺失的 profile（不再返回 404）；912 全量回归通过 |
| DEV-057 | 开发 | P4.Pre 协作底座收口 6 项前置任务实现 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | P4.Pre.4（审批人归属）：AgentApprovalRequest.approver_id 从 nullable→NOT NULL，create 必填，list_pending 按 approver_id 过滤，agent.py 只返回当前用户待审批，pi_agent_bridge.py 传入 user_id 作默认审批人，新增 3 项测试；P4.Pre.6（SkillConfig.status）：SkillConfig 新增 status+version 字段，list_all 默认过滤 active，toggle_status 启停方法，admin.py PUT toggle 端点，admin.js 开关 UI+api.js 方法，HTML 状态列，CSS toggle 样式，5 项测试；P4.Pre.5（消息锚点）：Message 新增 anchor_type+anchor_id，append_message 接受锚点参数，2 项契约测试；P4.Pre.2（对话模式）：Conversation 新增 mode+project_id，ChatRequest 新增 mode+project_id 可选参数，create_conversation 接受 mode+project_id，prepare_chat_session 透传，3 项契约测试；P4.Pre.1（审查可见性）：review.py list_projects 从 created_by 硬过滤改为 workspace 成员可见性（owner/admin 看全部，member/viewer 只看自建）；P4.Pre.3（快照回读）待后续实现；database.py 新增 4 个迁移函数；925 全量回归通过 |
| DEV-058 | 开发 | P4.A 数据模型与基础服务：ReviewRequest/ReviewRound/ReviewParticipant + API + 测试 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | review.py 新增 ReviewRequest（project_id/initiator_id/goal/status/current_round）、ReviewRound（request_id/round_no/snapshot_ref/artifact_ref/approver_id/decision/comment）、ReviewParticipant（request_id/user_id/role/status）ORM 模型；新增 review_request_repository.py（3 个 Repository CRUD+状态变更+轮次决策+参与者管理）；新增 review_request.py router（8 个 API：create/list/get/rounds/decide/resubmit/participants/add_participant）；main.py 注册 /api/review 协作审查路由；conftest.py 补充 review_request router 注册；新增 test_review_request.py（10 项测试覆盖模型字段/创建请求/404/列表/通过/驳回+重新提交/非rejected驳回/参与者）；935 全量回归通过 |
| DEV-059 | 开发 | P4.B 知识快照与产物 + P4.D 通知与评论 + P4.A.6 文档版本链 + P4.Pre.3 快照回读 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | P4.B.1 KnowledgeSnapshot 表+Repository+API；P4.B.2 Artifact 表+Repository（confirm/unconfirm/update_content）+API；P4.B.3 build_presentation_context() 注入审查结果；P4.B.4 物料确认冻结+通知；P4.D.1 Notification 表+Repository；P4.D.2 Comment 表+Repository；P4.D.3 NotificationService（5种通知事件+SSE推送）；P4.D.4 Notification SSE端点；P4.D.5/6 通知列表+评论CRUD API；P4.A.6 ReviewDocument.parent_document_id+迁移；P4.Pre.3 _load_project_knowledge_context 支持 snapshot_version；新增 artifact_repository.py/notification_repository.py/notification_service.py/notification.py/artifact.py；新增 test_p4_collaborative.py（28项）；963全量回归通过 |
| DEV-060 | 优化 | 测试套件整合：消除冗余、合并小文件、集中模型字段验证 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 合并5个小文件→3个大文件：test_frontend_api_contract.py(2)+test_frontend_inline_handlers.py(4)+test_app_frontend_contract.py(5)→test_web_interface_guidelines.py(+10)；test_http_middleware.py(1)→test_api.py(+1)；test_time_utils.py(2)→test_database.py(+2)；删除9个重复模型字段测试（p4_collaborative: Notification/Comment/KnowledgeSnapshot/Artifact/parent_document_id/mode/anchor + review_request: ReviewRequest/ReviewRound/ReviewParticipant）集中迁入test_database.py(+9)；4个inline handler合并为1个test_js_global_objects_exposed_for_inline_handlers；963→958（减5个纯冗余），958全量回归通过 |
| DEV-061 | 开发 | P4.D.5 通知铃铛+Inbox + P4.D.6 评论组件 + P4.B.5 讲解准备 + P4.B.2 产物卡片 + P4.A.4 协作审查前端 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | P4.D.5：4个页面 topbar 新增通知铃铛(bell+badge+dropdown+tabs)，notification.js 模块（SSE实时推送+未读数+列表+标记已读/归档/跳转），app.js 登录后初始化 Notification.init()；P4.D.6：审查页评论组件(comment-input+comment-list+reply+mention-tag)，review.js 新增 _loadComments/_renderComments/_submitComment/_replyToComment；P4.B.5：讲解准备入口(presentation-entry+prepare-presentation-btn)，review.js _startPresentation，chat.js createConversationWithMode(mode,projectId)；P4.B.2：产物卡片(artifact-section+artifact-card+confirm/unconfirm)，review.js _loadArtifacts/_renderArtifactCards/_showCreateArtifactDialog/_showArtifactContent；P4.A.4：协作审查(collab-section+collab-request-card+initiate/resubmit/detail)，review.js _loadCollabRequests/_renderCollabCards/_showInitiateCollabDialog/_showCollabDetail；api.js 新增23个 P4 API 方法（通知7+评论3+协作审查7+产物6+快照2）；CSS 新增通知+评论+讲解+产物+协作审查样式；新增23项前端契约测试覆盖HTML/CSS/JS/API契约；987全量回归通过 |
| DEV-062 | 开发 | US-01 密码可见性切换：登录/注册/修改密码弹窗所有密码输入框添加 eye/eye-off 切换按钮，点击切换明文与隐藏 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | index.html 登录/注册密码框加 .field-password 包裹+.toggle-password 按钮+eye/eye-off SVG；auth.js showChangePassword 三字段同加切换按钮+_bindPasswordToggles 方法；app.js 登录/注册表单初始化调用；main.css 新增 .field-password/.toggle-password/.icon-eye/.icon-eye-off/.showing 样式；1000 全量回归通过 |
| DEV-063 | 开发 | P5.D.1 自动化测试：个人空间隔离、Agent 跨人对话审批链、消息 CRUD、@提及通知、评论 resolve（10+ 条） | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 已隐含在 DEV-051/DEV-060/DEV-061 中实现；P5/P6 专项 26 项测试覆盖个人知识隔离+Agent对话审批+消息CRUD+@提及+评论resolve+成本统计+质量统计+Skill回归+Agent退役+权限审计+配额管理；1051 全量回归通过 |
| DEV-064 | 开发 | P6.D.1 自动化测试：成本统计、质量统计、Skill 回归、配额拦截、Agent 退役（8+ 条） | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | 已隐含在 DEV-061 中实现；governance.py 12 个端点均有测试覆盖；Skill 回归测试框架 6 个参数化测试；26 项 P5/P6 专项测试；1051 全量回归通过 |
| DEV-065 | 开发 | P4 代码差距修复 #1：list_projects 改为基于 workspace 成员可见性而非 created_by 硬过滤 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | DEV-057 P4.Pre.1 已实现；review.py list_projects 从 created_by 硬过滤改为 workspace 成员可见性（owner/admin 看全部，member/viewer 只看自建） |
| DEV-066 | 开发 | P4 代码差距修复 #2：Conversation 增加 mode/project_id 字段 + chat router 支持 presentation 模式 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | DEV-057 P4.Pre.2 + DEV-059 P4.B.3 已实现；Conversation 新增 mode+project_id；ChatRequest 新增 mode；ChatApplicationService.build_presentation_context() 自动注入审查结果 |
| DEV-067 | 开发 | P4 代码差距修复 #3：_load_project_knowledge_context 按 snapshot_version 查询对应版本知识 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | DEV-059 P4.Pre.3 已实现；_load_project_knowledge_context 支持 snapshot_ref 参数，按 ProjectSourceRef.snapshot_version 过滤 KnowledgeDocument.version |
| DEV-068 | 开发 | P4 代码差距修复 #4：审批创建传入 approver_id + list_pending 按当前用户过滤 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | DEV-057 P4.Pre.4 已实现；AgentApprovalRequest.approver_id NOT NULL，list_pending 按 approver_id 过滤 |
| DEV-069 | 开发 | P5.A.1 个人私有知识作用域联动改造 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | KnowledgeSource 新增 owner_type(workspace/user)+visibility(team/private)，workspace_id 改 nullable；KnowledgeChunk 同步新增 visibility；KnowledgeSourceRepository.create 签名重构（source_type,title 位置参数）；workspace.py 新增 6 个个人资料 API（GET/POST/DELETE /api/personal/sources + POST /api/personal/retrieve）；KnowledgeVectorService.search 新增 personal scope（按 owner_id+visibility 过滤）；RetrievalService.retrieve 新增 user_id+scope 参数；前端 workspace.js 新增"团队资料/我的资料"子视图切换+个人上传/删除；api.js 新增 4 个个人资料方法；index.html 资料库 Tab 新增 ws-scope-toggle 按钮；main.css 新增 .ws-scope-toggle 样式；更新 5 项已有测试 + 新增 P5/P6 专项 14 项；1051 全量回归通过 |
| DEV-070 | 开发 | P4 代码差距修复 #6：SkillConfig 增加 status 字段 + ResourceACL 模型设计与实现 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | DEV-057 P4.Pre.6 已实现 SkillConfig.status/version；ResourceACL 按 ADJ-017 修正为基于现有 workspace_access+审计日志演进，不新建独立表 |
| DEV-071 | 开发 | P5.A.2 个人 Agent 默认行为 + P5.A.3 Agent 间对话审批 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | AgentProfile 新增 default_scope_type(personal/workspace) 字段+迁移；AgentProfileUpdate schema 新增 default_scope_type 参数；_serialize_profile 输出 default_scope_type；NotificationService 新增 notify_agent_conversation() 方法；notification.js _jumpToObject 新增 agent_conversation 跳转；1051 全量回归通过 |
| DEV-072 | 开发 | P5.A.4 个人 Agent 设置前端入口 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | index.html 用户菜单新增"Agent 设置"按钮；app.js 新增 _showAgentSettingsModal() 模态框（名称/默认范围/状态）；api.js 已有 getAgentProfile/updateAgentProfile 可复用；1051 全量回归通过 |
| DEV-073 | 开发 | P5.B 消息中心完善 + P5.C 评论 resolve 与 @提及 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | P5.B.1-3 消息中心 API 已存在于 notification.py（GET/PUT/batch-read/archive）；P5.C.1 @提及已在 create_comment 中用正则实现；P5.C.2 Comment 模型新增 resolved/resolution/resolved_by 三个字段+迁移；notification.py 新增 PUT /comments/{id}/resolve 端点+序列化输出 resolve 字段；1051 全量回归通过 |
| DEV-074 | 开发 | P6.A 成本与质量统计 + P6.B Skill/Agent 治理 + P6.C 配额管理 | 2026-06-11 00:00 | 2026-06-11 00:00 | 已完成 | ① CostDailySummary+QualityWeeklySummary+WorkspaceBudget 三张新表及模型；② cost_stats_service.py（从 JSONL 聚合+查询+汇总）；③ quality_stats_service.py（周聚合+查询）；④ governance.py router（12 个端点：成本查询/聚合/汇总+质量查询+Skill 列表/状态更新+Agent 归档+权限审计+配额读写）；⑤ main.py 注册 governance router；⑥ Skill 回归测试框架（6 个参数化测试）；⑦ P5/P6 自动化测试 26 项；1051 全量回归通过 |
| DEV-075 | 开发 | P6.D.2 治理 Admin UI：成本概览、配额管理、质量趋势、Skill 治理、Agent 退役、权限审计 | 2026-06-26 00:00 | 2026-06-26 00:00 | 已完成 | index.html 新增「治理与运营」侧栏与 tab-governance 面板；admin.js loadGovernance 六区块（成本/配额/质量/Skill/Agent 退役/权限审计）+ 刷新按钮；api.js 治理 API 封装；governance.py get_budget 增加 current_month_tokens + GET /governance/agents；修复 usageBarColor const 赋值 bug；新增 test_admin_has_governance_tab + budget/agents API 测试 |
| DEV-076 | 开发 | LLM 模型配置新增 `context_window` 字段（上下文窗口大小），与 `max_tokens`（输出上限）分离；达到窗口上限时触发自动压缩，截断早期对话历史 | 2026-08-12 05:00 | 2026-08-12 05:30 | 已完成 | **DB 模型**：`user.py` ModelConfig 新增 `context_window: Mapped[int]` 列（默认 0=不启用）。**DB 迁移**：`database.py` `_ensure_review_schema` 新增 `ALTER TABLE model_configs ADD COLUMN context_window INTEGER NOT NULL DEFAULT 0`；种子数据支持 `context_window` 字段。**Admin API**：`admin.py` ModelConfigCreate/Update 新增 `context_window` 字段 + `validate_context_window` 校验器（不允许负数）；`_serialize_model` 输出 `context_window`；create/update 端点传递字段。**Repository**：`model_config_repository.py` update() 新增 `context_window` 参数。**模型配置字典**：`chat_application_service.py` `get_model_config()` 和 `review.py` `_get_model_config()` 均输出 `context_window`。**自动压缩逻辑**：`llm.py` 新增 `estimate_tokens()`（粗略 token 估算：~3 字符/token）、`compress_messages_to_budget()`（预算 = context_window - max_tokens - 512 安全余量；超限时从最旧历史开始截断，保留 system + 最近 user + 压缩提示）；`build_messages()` 新增 `context_window` 和 `max_tokens` 参数，组装后自动调用压缩。**前端**：`admin.js` 新建/编辑模型表单新增 Context Window 输入框 + 说明文案（"设为 0 不启用自动压缩"）。**测试**：新增 `tests/test_context_window.py` **22 项**（token 估算 4 + 压缩逻辑 8 + build_messages 集成 3 + Admin 校验 6 + 边界 1）。定向回归 **114 passed / 0 failed**。Ruff 无新增告警 |
| DEV-077 | 开发 | GitHub Issue #7：智能对话/评审报告 SVG 代码块隔离式预览（拒绝 rawCode 内联方案）——`renderer.code` 只输出转义源码占位容器，DOM 插入后经 DOMPurify SVG profile 清洗并生成 Blob URL 经 `<img>` 隔离展示，支持「查看源码/查看图形」切换 | 2026-08-27 20:45 | 2026-08-27 21:40 | 已完成 | 已完成：rich-content.js 新增 `enhanceSvg()`/`sanitizeSvg()`/`revoke()/revokeAll()`——原始输入预检 `_SVG_DANGEROUS_RE`（script/style/foreignObject/image/use/a/animate 等 13 类标签 + on* 事件属性 + href/xlink:href 含 javascript: 与外链）命中即整体拒绝；DOMPurify（svg+svgFilters profile、FORBID 同类标签属性、KEEP_CONTENT:false）二次清洗；DOMParser 校验 XML 合法且单一 `<svg>` 根，非 SVG 内容不得进入预览；清洗结果序列化后生成 Blob URL 挂 `<img class=svg-preview-img>`；主 Markdown 的 DOMPurify 收紧为 `USE_PROFILES:{html:true}`（chat.js/review.js 同步）；Blob URL 生命周期：会话切换/报告重渲染/错误替换前 `RichContent.revoke(scope)` + beforeunload 全量回收，防内存泄漏。index.html 引入 katex CSS/JS ×3 + rich-content.js（先于 chat/review 加载），cache-busting 统一提升为 `?v=20260827-1`；main.css 新增 `.svg-container/.svg-toggle-btn/.show-source` 切换样式；vendor/NOTICE.md 登记 KaTeX 来源与更新说明 |
| DEV-079 | 开发 | 个人 Agent 信息架构收口：四页统一账号菜单；系统级 Pi Agent 与「我的 Agent」彻底拆分；已授权团队资料可在全员面板管理 | 2026-08-28 20:55 | 2026-08-28 21:20 | 已完成 | 管理后台侧栏移除「Agent · 个人」/tab-agent；`loadAgentSettings` 迁入 `App._showAgentSettingsModal`（范围/授权 CRUD/运行/审批，无授权禁用「已授权的团队资料」并列出当前已授权团队空间）；四页用户名改为 button+aria-haspopup；agent_approval 通知改开个人面板。测试 tests/test_personal_agent_ia.py（10 项静态契约 + 1 项 Playwright 端到端，挂钩新工具 tools/verify_account_menu_browser.py：file:// 加载真实 index.html，相对资源路径重写为绝对 URL，四页逐页 .active 切换验证触发器 button/3 菜单项/下拉开合 4/4）；同步改写 3 个旧契约测试（workspace/web-guidelines/review 的旧独立退出按钮与 // Logout 切分断言 → 统一菜单断言）；清除孤儿 CSS .topbar-logout:hover；缓存 `?v=20260828-4`。验证：全量 1737 passed / 16 skipped / 0 failed；Playwright 四页菜单均为「个人 Agent / 修改密码 / 退出登录」、侧栏 3 组且无「我的 Agent」；nav 分组工具 16/16 |

## 调研事项

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| RES-001 | 检查 | Pi Agent 开源项目调研（earendil-works/pi，60K+ stars） | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | TypeScript agent toolkit mono repo：pi-ai（统一 LLM API）、pi-agent-core（agent 运行时+工具调用+事件流）、pi-coding-agent（终端编码 agent CLI）、pi-tui（终端 UI）；双循环 ReAct 架构（外层 follow-up 队列 + 内层 tool call 循环）；7 个内置工具（read/bash/edit/write/grep/find/ls）+ TypeScript 扩展系统 + Skills 标准；beforeToolCall/afterToolCall 钩子实现审批流；无内置权限弹窗，通过扩展或容器化（OpenShell/Gondolin/Docker）实现安全边界；明确不内置 MCP 支持（Skills 是替代方案，MCP 可通过扩展添加）；30+ LLM 提供商；TypeBox schema 验证；关键抽象：Agent/AgentState/AgentMessage/AgentTool/AgentEvent/AgentContext/AgentLoopConfig/AgentSession/ExtensionAPI/Skill |
| RES-002 | 检查 | Pi Agent 子进程/RPC 集成模式与 Agent-as-Subprocess 行业调研 | 2026-06-08 00:00 | 2026-06-08 00:00 | 已完成 | Pi 官方支持三种嵌入模式：(1) SDK 模式——`createAgentSession()` 直接嵌入 Node.js 进程，类型安全、可编程控制工具/扩展/会话；(2) RPC 模式——`pi --mode rpc` 启动 JSONL stdio 子进程，支持 prompt/steer/followUp/abort/set_model/compact 等全套命令+流式事件，官方提供 Python 和 Node.js 客户端示例；(3) JSON 模式——`pi --mode json` 单次输出所有事件流。开源集成案例：openclaw/openclaw（377K stars）使用 `@earendil-works/pi-tui` 组件但自建 agent-core；MaliNamNam/pi-phone 使用 WebSocket+RPC 实现手机远程 UI；geraschenko/pi-rpc-rs 是 Rust 类型安全 RPC 客户端；earendil-works/pi-chat 通过 Gondolin 微 VM 沙盒桥接 Discord/Telegram。Python 生态：PyPI 上 `pi-agent-core` 0.1.1 为第三方非官方端口，Mohwit/pi-mono-sdk 提供异步 Python SDK 包装 Node 桥接。Agent-as-subprocess 在行业中有明确趋势：Multica（35K stars）统一管理 Claude Code/Codex/Pi/Cursor 等 CLI 作为子进程；WebCode 用 Blazor Server 包装多 CLI agent 为 Web 控制台；Cline 新增 `@cline/sdk` 支持 headless 嵌入。Pi 的 RPC 协议是当前最完整的 agent subprocess JSONL 协议之一，含 command/response correlation、extension UI sub-protocol（select/confirm/input/editor）、steering/follow-up 消息队列和 compaction/retry 事件 |

## 配置运维

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| OPS-001 | 运维 | .gitignore 增加 SQLite 附属文件忽略规则（*.db-wal / *.db-shm / *.db-journal），完善 runtime/ 之外的 DB 文件兜底覆盖 | 2026-06-29 | 2026-06-29 | 已完成 | 检查确认：runtime/ 全量 ignore 正确，仅 README.md 与 config/ui-branding.example.yaml 作为模板入库；品牌资产 Haier.White.logo.png 未入库（符合开源合规原则）；package-lock.json / runtime/server.pid 已正确 ignore；新增 3 条 sidecar 模式并经 git check-ignore 验证生效 |
| OPS-002 | 运维 | 新增 package.sh 代码-配置打包脚本，生成符合 update.sh 校验规则的 *-code-config.tar.gz 分发包，支撑跨服务器部署/迁移 | 2026-07-07 16:05 | 2026-07-07 16:05 | 已完成 | 打包成员与 update.sh copy_versioned_files 对齐（src/tools/tests/docs/skills + start.sh/update.sh/requirements.txt/pyproject.toml/package.json/README.md/CLAUDE.md/LICENSE/.env.example + runtime/config/ui-branding.example.yaml），排除 node_modules/.git/.env(密钥)/__pycache__/runtime 业务数据/poc-*/eval；内置自检复刻 validate_update_package 规则（含 src/main.py、无危险路径、无业务数据目录、无 .env/node_modules/.git）；实跑产出 v0.3.1-build202607071605 包(4.5M/320 文件)，自检 + 独立交叉验证通过（应当存在的依赖文件均为 1，敏感/业务数据均为 0）；dist/ 已 git-ignored 不入库 |
| OPS-003 | 运维 | 按部署交付需求生成 zip 格式分发包（与 package.sh 同清单），输出到项目上级目录 | 2026-07-07 16:22 | 2026-07-07 16:22 | 已完成 | 产物 `prd-draft-review-workflow-v1-code-config-v0.3.1-build202607071622.zip`（4.6M/321 文件，含最新 README 与「打包与部署指南」），位于项目上级目录（项目外，不入库）；成员清单与 package.sh 一致（src/tools/tests/docs/skills + 根脚本 + 配置模板），排除 node_modules/.git/.env(密钥)/__pycache__/*.pyc/runtime 业务数据/POC/eval；直接 `unzip -l \| grep` 逐项验证：main.py/start.sh/update.sh/requirements.txt/package.json/.env.example/ui-branding.example.yaml 均在，危险项（node_modules/.git/__pycache__/*.pyc/runtime:data·uploads/裸 .env/真实 ui-branding.yaml）全为 0 |
| OPS-004 | 优化 | package.sh 增强：默认输出 zip 格式、新增 --format 选项；zip 改用 Python zipfile 打包以设置 UTF-8 文件名标志位；tar.gz 加 COPYFILE_DISABLE=1 消除 macOS AppleDouble 垃圾文件 | 2026-07-07 16:30 | 2026-07-07 16:47 | 已完成 | 默认格式由 tar.gz 改为 zip（`--format tar.gz`/`both` 可选）；zip 打包从 Info-ZIP CLI 改为 Python zipfile——macOS Info-ZIP 不支持 -UN=u、无法设 UTF-8 标志，导致 zipinfo 显示乱码且 Windows 解压中文乱码，Python zipfile 自动设 bit 11 UTF-8 标志实现跨平台正确；发现并修复 tar.gz 含 ._ AppleDouble 垃圾文件的老问题（bsdtar 默认行为，加 `COPYFILE_DISABLE=1` + `--exclude='._*'`，自检新增 `._` 拦截）；三种模式实跑通过自检，Python 校验：zip 与 tar.gz 文件清单 265=265 完全一致、29 个非 ASCII 条目全部设 UTF-8 标志、无 ._ 文件；已用新脚本重新生成正式 zip 到项目上级目录并删除旧的 build202607071622.zip（旧 Info-ZIP 版无 UTF-8 标志） |
| OPS-005 | 运维 | 按 V0.3.3 版本生成 zip 分发包到项目上级目录（配合 DOC-055 版本号同步） | 2026-07-07 20:12 | 2026-07-07 20:12 | 已完成 | 产物 `prd-draft-review-workflow-v1-code-config-v0.3.3-build202607072012.zip`（267 文件/4.6M，位于项目上级目录、不入库）；package.sh 默认 zip 输出 + 内置自检通过；独立 Python zipfile 交叉验证：关键文件（main.py/start.sh/update.sh/requirements.txt/package.json/.env.example/ui-branding.example.yaml + 新增 test_bugfix_106_107.py/test_bugfix_108_110.py + BUG-106~110 涉及 8 个源文件）均在包内，包内 main.py 版本号=0.3.3；危险项全 0（node_modules/.git/__pycache__/*.pyc/裸 .env/runtime 业务数据/真实 ui-branding.yaml/AppleDouble ._）；29 个非 ASCII 条目全部设 UTF-8 标志位(bit 11)、中文部署指南文件名可读；文件数 267 = 上版 265 + 2 新测试；package.sh 本身不入包属设计（目标服务器只需 update.sh 更新、不需打包能力，与 copy_versioned_files 清单一致）；上级目录旧 v0.3.1 包(build202607071647)保留未删 |
| OPS-006 | 运维 | 按 V0.3.4 版本生成 zip 分发包到项目上级目录（配合 DOC-056 版本号同步） | 2026-07-09 17:18 | 2026-07-09 17:18 | 已完成 | 产物 `prd-draft-review-workflow-v1-code-config-v0.3.4-build202607091718.zip`（268 文件/4.6M，位于项目上级目录、不入库）；package.sh 自检通过；交叉验证：关键文件齐全、包内 main.py 版本=0.3.4、危险项 0、29 个非 ASCII 条目均设 UTF-8 标志；文件数 268 = 上版 267 + 1（test_bugfix_111_relative_static_assets.py） |
| OPS-007 | 运维 | 按 V0.3.5 版本生成 zip 分发包到项目上级目录（配合最新提交 V0.3.5-Build0671 同步） | 2026-07-17 16:20 | 2026-07-17 16:20 | 已完成 | 产物 `prd-draft-review-workflow-v1-code-config-v0.3.5-build0671-20260717.zip`（272 文件/4.6M，位于项目上级目录、不入库）；复用 package.sh 默认 zip 输出 + 内置自检通过；构建号对齐 git 最新提交 V0.3.5-Build0671-20260717；独立 Python zipfile 交叉验证：10 个关键文件（main.py/start.sh/update.sh/requirements.txt/package.json/.env.example/ui-branding.example.yaml/README.md/CLAUDE.md/LICENSE）齐全、包内 main.py 版本=0.3.5、危险项全 0（node_modules/.git/__pycache__/*.pyc/裸.env/runtime 业务数据/真实 ui-branding.yaml/AppleDouble ._）、29 个非 ASCII 条目全部设 UTF-8 标志位(bit 11)；成员清单与 package.sh 一致，排除 node_modules/.git/.env(密钥)/__pycache__/*.pyc/runtime 业务数据/POC/eval |
| OPS-008 | 运维 | 按 V0.3.6 版本生成 zip 分发包到项目上级目录供安装使用（配合最新提交 V0.3.6-Build0711 同步） | 2026-07-29 19:35 | 2026-07-29 19:35 | 已完成 | 产物 `prd-draft-review-workflow-v1-code-config-v0.3.6-build0711-20260729.zip`（273 文件/4.6M，位于项目上级目录、不入库）；复用 package.sh 默认 zip 输出 + 内置自检通过；构建号对齐 git 最新提交 V0.3.6-Build0711-20260729；独立 Python zipfile 交叉验证：10 个关键文件无缺失、包内 main.py 版本=0.3.6、危险项全 0（node_modules/.git/__pycache__/*.pyc/裸.env/runtime 业务数据/真实 ui-branding.yaml/AppleDouble ._）、29 个非 ASCII 条目全部设 UTF-8 标志位(bit 11)；上级目录现仅保留本 v0.3.6 包（旧 v0.3.5 包已不在） |
| OPS-009 | 运维 | 部署脚本链硬编码与丢失修复（CHK-087 审计落地）：package.sh 补 package-lock.json 成员 + 自检新增 src/static/index.html 存在与 skills/ 非空；update.sh 业务数据黑名单补 runtime/vector、copy_versioned_files 与备份清单补 package.json/package-lock.json/.env.example（移除不存在的 AGENTS.md）、sync_yaml_version 改用包内实际读出的 PACKAGE_VERSION、版本提取正则对齐 main.py 新 APP_VERSION 常量；start.sh 新增 SERVER_HOST（默认 127.0.0.1，对齐 Ubuntu 安全方案 Phase 1）与 ROOT_PATH 透传 uvicorn --root-path、健康检查 curl 加 --max-time 5、status 补 .env 加载；.env.example 补 SERVER_HOST/ROOT_PATH/RUNTIME_ROOT/CONFIG_PATH/AGENT_API_BASE 说明 | 2026-07-31 17:50 | 2026-07-31 18:20 | 已完成 | 三脚本 `bash -n` 通过；实跑 `package.sh --format both` 自检通过（zip 274 文件/tar.gz 330 文件，包内确认含 package-lock.json 与 src/static/index.html，版本号 v0.3.6 从 APP_VERSION 正确提取）；ROOT_PATH=/prd-review ASGI 冒烟：带前缀 /prd-review/api/health 200 + no-store 正常、直连 /api/health 200、/prd-review/ 页面 200 |
| OPS-010 | 运维 | .gitignore 将 eval/ 目录整体加入忽略 | 2026-07-31 20:44 | 2026-07-31 20:52 | 已完成 | 原三条 eval/p2d1 细粒度规则（runtime/、p2d1_results.json、评估报告）替换为一条 `eval/` 整体忽略；`git check-ignore` 抽查脚本与说明文档均生效，`git status` 未跟踪列表已无 eval/；gitignore 契约 + p2d1 定向回归 36 passed。注意：poc-a/b/c 段的 allowlist 规则（!poc-*/scripts/ 等）与 `poc-c/openai-key.txt` 规则因目录已迁入 eval/ 而失效，保留未清理 |
| OPS-011 | 运维 | .gitignore 将 design/4-竞赛汇报-20260731/ 竞赛汇报材料目录加入忽略 | 2026-08-09 10:00 | 2026-08-09 10:00 | 已完成 | 在 .gitignore eval/ 规则后追加 design/4-竞赛汇报-20260731/；该目录含竞赛汇报 md/SVG/PNG 等内部材料，git status 确认目录已从 untracked 变为忽略（check-ignore 命中第 105 行）；目录此前无文件被 git 跟踪，无需 git rm --cached |
| OPS-012 | 运维 | 通过 lwa CLI 将实例 prd-review-v035 从 V0.3.10 更新部署到 V0.3.12（本机 lwa 基座 V0.8.5） | 2026-08-21 13:08 | 2026-08-21 13:10 | 已完成 | 流程：`./package.sh` 产出 v0.3.12-build202608211309 zip 包（285 文件/1.8M，自检通过）-> `lwa import <zip> --update prd-review-v035` 先 dry-run 预演（sha256 64d53038514c->ed01647184a5、形态不变 fullstack-sqlite、needs_rebuild=True）后正式执行；lwa 自动重建 Dockerfile/compose.yaml 并 rebuild 镜像、复用端口 18004、保留 data/ 与实例 ID；验证：本地 `curl 127.0.0.1:18004/api/health` 与 Caddy 网关路由 `http://10.180.105.86:8080/prd-review/api/health` 均 200 且 version=0.3.12，实例状态 running；SQLite 数据经 data/ 卷挂载保留（Gate-C 回退检测命中 app.db） |
| OPS-013 | 运维 | 通过 lwa CLI 将实例 prd-review-v035 从 V0.3.12 更新部署到 V0.3.13（GitHub 最新版，本机 lwa 基座 V0.8.5） | 2026-08-27 19:20 | 2026-08-27 19:26 | 已完成 | 前置确认：本地 main 与 origin/main 一致（d16c577 V0.3.13-Build1233-20260827，工作区干净）。流程：`./package.sh` 产出 v0.3.13-build202608271924 zip 包（295 文件/1.8M，自检通过）-> 在 lwa 基座工作区 `lwa import <zip> --update prd-review-v035` 先 dry-run 预演（sha256 ed01647184a5->041be6011a58、形态不变 fullstack-sqlite、needs_rebuild=True）后正式执行；lwa 自动重建 Dockerfile/compose.yaml 并 rebuild 镜像、复用端口 18004、保留 data/ 与实例 ID；验证：本地 `curl 127.0.0.1:18004/api/health` 与网关 `http://10.180.105.86:8080/prd-review/api/health` 均 200 且 version=0.3.13，实例 running；data/ 卷保留 app.db/converted/review_uploads（Gate-C 回退检测命中 app.db）。本次使 ADJ-021（max_tokens 上限 100000）与 ADJ-022（重试 7 次/2-64s 退避/toast 推送）在部署实例生效。审计警告（Dockerfile 未声明 USER、A.R01 DATABASE_URL 跳过注入）与历次部署一致，非阻塞 |
| OPS-014 | 运维 | 通过 lwa CLI 将实例 prd-review-v035 从 V0.3.13 更新部署到 V0.3.14（GitHub 最新版，本机 lwa 基座 V0.8.5） | 2026-08-27 22:45 | 2026-08-27 22:48 | 已完成 | 前置确认：本地 main 与 origin/main 一致（4728d62 V0.3.14-Build1289-20260827，工作区干净；V0.3.14 含 BUG-170~174/ADJ-023/CHK-105~107/DEV-077，docx skill 上游同步检查已由 CHK-106 履行）。流程：`./package.sh` 产出 v0.3.14-build202608272247 zip 包（371 文件/2.8M，含 KaTeX 0.16.22 本地化资源，自检通过）-> 在 lwa 基座工作区 `lwa import <zip> --update prd-review-v035` 先 dry-run 预演（sha256 041be6011a58->ed896d0c1a88、形态不变 fullstack-sqlite、needs_rebuild=True）后正式执行；lwa 自动重建 Dockerfile/compose.yaml 并 rebuild 镜像、复用端口 18004、保留 data/ 与实例 ID；验证：本地 `curl 127.0.0.1:18004/api/health` 与网关 `http://10.180.105.86:8080/prd-review/api/health` 均 200 且 version=0.3.14，新增 `/vendor/katex/katex.min.js` 静态资源 200，实例 running；data/ 卷保留 app.db/converted/review_uploads（Gate-C 回退检测命中 app.db）。本次使 BUG-170~174（report-polish 纯文本链路/insights 子步骤 schema/KaTeX 公式渲染）、ADJ-023（状态徽章配色）在部署实例生效。审计警告与历次部署一致，非阻塞 |
| OPS-015 | 运维 | 版本号提升 V0.3.14 → V0.3.15（含 BUG-175 SVG 预览黑底修复） | 2026-08-28 15:20 | 2026-08-28 16:30 | 已完成 | 单源 VERSION 文件更新为 0.3.15（无尾换行保持原格式），package.json 与 package-lock.json 两处 version 字段（原 lock 滞留 0.3.1 一并同步）、update.sh NEW_VERSION 回退值同步；README 中英、index.html 顶栏兜底×4、api-reference×3、packaging×4（适用日期 2026-08-28）、troubleshooting×4、getting-started×2 全部对齐 0.3.15；tests/test_version_single_source.py 版本钉同步。上游同步检查由 [[CHK-108]] 履行，全量文档一致性检查由 [[CHK-109]] 履行，SVG 文档口径由 [[DOC-082]] 补齐；PLN-002（上游 V0.1.7 合并）为遗留待办，不阻塞发版（本地兼容层行为不变） |
| OPS-016 | 运维 | 通过 lwa CLI 将实例 prd-review-v035 从 V0.3.14 更新部署到 V0.3.15（GitHub 最新版，本机 lwa 基座 V0.8.5） | 2026-08-28 16:19 | 2026-08-28 16:22 | 已完成 | 前置确认：本地 main 与 GitHub 远程最新一致（f173d0f V0.3.15-Build1300-20260828；git fetch 因网络超时失败两次，改用 GitHub API 确认远程 HEAD=f173d0f 无更新提交；工作区有 README.md/docs/user-guide.md 未提交文档增强，为 V0.3.15 已发布 SVG 特性的说明补充，随包部署无害）。流程：`./package.sh` 产出 v0.3.15-build202608281619 zip 包（372 文件/2.8M，自检通过，sha256 7c47c189746b）-> 在 lwa 基座工作区 `lwa import <zip> --update prd-review-v035` 先 dry-run 预演（sha256 ed896d0c1a88->7c47c189746b、形态不变 fullstack-sqlite、needs_rebuild=True）后正式执行；lwa 自动重建 Dockerfile/compose.yaml 并 rebuild 镜像、复用端口 18004、保留 data/ 与实例 ID；验证：本地 `curl 127.0.0.1:18004/api/health` 与网关 `http://10.180.105.86:8080/prd-review/api/health` 均 200 且 version=0.3.15，`/js/rich-content.js?v=20260828-2` 与 `/css/main.css?v=20260828-2` 均 200 且包含新清洗器代码（_sanitizeSvgUrlAttrs/_SVG_SIZE_MAX）与 max-height:60vh 兜底，index.html 16 处资产引用均为 v=20260828-2，实例 running；data/ 卷保留（Gate-C 回退检测命中 app.db）。本次使 BUG-175（SVG 预览黑底 + 三轮安全加固：URL 型属性统一校验/布局 DoS 上限/渐变 fallback 保留）与 PLN-001（测试 aiosqlite 连接泄漏修复）随包生效。审计警告与历次部署一致，非阻塞 |
| OPS-017 | 运维 | Pi Agent npm 依赖升级：@earendil-works/pi-coding-agent 0.79.0（2026-06-08）→ 0.84.3（2026-08-24，dist-tag latest，8月最新 stable），保持精确锁定惯例 | 2026-08-28 18:55 | 2026-08-28 19:15 | 已完成 | 兼容性核查：CLI 启动参数全兼容（--mode rpc/--no-session/--provider/--model/--extension/-e/--system-prompt 均在 0.84.3 --help 确认）；engines 要求 >=22.19.0 与 0.79.0 相同（非新增约束，本机 Node v24.19.0 满足）；RPC JSONL 协议事件名与 PiAgentBridge._KEY_EVENTS 全一致。实测冒烟：复刻桥接层拉起 pi --mode rpc 子进程（含 agent-limiter.ts Extension 与注入 env），发送 prompt 命令 → response(success:true)+agent_start/turn_start/message_start/message_end/turn_end/agent_end 事件流正常，Extension turn_end/agent_end 处理器执行（stderr limiter 日志），followUp 发送无异常；LLM 以假 key+不可达端点快速失败属预期（错误路径仍产出干净 agent_end）。npm install 后 2 个传递依赖（@google/genai preinstall、protobufjs postinstall）install script 被本机 allowScripts 策略拦截，均为 no-op/构建优化，不影响 CLI。验证：pi 相关 3 测试文件 47 passed；全量回归 1720 passed / 16 skipped / 0 failed；package-lock 无 0.79 残留、npm ls 单一 0.84.3 |
| OPS-018 | 运维 | 版本号提升 V0.3.15 → V0.3.16 + 全量文档时效性检查（含 Pi Agent 0.84.3 引出的 Node 口径修正） | 2026-08-28 19:25 | 2026-08-28 20:25 | 已完成 | ① 版本链全量更新：VERSION（单源无尾换行）、package.json + package-lock.json 两处 version、update.sh NEW_VERSION 回退值、README 中英 ×2、index.html 顶栏 ×4、ui-branding.example.yaml、api-reference ×3、troubleshooting ×4、getting-started ×2、packaging ×8、test_version_single_source 版本钉同步。② 文档时效性修正：getting-started ×4 + packaging ×2 的「Node.js 18+」改为「Node.js 22.19+」（pi-coding-agent 0.79.0 起 engines 即要求 >=22.19.0，18 为错误口径）；packaging 英文版适用日期 2026-08-27 → 2026-08-28（与中文对齐，OPS-015 遗留漂移）；package.json engines >=18.0.0 → >=22.19.0（其唯一依赖即 pi，声明对齐实际约束）；admin-guide §9 Pi Agent 章节核查为配置口径描述无需改动。③ 验证：npm ls 0.84.3 无漂移；test_version_single_source + test_doc_version_contract + test_bugfix_128_129 32 passed；全量回归 1723 passed / 16 skipped / 0 failed（含并行会话 [[DEV-078]] 新增 3 项侧栏分组测试与 [[BUG-177]] 修复后的转换契约测试）；期间发现并行会话同仓开发管理侧栏分组（[[CHK-111]]/[[DEV-078]]），统计已并入其增量 |
| OPS-019 | 运维 | 通过 lwa CLI 将实例 prd-review-v035 从 V0.3.15 更新部署到 V0.3.16（含 DEV-078 侧栏分组/DEV-079 个人 Agent 信息架构收口/授权纵深校验/docx skill 上游 V0.1.7/Pi Agent 0.84.3） | 2026-08-28 23:00 | 2026-08-28 23:26 | 已完成 | 流程：`./package.sh` 产出 v0.3.16-build202608282302 zip 包（382 文件/2.8M，自检通过，sha256 8cf7100627a5）-> lwa import --update prd-review-v035 先 dry-run 预演（sha256 7c47c189746b->8cf7100627a5，形态不变 fullstack-sqlite，needs_rebuild=True）后正式执行；lwa 自动重建 Dockerfile/compose.yaml 并 rebuild 镜像（requirements 新增 openpyxl 致 pip 层全量重装，构建约 23 分钟）、复用端口 18004、保留 data/ 卷。验证：本机 127.0.0.1:18004 与网关 http://10.180.105.86:8080/prd-review/ /api/health 均 200 且 version=0.3.16；index.html 16 处资产引用均为 v=20260828-4；app.js 含 _bindUserMenu（统一账号菜单已上线）；容器内 /app/runtime/data/app.db（372K，2026-08-21）等历史数据完整保留（Gate-C 回退检测命中）。审计警告（Dockerfile 无 USER）与历次部署一致，非阻塞 |
| OPS-020 | 运维 | 版本号提升 V0.3.16 → V0.3.17（BUG-191~194 安全修复与个人 Agent 信息架构收口随版发布） | 2026-08-30 12:10 | 2026-08-30 12:30 | 已完成 | 版本链全量更新：VERSION（单源无尾换行）、package.json + package-lock.json 两处、update.sh NEW_VERSION、README 中英 ×2、index.html 顶栏 ×4、ui-branding.example.yaml、api-reference ×3、troubleshooting ×4、getting-started ×2、packaging ×10（含中英适用日期同步 2026-08-30）、test_version_single_source 版本钉。前端资产无内容变更，缓存版本维持 ?v=20260830-2（BUG-192 已升）。上游同步检查见 [[CHK-115]]。验证：test_version_single_source + test_doc_version_contract + 版本钉 56 passed；全量回归 1786 passed / 16 skipped / 0 failed；全仓 0.3.16 残留扫描为空 |
| OPS-022 | 运维 | lwa 部署 prd-review-v035 更新 V0.3.16 → V0.3.17：遭遇 BUG-188 默认口令阻断并完整恢复，另修复回滚丢失网关别名副作用 | 2026-08-30 15:00 | 2026-08-30 15:25 | 已完成 | 打包 v0.3.17-build202608301500.zip（385 文件/2.8M）→ dry-run（sha 8cf7100627a5→60870fa14bb0，形态不变）→ 正式 import：容器启动被 BUG-188 防线阻断（实例 admin 仍为内置预设口令且未先改密），必选探针 30 次超时触发回滚。恢复三步：① docker/.env.local（0600）临时 ALLOW_DEFAULT_ADMIN_PASSWORD=1，因 lwa 轻量 start 不重读 env_file 改用 docker compose up -d --force-recreate 生效；② API 改密为随机强口令（存 runtime/secrets/lwa_prd_admin_password.txt，0600），新密登录验证通过；③ 删除临时标志重建容器，干净启动（无告警无阻断）。回滚副作用×2：别名片段被删 + manifest routeMode name→port/routeHost→null，网关 /prd-review/* 返回 200 空体静默失效，recover/restart 不恢复 → 手工还原 manifest 别名字段后 restart 由 _sync_alias_port 重建并 reload；已提 [[lwa#21]]。verificationOverrides（/api/health 探针）与契约 discovered 探针经回滚幸存。验证：本机+网关 /api/health 200 version=0.3.17；页面/资产 200（含统一菜单 _bindUserMenu）；data 卷完整；启动日志零异常。教训：**升级存在默认口令的实例前必须先改密或预置 ALLOW 标志** |
| OPS-021 | 运维 | GitHub Issue #8（[[BUG-184]]）修复说明评论并关闭；全仓 open issue 盘点 | 2026-08-30 14:45 | 2026-08-30 14:55 | 已完成 | 评论沿用 OPS-131 格式：四点根因（[[CHK-114]]）/P0+P1 修复方案（终态写 docMap+统一刷角标、taskMap 终态优先、回调 await 串行化、map 原子替换、_reattachProgressListener 重挂监听、SSE 闭包收口、event_generator 补 completed_with_warnings）/契约测试 5 项/全量 1751 passed·16 skipped·ruff·node --check/台账关联；补充评论修正发布版本为 V0.3.17（b97ac55，BUG-184 修复已随版提交，git log -S 验证）。盘点其余 open：LWA #18（pip 多源 fallback）为用户指示保持开放的未实施项不动；skills-doc-to-md #4（符号链接越界，[[BUG-178]] 向上游通报）上游仓库未修不动，待上游发版后收口。至此本仓 open issue 清零 |

| DEV-078 | 开发 | 管理后台侧栏导航分组（9 个平级 tab → 4 组两级导航）：运营概览/团队与内容/AI 能力·全局/Agent·个人，把「Pi Agent 配置（全局引擎）vs Agent 设置（个人）」的结构区别做进导航语义 | 2026-08-28 20:10 | 2026-08-28 20:50 | 已完成 | index.html admin-nav 内插入 4 个 `<div class="admin-nav-group" aria-hidden="true">` 纯视觉分组头（不带 data-tab，天然被 `closest('.admin-nav-item')` 点击委托忽略，tab 切换/localStorage 记忆/_showAgentApprovals 直达跳转零改动）；main.css 新增 `.admin-nav-group`（fs-12/muted/medium/上间距 16px）+ 收起态与 ≤1100px 窄屏图标栏下统一退化为 1px 细分隔线（灰3，保留分组线索且不占高）。测试：tests/test_admin_frontend_contract.py 新增 test_admin_nav_groups_tab_items_by_scope（四组齐备/顺序/组头位于组内首 tab 前/全局引擎与个人 Agent 异组）+ test_admin_nav_groups_do_not_interfere_with_tab_logic（分组头无 data-tab 非按钮、委托与 localStorage 原样）+ 可选 Playwright 真实浏览器用例 test_browser_admin_nav_groups_end_to_end（调用新工具 tools/verify_admin_nav_groups_browser.py，16 项检查：展开态分组归属、点分组头不切 tab、点 pi-agent/skills 正常切换、收起态 1px 分隔线与文字隐藏、窄屏 56px 图标栏；file:// 环境需清空内联宽度还原纯 CSS 布局）；文件头新增 CSS_MAIN 加载与 _extract_css_block 花括号配对提取助手。验证：契约 27 passed（含浏览器用例）；全量回归 1723 passed / 16 skipped / 0 failed；ruff 通过。触发背景：用户指出 9 个平级 tab 且「Agent 设置/Pi Agent 设置」相邻易混（[[CHK-110]] 后续 UX 收敛） |

## 规划事项

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| PLN-001 | 规划 | 全量 pytest 结束后出现多条 `aiosqlite: Event loop is closed` 后台线程异常（`aiosqlite/core.py` connection worker 在事件循环关闭后仍尝试 `call_soon_threadsafe`）；不影响测试退出码与结果，但属于测试资源清理缺陷，疑似 async fixture/连接未在 loop 关闭前显式 close | 2026-08-28 12:00 | 2026-08-28 | 已完成 | 已修复：① `tests/conftest.py` 在 import app.database 之前 patch `sqlalchemy.ext.asyncio.create_async_engine`，登记测试进程创建的所有 async engine（含 app.database 全局 engine）；autouse fixture `clear_review_progress_queues` teardown 在 drain pipeline 后统一 `engine.dispose()`（幂等、含跨用例存活的全局 engine），确保 aiosqlite 工作线程在用例 loop 关闭前收完所有结果；② 根因定位：`tests/test_agent_p3.py` TestApproval 两个用例手动 `async for db in app.dependency_overrides[get_db](): ... break`，break 不 await aclose()，session 持有的连接持续 checked-out，dispose 无法回收，loop 关闭后 GC 触发 "was deleted before being closed" + 工作线程异常（ResourceWarning 取证逐类隔离确认 2 条泄漏恰对应该 2 个用例）；改为 `_override_db()` async context manager（`async with` 内 yield session，finally 中 `await gen.aclose()`）。验证：test_agent_p3 18 passed 且泄漏/线程异常清零；全量 `python3 -W always::ResourceWarning -m pytest tests` 1629 passed / 8 skipped，aiosqlite 泄漏连接=0、线程异常=0、Event loop is closed=0（残余 5 条 warnings 为与 aiosqlite 无关的 unclosed file/loop）；ruff 通过。注：本地 .venv 缺 pillow/ruff 等环境依赖致 5 项环境性失败，与本次修复无关，以系统 python3.13 为准 |
| PLN-002 | 规划 | docx skill 与上游 skills-doc-to-md V0.1.7（93afbb1）合并（CHK-108 结论） | 2026-08-28 16:10 | 2026-08-28 18:10 | 已完成 | 按用户收敛指令「就用远端源头 skills 的内容替换本地」以全量替换方式收口（放弃原方案②回植本地兼容层——上游 V0.1.7 已内置等价安全防线且经其测试套件验证）；原要点③由 worker 传 `on_limit="skip"` 落实；④python3 用法契约随上游文档（"python"）放弃，md_to_pdf.py 移出 python3 docstring 检查清单；⑤路径结构对齐；⑥上游 tests 采纳 + 本仓回归已跑。详见 [[CHK-110]] |

## 优化事项

| ID | 动作 | 事项 | 发现时间 | 完成时间 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| OPT-001 | 优化 | 版本号与配置单源化遗留（CHK-087 审计低优先项）：update.sh `NEW_VERSION` 仍写死需随发布手工同步、package.json 版本与 main.py 独立维护；`src/config.yaml` 的 `server.host/port` 为死配置（无代码读取，实际由 start.sh 决定，误导运维）；`src/agent/extensions/agent-limiter.ts:57` 与 `pi_agent_bridge.py:280` 默认端口逻辑不一致（前者不读 SERVER_PORT） | 2026-07-31 17:50 | 2026-08-13 14:58 | 已完成 | 2026-08-13 落地剩余两项：① 删除 config.yaml 无效 server.host/port（监听只认 start.sh 的 SERVER_HOST/SERVER_PORT，默认 127.0.0.1:17957）② agent-limiter.ts 不再写死 http://127.0.0.1:17957，优先 AGENT_API_BASE，否则拼 http://127.0.0.1:${SERVER_PORT:-17957}，与 pi_agent_bridge.py 对齐。configuration.md 中英已改。测试：test_config + BUG-113 契约 23 passed。版本单源（VERSION/package.json/NEW_VERSION 回退）此前已由 DOC-077 收口。 |
| OPT-002 | 调整 | 行为与文档矛盾遗留（CHK-087 审计行为变更类，需决策后改）：① JWT_SECRET 缺失时 start.sh:31-34 与 config.py 静默生成随机密钥继续启动，与部署文档「拒绝启动」矛盾（随机密钥导致重启后已签发 token 全部失效）② `src/config.yaml` `allow_public_registration: true` 默认开放注册，生产要求关闭但无 env 开关 ③ start.sh 同时 source 根目录与 src/ 两个 .env（后者覆盖前者且文档未提及） | 2026-07-31 17:50 | 2026-08-13 15:19 | 已完成 | 2026-08-13 已落实。① JWT 空值仍允许启动：./start.sh 首次生成至少 32 字符随机密钥并写入项目根目录 .env，之后重启复用，登录 token 不再因重启失效；示例占位值/过短密钥仍拒绝。config.py 不落盘（避免测试污染仓库 .env）。② 公开注册维持默认 true，内网阶段不加关停（明确不做）。③ 环境文件只认项目根目录 .env：根目录已有则忽略 src/.env 并告警；根目录没有则把 src/.env 复制到根目录并提示迁移。涉及 src/app/env_file.py、start.sh、src/main.py、update.sh get_server_port；测试 tests/test_opt002_env_canonical.py；全量 pytest 1453 passed / 8 skipped。 |
| OPT-003 | 优化 | 清理 poc-c/openai-key.txt 明文 API key 文件（POC 遗留，违反开源合规原则） | 2026-07-31 20:18 | 2026-07-31 20:18 | 已完成 | 直接删除文件；`git log --all -- poc-c/openai-key.txt` 与 `git status` 均无记录，确认该文件从未被 git 跟踪，无需清理提交历史；密钥本身仍建议作废轮换 |
| OPT-004 | 优化 | 仓库目录治理与部署工件增强（2026-07-31 一次性重构）：① 设计类文档整体从 docs/ 迁入新顶级 design/ 并重组为 1-archive/2-discussion/3-plan/(含 SVGs/)/4-竞赛汇报-20260731/，原 docs/1-archive、2-discussion、3-design 同步撤销；docs/4-deployment/ 撤销，两篇部署文档提升到 docs/ 根（2026-05-22-ubuntu-nginx-systemd-security-plan.md、2026-07-07-打包与部署指南.md）；新增 docs/5-competition/ 参赛材料目录（业务背景与痛点.md、业务背景与痛点-流程对比.svg）② POC 实验从散落的 poc-a/b/c 整合进新顶级 eval/（eval/poc/poc-a、eval/poc/poc-b、eval/p2d1），与 OPT-003 密钥清理衔接 ③ package.sh 增强：版本提取正则跟随 main.py 变量重命名由 `version=` 改为 `APP_VERSION=`；打包成员新增 package-lock.json 以支持可复现 npm install；self_check 新增 src/static/index.html 前端入口与 skills/ 非空两道校验 ④ .env.example 补齐 SERVER_HOST/ROOT_PATH/RUNTIME_ROOT/CONFIG_PATH/AGENT_API_BASE 五项部署环境变量说明 ⑤ main.py version 常量重命名为 APP_VERSION 单源化（/api/health 复用，推进 OPT-001）⑥ 新增 tests/test_bugfix_128_129.py | 2026-07-31 20:35 | 2026-07-31 20:35 | 已完成 | 目录迁移保留全部内容（旧路径 git 显示为 D、新路径为 ??，尚未 git add）；README.md 中英文两处指向 docs/4-deployment/ 的部署指南链接已同步修正为 docs/ 根新路径 |

## 统计摘要

| 分类 | 总数 | 已完成 | 待开发/待修复 | 完成率 |
| --- | --- | --- | --- | --- |
| 代码 Bug | 195 | 195 | 0 | 100% |
| 调整事项 | 24 | 24 | 0 | 100% |
| 检查事项 | 115 | 115 | 0 | 100% |
| 测试数据 | 1 | 1 | 0 | 100% |
| 文档维护 | 83 | 83 | 0 | 100% |
| 功能开发 | 79 | 79 | 0 | 100% |
| 调研事项 | 2 | 2 | 0 | 100% |
| 配置运维 | 22 | 22 | 0 | 100% |
| 规划事项 | 2 | 2 | 0 | 100% |
| 优化事项 | 4 | 4 | 0 | 100% |
| **总计** | 526 | 526 | 0 | 100% |
