# 任务跟踪列表

记录本项目所有任务：代码 bug、bug 转需求、新增需求、需求调整、功能开发、代码审查、测试数据、文档维护、配置运维等。

> 说明：本文件是当前项目的任务清单。所有新增事项、状态变更和完成记录都应同步写入本文件。
> 字段说明：动作字段只允许以下 8 个固定枚举：修复、开发、优化、调整、规划、检查、文档、运维。
> 归并规则：审计、复核、核查、审查、验证、评估统一记为“检查”；重构、清理统一记为“优化”；方案、梳理统一记为“规划”；记录类文档事项统一记为“文档”。

## 代码 Bug

| ID | 动作 | 问题描述 | 发现日期 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| BUG-001 | 修复 | 对照外部任务清单发现当前代码缺少 BUG-111~114 的部分修复：新对话未清理附件/URL、会话 ID 类型不一致导致高亮丢失、预分类缺少 prompt 级 schema、前端内联 onclick 依赖对象未挂到 window | 2026-06-01 | 已修复 | `chat.js` 新增 `_clearAttachments()` 调用并改为字符串 ID 比较；`SkillSchemaLoader` 支持 prompt 级 schema 与递归校验/修复；`auth.js`/`chat.js`/`admin.js`/`review.js` 暴露全局对象；新增 `tests/test_skill_schema.py`、`tests/test_frontend_inline_handlers.py` 并扩展聊天前端契约测试；全量 `python3 -m pytest` 355 通过 |
| BUG-002 | 修复 | CHK-006 审查发现的三个 P2 回退：① 删除不存在上下文项误报成功（应返回 404）② FTS 搜索排序从相关性 rank 回退为时间倒序 ③ 逐篇分析缓存校验从固定 6 项 rule_key 放宽为 checks 长度 ≥ 6 | 2026-06-01 | 已修复 | `context_item_repository.py` delete_item 返回 bool + chat.py 404 判断；`conversation_repository.py` ORDER BY rank 恢复；`review_task_repository.py` 引入 `_REQUIRED_EXPERT_REVIEW_RULE_KEYS` frozenset 做集合校验；`.gitignore` 改用 `**/__pycache__/` + `*.py[cod]`；新增 `tests/test_code_review_fixes.py`(7)；全量回归 381 passed |
| BUG-003 | 修复 | CHK-008 审查发现的四个问题：① expert_review summary 非空校验缺失 ② review 文件删除仍在 router（os.remove 直接调用）③ 持久化扫描漏扫 os.remove/os.unlink ④ ChatApplicationService 未落地 | 2026-06-01 | 已修复 | `review_task_repository.py` 补充 summary 非空校验；`review_file_storage.py` 新增 delete_document_files 承接文件删除；`test_router_persistence_scan.py` 新增 os_remove/os_unlink 扫描模式；`chat_application_service.py` 创建 + chat.py 重构委派 prepare_chat_session；全量回归 388 passed |
| BUG-004 | 修复 | CHK-009 审查发现 ReviewFileStorage 固定使用 runtime_path("data", "review_uploads")，忽略 review.upload.upload_dir 配置 | 2026-06-02 | 已修复 | `review_file_storage.py` 新增 __init__(upload_dir)、_resolve_config_dir、_resolve_upload_root，优先注入路径→配置值→默认回退；save_uploaded_docx 和 delete_project_files 改用 _resolve_upload_root()；新增 `tests/test_code_review_fixes.py`(4)；全量回归 392 passed |
| BUG-005 | 修复 | 旧 runtime 实机复核发现 `prd-per-analysis/prompts/system-context.md` 缺失，且逐篇分析 prompt 未显式要求模型输出 `doc_id`，导致日志出现 prompt 缺失和 doc_id schema 自动修复告警 | 2026-06-02 | 已修复 | 新增 `skills/prd-per-analysis/prompts/system-context.md`；`per-doc-analysis.md` 增加文档 ID 输入、输出示例和规则；`SkillRunner._run_per_analysis()` 将 `doc_id` 传入 prompt 变量；新增/扩展 `tests/test_admin_skills.py`、`tests/test_skill_runner_integration.py`；全量回归 393 passed |
| BUG-006 | 修复 | P1 模型思考级别审查发现：前端选择”关”时 chat/review 请求不传 `thinking_level`，后端会回退到模型默认思考级别，导致无法关闭默认开启的思考参数 | 2026-06-02 | 已修复 | `chat.js`/`review.js` `_getThinkingLevel()`/`_getReviewThinkingLevel()` 改为始终返回当前选中值（含 `off`）；新增 `test_chat_sends_off_to_override_default` 测试；430 全量回归通过 |
| BUG-007 | 修复 | P1 模型思考级别审查发现：后台模型配置清空或关闭 custom JSON 时 `thinking_payload: null` 不会落库清空，旧自定义参数会继续残留在模型配置中 | 2026-06-02 | 已修复 | `model_config_repository.py` 引入 `_UNSET` sentinel 区分”未提供”和”显式清空”；`admin.py` update endpoint 使用 `req.model_fields_set` 判断是否传了 thinking_payload；新增 `test_thinking_payload_can_be_cleared` + `test_update_without_payload_preserves_existing` 测试；430 全量回归通过 |
| BUG-008 | 修复 | P2 Markdown/Mermaid 审查发现：`chat.js` 将 `_queueMermaidRender()` 改为空操作，但历史消息 `_appendMessage()` 仍调用它，导致刷新或切换历史会话后 Mermaid 图不再渲染 | 2026-06-02 | 已修复 | `_appendMessage()` 改为调用 `_renderMermaidOnStreamEnd(div)`，历史消息可立即渲染 Mermaid；新增契约测试覆盖；463 全量回归通过 |
| BUG-009 | 修复 | P2 Markdown fallback 审查发现：chat/review fallback 仅把列表项替换成裸 `<li>`，没有生成 `<ul>`/`<ol>` 容器，库不可用时列表 HTML 结构无效且与验收不一致 | 2026-06-02 | 已修复 | 新增 `_wrapListItems(html, pattern, tag)` 方法为连续无序/有序列表生成 `<ul>`/`<ol>` 容器；chat.js/review.js fallback 列表改用 `_wrapListItems`；新增契约测试覆盖；463 全量回归通过 |
| BUG-010 | 修复 | P2 Markdown fallback 复查发现：`_wrapListItems()` 会移除所有列表行并把 `<ul>/<ol>` 追加到内容末尾，导致”段落-列表-段落”等混排内容顺序被改变 | 2026-06-02 | 已修复 | `_wrapListItems` 重写为按行扫描、原地包裹连续列表块，保留列表在文档中的位置；chat.js/review.js 同步修复；新增混排顺序契约测试；465 全量回归通过 |
| BUG-011 | 修复 | P2 Markdown fallback 复查发现：`_wrapListItems()` 复用带 `g` 标志的 RegExp 连续 `exec` 不同字符串，连续列表项会被拆成多个 `<ul>/<ol>` 容器 | 2026-06-02 | 已修复 | `chat.js`/`review.js` `_wrapListItems()` 创建 RegExp 时用 `pattern.flags.replace('g', '')` 去掉 global 标志，防止 lastIndex 在不同字符串间泄漏；新增 2 项契约测试验证 g 标志已移除；612 全量回归通过 |
| BUG-012 | 修复 | 旧 runtime 实机体验发现 Mermaid 仍显示为蓝色空条：浏览器可能沿用缓存脚本，模型输出的未加引号长中文节点标签会触发 Mermaid parse 失败，且 DOMPurify 会清掉多行 `data-mermaid` 属性导致渲染函数找不到源码 | 2026-06-02 | 已修复 | `index.html` 为 vendor 与前端脚本增加 `?v=20260602-6` cache busting；`chat.js`/`review.js` 改为用隐藏 `<pre class="mermaid-source">` 保存 Mermaid 源码，避免 DOMPurify 清理多行属性；新增 `_getMermaidSource()` 与 `_normalizeMermaidCode()`，parse/render 前自动 quote 未加引号的 `[]`/`{}` 节点标签；真实 Chrome 验证最新消息 ID 58 可 render SVG；相关契约 149 passed，全量回归 472 passed |
| BUG-013 | 修复 | Mermaid 二次复核发现浏览器仍命中旧版 `chat.js`/`review.js` 缓存，页面实际运行代码仍依赖被 DOMPurify 清理掉的 `data-mermaid`，导致修复后仍显示空白占位 | 2026-06-02 | 已修复 | `index.html` 将 vendor 与前端脚本版本统一提升到 `?v=20260602-6`，强制刷新后加载新版 Mermaid 源码节点实现；定向回归 `pytest tests/test_markdown_contract.py -q` 42 passed；新开页面实测 `window.Chat` 已使用 `_getMermaidSource()`，并成功渲染出 SVG |
| BUG-014 | 修复 | reasoning_content 审查发现：当模型只返回思考内容无正文时，`chat.js` 流结束回退逻辑误清已渲染的 `.msg-reasoning`，显示"未收到回复"掩盖真实思考过程 | 2026-06-03 | 已修复 | `chat.js` 将 `!fullText && !hadError` 改为 `!fullText && !reasoningText && !hadError`；新增 `!fullText && reasoningText` 分支保留 `.msg-reasoning`；新增契约测试覆盖；494 全量回归通过 |
| BUG-015 | 修复 | reasoning_content 审查发现：`llm.py` `stream_chat()` 的 `delta_obj.get("reasoning_content", "")` 当 API 返回 `null` 时得到 `None` 而非 `""`，破坏 `StreamChunk.reasoning_content: str` 类型约束 | 2026-06-03 | 已修复 | `llm.py:195` 改为 `delta_obj.get("reasoning_content") or ""` 兜底 null；与 `non_stream_chat` line 341 的 `or ""` 保持一致；494 全量回归通过 |
| BUG-016 | 修复 | 品牌迁移工具资产路径安全未收口 | 2026-06-03 | 已修复 | `migrate_branding.py` 新增 `_validate_asset_path_safe()` 复用 P3 同等约束（拒绝外部 URL、.. 穿越、绝对路径前缀），`build_branding_yaml()` 资产字段校验后拒绝非法值置空+警告日志，`copy_assets()` dest 写出前 `resolve().relative_to(branding_root)` 边界检查；新增 22 项路径安全+输入边界回归测试；598 全量回归通过 |
| BUG-017 | 修复 | 品牌迁移工具输入边界与需求不一致 | 2026-06-03 | 已修复 | `migrate_branding.py` 新增 `_discover_static_dir()` 自动发现 static 目录（传入目录→src/static→static），`migrate()` 使用自动发现而非直接把传入路径当 static；CLI help 改为"旧项目代码目录路径（根目录或 static 目录均可）"；新增 5 项自动发现回归测试（含从项目根目录完整迁移）；598 全量回归通过 |
| BUG-018 | 修复 | 品牌迁移工具绝对路径校验仍有缺口：`/etc/passwd` 会被 `_validate_asset_path_safe()` 先 `lstrip(“/”)` 成 `etc/passwd` 并写入 YAML，未真正满足 P3 “拒绝绝对路径”约束 | 2026-06-03 | 已修复 | `_validate_asset_path_safe()` 新增多段绝对路径拒绝：原路径以 `/` 开头且 lstrip 后仍含 `/` 分隔符的（如 `/etc/passwd`→`etc/passwd`）一律拒绝；仅允许单段前缀路径（如 `/favicon.svg`→`favicon.svg`）；新增 2 项 `_validate_asset_path_safe` 回归测试 + 2 项 `build_branding_yaml` 路径安全回归测试；604 全量回归通过 |
| BUG-019 | 修复 | 品牌迁移工具复制合法相对子目录资产时会崩溃：`icons/favicon.svg` 通过校验并写入配置，但 `copy_assets()` 未创建目标父目录，`shutil.copy2()` 抛 `FileNotFoundError` | 2026-06-03 | 已修复 | `copy_assets()` 三处 `shutil.copy2()` 调用前均新增 `dest.parent.mkdir(parents=True, exist_ok=True)`；新增 2 项嵌套资产路径回归测试（favicon 嵌套 + logo 嵌套）；604 全量回归通过 |
| BUG-020 | 修复 | 审查页顶栏标题与智能对话/管理后台不一致：审查页 topbar-title 使用 `data-branding="review-title"` 显示"需求审查工作台"，应与其他页面保持一致显示产品名"AI产品需求初审" | 2026-06-03 | 已修复 | 将审查页 `<span class="topbar-title" data-branding="review-title">需求审查工作台</span>` 改为 `<span class="topbar-title" data-branding="topbar-title">AI产品需求初审</span>`；auth.js textMap 移除 `review-title` 映射；三个页面标题一致使用 `topbar-title` branding；badge（智能对话/审查模式/管理后台）仍区分页面功能 |
| BUG-021 | 修复 | 顶栏标题下方缺少版本号标注 | 2026-06-03 | 已修复 | branding_config.py DEFAULT_BRANDING 新增 `app_version: "0.2.7"` 字段；HTML 三页面 topbar-brand 内新增 `<div class="topbar-brand-text">` 包裹 title + version；CSS 新增 `.topbar-brand-text`（flex column）和 `.topbar-version`（10px、color-text-secondary）；auth.js textMap 新增 `'app-version': c.app_version`；API 返回 `app_version` 字段；迁移工具 `build_branding_yaml` 和 `scan_runtime` 同步新增 `app_version`；610 全量回归通过 |
| BUG-022 | 修复 | .gitignore 规则 `!runtime/config/` 会让本地 ui-branding.yaml 和扫描报告作为未跟踪文件出现在工作区，违反运行时数据不提交约定 | 2026-06-03 | 已修复 | .gitignore 新增两条 re-ignore 规则：`runtime/config/ui-branding.yaml` 和 `runtime/config/ui-branding.scan-report.md`，仅保留 example.yaml 可被追踪 |
| BUG-023 | 修复 | `_merge_branding()` 非法资产路径会覆盖已有合法值为空：runtime YAML 写 `https://...` 时会把 config.yaml 的合法 Logo 清空 | 2026-06-03 | 已修复 | `_merge_branding()` 资产路径校验逻辑改为：非法值不替换已有合法值，只记录警告日志并保留原配置；新增回归测试 `test_invalid_asset_path_preserves_valid_base` |
| BUG-024 | 修复 | HTML 缺少 Logo 挂载点：登录页 `.brand-mark` 和三页面 `.brand-dot` 没有 `data-branding` 属性，Branding.apply() 的 Logo 替换代码无法生效 | 2026-06-03 | 已修复 | HTML 登录页 `.brand-mark` 新增 `data-branding="login-logo"`；三页面 `.brand-dot` 新增 `data-branding="topbar-logo"`；新增前端契约测试 `test_html_has_data_branding_login_logo` 和 `test_html_has_data_branding_topbar_logo` |
| BUG-025 | 修复 | `review_workspace_label` 配置字段前端不生效：go-review 按钮和 review 页的"需求审查工作台"仍是硬编码，auth.js textMap 未映射该字段 | 2026-06-03 | 已修复 | HTML go-review 按钮新增 `data-branding="review-workspace-label"`；auth.js textMap 新增 `'review-workspace-label': c.review_workspace_label`；新增契约测试覆盖 |
| BUG-026 | 修复 | 品牌迁移工具 `discover_assets()` 找到 `logo.png` 但不会写入 YAML 配置：通用 logo_file 只在 discovered_assets 中，不进 merged，导致迁移后配置为空 | 2026-06-03 | 已修复 | `migrate()` 在合并后补充：将 discovered_assets 的 logo_file 以低置信写入 merged（不覆盖已有高置信值）；新增回归测试 `test_migrate_writes_discovered_logo_to_yaml` |
| BUG-027 | 修复 | 版本号显示丢失 "Ver." 前缀：auth.js textMap 直接用 `c.app_version` 替换 textContent，把 HTML 中的"Ver. 0.2.7"变成裸"0.2.7" | 2026-06-03 | 已修复 | auth.js textMap 的 `'app-version'` 改为 `'Ver. ' + c.app_version`，保留前缀；新增契约测试验证 `'Ver. '` 在 AUTH_JS 中 |
| BUG-028 | 修复 | P3.6 迁移工具分类语义未真正生效：`confirm`/低置信来源仍会被 `apply()` 自动写入 YAML，与"需人工确认后才进入 apply"不一致 | 2026-06-03 | 已修复 | `migrate_branding.py` 新增 `_can_auto_apply()` 门禁，`build_branding_yaml()` 仅写入 `auto` 且非低置信字段；`copy_assets()` 仅复制最终 YAML 会引用的自动 apply 资产；`confirm` 和低置信 auto 项进入报告人工确认区。新增回归测试覆盖 `theme_primary_svg` 不写入、项目根目录低置信 Logo 不写入/不复制；迁移工具测试 79 passed，品牌配置测试 68 passed，全量 645 passed |
| BUG-029 | 修复 | 品牌配置未区分登录页与工作页 Logo：通用 logo 会被自动同时映射到 `login_logo` 和 `topbar_logo`，导致顶栏错误复用登录页大字标 | 2026-06-03 | 已修复 | `branding_config.py` 自动发现改为通用 logo 默认只填 `login_logo`，仅显式 `topbar`/`icon` 命名资产才填 `topbar_logo`；`migrate_branding.py` 的 scan/plan/apply 不再把通用 `logo_file` 自动写入 `topbar_logo_file`；`auth.js`/`main.css` 为 login/topbar logo 拆分独立类；本地 `runtime/config/ui-branding.yaml` 清空 `topbar_logo` 以恢复工作页默认小图标；定向 `tests/test_branding.py tests/test_migrate_branding.py` 148 passed，全量 `python3 -m pytest -q` 651 passed |
| BUG-030 | 修复 | 单 logo 文件时登录页与顶栏应互作 fallback 并按各自尺寸渲染：BUG-029 阻止了通用 logo 回退到顶栏，导致用户只放一张图时顶栏仍显示默认 SVG | 2026-06-03 | 已修复 | `branding_config.py` `_discover_branding_assets()` 单文件兜底：识别出的 login_logo/topbar_logo 若只有一个，回退填充到另一处；`auth.js` `Branding.apply()` `logoUrlMap` 互作 fallback：顶栏缺图时回退到 login_logo，登录页缺图时回退到 topbar_logo；CSS 已保留 `.branding-logo-login`(72px) 与 `.branding-logo-topbar`(28px) 独立尺寸；更新 `test_discover_logo_from_branding_dir`、`test_auto_discovery_used_when_yaml_missing` 断言，新增 `test_auto_discovery_topbar_fallback_to_login`、`test_auto_discovery_login_fallback_to_topbar`、`test_auth_js_has_logo_fallback`；72 品牌测试 passed，全量 `python3 -m pytest -q` 654 passed |
| BUG-031 | 修复 | 旧版 runtime 使用历史字段 `login_notice_text` 时，新版运行时和迁移扫描只识别 `login_notice`，导致登录页提示内容没有被保留 | 2026-06-03 | 已修复 | `branding_config.py` 新增旧字段别名归一化：`login_notice_text` fallback 映射为 `login_notice`，并覆盖 runtime YAML、config.yaml、`_merge_branding()`；`migrate_branding.py scan_runtime()` 支持旧字段兜底且不覆盖新版字段；`update.sh` 同步收口为只 scan/plan 不自动 apply；新增 3 项旧字段兼容回归测试；定向 154 passed，全量 657 passed |
| BUG-032 | 修复 | `scan_runtime()` 对通用 `logo.png` 只写入 `topbar_logo_file`（medium）而不写 `login_logo_file`，手动 `apply` 时顶栏误用大 Logo、登录页无图，与 BUG-029 语义相反 | 2026-06-04 | 已修复 | `scan_runtime()` logo 映射逻辑改为：显式 topbar/header/nav/icon/mark/symbol → `topbar_logo_file`(medium)；显式 login/brand → `login_logo_file`(high)；通用 logo → `login_logo_file`(medium) + `topbar_logo_file`(low 回退)；与 `_discover_branding_assets()` 语义一致；新增 4 项回归测试；全量 663 passed |
| BUG-033 | 修复 | 存在空的 `ui-branding.yaml`（含空 `login_logo`/`topbar_logo`）时，运行时资产自动发现被跳过，仅有 branding 资产目录的旧部署无法兜底 | 2026-06-04 | 已修复 | `get_branding_config()` 改为始终调用 `_discover_branding_assets()`，发现结果只填充 yaml 中为空的资产字段，不覆盖非空 yaml 值；yaml 优先级不变，自动发现只补空；新增 2 项回归测试（空资产字段填充、非空字段不被覆盖）；全量 663 passed |
| BUG-034 | 修复 | CHK-041 P0 对照审查发现团队资料库 MVP 仍有验收缺口：新建审查项目未写入 workspace_id，项目引用资料未校验同 workspace/成员权限/资料状态，资料列表缺少 tag/status API 过滤，资料详情缺少正文预览、原文件下载和引用项目信息，前端资料库管理按钮受成员加载顺序影响初始隐藏 | 2026-06-04 | 已修复 | `_project_to_info()` 补齐 `workspace_id` 响应映射；前端资料下载从裸链接改为 `API.downloadWorkspaceSource()` 携带 Bearer 鉴权后下载 blob；补充前端下载鉴权契约测试；P0 定向 91 passed，全量 `python3 -m pytest -q` 754 passed（3 条 aiosqlite 线程警告，无失败） |
| BUG-035 | 修复 | update.sh 旧 yaml 中 app_version 与代码实际版本不一致：runtime/config/ui-branding.yaml 中的旧版本号会覆盖代码 DEFAULT_BRANDING 中的新版本号，导致前端显示版本滞后 | 2026-06-05 | 已修复 | update.sh 新增 `sync_yaml_version()` 函数，在代码替换后、启动前自动将 yaml 中的 app_version 同步为 $NEW_VERSION；Python 行级替换保留注释、格式和其他字段；新增 5 项契约测试覆盖函数存在/调用顺序/已有版本更新/缺失版本插入/注释跳过/无引号版本处理；759 全量回归通过 |
| BUG-036 | 修复 | 四个页面侧边栏宽度应跟随各页面 topbar 徽章右侧分割线动态对齐，收起按钮应始终右对齐（展开和折叠状态下均靠右侧） | 2026-06-05 | 已修复 | 恢复 `_alignSidebarToDivider()` 函数及所有调用点（init/showChat/showAdmin/showReview/showWorkspace）+ resize handler；`.sidebar-toggle` 改为 `justify-content: flex-end; padding: var(--sp-3) var(--sp-5);`（右对齐，移除 margin-top: auto 和 center）；移除所有 collapsed 状态的 justify-content override（展开和折叠均 flex-end）；`test_web_interface_guidelines.py` JS-003 改为检查 `_alignSidebarToDivider` 和 `_resizeTimer` 存在；759 全量回归通过 |

## 调整事项

| ID | 动作 | 事项 | 完成日期 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| ADJ-001 | 调整 | `.gitignore` 补充 Python 构建产物和前端依赖排除规则 | 2026-06-01 | 已完成 | 新增 `*.egg-info/`、`*.egg`、`*.whl`、`node_modules/`；验证 git 追踪中无缓存、临时或安装包文件 |

## 检查事项

| ID | 动作 | 事项 | 完成日期 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| CHK-001 | 检查 | 项目运行方式检查 | 2026-06-01 | 已完成 | 确认项目为 FastAPI + SQLite + 原生 JS SPA；端口 17957；启动方式为 `./start.sh`，包含 start/stop/restart/status；依赖文件为 `requirements.txt`，尚未创建 `.venv` |
| CHK-002 | 检查 | 敏感信息排查 | 2026-06-01 | 已完成 | 全量搜索具体企业和个人信息；docs/、skills/、src/、tests/ 及 git 历史均未发现目标敏感词 |
| CHK-003 | 检查 | 运行时兼容性验证 | 2026-06-01 | 已完成 | 确认匿名化修改不破坏现有数据结构和运行逻辑；数据库 category/title 等字段为自由文本；SkillRunner 分类关键词动态注入；默认分类为空列表；此前全量回归 pytest 344 通过，两次验证均无破坏 |
| CHK-004 | 检查 | 外部任务清单 2026-05-31/2026-06-01 bug 记录差异检查 | 2026-06-01 | 已完成 | 已确认 2026-05-31 巡检提到的运行中任务后端去重、DOCX source hash 写入失败非致命化、登录页低高度响应式回退在当前代码已有实现或测试覆盖；随后修复 BUG-001 |
| CHK-005 | 检查 | 三份持久化与分层文档合理性检查 | 2026-06-01 | 已完成 | 阅读 `后端持久化接口草案.md`、`架构分层改造清单.md`、`运行时数据与持久化接口基线.md`；对照当前 models、runtime 路径、FTS、SSE ticket、JSONL 日志及 review/chat/upload/admin/auth router 耦合点；结论为主方向合理，但发现接口草案重复、相对链接不准、第一阶段优先级口径需收敛 |
| CHK-006 | 检查 | 未提交更改代码审查 | 2026-06-01 | 已完成 | 审查 staged、unstaged、untracked 当前改动；发现上下文项删除 404 语义回退、FTS 搜索排序回退、逐篇分析缓存校验放宽、未跟踪 `__pycache__` 文件等问题；本次为审查任务，未修改业务代码，未运行自动化测试 |
| CHK-007 | 检查 | CHK-006 修复结果复查 | 2026-06-01 | 已完成 | 复查上下文项删除、FTS rank 排序、逐篇分析缓存 rule_key 校验和 `.gitignore`；相关测试 `python3 -m pytest tests/test_code_review_fixes.py tests/test_review_backend_contract.py -q` 39 通过，全量 `python3 -m pytest -q` 384 通过；仍发现 expert_review summary 非空校验未完全保留、项目删除文件清理异常处理语义变化 |
| CHK-008 | 检查 | 架构分层 WBS 完成度与 bug 复查 | 2026-06-01 | 已完成 | 对照 `docs/3-design/架构分层改造清单.md` 复查 WBS 0~F；专项 `python3 -m pytest tests/test_router_persistence_scan.py tests/test_code_review_fixes.py tests/test_review_backend_contract.py -q` 47 通过，全量 `python3 -m pytest -q` 384 通过；发现 `ChatApplicationService` 未落地、Review 文件删除仍在 router、扫描测试漏扫 `os.remove()`、expert_review summary 非空校验缺失等问题 |
| CHK-009 | 检查 | 未提交更改代码审查 | 2026-06-02 | 已完成 | 审查 staged、unstaged、untracked 当前改动；重点覆盖 repository/storage/service 分层、chat/review/admin/auth 路由重构、prompt 级 schema、前端全局对象暴露和运行时数据隔离；发现 ReviewFileStorage 上传目录忽略 `review.upload.upload_dir` 配置的回归风险；相关测试 `python3 -m pytest tests/test_data_compat.py tests/test_review_backend_contract.py tests/test_chat_integration.py tests/test_context_panel.py tests/test_skill_schema.py tests/test_router_persistence_scan.py` 82 通过 |
| CHK-010 | 检查 | V0.2.5-Build0166-20260602 提交前变更汇总与 commit message 准备 | 2026-06-02 | 已完成 | 汇总所有未提交代码、测试、文档、配置和任务清单变更；输出不超过 200 字的 commit message；本次仅做提交前整理说明，未修改业务实现 |
| CHK-011 | 检查 | 新版代码替换旧版代码并复用旧 runtime 的验证方案梳理 | 2026-06-02 | 已完成 | 梳理基于旧 runtime 副本的启动验证、数据库完整性检查、业务冒烟、日志观察、正式替换与回滚要点；强调保留旧 `JWT_SECRET` 和先备份 runtime |
| CHK-012 | 检查 | 新版代码使用旧版 runtime 实机启动验证 | 2026-06-02 | 已完成 | 使用历史参考文件中的旧 runtime 与旧 `src/.env` 启动新版代码，端口 17957；验证 health、首页静态资源、SQLite integrity、旧用户/会话/审查项目/报告读取、admin 模型 API Key 解密均正常；服务已通过 `screen` 后台会话 `prd-runtime-17957` 保持运行 |
| CHK-013 | 检查 | 旧版 runtime 登录操作日志与数据落库复核 | 2026-06-02 | 已完成 | 复核登录后的审计日志、应用日志、SQLite 落库和报告 API；确认 admin 登录、页面跳转、审查启动和报告读取均有成功日志；任务 118 已 completed，1 条逐篇分析结果有效落库并可读取；仅发现提示词文件缺失与 doc_id 自动修复两条非阻断告警 |
| CHK-014 | 检查 | V0.2.6 功能迭代任务分解合理性检查 | 2026-06-02 | 已完成 | 对照 `docs/2-discussion/2026-06-02-小版本功能迭代讨论稿.md` 与 `docs/3-design/V0.2.6-功能迭代任务分解.md`，复核模型思考级别、Markdown/Mermaid、公共文件库、飞书导入四阶段拆解；结论为总体合理但需补充迁移命名、思考参数映射准确性、公共库引用/快照策略、飞书凭证存储和验收口径 |
| CHK-015 | 检查 | P1 模型思考级别全栈开发代码审查 | 2026-06-02 | 已完成 | 审查前端 Chat/Review 下拉、Admin 模型配置、后端 Schema/Repository/Adapter/LLM extra_body 透传链路；发现 BUG-006、BUG-007；验证 `python3 -m pytest tests/test_thinking_adapter.py -q` 34 passed |
| CHK-016 | 检查 | P1 模型思考级别 BUG-006/BUG-007 修复复查 | 2026-06-02 | 已完成 | 复查 `chat.js`/`review.js` 已显式发送 `off`，`model_config_repository.py` + `admin.py` 已支持 `thinking_payload` 显式置空；针对性回归 208 passed，全量 `python3 -m pytest -q` 430 passed；结论为可进入下一个需求 |
| CHK-017 | 检查 | P2 Markdown 与 Mermaid 渲染修复方案选型调研 | 2026-06-02 | 已完成 | 对照当前 [src/static/index.html](src/static/index.html)、[src/static/js/chat.js](src/static/js/chat.js)、[src/static/js/review.js](src/static/js/review.js) 与 Marked、markdown-it、Mermaid、DOMPurify 官方文档；结论为继续沿用 `marked + DOMPurify + mermaid` 主组合最稳妥，优先补本地 vendor、完整 fallback、Mermaid 在流结束后统一 `parse/render` 与错误源码回显；`markdown-it` 作为后续增强备选，本次调研未修改业务代码、未运行自动化测试 |
| CHK-018 | 检查 | P2 Markdown/Mermaid 二次评审意见评估 | 2026-06-02 | 已完成 | 评估 CDN fallback、fallback 复杂度、流式结束兜底、P2.3/P2.4 合并交付、浏览器真实验证、DOMPurify 与 Mermaid SVG 顺序/配置风险；结论为意见基本可采纳，建议后续同步修订两份 V0.2.6 文档 |
| CHK-019 | 检查 | P2 Markdown 与 Mermaid 全栈开发代码审查 | 2026-06-02 | 已完成 | 审查 `src/static/vendor/`、`index.html`、`chat.js`、`review.js`、`main.css` 与 P2 契约测试；发现 BUG-008、BUG-009；验证 `python3 -m pytest tests/test_markdown_contract.py tests/test_chat_frontend_contract.py tests/test_review_frontend_contract.py -q` 137 passed、`python3 -m pytest tests/test_chat_integration.py tests/test_review_backend_contract.py -q` 44 passed、全量 `python3 -m pytest -q` 460 passed |
| CHK-020 | 检查 | 17957 本机服务替换可行性判断 | 2026-06-02 | 已完成 | `curl -sS http://127.0.0.1:17957/api/health` 返回 `{"status":"ok","version":"0.1.0"}`；结合 CHK-019 发现的 BUG-008/BUG-009，结论为当前代码可用于受控冒烟测试，但不建议立即作为可替换旧服务的稳定版本运行 |
| CHK-021 | 检查 | P2 BUG-008/BUG-009 修复复查 | 2026-06-02 | 已完成 | 复查历史消息 Mermaid 已改为 `_renderMermaidOnStreamEnd(div)`，原裸 `<li>` 问题已补 `<ul>/<ol>` 容器；但发现 BUG-010：fallback 列表容器会被移动到内容末尾；验证 P2 前端契约 140 passed、chat/review 后端相关 44 passed、全量 `python3 -m pytest -q` 463 passed |
| CHK-022 | 检查 | 17957 本机服务替换为当前新版代码 | 2026-06-02 | 已完成 | 复查 BUG-008/BUG-009/BUG-010 修复后，P2 前端契约 142 passed、chat/review 后端相关 44 passed、全量 `python3 -m pytest -q` 465 passed；停止旧 17957 进程后用 `screen` 会话 `prd-runtime-17957` 启动当前代码，`/api/health`、首页 `/`、`/vendor/mermaid.min.js` 均返回 200；另记录非阻断 fallback 边界 BUG-011 |
| CHK-023 | 检查 | 17957 本机服务切换为新版代码复用历史 runtime | 2026-06-02 | 已完成 | 停止原先使用当前仓库 `runtime/` 的 17957 进程；复用历史站点 `src/.env`，以 `RUNTIME_ROOT=/Users/fenix-macmini/Documents/VSCode/1-AI-Coding/10-真实业务工作流系统-2026/1-需求评审和团队协作工作流V1-2026/历史参考文件/20260515-需求评审内网小网站/runtime` 重新启动新版代码；确认进程已打开历史 runtime 下的 `logs/app.log` 与 `data/app.db`，`/api/health`、首页 `/`、`/vendor/mermaid.min.js` 均返回 200 |
| CHK-024 | 检查 | 品牌与界面个性化配置需求可行性讨论 | 2026-06-03 | 已完成 | 盘点当前硬编码项：登录页 Logo、工作页顶栏 Logo、favicon、页面 title、产品名、登录页说明、顶栏入口文案与主题色；结论为适合新增 runtime/local 配置优先机制，用于新版通用代码替换旧项目时保留本地品牌资产与展示文案 |
| CHK-025 | 检查 | reasoning_content 全链路代码审查 | 2026-06-03 | 已完成 | 审查 StreamChunk/log_llm_session/chat.js/review.js reasoning_content 链路；发现 BUG-014（思考内容误清）和 BUG-015（null 类型不安全）；确认零测试覆盖和 4 处文档滞后；修复后新增 `tests/test_reasoning_content.py`(22)；494 全量回归通过 |
| CHK-026 | 检查 | DEV-012 品牌迁移工具代码审查 | 2026-06-03 | 已完成 | 审查 `tools/migrate_branding.py` 与 `tests/test_migrate_branding.py`；定向测试 `python3 -m pytest tests/test_migrate_branding.py -q` 30 passed，全量 `python3 -m pytest -q` 576 passed；发现 BUG-016（资产路径安全未收口）和 BUG-017（旧项目根目录输入/JS 扫描能力不一致） |
| CHK-027 | 检查 | BUG-016/BUG-017 修复后品牌迁移工具复审 | 2026-06-03 | 已完成 | 定向测试 `python3 -m pytest tests/test_migrate_branding.py -q` 52 passed，全量 `python3 -m pytest -q` 598 passed；复审发现 BUG-018（绝对路径被错误改写为相对路径写入 YAML）和 BUG-019（嵌套相对资产复制缺少父目录导致崩溃），建议修复后再实际应用 |
| CHK-028 | 检查 | BUG-018/BUG-019 修复后品牌迁移工具上线前复核 | 2026-06-03 | 已完成 | 复核 `_validate_asset_path_safe()` 已拒绝 `/etc/passwd` 并保留 `/favicon.svg`、`icons/favicon.svg` 合法路径；复核 `copy_assets()` 可复制 `icons/favicon.svg` 并创建父目录；定向测试 `python3 -m pytest tests/test_migrate_branding.py -q` 58 passed，全量 `python3 -m pytest -q` 604 passed；未发现阻断实际应用的问题 |
| CHK-029 | 检查 | DEV-014 P3.6 迁移工具架构升级完成度审查 | 2026-06-03 | 已完成 | 审查 `tools/migrate_branding.py`、`src/app/services/branding_config.py`、`tests/test_migrate_branding.py`、`tests/test_branding.py`；定向测试 `python3 -m pytest tests/test_migrate_branding.py tests/test_branding.py -q` 146 passed，全量 `python3 -m pytest -q` 644 passed；发现 BUG-028（confirm/低置信项仍自动进入 apply），DEV-014 不建议按已完成验收 |
| CHK-030 | 检查 | BUG-028 修复后 P3.6 迁移工具复核 | 2026-06-03 | 已完成 | 红绿验证：新增 `test_build_branding_yaml_only_includes_auto_fields` 期望 `theme_primary_svg` 不写入、`test_apply_does_not_write_low_confidence_project_root_logo` 期望低置信扩展 Logo 不写入不复制，修复前 2 failed、修复后 2 passed；定向 `python3 -m pytest tests/test_migrate_branding.py -q` 79 passed，`python3 -m pytest tests/test_branding.py -q` 68 passed，全量 `python3 -m pytest -q` 645 passed |
| CHK-031 | 检查 | `update.sh` 更新脚本方案与当前草稿审查 | 2026-06-03 | 已完成 | 认可版本更新脚本作为标准交付组件的方向；检查当前草稿并执行 `bash -n update.sh` 语法通过；发现正式落地前需收口：不能把 `task-list.md` 作为旧代码包覆盖对象、不能在 update 脚本内自动 apply 品牌迁移、回滚恢复 `.env` 逻辑有路径缺陷、`rm -rf src/tools/tests/docs` 覆盖前需更严格包校验、脚本尚未 chmod 可执行 |
| CHK-032 | 检查 | DEV-015 标准更新脚本修复完善后复核 | 2026-06-03 | 已完成 | 红绿验证：`tests/test_runtime_config.py` 新增 5 项 update.sh 契约测试，修复前 5 failed，修复后 11 passed；补充验证 `bash -n update.sh`、`python3 -m pytest tests/test_migrate_branding.py tests/test_branding.py -q` 147 passed，全量 `python3 -m pytest -q` 650 passed |
| CHK-033 | 检查 | BUG-031 旧版 `login_notice_text` 兼容修复验证 | 2026-06-03 | 已完成 | 红绿验证：新增 3 项测试覆盖 `_merge_branding()`、`_load_runtime_branding()`、`scan_runtime()` 对 `login_notice_text` 的兼容，修复前 3 failed、修复后 3 passed；定向 `python3 -m pytest tests/test_branding.py tests/test_migrate_branding.py -q` 154 passed；`tests/test_runtime_config.py` 更新脚本契约 2 passed；全量 `python3 -m pytest -q` 657 passed |
| CHK-034 | 检查 | 未提交代码（P3.6 品牌/迁移/update.sh）缺陷审查 | 2026-06-04 | 已完成 | 全量 `python3 -m pytest -q` 657 passed；`bash -n update.sh` 通过；发现 BUG-032（`scan_runtime` 通用 logo 只映射顶栏）、BUG-033（空 yaml 阻断资产自动发现）；`update.sh` 仅 scan/plan 不自动 apply，上线路径相对安全；`task-list.md` 存在重复 DEV-015 行（文档问题） |
| CHK-035 | 检查 | 外部开源项目调研：Cloud co-worker、团队知识库、AI Agent 对话、RAG/内部知识库检索、MCP/工具调用、协作消息、需求/文档审查与 AI 原型生成相关项目 | 2026-06-04 | 已完成 | 已按 star >= 600 过滤，整理 12 个候选项目，并精选 8 个项目做架构/设计拆解；结论已沉淀到 `docs/2-discussion/2026-06-04-Cloud-coworker-远期规划与开源项目调研.md`，并进一步产出 `docs/3-design/Cloud-co-worker远期规划与架构演进方案.md` 作为现有系统延续式演进方案 |
| CHK-036 | 检查 | 对照 cowork 版工具设计规划复核 V0.2.x P4/P5 后续归位 | 2026-06-04 | 已完成 | 结论：旧 P4“公共文件库”不应按原范围直接开发，应改名并收束为新规划 P0“团队资料库 MVP”；旧 P5“飞书导入”可整体后置为后续连接器/知识源导入能力，待团队资料库、空间权限和检索底座稳定后再做 |
| CHK-037 | 检查 | Milvus 作为 cowork 规划向量库候选的选型判断 | 2026-06-04 | 已完成 | 结论：Milvus 可纳入向量库候选，尤其适合中后期数据规模扩大、需要 standalone/distributed 服务化部署时评估；近期 P0/P2 仍建议以 SQLite FTS5 + LanceDB/Milvus Lite/Chroma 小样本 POC 为主，不直接把 Milvus 集群作为默认底座 |
| CHK-038 | 检查 | workflow-cookbook 对 cowork 版工具设计的借鉴价值评估 | 2026-06-04 | 已完成 | 阅读 `AGI-is-going-to-arrive/workflow-cookbook` README 与关键章节；结论：不直接复用其 Claude Code Workflow 运行时，但可吸收确定性编排、phase 进度、schema 结构化输出、parallel/pipeline、预算/续传、对抗验证和 Workflow 库版本化等设计模式，用于补强 SkillRunner、AgentRun、ReviewStageExecution 和多角色审查流程 |
| CHK-039 | 检查 | OpenCoworkAI/open-cowork 对 cowork 版工具设计的借鉴价值评估 | 2026-06-04 | 已完成 | 阅读 `OpenCoworkAI/open-cowork` README、README_zh、ROADMAP 与 package 信息；结论：不直接复用其 Electron 桌面形态，但可借鉴本地工作区授权、WSL2/Lima VM 沙盒、Skills/MCP 管理、Trace Panel、Feishu/Slack 远程控制与审批面板、多模型配置、记忆检索和工作区模板等产品化设计，用于补强个人 Agent、团队空间、工具调用观测与人在环控制 |
| CHK-040 | 检查 | onyx-dot-app/onyx 对 cowork 版工具设计的借鉴价值评估 | 2026-06-04 | 已完成 | 阅读 `onyx-dot-app/onyx` README、中文 README、官方连接器与核心概念文档；结论：Onyx 对团队资料库、连接器同步、权限感知检索、混合索引、Agent/Action/MCP、查询审计和使用分析有强参考价值，但其 Standard 栈包含 PostgreSQL、Redis、MinIO、Vespa/OpenSearch、后台 worker 等，近期不宜直接照搬；P0/P1 先吸收 Document/Section/Connector/Credential/AccessType/SyncStatus 模型与轻量索引状态管理 |
| CHK-041 | 检查 | Cowork 版 WBS P0 开发完成度对照审查 | 2026-06-04 | 已完成 | 对照 `docs/3-design/Cowork版-WBS任务分解.md` 与 `task-list.md` 审查 P0 改动；全量 `python3 -m pytest -q` 747 passed；结论：P0 主体已形成，但发现 BUG-034 所列验收缺口，暂不建议直接进入 P1/P2 下一阶段开发 |
| CHK-042 | 检查 | BUG-034 修复结果复查 | 2026-06-04 | 已完成 | 复查 P0 五项验收缺口：引用资料权限/状态校验、资料列表 tag/status 过滤、资料详情正文/引用项目/后端下载、成员加载顺序已有改动；但新建项目 `workspace_id` API 响应仍为 `None`，前端下载仍使用裸链接无法携带 Bearer 鉴权；定向测试 1 failed, 89 passed，暂不建议进入下一阶段 |
| CHK-043 | 检查 | BUG-034 最终修复验证 | 2026-06-04 | 已完成 | 红灯复现 `test_create_review_project_writes_workspace_id` 失败、前端下载契约 2 failed；修复后针对性 3 passed，P0 定向 `python3 -m pytest tests/test_workspace_api.py tests/test_workspace.py tests/test_frontend_workspace_contract.py -q` 91 passed，全量 `python3 -m pytest -q` 754 passed；结论：BUG-034 已收口 |
| CHK-044 | 检查 | 扣子 3.0 最新更新对 Cowork 版协作工作流启示调研 | 2026-06-04 | 已完成 | 调研扣子 3.0 项目空间、多端协同、本地/云端 Agent 接入、成员与 Agent 混合协作、Agent 待办和状态可视化等更新；结论：应将本项目后续规划从单一审查工具升级为团队空间内的人机协同编排面，优先补强 Agent 注册/运行/待办/审计、项目级协作上下文和权限边界 |
| CHK-045 | 检查 | Cowork 版 WBS 完成状态快速核验 | 2026-06-05 | 已完成 | 对照 `docs/3-design/Cowork版-WBS任务分解.md`、`task-list.md`、`src/app/routers/workspace.py`、`src/static/js/{workspace,review,api}.js` 与相关测试，定向执行 `pytest -q tests/test_workspace.py tests/test_workspace_api.py tests/test_frontend_workspace_contract.py`，91 passed；结论：P0.A.1~P0.E.4 已有实现与测试证据，P1~P6 本轮未标记完成 |

## 测试数据

| ID | 动作 | 事项 | 完成日期 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |

| ID | 动作 | 事项 | 完成日期 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| TD-001 | 调整 | 测试数据开源合规匿名化 | 2026-06-01 | 已完成 | `tests/test_review_backend_contract.py`、`tests/test_review_report_contract.py` 中将“有埋点”改为“有数据采集”，共 3 处 |

## 文档维护

| ID | 动作 | 事项 | 完成日期 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| DOC-001 | 文档 | docs/ 文档开源合规匿名化 | 2026-06-01 | 已完成 | `3-design/需求审查工作流平台-需求说明书.md`、`2-archive/20260515-业务逻辑层整合技术方案.md`、`2-archive/三栏式UI界面设计指南.md`、`4-deployment/2026-05-22-ubuntu-nginx-systemd-security-plan.md`、`1-discussion/Skill-as-a-Service人机协同工具开发范式.md` 完成示例产品名、内部目录名、服务名和 LLM 选型口径泛化 |
| DOC-002 | 文档 | skills/ Prompt 与模板开源合规匿名化 | 2026-06-01 | 已完成 | 覆盖 `prd-overview-classify/`、`prd-per-analysis/`、`system-review/`、`requirement-insights/`、`report-generator/`；将具体业务词汇泛化为核心策略、智能联动、交互体验、功能预约、数据追踪、响应时延等中性表达 |
| DOC-003 | 文档 | 持久化与分层文档合并重构 | 2026-06-01 | 已完成 | 将三份文档收敛为"两份主文档 + 一个兼容入口"：`运行时数据与持久化接口基线.md` 升级为数据结构与持久化接口规约；`架构分层改造清单.md` 重写为 WBS 计划书；`后端持久化接口草案.md` 改为已合并说明；验证不再存在旧 `docs/...` 错误相对链接，接口草案重复标题已消除，三份文档行数收敛为 474/344/21 |
| DOC-004 | 文档 | 任务清单格式规范化 | 2026-06-01 | 已完成 | 将原来的日期分组 bullet list 改为与外部项目一致的分类表格结构；新增固定动作枚举、归并规则、ID 编号、状态字段和备注字段；按代码 Bug、调整事项、检查事项、测试数据、文档维护重新归类现有记录 |
| DOC-005 | 文档 | V0.2.6 小版本功能迭代讨论稿 | 2026-06-02 | 已完成 | 新增并更新 `docs/2-discussion/2026-06-02-小版本功能迭代讨论稿.md`；覆盖模型思考级别、后台公共文件库、飞书链接导入/CLI 集成、Markdown/Mermaid 渲染修复四项需求的调研结论、推荐方案、前后端改造建议、风险和实施顺序；已将模型思考级别修正为模型选择器右侧 `关/low/high` 三档运行时下拉，作用于智能对话和需求审查后续请求 |
| DOC-006 | 文档 | V0.2.6 功能迭代任务分解 | 2026-06-02 | 已完成 | 新增并持续更新 `docs/3-design/V0.2.6-功能迭代任务分解.md`；4 阶段 79 子任务：P1 模型思考级别(16)、P2 Markdown/Mermaid 修复(36)、P3 公共文件库(19)、P4 飞书导入(8)；含 7 项待决策问题；已根据 CHK-014、CHK-017、CHK-018 审查/调研意见补强 |
| DOC-007 | 文档 | P2 Markdown 与 Mermaid 渲染修复方案专项修订 | 2026-06-02 | 已完成 | 更新 `docs/2-discussion/2026-06-02-小版本功能迭代讨论稿.md` 与 `docs/3-design/V0.2.6-功能迭代任务分解.md`；明确 P2 不换栈，继续采用 `marked + DOMPurify + mermaid`，本地 vendor 为主、CDN 仅可选兜底、fallback 只做最小保底且控制复杂度、Mermaid 完整消息后 `parse/render`，补 SSE 异常/idle debounce 兜底，并显式处理 DOMPurify 与 Mermaid SVG 顺序风险 |
| DOC-008 | 文档 | README 双语重写并聚焦架构与需求评审工作流 | 2026-06-02 | 已完成 | 重写 `README.md` 为中文在前、英文在后的双语结构；在主标题下增加中英文跳转标签；内容聚焦当前代码已落地的分层架构、SkillRunner 驱动的需求评审工作流、功能要点与后台管理能力，并保留最小启动说明与 runtime 数据隔离说明 |
| DOC-009 | 文档 | Claude Cowork 对需求初审工作流的迭代启示调研文档 | 2026-06-02 | 已完成 | 新增 `docs/2-discussion/2026-06-02-Claude-Cowork-需求初审工作流迭代启示.md`；调研 Claude Cowork 官方博客、产品页和帮助中心资料，拆解任务委托、项目工作空间、插件包、多角色并行、计划任务、人在环审批、企业观测等设计思路，并映射为当前需求初审平台的迭代方向 |
| DOC-010 | 文档 | V0.2.6 P3 品牌与本地个性化配置文档修订 | 2026-06-03 | 已完成 | 更新 `docs/2-discussion/2026-06-02-小版本功能迭代讨论稿.md` 与 `docs/3-design/V0.2.6-功能迭代任务分解.md`；将品牌与本地个性化配置设为 P3，明确 `runtime/config/ui-branding.yaml`、`runtime/assets/branding/`、配置优先级、后端 branding API、前端 Logo/title/favicon/主题色覆盖、路径安全和旧 runtime 覆盖验收；公共文件库顺延 P4、飞书导入顺延 P5 |
| DOC-011 | 文档 | reasoning_content 功能描述文档更新 | 2026-06-03 | 已完成 | 更新 4 处文档：① `V0.2.6-功能迭代任务分解.md` 补充 P1.7 思考内容展示子任务 ② `小版本功能迭代讨论稿.md` 将"展示推理过程是另一个功能"改为已交付说明 ③ `运行时数据与持久化接口基线.md` llm_sessions.jsonl 典型字段补 reasoning_content ④ `README.md` 功能要点和管理功能补思考过程流式展示与适配方式 |
| DOC-012 | 文档 | V0.2.7 版本号统一与文档完成状态标注 | 2026-06-03 | 已完成 | ① 全代码版本号统一为 0.2.7：main.py(2处)、branding_config.py、HTML(3处)、ui-branding.example.yaml、conftest.py、test_app_frontend_contract.py ② `V0.2.6-功能迭代任务分解.md` 实施顺序表新增状态列，P1/P2/P3 标注已完成，P4/P5 标注未开始；各阶段标题加完成标记 ③ README.md 中英文新增版本号、品牌配置功能要点、品牌配置管理功能、品牌目录和迁移工具目录说明 |
| DOC-013 | 文档 | Cloud co-worker 远期规划与架构演进方案 | 2026-06-04 | 已完成 | 新增 `docs/3-design/Cloud-co-worker远期规划与架构演进方案.md`；基于 star >= 600 的开源项目调研与当前仓库基线，系统化梳理团队空间、账号 Agent、知识层、消息协作、审查平台升级和分阶段路线图，并明确 Dify/AnythingLLM/RAGFlow/Onyx/Rowboat/Mem0/n8n/Figma MCP 的可借鉴边界 |
| DOC-013 | 文档 | Cloud co-worker 远期规划与开源项目调研文档 | 2026-06-04 | 已完成 | 新增 `docs/2-discussion/2026-06-04-Cloud-coworker-远期规划与开源项目调研.md`；覆盖 GitHub star>=600 项目调研、精选项目架构拆解、团队空间/知识库/Agent 对话/消息/审查平台升级业务逻辑、现有代码承接点、阶段路线图和风险约束 |
| DOC-014 | 文档 | Cloud co-worker 子 Agent 调研与 Plan 规划汇总 | 2026-06-04 | 已完成 | 新增 `docs/2-discussion/2026-06-04-Cloud-co-worker-子Agent调研与Plan规划汇总.md`；独立汇总 6 路子 Agent 调研 + Plan 模式四大支柱/技术选型/三阶段路线图，不引用另两份规划稿；含 50+ 项目池、9 条设计模式、讲解管线 MVP 与模块增量建议 |
| DOC-015 | 文档 | 需求业务工作流 cowork 版工具设计规划终稿 | 2026-06-04 | 已完成 | 新增 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；整合 5 份讨论稿并补充团队评审、资源成本估算、技术选型对比、POC 计划、技术架构、数据层级、开源工具支持和功能业务流程流转 |
| DOC-016 | 文档 | V0.2.x P4/P5 与 cowork 规划归位说明更新 | 2026-06-04 | 已完成 | 更新 `docs/3-design/V0.2.x-功能迭代任务分解.md` 和 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；明确原 P4 公共文件库转入 cowork P0“团队资料库 MVP”，原 P5 飞书导入后置为连接器/知识源导入规划，并将原 P4/P5 清单标记为归档参考 |
| DOC-017 | 文档 | cowork 版工具设计规划补充 Milvus 向量库候选 | 2026-06-04 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；将 Milvus Lite 纳入 Phase 2 本地向量库 POC，将 Milvus Standalone/Distributed 与 Qdrant 归入中后期服务化向量库评估，并同步补充 POC 决策门槛和官方资料来源 |
| DOC-018 | 文档 | cowork 版工具设计规划补充角色、权限与个人 Agent 分工 | 2026-06-04 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；新增空间角色、流程角色、个人 Agent/团队 Agent/审查 Agent 分工，明确个人空间默认私有、团队空间粗粒度角色、审查关键动作确认人，并补充 RolePolicy、AgentAuthorization、ReviewParticipant 等领域对象 |
| DOC-019 | 文档 | 三个 cowork 相关开源项目设计借鉴评估文档 | 2026-06-04 | 已完成 | 新增 `docs/2-discussion/2026-06-04-三个cowork相关开源项目设计借鉴评估.md`；系统整理 `workflow-cookbook`、`open-cowork`、`onyx` 三个项目对确定性工作流、本地 Agent 产品化、团队知识库/权限检索的借鉴点、不建议照搬边界、领域对象映射和分阶段吸收路线 |
| DOC-020 | 文档 | cowork 版工具设计规划吸收调研报告并正式修订 | 2026-06-04 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；参考 `docs/2-discussion/调研报告/` 下 4 篇 Markdown 和 1 份 PDF，补充评审快照空间、M 型监督者/T 型专家/AI 增强型一线人员、Agent 生命周期治理、MCP Hub/Gateway、ToolCallTrace、MCPToolPolicy、AgentApprovalRequest、WorkflowEvent、AI 讲解产物管线、黑板/发布订阅协作机制和风险控制 |
| DOC-021 | 文档 | cowork 版工具设计规划吸收三个开源项目过程讨论稿 | 2026-06-04 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md`；参考 `docs/2-discussion/过程讨论稿/2026-06-04-三个cowork相关开源项目设计借鉴评估.md`，补充 `workflow-cookbook`、`open-cowork`、`onyx` 的分层借鉴表、阶段吸收路线，并新增/强化 WorkflowTemplate、WorkflowRun、WorkflowBudget、DocumentSet、ConnectorCredential、SyncJob、IndexAttempt、AccessPolicy、AnswerFeedback、SkillPackage、SandboxPolicy 等领域对象和“不照搬”边界 |
| DOC-024 | 文档 | Cowork 版 WBS 单团队部署与团队空间页面口径修订 | 2026-06-04 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；补充单团队部署范围修正，明确系统只初始化 1 个默认团队空间、注册用户默认加入本团队、新增“团队空间”一级并列页面；P0 前置团队空间页面壳/导航/资料库主入口，P1 收束为默认团队设置与成员管理，移除多空间创建/切换器，P5 个人资料改为私有知识作用域而非一级个人空间，并修正实施总览子任务数 |
| DOC-022 | 文档 | V0.2.8 版本号统一与 BUG-032/033 修复记录 | 2026-06-04 | 已完成 | 全代码版本号 0.2.7→0.2.8：main.py(2处)、branding_config.py、HTML(3处)、update.sh、conftest.py、test_app_frontend_contract.py、README.md(2处)、ui-branding.example.yaml；BUG-032 修复 scan_runtime 通用 logo 映射；BUG-033 修复 get_branding_config 空资产字段自动发现；663 全量回归通过 |
| DOC-023 | 文档 | Cowork 版 WBS 任务分解文档 | 2026-06-04 | 已完成 | 新增 Cowork版-WBS任务分解.md；将规划文档 P0~P6 六个阶段、3 类 POC、领域对象和业务流程拆为 124 条可量化任务，含验收标准、工时估算和前置条件 |
| DOC-025 | 文档 | Cowork 版 WBS 并行开发分组与命名开发规范补充 | 2026-06-04 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；为 P0~P6 和 POC 增加按依赖关系拆分的并行开发组，补充任务 ID、数据模型、API、前端、runtime、状态枚举、分层开发、权限测试等命名标准和开发规范 |
| DOC-026 | 文档 | Cowork 版 WBS 完成状态快速标记 | 2026-06-05 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；新增”快速验证标记（2026-06-05）”说明，明确仅 P0.A.1~P0.E.4 标记为”已验证完成”，并在 Phase 0 及 P0.A~P0.E 标题补充完成标记与测试依据 |
| DOC-027 | 文档 | Cowork 版 WBS P1~P6 完成状态补充标记 | 2026-06-05 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md`；P1.A.4、P1.B.4 标记 ✅ 已验证完成，P1.A.1、P1.A.2、P1.C.2 标记 ⚠️ 部分完成，P1.A.3/P1.B.1~B.3/P1.C.1 标记未开始；Phase 2~6 和 POC 标题补充（未开始） |
| DOC-028 | 文档 | Cowork 版 WBS P0.E 前端设计要素记录 | 2026-06-05 | 已完成 | 更新 `docs/3-design/Cowork版-WBS任务分解.md` P0.E 小节；补充前端设计要素记录：页面结构(topbar+sidebar+content)、资料库表格列定义、详情页结构(元数据网格+正文预览+引用项目+鉴权下载)、空状态规范、权限显隐规则(owner/admin/member/viewer)、品牌注入点(data-branding 属性)、CSS 类名体系(ws-/workspace- 前缀) |
| DOC-029 | 文档 | Cowork 版设计规划补充团队空间 MVP 前端 UI 原则 | 2026-06-05 | 已完成 | 更新 `docs/2-discussion/需求业务工作流的cowork版工具设计规划-20260604.md` §4.2.1；补充 7 条前端 UI 原则：页面并列原则、sidebar+content 布局、权限显隐而非阻断、资料库交互规范、空状态组件、品牌注入机制、CSS 前缀规范 |

## 功能开发

| ID | 动作 | 事项 | 完成日期 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| DEV-001 | 开发 | WBS E.2 Admin 配置域分层：新增 ModelConfigRepository、PromptTemplateRepository、SkillConfigRepository、UserRepository；重构 admin.py router 全部持久化调用收口到 repository 层 | 2026-06-01 | 已完成 | admin.py db_add 从 2 降为 0；新增 `src/app/repositories/{model_config,prompt_template,skill_config,user}_repository.py`、`src/app/stores/sse_ticket_store.py` |
| DEV-002 | 开发 | WBS E.3 Auth 账户域分层：新增 SseTicketStore（包装 in-memory SSE ticket dict）、UserRepository 收口注册/改密/登录活跃时间更新；重构 auth.py router、services/auth.py、middleware/auth.py | 2026-06-01 | 已完成 | auth.py db_add 从 1 降为 0；services/auth.py SSE ticket 逻辑委派到 SseTicketStore；middleware/auth.py 用户查询委派到 UserRepository |
| DEV-003 | 检查 | 全量回归验证（排除 4 个预存失败） | 2026-06-01 | 已完成 | 373 passed, 4 deselected；预存失败为 test_docx_conversion_accepts_skill_string_return、test_legacy_runtime_file_path_resolves_to_workspace_runtime、test_parent_relative_runtime_file_path_resolves_to_workspace_runtime、test_source_hash_write_failure_is_non_fatal |
| DEV-004 | 开发 | WBS 0+A+B+C.1 基线保护与分层模板实现：数据兼容性测试、持久化扫描、ChatFileStorage、ConversationRepository、ContextItemRepository、AuditLogWriter/Reader、FrontendLogWriter、LlmSessionLogWriter、ReviewFileStorage | 2026-06-01 | 已完成 | 新增 `tests/test_data_compat.py`(14)、`tests/test_router_persistence_scan.py`(8)；新增 `src/app/storage/{chat_file_storage,review_file_storage}.py`、`src/app/log_writers/{audit_log_writer,audit_log_reader,frontend_log_writer,llm_session_log_writer}.py`、`src/app/repositories/{conversation_repository,context_item_repository}.py`；重构 upload/chat/history/auth/admin/review router 委派到 storage/repository/log_writer 层；修复 4 个契约测试（skill 路径、runtime_path monkeypatch、_write_source_hash 导入）；377 全量回归通过 |
| DEV-005 | 开发 | WBS D.1+E.1+D.2+F 审查 pipeline 分层收口：ReviewTaskRepository、ReviewProjectRepository、ReviewContextRepository、ReviewPromptRepository、ReviewPipelinePersistenceService；review.py db_add 从 9 降为 0，builtin_open 从 2 降为 0 | 2026-06-01 | 已完成 | 新增 `src/app/repositories/{review_task,review_project,review_context,review_prompt}_repository.py`、`src/app/services/review_pipeline_persistence.py`；重构 review.py 所有 Task/DocAnalysis/SystemReview/Project/Document/Context/Prompt 的 db.add 收口到 repository 层；pipeline MD 文件读取改用 ReviewFileStorage.read_markdown()；384 全量回归通过 |
| DEV-006 | 开发 | CHK-008 四项问题修复：summary 非空校验补回、文件删除委派到 ReviewFileStorage、扫描测试补扫 os.remove/os.unlink、ChatApplicationService 落地重构 chat.py | 2026-06-01 | 已完成 | BUG-003 修复：`review_task_repository.py` _analysis_has_required_expert_review 增加 summary 非空判断；`review_file_storage.py` delete_document_files 承接 os.remove 调用；`test_router_persistence_scan.py` 新增 os_remove/os_unlink 模式+白名单+测试方法；`chat_application_service.py` 新建+`chat.py` 重构将准备逻辑委派到 ChatApplicationService.prepare_chat_session，移除 _get_jwt_secret/_get_model_config/_truncate_context_text 死代码+清理未用导入；388 全量回归通过 |
| DEV-007 | 开发 | CHK-009 ReviewFileStorage upload_dir 配置注入：ReviewFileStorage 新增 __init__(upload_dir)、_resolve_config_dir、_resolve_upload_root，优先注入路径→配置值→默认回退 | 2026-06-02 | 已完成 | BUG-004 修复：`review_file_storage.py` 新增 __init__(upload_dir=None) + _resolve_config_dir + _resolve_upload_root（优先注入→review.upload.upload_dir 配置→runtime/data/review_uploads 默认回退）；save_uploaded_docx 和 delete_project_files 改用 _resolve_upload_root()；新增 `tests/test_code_review_fixes.py`(4) 覆盖默认/注入/配置/相对路径四种场景；392 全量回归通过 |
| DEV-008 | 开发 | LLM 思考过程记录：llm.py StreamChunk 新增 reasoning_content 字段 + stream_chat 捕获 delta.reasoning_content；non_stream_chat/retryable_chat 提取 message.reasoning_content；log_llm_session/LlmSessionLogWriter 新增 reasoning_content 参数，content 非空时写入 JSONL；流式路径 chat.py 收集 reasoning_content 并通过 SSE 下发前端 | 2026-06-02 | 已完成 | 影响到 `src/app/services/llm.py`、`src/app/services/retry.py`、`src/app/logging_config.py`、`src/app/log_writers/llm_session_log_writer.py`、`src/app/routers/chat.py`、`src/static/js/chat.js`、`src/static/css/main.css`；修复 3 个 bug：non_stream_chat content=None 处理、llm_session_log_writer 缺换行、chat.py 流式路径 reasoning_content 丢失；468 全量回归通过 |
| DEV-009 | 开发 | P1 模型思考级别全栈开发（P1.1~P1.6）：DB migration + ORM 4字段、Schema/Repository/Router 扩展、ThinkingAdapter 服务层、LLM extra_body 透传、前端 Chat/Review/Admin UI、34 项自动化测试 | 2026-06-02 | 已完成 | 新增 `src/app/services/thinking_adapter.py`（ThinkingLevel/ThinkingAdapter 枚举 + build_thinking_payload + THINKING_ADAPTER_TEMPLATES）；ORM 新增 thinking_supported/thinking_level/thinking_adapter/thinking_payload 四字段 + DB migration；Schema 新增 field_validator 校验；Repository update 扩展；llm.py/retry.py stream_chat/non_stream_chat/retryable_chat/structured_chat 新增 extra_body 参数；chat_application_service.py 集成 thinking adapter；review.py 集成 thinking adapter；前端 chat.js/review.js 新增思考等级下拉联动；admin.js 新增模型编辑/新建思考配置表单 + 条件显隐逻辑 + 表格思考标签；新增 `tests/test_thinking_adapter.py`(34) 覆盖单元测试(25) + API 集成测试(9)；427 全量回归通过 |
| DEV-010 | 开发 | P2 Markdown 与 Mermaid 渲染修复全栈开发（P2.1~P2.5）：前端依赖本地化、Fallback 增强、Mermaid 渲染时机修复、加载态/错误态样式、30 项自动化测试 | 2026-06-02 | 已完成 | 新增 `src/static/vendor/{purify,marked,mermaid}.min.js` + `NOTICE.md`；`index.html` CDN 引用改为本地路径 + onerror 加载失败检测；`chat.js`/`review.js` `_renderMarkdown` 拆分为 `_renderMarkdownWithLibraries` + `_renderMarkdownFallback`；DOMPurify `USE_PROFILES` 新增 `svg:true, svgFilters:true` 解决 SVG 兼容；fallback 支持 h1~h6、列表、粗斜体、行内代码、fenced code block、Mermaid 容器；`chat.js` 流式过程 `_queueMermaidRender` 改为空操作，新增 `_schedulePostStreamMermaid`(debounce 兜底) + `_renderMermaidOnStreamEnd`；Mermaid 渲染前先 `parse` 校验；错误态保留源码 + 折叠查看；`main.css` 新增加载动画(`mermaid-spin`) + 错误态 + 源码折叠样式；更新 `test_chat_frontend_contract.py`/`test_review_frontend_contract.py` 契约测试；新增 `tests/test_markdown_contract.py`(30) 覆盖 vendor 本地化(6) + 渲染拆分(10) + 渲染时机(7) + 错误处理(4) + 样式(3)；460 全量回归通过 |
| DEV-011 | 开发 | P3 品牌与本地个性化配置全栈开发（P3.1~P3.5）：BrandingConfigService 配置服务、GET /api/app/branding API、/assets/branding/ 安全静态路由、前端 Branding JS 初始化与 DOM 替换、ui-branding.example.yaml、51 项自动化测试 | 2026-06-03 | 已完成 | 新增 `src/app/services/branding_config.py`（DEFAULT_BRANDING + get_branding_config + _validate_asset_path + _merge_branding + _load_runtime_branding + ensure_branding_dirs）；`main.py` 新增 GET /api/app/branding + /assets/branding/{path} 安全资产路由 + BrandingConfigService 引入 + lifespan 调用 ensure_branding_dirs；`index.html` 关键品牌元素加 data-branding/id 属性（login-title、login-subtitle、login-notice、topbar-title、review-title、admin-label、admin-badge、page-title、favicon-link、theme-color-meta）；`auth.js` 新增 Branding 对象（load/apply/renderLoginNotice），支持 `login_notice` 多行纯文本安全渲染；`app.js` App.init() 调用 Branding.load()；`.gitignore` 保留 runtime/config/ 和 ui-branding.example.yaml；新增 `runtime/config/ui-branding.example.yaml` 并补充通用多行提示示例；新增 `tests/test_branding.py`(51) 覆盖配置结构与默认值(3) + 路径安全(8) + 配置合并(5) + Runtime 加载(3) + 服务层(4) + API 集成(4) + 前端契约(23)；修复 3 个旧契约测试；546 全量回归通过 |
| DEV-012 | 开发 | 品牌迁移工具：本地静态扫描旧代码 HTML/CSS/JS + 旧 runtime 配置和资产，生成 ui-branding.yaml + 复制资产 + 扫描报告，不自动覆盖已有配置（需 --force） | 2026-06-03 | 已完成 | 新增 `tools/migrate_branding.py`（scan_html/scans_css/scans_runtime/discover_assets/merge_findings/build_branding_yaml/build_scan_report/copy_assets/migrate CLI）；新增 `tests/test_migrate_branding.py`(30)；576 全量回归通过 |
| DEV-013 | 规划 | 品牌迁移工具架构升级：① 拆分 scan/plan/apply 三模式 ② 扫描结果增加三级分类标记 ③ branding_config.py 运行时兼容层 ④ 保障业务数据层完全不动 | 2026-06-03 | 已完成 | 规划已转化为 DEV-014 开发并完成 |
| DEV-014 | 开发 | P3.6 迁移工具架构升级与运行时兼容层 | 2026-06-03 | 已完成 | BUG-028 已修复；CHK-030 复核通过 |
| DEV-015 | 开发 | update.sh 标准化版本更新脚本：停服务→备份→迁移工具scan+plan+apply（代码替换之前执行，确保旧HTML文本配置不丢失）→替换代码→启动→健康检查→失败回滚 | 2026-06-03 | 已完成 | 迁移必须在代码替换前执行，否则旧版HTML中的login_notice等文本配置会丢失 |
| DEV-015 | 开发 | 标准版本更新脚本 `update.sh`：作为随版本交付的保守更新入口，支持更新包校验、停止服务、备份代码与本地配置、替换代码、品牌迁移 scan/plan、启动健康检查和失败回滚 | 2026-06-03 | 已完成 | `update.sh` 修复为可执行脚本；移除自动品牌 apply，仅生成 scan/plan 报告；不复制过程清单；更新包校验拒绝绝对/穿越路径和 `runtime/data`、`runtime/uploads`、`runtime/logs`、`runtime/results`、`runtime/storage`；`.env` 备份恢复拆分为 `project.env`/`src.env`；成功健康检查后才自更新脚本；新增 5 项契约测试；CHK-032 复核通过，全量 650 passed |
| DEV-016 | 开发 | P0.A.1 新增 Workspace 表：id/name/description/created_by/status/created_at/updated_at，种子数据 1 条默认 workspace，migration 幂等 | 2026-06-04 | 已完成 | 新增 src/app/models/workspace.py Workspace 模型、src/app/repositories/workspace_repository.py CRUD、database.py _ensure_default_workspace 种子与注册；25 项自动化测试覆盖表创建/CRUD/种子/幂等；688 全量回归通过 |
| DEV-017 | 开发 | P0.A.2 新增 WorkspaceMember 表：workspace_id/user_id/role/status/created_at，外键约束，角色枚举(owner/admin/member/viewer) | 2026-06-04 | 已完成 | WorkspaceMember 模型含 FK CASCADE、role/status 枚举常量；Repository 方法 add_member/get_member/list_members/update_member_role/remove_member/get_user_workspaces；__init__.py 导出 WorkspaceRepository；测试覆盖角色枚举/软删除/跨空间隔离；688 全量回归通过 |
| DEV-018 | 开发 | P0.A.3 新增 KnowledgeSource 表：workspace_id/source_type/title/filename/content_hash/version/owner_id/status/metadata_json | 2026-06-04 | 已完成 | KnowledgeSource 模型含 FK CASCADE、source_type/status 枚举常量；KnowledgeSourceRepository 含 create/list_by_workspace/count/archive/set_status/update_version；50 项 workspace 测试全量通过；722 全量回归通过 |
| DEV-019 | 开发 | P0.A.4 ReviewProject 新增 workspace_id 外键（nullable，旧数据兼容）+ P0.A.5 project_source_refs 关联表 | 2026-06-04 | 已完成 | ReviewProject 新增 workspace_id FK(nullable)、source_refs relationship；ProjectSourceRef 模型含 project_id/source_id/ref_type/snapshot_version；ProjectSourceRefRepository 含 add_ref/list_by_project/remove_ref/freeze_snapshot；database.py _ensure_review_schema 添加 workspace_id 列迁移；50 项 workspace 测试全量通过；722 全量回归通过 |
| DEV-020 | 开发 | P0.A.6 数据迁移脚本：为所有现有 ReviewProject 自动归入默认 workspace | 2026-06-04 | 已完成 | database.py _ensure_default_workspace 调用 _migrate_projects_to_default_workspace，将 workspace_id=None 的项目归入默认空间；50 项 workspace 测试覆盖迁移与兼容；722 全量回归通过 |
| DEV-021 | 开发 | P0.A.7 团队空间页面壳 + 导航：新增并列页面、侧栏（资料库+成员两个Tab）、workspace.js、workspace router（list_workspaces/list_members/list_sources）、CSS 样式 | 2026-06-04 | 已完成 | index.html 新增 #workspace-page 含 topbar+sidebar+content+两个Tab；workspace.js 含 init/load/tab切换/API调用；app.js 新增 _showWorkspacePage+导航绑定；workspace.py router 含 GET /api/workspace + GET /api/workspace/{id}/members + GET /api/workspace/{id}/sources；main.css 新增 workspace 样式；722 全量回归通过 |
| DEV-022 | 开发 | P0.E.1 Workspace 权限校验：delete_source 和 update_source_tags 端点新增 WorkspaceMember 权限检查，非成员返回 403；2 项权限回归测试 | 2026-06-04 | 已完成 | workspace.py delete_source/update_source_tags 新增 ws_repo.get_member 校验；移除冗余 db.flush()（commit 隐含 flush）；test_workspace_api.py 新增 test_delete_source_non_member_blocked + test_update_tags_non_member_blocked；持久化白名单 workspace.py db_flush 1→0；723 全量回归通过 |
| DEV-023 | 开发 | P0.B.3 删除资料 API（软删除）：DELETE /api/workspace/{id}/sources/{source_id}，status→archived，含空间存在/成员权限/跨空间隔离校验 | 2026-06-04 | 已完成 | workspace.py delete_source 端点含 ws_repo.get_by_id + get_member 权限校验 + ks_repo.archive 软删除 + audit log；test_workspace_api.py 4 项测试覆盖成功删除/404/跨空间/非成员阻断；723 全量回归通过 |
| DEV-024 | 开发 | P0.B.4 更新标签 API：PUT /api/workspace/{id}/sources/{source_id}/tags，tags 数组写入 metadata_json，version 自增 | 2026-06-04 | 已完成 | workspace.py update_source_tags 端点含权限校验 + tags 类型验证(422) + metadata_json 合并 + version+1 + audit log；test_workspace_api.py 3 项测试覆盖更新成功/无效类型/非成员阻断；723 全量回归通过 |
| DEV-025 | 开发 | P0.C.1 项目引用资料 API：POST /api/review/project/{id}/sources 添加引用(ref_type/snapshot_version)、GET 列表 | 2026-06-04 | 已完成 | review.py 新增 add_project_source_ref + list_project_source_refs 端点；ProjectSourceRefRepository.add_ref/list_by_project；ref_type 枚举校验(reference/context/background)；snapshot_version 可选；审计日志；test_workspace_api.py 3 项测试覆盖添加/非owner/无效ref_type；723 全量回归通过 |
| DEV-026 | 开发 | P0.A.7 注册自动加入默认 workspace：注册后自动查找默认空间并加入为 member 角色，幂等处理 | 2026-06-04 | 已完成 | auth.py register 新增 WorkspaceRepository.get_default + add_member 逻辑；conftest.py init_test_db 种子默认 workspace + admin 为 owner；test_workspace_api.py 2 项测试覆盖自动加入与幂等 |
| DEV-027 | 开发 | P0.B.1 资料上传 API：POST /api/workspace/{id}/sources，文件保存+hash+文本提取+KnowledgeSource 创建 | 2026-06-04 | 已完成 | KnowledgeFileStorage 新增 save_upload/read_file/delete_file；workspace.py upload_source 端点含 20MB 限制+权限校验+audit log；test_workspace_api.py 4 项测试覆盖上传成功/大小限制/非成员阻断/详情获取 |
| DEV-028 | 开发 | P0.D.1 角色权限校验（owner/admin/member/viewer 区分）：workspace 所有端点新增 _require_role 角色校验 | 2026-06-04 | 已完成 | workspace.py 新增 _MANAGE_ROLES/_WRITE_ROLES/_READ_ROLES + _require_role helper；delete/tags→owner/admin，upload→owner/admin/member，list/detail→owner/admin/member/viewer；test_workspace_api.py 4 项测试覆盖 member 阻断删除/标签、viewer 阻断上传、admin 可操作 |
| DEV-029 | 开发 | P0.C.3 审查启动时自动冻结引用资料快照：_run_pipeline 在 task.status=running 前调用 freeze_snapshot | 2026-06-04 | 已完成 | review.py _run_pipeline 新增 ProjectSourceRefRepository.freeze_snapshot + db.flush()；ProjectSourceRefRepository.freeze_snapshot 将 source.version 写入 snapshot_version；test_workspace_api.py 1 项测试覆盖 freeze_snapshot |
| DEV-030 | 开发 | P0.B.5 前端资料库 Tab（上传+列表+删除）+ P0.B.6 前端资料详情页 | 2026-06-04 | 已完成 | workspace.js 含 upload/list/delete/detail UI；AgentB 开发，DEV-031 修复后通过验收 |
| DEV-031 | 开发 | P0.C.4 前端资料选择器（审查项目引用资料） | 2026-06-04 | 已完成 | review.js _openSourcePicker/_confirmSourcePicker；AgentB 开发，DEV-032 修复后通过验收 |
| DEV-032 | 开发 | AgentB 前端代码 bug 修复：6 个关键问题 | 2026-06-04 | 已完成 | ① api.js 移除不存在的 getDefaultWorkspace() ② workspace.js 移除 getDefaultWorkspace fallback ③ workspace.js 移除 listProjectSourceRefs('__all__') ④ workspace.js 移除不存在下载链接 ⑤ workspace.js 删除按钮按角色隐藏(owner/admin) ⑥ index.html 移除重复 workspace-page HTML 块 ⑦ main.css 修复未定义 --color-bg-hover → --gray-1；747 全量回归通过 |
