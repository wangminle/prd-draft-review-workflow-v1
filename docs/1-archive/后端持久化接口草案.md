# 后端持久化接口草案（已合并）

## 状态

本文件原本用于定义 repository / storage / log writer 的接口草案。为避免与数据基线文档重复维护，相关内容已经合并到 [运行时数据与持久化接口基线.md](运行时数据与持久化接口基线.md)。

后续请以以下两份文档为准：

1. [运行时数据与持久化接口基线.md](运行时数据与持久化接口基线.md)
   - 定义 runtime 目录边界、数据库表语义、文件落盘规则、日志语义、持久化接口契约。
   - 用作新开源项目继续兼容旧数据和持续升级的“数据结构与持久化接口规约”。

2. [架构分层改造清单.md](架构分层改造清单.md)
   - 定义基于上述数据规约推进分层重构的 WBS 计划。
   - 用作后续实施 repository / storage / application service 分层的任务拆解和验收清单。

## 为什么保留这个文件

保留本文件是为了避免已有链接失效。本文不再承载独立接口定义，也不再维护另一套实现顺序。

如需查阅接口，请直接阅读 [运行时数据与持久化接口基线.md](运行时数据与持久化接口基线.md) 第 8-10 节。
